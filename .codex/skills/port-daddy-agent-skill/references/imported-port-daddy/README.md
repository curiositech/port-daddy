# Imported Port Daddy support material

These files were preserved from the former `skills/port-daddy/` bundle when
its duplicate `SKILL.md` authority was retired. They are historical supporting
material, not current operating instructions. Use the canonical
[`../../SKILL.md`](../../SKILL.md) and its current references for present-day
behavior; verify source and installed-runtime truth before relying on older API,
SDK, or workflow details here.

The original subtree layout is retained: `references/` contains the former API,
SDK, multi-agent-patterns, and `.portdaddyrc` references; `diagrams/` contains
the old decision flow; `CHANGELOG.md` and `affordance-scorecard.json` retain
their provenance.
