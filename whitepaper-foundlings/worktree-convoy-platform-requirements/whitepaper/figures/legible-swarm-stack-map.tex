% Figure 1 — system strata. Geometry carries dependency and audience; the
% highlighted aperture marks the exact L1/L2 surface owned by this chapter.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \fill[hhsand!15] (0,0) rectangle (14.75,1.05);
  \fill[hhsand!25] (0,1.05) rectangle (14.75,2.10);
  \fill[hhteal!8]  (0,2.10) rectangle (14.75,3.15);
  \fill[hhsand!15] (0,3.15) rectangle (14.75,4.20);
  \foreach \y in {0,1.05,2.10,3.15,4.20}{\draw[pd hairline] (0,\y)--(14.75,\y);}

  \foreach \y/\n/\name/\aud in {
    .525/0/daemon kernel/machine,
    1.575/1/coordination protocol/agents,
    2.625/2/legibility \& authority/operator,
    3.675/3/economy \& federation/market}{
    \node[font=\scriptsize\bfseries,text=hhgray,anchor=east] at (-.18,\y) {L\n};
    \node[font=\small\bfseries,anchor=west] at (.28,\y+.12) {\name};
    \node[pd axis label,anchor=west] at (.28,\y-.22) {for the \aud};
  }

  \foreach \x/\lab in {5.0/state,6.55/order,8.10/identity,9.65/reason,11.20/outcome,12.75/reputation,14.30/exchange}{
    \draw[pd guide] (\x,.12)--(\x,4.08);
    \node[pd axis label,anchor=north,rotate=35] at (\x,-.06) {\lab};
  }
  \foreach \x in {5.0}{\node[pd datum] at (\x,.525) {};}
  \foreach \x in {5.0,6.55,8.10}{\node[pd datum] at (\x,1.575) {};}
  \foreach \x in {5.0,6.55,8.10,9.65,11.20}{\node[pd focus datum] at (\x,2.625) {};}
  \foreach \x in {8.10,9.65,11.20,12.75,14.30}{\node[pd datum] at (\x,3.675) {};}

  \draw[pd focus rule,line width=1.3pt] (0.02,2.14)--(0.02,3.11);
  \node[pd axis label,anchor=east] at (14.55,4.43)
    {$\uparrow$ provides \qquad $\downarrow$ assumes};
\end{tikzpicture}
\caption{The system as four strata rather than four products. Dots mark the
capabilities each layer contributes or consumes; alignment exposes reuse across
layers. This chapter owns the L2 operator surface and uses the L1 directory
substrate. It assumes ordered kernel state below and supplies accountable
identity, reasons, and outcomes to reputation and exchange above.}
\label{fig:stack-map}
\end{figure}
