# Fixture: a doc that violates the standing rule

This paragraph is the LOAD-BEARING one — a mixed-case hit the gate must still catch.

Somewhere else, an executive is Held To Account for the outcome.

The phrase "load bearing" (no hyphen) is also banned.
