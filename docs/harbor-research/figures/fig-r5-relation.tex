% R5 relation-map: structural diagram (no numeric axes), transcribed from
% create_relation_map() in figures/src/r5_figures.py -- same base/target
% two-column layout, same three rows (bouncer analogy; the Sigma_c(5) vs
% Sigma_u(3) event split; the compound-case punchline), same connective
% labels. Layout note: the coordinate grid and text widths below were
% redone from the first CI render (row spacing and text widths were too
% tight for the wrapped paragraph nodes at this font size, so every row's
% text bled into its neighbors and the row-2 connector label -- text
% width=6.6cm literal -- overran both side boxes). Content/meaning is
% unchanged from the source script; only geometry and light prose trims.
% [internal, script r5_figures.py]
\begin{figure}[t]
\centering
\begin{tikzpicture}[x=1cm,y=1cm]
\node[align=center,font=\normalsize\bfseries] at (7.5,17.05)
  {R5 --- gate the door, never the thought};
\node[align=center,font=\footnotesize\itshape,text=black!55] at (7.5,16.6)
  {hypervisor enforceability = Ramadge--Wonham supervisory control};

% Base domain (left): the door
\draw[draw=harborblue,thick,fill=harborblue,fill opacity=0.10] (0.2,-0.4) rectangle (5.0,16.1);
\node[align=center,font=\small\bfseries,text=harborblue,text width=4.4cm] at (2.6,15.7) {Base: the door};

% Target domain (right): the daemon
\draw[draw=seagreen,thick,fill=seagreen,fill opacity=0.10] (10.0,-0.4) rectangle (14.8,16.1);
\node[align=center,font=\small\bfseries,text=seagreen,text width=4.4cm] at (12.4,15.7) {Target: the daemon};

% ===== Row 1: the analogy =====
\node[align=center,font=\small\bfseries,text width=4.4cm] at (2.6,15.0) {Bouncer at the one door};
\node[align=center,font=\footnotesize,text width=4.3cm] at (2.6,13.7)
  {Refuses \emph{entry} (controllable). Cannot refuse a \emph{thought} (uncontrollable) --- it never crosses the door.};

\node[align=center,font=\small\bfseries,text width=4.4cm] at (12.4,15.0) {Daemon at the mediation boundary};
\node[align=center,font=\footnotesize,text width=4.3cm] at (12.4,13.7)
  {Refuses mediated \emph{effects} (writes, calls, spawns). Cannot refuse internal \emph{state} --- it never crosses.};

\draw[<->,>=Stealth,thick,shipred] (5.15,10.65) -- (9.85,10.65);
\node[align=center,font=\small\bfseries\itshape,text=shipred,text width=4.4cm] at (7.5,11.65)
  {the analogy holds exactly:\\ gate the channel, never the mind};

% ===== Row 2: the 5-vs-3 split =====
\node[align=center,font=\small\bfseries,text=harborblue,text width=4.4cm] at (2.6,9.7) {$\Sigma_c$ --- controllable (5 events)};
\node[align=center,font=\footnotesize,text width=4.3cm] at (2.6,8.4)
  {\texttt{fs\_write} $\cdot$ \texttt{net\_egress} $\cdot$ \texttt{exec\_tool}\\ \texttt{git\_push} $\cdot$ \texttt{spawn\_child}\\[3pt] cross the boundary --- each can be refused.};

\node[align=center,font=\small\bfseries,text=seagreen,text width=4.4cm] at (12.4,9.7) {$\Sigma_u$ --- uncontrollable (3 events)};
\node[align=center,font=\footnotesize,text width=4.3cm] at (12.4,8.4)
  {\texttt{model\_emit\_token}\\ \texttt{in\_context\_read} $\cdot$ \texttt{internal\_plan}\\[3pt] stay inside the model --- they never reach the daemon.};

\draw[<->,>=Stealth,thick,shipred] (5.15,5.35) -- (9.85,5.35);
\node[align=center,font=\small\bfseries\itshape,text=shipred,text width=4.4cm] at (7.5,6.35)
  {5 refusable vs.\ 3 that never arrive\\ criterion: $\overline{K}\Sigma_u\cap\overline{L}\subseteq\overline{K}$};

% ===== Row 3: the compound-case punchline =====
\node[align=center,font=\small\bfseries,text width=4.4cm] at (2.6,4.4) {naive read: forbid the secret};
\node[align=center,font=\footnotesize,text width=4.3cm] at (2.6,3.1)
  {forbid \texttt{in\_context\_read} directly $\to$ a $\Sigma_u$ event $\to$ detect-only forever: the daemon never sees it coming.};

\node[align=center,font=\small\bfseries,text width=4.4cm] at (12.4,4.4) {regimentable rewrite: gate egress};
\node[align=center,font=\footnotesize,text width=4.3cm] at (12.4,3.1)
  {permit the read ($\Sigma_u$), record taint, forbid \texttt{net\_egress} while tainted ($\Sigma_c$) $\to$ regimentable.};

\draw[<->,>=Stealth,thick,shipred] (5.15,0.05) -- (9.85,0.05);
\node[align=center,font=\small\bfseries\itshape,text=shipred,text width=4.4cm] at (7.5,1.05)
  {the clean-room design rule:\\ gate the channel, never the token};
\end{tikzpicture}
\caption{The relation map. Base domain: a
club's single door --- the bouncer refuses entry (controllable) but cannot refuse thought (uncontrollable). Target domain:
the daemon's mediation boundary --- the runtime can forbid fs\_write, net\_egress, exec\_tool, git\_push, spawn\_child, and
cannot forbid token emission, in-context reads, or internal plans. The correspondence arrows carry the theorem:
refusable-at-the-boundary $\Leftrightarrow$ preventable (regimentable); internal $\Leftrightarrow$ detect-only forever. The
criterion under the map, $\overline{K}\Sigma_u\cap\overline{L}\subseteq\overline{K}$, is Ramadge--Wonham controllability [verified
framework].}
\label{fig:relation}
\end{figure}
