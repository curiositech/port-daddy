# Freeze-probe design, not a human-performance experiment

This archive preserves Goodall's bounded design proposal and pure synthetic
scoring fixtures. Parent read all three files and independently reran the 14
tests on 20 September 2026. All passed. This verifies finite arithmetic and
input-rejection cases only; no participant, coding-agent forecast, replay
controller, sealed-registration system or real merge dataset was evaluated.

`REPORT.md` is a preregistration **draft**: corpus, pilot-informed sample size,
smallest worthwhile effect and study permissions are not settled. Do not call
it a registered or executed trial. The $50 ceiling is global to the Book
experiment program, not a per-agent allowance. No expense was incurred here.

The inspected Beacon repository did not supply a verified historical merge
corpus. Runtime data paths were identified in source but not opened. That is
a scoped discovery limit, not a claim that the operator has no such dataset.

Run the pure tests from this directory:

```sh
PYTHONDONTWRITEBYTECODE=1 /usr/bin/python3 -m unittest test_scorer.py -v
```

The key distinction is `NONE` (observed no merge) versus Python `None`
(unavailable truth). Abstentions retain the denominator; censored outcomes
produce bounds instead of an optimistically selected complete-case result.
The scorer does not authenticate event order or enforce prediction-before-reveal.

Source sidecar: `/Users/erichowens/coding/tmp/book-atlas-evidence-20260920/freeze-probe-study-design`.
The archived report changes only the budget sentence to make the global cap explicit.
Original source hashes inside the report are readback-time provenance, not a
claim that the concurrently edited Book chapter still has that hash.
