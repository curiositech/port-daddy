\documentclass[11pt,a4paper]{article}
\input{preamble}
\usepackage{graphicx}
\graphicspath{{../figures/}}
\usepackage{mdframed}
\newmdenv[backgroundcolor=blue!4,linecolor=harborblue,linewidth=0.8pt,skipabove=6pt,skipbelow=6pt]{thebox}
\newmdenv[backgroundcolor=red!4,linecolor=shipred,linewidth=0.8pt,skipabove=6pt,skipbelow=6pt]{boundary}
\newcommand{\onebreath}[1]{\begin{center}\emph{#1}\end{center}}
\newtheorem{corollary}{Corollary}

\title{\textbf{The Cohomology of Equivocation}\\[4pt]
\large Detecting Split-View Lies in Federated Witness-Log Gossip by Sheaf Consistency\\[3pt]
\normalsize Paper 7 of the Harbor program --- the R6 mechanism, its statistical harness, and the consistency-radius theorems}
\author{Erich Owens\\ \normalsize The Harbor Research Program \quad \texttt{erich.owens@gmail.com}}
\date{August 2026}
\begin{document}
\maketitle

\begin{abstract}
\noindent In a federation of witnesses gossiping signed log heads, an equivocator shows different histories to different
neighbors. Pairwise cross-checking catches a lie exactly on the links that are checked; this paper answers, and deliberately
answers \emph{only}, the next question: when can an analyst convict inconsistency across a link that was \emph{never} checked?
The answer is a sheaf-consistency statement with a sharp topological boundary. Model the gossip graph with a cellular sheaf whose
restriction maps are shared-prefix coordinate selections, and compute the least-squares \emph{completion residual} $r$ of the
compared-plus-relayed data. Then (i) detection beyond pairwise comparison occurs iff the unchecked edge lies on a cycle whose
endpoint reports are relayed to the analyst --- on a cut edge $r=0$ by algebra, and across a truly severed edge equivocation is
provably dark; (ii) $r>0$ is sound: no global history explains the visible data, and \emph{any} offset pattern consistent with it
has norm at least $r$, with equality achieved --- $r$ is a certified lower bound on the injected lie, in closed form
$r=|s|\sqrt{1-R_{\mathrm{eff}}(e)}$ for a single-edge lie (the harness's $1.2247$ \emph{is} $3\sqrt{1-5/6}$); (iii) the residual
localizes to cycles through the equivocator, and computes in $\widetilde{O}(|E|\cdot L)$ via per-coordinate graph Laplacians. The
scope discipline is the signature: single equivocator (coalitions on a cycle cancel to zero, measured at $6\times10^{-15}$), no
attribution ever (signatures attribute; cohomology localizes), and vanishing $r$ is never an all-clear. A pre-registered
statistical harness returned verdict COMMIT with the cut-edge silence at float epsilon and the severed arm at $0/200$.
\emph{Express lane: the one-breath sentence opens each section; the formal statement is in the box below it; a reader in a hurry
needs nothing else.}
\end{abstract}

\section{The problem: a lie across a link nobody checked}\label{sec:problem}

\textbf{The scene.} Six relays in a federation gossip the heads of their signed witness logs. Adjacent relays cross-check:
each pair compares the log prefix they share, signs the comparison, and forwards what it heard. Relay $q$ equivocates --- it
shows one head to its clockwise neighbor and a different head to its counterclockwise neighbor --- and it is careful: the one
link on which its two stories would collide is exactly the link that is never directly compared, because those two neighbors
never reconcile. Every pairwise check in the system passes. The auditor's table of signed comparisons is green. Has the
federation lost?

This is the equivocation problem in its federated form: not ``did two conflicting signed statements land on one desk''
(that is classical fork detection, and signatures settle it), but ``can the \emph{pattern} of everyone else's consistent
reports convict an inconsistency that no single comparison witnessed?'' The Harbor program's ledger scoped result R6 to
precisely this question, and the scope discipline is the paper's signature: we claim detection --- a certificate that no
single global history explains the visible reports --- on an exactly characterized set of topologies and visibility patterns,
and we prove darkness everywhere else, rather than letting the word ``cohomology'' imply more.

\textbf{Express lane.} One sentence per result, formal statements in the boxes. Detection beyond pairwise comparison happens
iff the unchecked edge lies on a cycle and its endpoints' reports are relayed to the analyst --- cut edges are provably
silent, severed edges provably dark (box in \S\ref{sec:mechanism}); the completion residual $r$ is a certified lower bound on
the lie any adversary must have injected, with closed form $r=|s|\sqrt{1-R_{\mathrm{eff}}(e)}$ (box in \S\ref{sec:radius});
the residual localizes to cycles through the equivocator and costs at most $L$ scalar Laplacian solves (same box); and the
statistical harness that validated all of it was pre-registered and returned COMMIT (box in \S\ref{sec:harness}).

\onebreath{The idea in one breath: honest gossip is a global section, so the disagreement data of an honest world is a
coboundary --- and around every cycle a coboundary must sum to zero, which lets relayed-but-never-compared reports convict an
equivocator exactly when the missing comparison closes a loop, and provably never otherwise.}

\textbf{The analogy, mapped by relations.} Wristwatches around a table (Figure~\ref{fig:r6rel}): each adjacent pair reports
the \emph{offset} between their watches, never the absolute times. If everyone is consistent, the offsets around the full
table telescope to zero --- there exists a global assignment of times inducing them. A nonzero loop-sum convicts someone of
showing different times to different neighbors, \emph{without knowing any absolute time and without the missing pairwise
check}: the rest of the loop substitutes for it. The relation that transfers, not the surface: loop-sum $=$ cocycle
condition; missing comparison on a loop $=$ unchecked edge on a cycle; and --- the misread to preempt --- a watch at the end
of a \emph{line} of tables can lie freely, because no loop passes through it: the telescoping argument needs the loop, and so
does the theorem.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.92\textwidth]{r6_relation.png}
\caption{Relation-map for the mechanism. Wristwatch offsets around a table map to signed-log
disagreements around a gossip cycle by \emph{relations}: loop-sum $\leftrightarrow$ cocycle condition; a loop through the
missing link $\leftrightarrow$ a cycle through the uncompared edge. The map predicts, correctly, where the mechanism dies:
no loop, no telescoping, no detection.}
\label{fig:r6rel}
\end{figure}

\section{The model and the observability contract}\label{sec:model}

\textbf{The sheaf.} Fix a finite gossip graph $G=(V,E)$. Witness $v$ carries a local log state $x_v\in\mathbb{R}^{d_v}$ (its
stalk). Each edge $e=\{u,v\}$ carries the \emph{shared prefix} the two endpoints both report --- a subset $S_e$ of
coordinates, $|S_e|\le L$ --- and the restriction maps $\rho_{v,e}:\mathbb{R}^{d_v}\to\mathbb{R}^{|S_e|}$ are
coordinate-subset selections. The observable on edge $e$ is the disagreement
$g_e=\rho_{u,e}(\text{$u$'s report})-\rho_{v,e}(\text{$v$'s report})$. Honesty means all reports restrict one global section
$x$: then $g=\delta x$ is a coboundary of the sheaf coboundary $\delta$, and every cycle-sum of $g$ vanishes. Equivocation is
an offset pattern $\varepsilon$ added to $q$'s per-neighbor reports; it is invisible pairwise on any edge not directly
compared, and the question is what survives in $H^1$ --- the obstruction space $\operatorname{coker}(\delta)$, which for
prefix restrictions decomposes per shared coordinate $c$ into the cycle spaces of the coordinate subgraphs $G_c$
[verified framework, \cite{curry,hansen-ghrist}].

\textbf{Three-tier visibility --- the observability contract.} Not every edge is equally visible to the analyst, and the
paper's claims are stated \emph{per tier} (Figure~\ref{fig:vis}):
\begin{itemize}
\item \textbf{compared} --- the analyst holds the direct cross-check of the edge: pairwise detection, no cohomology needed;
\item \textbf{relayed} --- both endpoints' reports about the edge reach the analyst by gossip, but the edge itself was never
checked by its endpoints: this is the tier where cohomology earns its keep, and only on cycles;
\item \textbf{severed} --- no report about the edge crosses to the analyst at all: the corresponding data block is a free
variable of the completion, and equivocation confined there is \emph{provably dark}.
\end{itemize}

\textbf{The detector.} The \emph{completion residual} is the least-squares defect of the best global explanation of
everything visible:
\[
r \;=\; \min_{x,\;g_{\mathrm{sev}}}\ \bigl\|\,g_K-(\delta x)\big|_K\,\bigr\|_2,
\]
the minimum over global sections $x$ and free severed blocks $g_{\mathrm{sev}}$, where $K$ is the compared-plus-relayed
edge set. Honest executions give $r=0$ exactly, for every visibility pattern; $r>0$ certifies that \emph{no} global history
explains the visible data. Equivalently $r=\|\Pi_K\,g_K\|$, the projection of the visible disagreement data onto the
per-coordinate cycle spaces: $r^2=\sum_c\|\mathrm{Proj}_{\mathrm{cycle}(G_c)}\,g^c\|^2$
[internal, \texttt{sheaf\_harness\_v2.py}].

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{paper7_visibility.png}
\caption{The three-tier observability contract. Left: one gossip graph wearing all three tiers ---
compared (solid), relayed (dashed), severed (dotted). Right: what each tier lets the completion residual prove. The
relayed tier splits by topology: on a cycle, $r>0$ detects ($200/200$); on a cut edge, $r=0$ by algebra (max residual
$1.5\times10^{-13}$ over 400 trials); severed blocks are free variables and provably dark ($0/200$)
[internal, \texttt{sheaf\_harness\_v2.py}].}
\label{fig:vis}
\end{figure}

\textbf{Two defects that taught the model.} The first harness (v1) got both halves of this contract wrong, and the rebuild
kept the autopsies as named defects with reintroduction mutants. \textbf{D1:} v1 drew \emph{random orthonormal projections}
as restriction maps; for those, $\operatorname{coker}(\delta)$ generically collapses to $0$ and the signal dies --- the
detector only exists because shared-prefix restrictions make the cokernel isomorphic to
$\bigoplus_c H^1(G_c)$, cycle space tensor shared coordinates. The rebuilt harness refuses D1's reintroduction by a
structural self-check (it observed $\operatorname{coker}=0$ against $\beta_1=7$ and aborted). \textbf{D2:} v1 scored
``detections'' on cut and severed edges --- by silently reading hidden-edge data the analyst was not entitled to; the rebuilt
harness flags D2's reintroduction because the mutant's ``partition'' residuals come out bit-identical to full visibility
[internal, \texttt{sheaf\_harness\_v2.py}]. Both defects are the same lesson: the theorem is about \emph{what the analyst
can see}, and an implementation that muddles the observability contract will manufacture either silence or false sight.

\section{The mechanism: detection iff the missing edge closes a cycle}\label{sec:mechanism}

\onebreath{Cohomology detects equivocation beyond pairwise comparison exactly when the uncompared
edge lies on a cycle whose reports are relayed --- the cocycle condition substitutes for the missing comparison; on a cut
edge there is nothing to substitute, and the residual is zero by algebra --- formal statement in the box below.}

\begin{thebox}
\textbf{Theorem 1 (mechanism).} In gossip of signed logs with prefix restrictions and the three-tier visibility model, the
completion residual detects equivocation beyond pairwise comparison iff the unchecked edge lies on a cycle of its coordinate
subgraph \emph{and} its endpoints' reports are relayed to the analyst: then $r>0$ proves no global explanation exists. On a
cut edge $r=0$ identically --- the coboundary map of a tree (or forest) is surjective onto the visible data, so the free
completion absorbs any lie. Across a severed edge, equivocation is provably dark: the severed block is unconstrained, and a
consistent counterfactual world always exists.

\textbf{Verification (minimal pair).} $C_6$ with scalar stalks, lie of size $3$ on the one uncompared cycle edge: every
pairwise check passes, harmonic signal $1.225\ne 0$. The same lie on a bridge of the path $P_6$: signal $0.000$
[internal, \texttt{sheaf\_mechanism\_proof.py}]. The structural identity behind the mechanism --- local consistency with no
global section --- is Abramsky--Brandenburger contextuality [verified framework, \cite{abramsky-brandenburger}]; the
equivocator manufactures for its neighbors exactly a locally-consistent, globally-unglueable family of views.
\end{thebox}

\textbf{Numbers by hand.} On $C_6$, project the lie onto the cycle: the harmonic component of a single-edge offset $s$ on an
$n$-cycle is $s/\sqrt{n}$, so the signal is $3/\sqrt{6}=1.2247$ --- the harness's $1.225$ to printed precision, and, in the
closed form of \S\ref{sec:radius}, $3\sqrt{1-5/6}$ [verified]. On $P_6$ there is no cycle: the cycle space is zero, the
projection is zero, the residual is zero --- not small, zero (Figure~\ref{fig:r6reg} puts the relayed-cycle, cut, and
severed cases side by side on the same six-node graph). \emph{Now you try:} $C_8$, lie of size $2$ on the relayed
edge --- the residual is $2/\sqrt{8}=0.7071$; the same lie on any bridge, $0$ [verified].

\textbf{The misread to preempt.} ``Cohomology sees hidden data'' is exactly what the theorem does \emph{not} say. The
residual reads only relayed evidence, and squeezes it through the one constraint honesty imposes --- cycle-sums vanish. Where
no cycle passes (cut edge) or no evidence arrives (severed edge), there is no constraint to violate and nothing is seen;
the harness's cut-edge maximum of $1.5\times10^{-13}$ over 400 trials is float epsilon, not a small detection
[internal, \texttt{sheaf\_harness\_v2.py}]. A second misread: the detection statistic is the \emph{data} residual, never
$\dim H^1$ --- a loopy topology has $\beta_1>0$ for purely topological reasons on honest data too, so any counting claim
must net $\beta_1$ out; the harness prints the structural cokernel separately (e.g.\ the two-path split-view topology:
$\beta_1=1$, $\operatorname{coker}=2$) and scores only the residual, which is identically zero for honest worlds.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.95\textwidth]{r6_regime.png}
\caption{Regime diagram for the mechanism: (a) relayed cycle edge --- residual $1.225$, detected
($200/200$ cohomology-only); (b) cut edge --- residual $0.000$, provably silent; (c) severed cycle edge --- provably dark
($0/200$). Detection requires the cycle \emph{and} the relayed reports; each panel is one clause of the observability
contract [internal, \texttt{sheaf\_harness\_v2.py}].}
\label{fig:r6reg}
\end{figure}

\begin{boundary}
\textbf{Honest boundary.} Detection requires the reports, not the check: severed links stay dark, cut edges silent, and the
v1 ``detections'' there were D2 artifacts. Vanishing signal over $\mathbb{R}$ is not an all-clear --- real coefficients
abelianize the obstruction, and contextual models exist whose cohomological invariant vanishes (the Car\`u gap
\cite{caru}); \S\ref{sec:boundary} prices this. And the mechanism convicts an inconsistency; it does not name its author
(\S\ref{sec:radius}, \S\ref{sec:boundary}).
\end{boundary}

\section{The harness: pre-registered gates, verdict COMMIT}\label{sec:harness}

\onebreath{Before running, the harness committed to its gates: the completion residual must detect
split-view equivocation on relayed cycles where pairwise comparison is blind, and must stay silent on honest worlds, cut
edges, and severed arms --- it passed, the rival CUT condition was refuted, and both v1 defects are guarded by mutants ---
verification in the box below.}

\textbf{The design.} Two hundred trials per arm at seed 20260816, stalks $\mathbb{R}^5$, shared-prefix sizes 2--4. The
detector is the completion residual of \S\ref{sec:model}; the score is \emph{cohomology-only} detections --- trials where
the residual fires and every pairwise comparison passes. The pre-registered Stage-1 gates from the rescued prototyping plan:
(i) the split-view arm must be detected cohomology-only; (ii) the control arms must be silent.

\begin{thebox}
\textbf{Harness results (all arms, seed 20260816).} Partition-on-a-cycle split view (\texttt{two\_path}): $200/200$
cohomology-only --- pairwise blind in every trial. Severed-cycle arm: $0/200$ --- the honest boundary, measured. Cut edge
(\texttt{single\_bridge}): max residual $1.5\times10^{-13}$ over 400 trials --- silence at float epsilon. Full visibility:
$0$ cohomology-only detections and $200/200$ redundant (pairwise catches everything first, as it must). Expander with 3
severed and 3 relayed edges: partition-straddling equivocator $113/200$ cohomology-only, $87$ dark --- both clauses of the
contract live in one topology. Localization: the max-residual edge lies on a known-graph cycle $200/200$ (upgraded to
``through the equivocator'' as a theorem in \S\ref{sec:radius}). Pre-registered gates (i) and (ii) \textbf{PASS}; verdict \textbf{COMMIT}. The rival CUT condition --- ``residual
detections are just pairwise detections in disguise'' --- is refuted: $313$ of $1101$ residual detections across arms were
pairwise-blind. Mutation suite: D1 reintroduction refused by the structural self-check ($\operatorname{coker}=0$ against
$\beta_1=7$); D2 reintroduction flagged (mutant ``partition'' residuals bit-identical to full visibility);
no-equivocator control $0/200$ [internal, \texttt{sheaf\_harness\_v2.py}].
\end{thebox}

\textbf{What it buys.} The W7 mechanism proof was a two-topology existence demonstration; the harness upgrades it to a
validated statistical detector with its false-positive behavior measured (honest and control arms identically zero), its
blindness measured (severed arm $0/200$, the expander's $87$ dark trials), and its guards regression-tested by
reintroducing the exact defects that fooled v1. The three-tier contract stopped being prose and became the experiment's
arm structure.

\begin{boundary}
\textbf{Honest boundary.} The harness validates the detector at the sharpened scope only: synthetic gossip, prefix
restrictions, single equivocator per trial, $\mathbb{R}^5$ stalks. The $113/200$ expander number is a property of that
topology's severed/relayed mix, not a universal rate. And a statistical PASS is not yet a theorem --- which is exactly why
\S\ref{sec:radius} exists.
\end{boundary}

\section{The consistency radius: what $r>0$ certifies, where it points, what it costs}\label{sec:radius}

\onebreath{The residual $r$ is the exact minimum lie any adversary must have injected to produce the
visible data, it localizes to cycles through the equivocator, and it computes as at most $L$ scalar graph-Laplacian
solves --- with the single-equivocator condition proved to be the honest scope, because coalition lies on a cycle can
cancel $r$ to zero --- formal statements in the box below.}

\textbf{Intuition.} Robinson's consistency radius asks how far local data sits from the nearest global section
\cite{robinson}; our $r$ is its least-squares form under the completion over severed blocks. The new content is that $r$ is
not merely a distance --- it is a \emph{certificate about the adversary}: every offset pattern that could have produced the
visible data is at least this large, and one of exactly this size exists. The topology enters through electrical network
theory: a lie on edge $e$ hides in proportion to how well the rest of the graph can imitate it, which is the effective
resistance $R_{\mathrm{eff}}(e)$ --- on a bridge $R_{\mathrm{eff}}=1$ and the lie hides completely; on a long cycle
$R_{\mathrm{eff}}\to1$ and the certified fraction $\sqrt{1-R_{\mathrm{eff}}}$ decays like $1/\sqrt{n}$
(Figure~\ref{fig:radius}).

\begin{thebox}
\textbf{Theorem CR-1 (soundness and achievability).} For the completion-residual detector with prefix restrictions and
three-tier visibility: $r>0$ iff no global section explains the compared-plus-relayed data, and honest executions give
$r=0$ exactly, for every visibility pattern. Every offset pattern $\varepsilon$ consistent with the visible data satisfies
$\|\varepsilon_K\|\ge r$, with equality at the completion --- $r$ is the exact minimum lie. For a single equivocator $q$
with offset vector $o$ and visible-lie operator $A_q$:
$\sigma^{+}_{\min}(\Pi_K A_q)\,\|o_\perp\|\le r\le\|o_\perp\|$, where $o_\perp$ is the component of $o$ outside
$\ker A_q$. For a single-edge lie of size $s$ in coordinate $c$,
\[
r \;=\; |s|\,\sqrt{1-R^{G_c}_{\mathrm{eff}}(e)}
\qquad\text{--- on } C_n:\ |s|/\sqrt{n};\ \text{the harness's }1.2247\ \emph{is}\ 3\sqrt{1-5/6}.
\]

\textbf{Theorem CR-2 (localization).} Noise-free, single equivocator: the support of the edge residual lies on cycles of
the coordinate subgraphs through $q$ --- by the electrical-flow support argument, the harness's $200/200$ localization is
now a theorem. With bounded noise $\eta$, localization is guaranteed while
$2\|\eta_K\|<\max_f\|(\Pi_K\varepsilon_K)_f\|$.

\textbf{Theorem CR-3 (complexity).} Because $\delta_K$ splits per shared coordinate into graph incidence operators, $r$ is
one sparse least-squares problem that decomposes into at most $L$ scalar graph-Laplacian systems:
$\widetilde{O}(|E|\cdot L\log(1/\epsilon))$ with SDD solvers \cite{spielman-teng}. (Bach's \#P-hardness of sheaf cohomology
\cite{bach} concerns coherent sheaves on projective space --- the wrong category; finite cellular sheaves over $\mathbb{R}$
on graphs are linear algebra.)

\textbf{Verification.} Soundness $0/600$ violations; achievability $600/600$ (a consistent pattern of norm exactly $r$
constructed every time). The $R_{\mathrm{eff}}$ closed form is exact on $C_4$--$C_{12}$ and 200 random graphs; the
$\sigma^{+}_{\min}$ lower bound is tight under the adversarial offset ($r/\|o_\perp\|=\sigma^{+}_{\min}=0.4082$); a
uniform kernel lie of norm $5$ gives $r=2\times10^{-14}$ --- invisible in principle, correctly not flagged. Localization
$200/200$ (two-cycles topology) plus $128/128$ (severed expanders); noise boundary measured: $\ge95\%$ localization up to
$\sigma_\eta/\sigma_{\mathrm{eq}}\approx0.1$, $76.5\%$ at $0.2$; sufficient-condition violations $0$. Timing slope versus
$|E|$: conjugate gradient $0.65$, dense solve $1.88$. Two coordinated liars on a common cycle: each alone certified at
$r\ge|s|/\sqrt{8}$, the coalition cancels to $r=6\times10^{-15}$. The D2-unmasked mutant fires on a severed lie at
$0.839$ --- exactly the full-visibility value, caught [internal, \texttt{sheaf\_consistency\_radius.py}].
\end{thebox}

\textbf{Numbers by hand.} $C_6$: $R_{\mathrm{eff}}$ of an edge is $\tfrac{n-1}{n}=\tfrac56$ (the rest of the cycle is a
5-edge series path in parallel with the edge), so a lie of $3$ certifies $r=3\sqrt{1/6}=1.2247$ [verified] --- the number
the mechanism script printed a week before the closed form existed. The $\sigma^{+}_{\min}$ tightness value $0.4082$ is
$1/\sqrt{6}$: the same geometry seen from the operator side [verified]. \emph{Now you try:} a federation ring of $12$
relays with one never-compared link and a head-offset lie of $0.5$ --- the certified minimum lie is
$0.5/\sqrt{12}=0.144$; double the ring and the certificate shrinks by $\sqrt2$: long cycles dilute conviction, which is an
argument for short reconciliation loops in federation design.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.9\textwidth]{paper7_radius.png}
\caption{The CR-1 closed form: certified residual per unit lie, $r/|s|=\sqrt{1-R_{\mathrm{eff}}(e)}$,
against the effective resistance of the lied-on edge in its coordinate subgraph. The cycle family $C_n$ sits at
$R_{\mathrm{eff}}=\tfrac{n-1}{n}$ (the harness's $C_6$ point marked, $3\sqrt{1-5/6}=1.2247$); the cut-edge corner
$R_{\mathrm{eff}}=1$ is provable silence [verified; closed form exact on $C_4$--$C_{12}$ and 200 random graphs,
\texttt{sheaf\_consistency\_radius.py}].}
\label{fig:radius}
\end{figure}

\textbf{What it buys.} The harness's statistic is upgraded to a certificate with an interpretation a federation operator
can act on: $r$ is the smallest lie consistent with what you saw, the hot edges lie on cycles through the culprit's
neighborhood, and the whole computation is a handful of Laplacian solves --- cheap enough to run on every gossip round.
The closed form also converts topology into policy: effective resistance is the price list for where a federation should
force direct reconciliation (bridges: mandatory --- the residual cannot help there) and where relayed gossip suffices
(short cycles: cheap certified detection).

\section{Related work: imported, adjacent, and new}\label{sec:related}

\textbf{Imported.} The structural identity --- local consistency without a global section --- is Abramsky--Brandenburger
contextuality \cite{abramsky-brandenburger}; the equivocator's split view is an empirical model that is locally consistent
and globally unglueable, and we use the sheaf language exactly as that program fixed it. Cellular sheaves on graphs and
their coboundary calculus are Curry \cite{curry}; the sheaf Laplacian and its harmonic/spectral reading are
Hansen--Ghrist \cite{hansen-ghrist}. The consistency radius --- distance from local data to the nearest global section,
with its sup-norm corollaries --- is Robinson \cite{robinson}; our $r$ is its least-squares completion form. The
nearly-linear Laplacian solvers behind CR-3 are the Spielman--Teng line \cite{spielman-teng}.

\textbf{Adjacent, and honestly positioned.} Car\`u \cite{caru} shows the \v{C}ech-cohomological obstruction with
abelian coefficients can vanish on contextual models --- imported here as the reason vanishing $r$ over $\mathbb{R}$ is
never an all-clear. Sheng et al.'s BFT forensics \cite{sheng} works the attribution side: with signed protocol messages,
equivocation yields a transferable cryptographic proof \emph{naming} the culprit, and their impossibility results bound
when. We sit deliberately on the other side of that line: cohomology reads unsigned \emph{structure} --- it can convict
``someone on these cycles lied'' from evidence no signature covers (relayed, never-compared links), and can never name the
liar (\S\ref{sec:boundary}); signatures attribute, cohomology localizes, and a deployed federation wants both. Bach
\cite{bach} is cited only to preempt a category error: \#P-hardness of coherent-sheaf cohomology does not touch finite
cellular sheaves on graphs.

\textbf{New, honestly.} No component of the mathematics is exotic: incidence matrices, cycle spaces, effective resistance,
least squares. The contribution is the assembled result at its exact scope: (1) the mechanism boundary --- detection beyond
pairwise iff the missing edge closes a relayed cycle, with cut-edge silence and severed darkness proved rather than
observed; (2) the three-tier observability contract as the object the theorems quantify over, extracted from two named
defects that each faked a different clause of it; (3) the certificate reading of the completion residual --- $r$ as the
exact minimum injected lie, with the $R_{\mathrm{eff}}$ closed form tying conviction strength to federation topology; and
(4) the coalition-cancellation construction that fixes the honest scope at single-equivocator, turning what would be a
reviewer's counterexample into the paper's own boundary statement.

\section{The boundary is load-bearing}\label{sec:boundary}

This paper's claims are narrow by construction, and the narrowness is the result. Each clause below is a theorem-grade
limitation, stated at the same prominence as the theorems, because a federation that deploys this detector believing any
one of them false will be harmed by the belief.

\begin{boundary}
\textbf{Honest boundary --- the four clauses.}
\begin{itemize}
\item \emph{Single equivocator is the scope, and provably so.} Detection and localization are single-equivocator
statements. A coalition whose combined lie is a coboundary is a consistent counterfactual world: two coordinated liars on
a common cycle, each alone certified at $r\ge|s|/\sqrt8$, jointly cancel to $r=6\times10^{-15}$
[internal, \texttt{sheaf\_consistency\_radius.py}]. $r$ never overstates the lie, but it can understate a coalition's to
zero --- soundness is unconditional, detection is not.
\item \emph{No attribution, ever.} A $+o$ lie by $q$ toward $w$ and a $-o$ lie by $w$ toward $q$ produce identical
observations --- the identical-data twin construction. The residual localizes to cycles through the equivocator; it cannot
distinguish which endpoint of the offending relationship lied. Naming culprits is the signatures-and-forensics business
\cite{sheng}, which this detector complements and must never be marketed as replacing.
\item \emph{Vanishing $r$ is not an all-clear.} Three independent reasons: uniform (kernel) lies move every neighbor's
view identically and are invisible in principle ($\|o\|=5$, $r=2\times10^{-14}$ [internal]); severed edges admit
consistent counterfactuals by construction; and over $\mathbb{R}$ the obstruction is abelianized, so a vanishing
real-coefficient signal does not certify gluability in richer coefficient systems (the Car\`u gap \cite{caru}). $r=0$
means ``no conviction from this evidence,'' never ``honest.''
\item \emph{Severed darkness, and the noise floor.} Where no report crosses, the theorem is that nothing can be seen ---
budget accordingly (force comparison on bridges; keep reconciliation loops short). Under measurement noise, soundness
needs a threshold above the honest floor $\|\Pi_K\eta\|$: the measured localization boundary is $\ge95\%$ at noise-to-lie
ratio $0.1$ falling to $76.5\%$ at $0.2$ [internal], and below threshold the certificate reverts to ``consistent with
noise.''
\end{itemize}
\end{boundary}

The scope discipline pays twice. Inside the boundary the claims are sharp enough to certify (a lower bound on the lie, a
support statement, a solver cost); outside it the paper states its own counterexamples with witnesses, so the failure
modes are part of the artifact rather than awaiting discovery by an adversary who read the appendix more carefully than
the authors.

\section*{Reproducibility}

Every [internal] number regenerates deterministically at seed 20260816 from three self-contained scripts shipped with the
program's results estate: \texttt{sheaf\_mechanism\_proof.py} (the $C_6$/$P_6$ minimal pair: harmonic signal $1.225$
versus $0.000$), \texttt{sheaf\_harness\_v2.py} (all harness arms of \S\ref{sec:harness}, the pre-registered gates, the
D1/D2 reintroduction mutants, and the structural $\beta_1$/cokernel printout), and
\texttt{sheaf\_consistency\_radius.py} (CR-1/2/3: the $0/600$--$600/600$ soundness/achievability sweep, the
$R_{\mathrm{eff}}$ closed-form checks on $C_4$--$C_{12}$ and 200 random graphs, the localization and noise-boundary
measurements, the timing slopes, and the coalition-cancellation witness). Each script exits nonzero on any failed check.
Every [verified] number is recomputable by hand from the closed forms restated in the boxes. Figures regenerate from
\texttt{figures/src/} at the same seed.

\begin{thebibliography}{9}
\bibitem{abramsky-brandenburger} S.~Abramsky and A.~Brandenburger. The sheaf-theoretic structure of non-locality and
contextuality. \emph{New Journal of Physics}, 13:113036, 2011.
\bibitem{curry} J.~M. Curry. \emph{Sheaves, Cosheaves and Applications}. PhD thesis, University of Pennsylvania, 2014.
\bibitem{hansen-ghrist} J.~Hansen and R.~Ghrist. Toward a spectral theory of cellular sheaves. \emph{Journal of Applied
and Computational Topology}, 3:315--358, 2019.
\bibitem{robinson} M.~Robinson. Sheaves are the canonical data structure for sensor integration. \emph{Information
Fusion}, 36:208--224, 2017.
\bibitem{caru} G.~Car\`u. On the cohomology of contextuality. In \emph{Proc.\ 13th International Conference on Quantum
Physics and Logic (QPL 2016)}, EPTCS 236, pp.~21--39, 2017.
\bibitem{sheng} P.~Sheng, G.~Wang, K.~Nayak, S.~Kannan, and P.~Viswanath. BFT protocol forensics. In \emph{Proc.\ ACM
Conference on Computer and Communications Security (CCS)}, pp.~1722--1743, 2021.
\bibitem{spielman-teng} D.~A. Spielman and S.-H. Teng. Nearly-linear time algorithms for graph partitioning, graph
sparsification, and solving linear systems. In \emph{Proc.\ 36th ACM Symposium on Theory of Computing (STOC)},
pp.~81--90, 2004.
\bibitem{bach} E.~Bach. Sheaf cohomology is \#P-hard. \emph{Journal of Symbolic Computation}, 27(4):429--433, 1999.
\end{thebibliography}

\end{document}
