% Paper 8 Figure 3: The Rosetta Stone mapping swarm primitives to cellular sheaves.
% Native vector TikZ diagram conforming to docs/harbor-research/figures/CONVENTION.md.
% 100% native document fonts without artificial downscaling or overlap.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
  font=\small,
  sbox/.style={draw=harborblue!70, very thick, fill=white, rounded corners=4pt, inner sep=8pt, text width=4.5cm, align=left, anchor=north},
  carrow/.style={very thick, ->, harborblue, line width=1.8pt},
]

% 0-Cells
\node[sbox, text width=4.3cm] (box0) at (2.2, 0) {
  \textbf{\color{harborblue}\normalsize 0-Cells: AgentNodes ($v \in V$)}\\[5pt]
  \textbf{\small Stalk Vector} $x_v \in \mathbb{R}^D$:\\[3pt]
  $\bullet$ Turn Epoch $t_v \in \mathbb{N}$ (Clock)\\
  $\bullet$ Token Spend $b_v \in \mathbb{R}_+$ (Budget)\\
  $\bullet$ AST Lease Hash $h_L \in \mathbb{R}$\\
  $\bullet$ Commit Root $d_v \in [-1, 1]$
};

% 1-Cells
\node[sbox, text width=4.3cm] (box1) at (7.6, 0) {
  \textbf{\color{harborblue}\normalsize 1-Cells: Channels ($e \in E$)}\\[5pt]
  \textbf{\small Restriction Map} $P_e: \mathbb{R}^D \to \mathbb{R}^d$:\\[3pt]
  $\bullet$ Coordinate-wise selection\\
  $\bullet$ \textbf{1-Cochain Discrepancy}:\\
  \quad $g_e = P_e x_v - P_e x_u$\\[2pt]
  $\bullet$ \textbf{Coboundary}: $(\delta_0 x)_e = x_v - x_u$
};

% 2-Cells
\node[sbox, text width=4.3cm] (box2) at (13.0, 0) {
  \textbf{\color{harborblue}\normalsize 2-Cells: Review Triads ($\tau \in F$)}\\[5pt]
  \textbf{\small Consensus Join Contract}:\\[3pt]
  $\bullet$ Producer $\leftrightarrow$ Critic\\
  $\bullet$ Critic $\leftrightarrow$ Manager\\
  $\bullet$ Manager $\leftrightarrow$ Producer\\
  $\bullet$ \textbf{Triadic Curl}:\\
  \quad $(\delta_1 g)_\tau = g_{01} + g_{12} - g_{02}$
};

% Cochain arrows between boxes
\draw[carrow] (box0.east |- 0, -1.8) -- node[above=2pt, font=\normalsize\bfseries, color=harborblue] {$\delta_0$} (box1.west |- 0, -1.8);
\draw[carrow] (box1.east |- 0, -1.8) -- node[above=2pt, font=\normalsize\bfseries, color=harborblue] {$\delta_1$} (box2.west |- 0, -1.8);

% Cochain complex banner below (generous clearance with anchor=north at y = -5.2)
\node[draw=slate!40, fill=slate!5, rounded corners=4pt, thick, inner sep=7pt, text width=14.8cm, align=center, anchor=north] at (7.6, -5.2) {
  \textbf{\normalsize Discrete Cochain Complex on Simplicial 2-Complex $X = (V, E, F)$}:\\[4pt]
  $\displaystyle 0 \longrightarrow C^0(X; \mathcal{F}) \xrightarrow{\quad\delta_0\quad} C^1(X; \mathcal{F}) \xrightarrow{\quad\delta_1\quad} C^2(X; \mathcal{F}) \longrightarrow 0$\\[4pt]
  {\small \textbf{Fundamental Nilpotent Identity}: $\delta_1 \circ \delta_0 \equiv 0 \iff \mathrm{im}(\delta_0) \subseteq \ker(\delta_1)$ (honest agent potential fields automatically satisfy all triadic contracts with zero curl).}
};

\end{tikzpicture}
\caption{The Rosetta Stone: exact algebraic translation from multi-agent swarm primitives into cellular sheaves and simplicial complexes over cell complex $X = (V, E, F)$.}
\label{fig:sheaf-rosetta}
\end{figure}
