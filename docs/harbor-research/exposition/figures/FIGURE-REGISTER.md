# Figure register

This is the full list of what the book has, and what it could or should render,
for pedagogical clarity — one row per idea that earns a visual, whether or not
a figure for it exists yet. It is medium-agnostic: a row names the *structure*
a picture would need to carry and the *reader question* it answers, not a TikZ
spec. The author renders each row outside TikZ, in whatever medium fits (a
plot from a script's own output, a hand-drawn diagram, a table, a terminal
transcript); this register does not draw anything and does not prescribe a
drawing tool.

The register has three layers, built in three passes:

1. **The skeleton** — the reading contract (the row schema below) and the
   candidates the book's own plan already named, before anyone enumerated a
   chapter page by page.
2. **The enumeration pass** — a full page-by-page sweep of each chapter,
   populating the eight chapter sections below with one row per idea that
   could earn a figure.
3. **The reconciliation pass** — every plan candidate matched to the row that
   covers it, or marked `not enumerated` if the sweep missed it, plus a
   summary table.

All three passes are done. The enumeration below counts 378 rows across eight
chapters, run 2026-09-07. Chapter 3 is a known exception — see the note under
its heading.

## Register summary

Totals from the enumeration pass, 2026-09-07. "Folded" rows (1, `ch2-34`)
duplicate an adjacent row's figure and carry no independent priority; they
are excluded from the must/should/could/no columns but counted in rows.

| chapter | rows | must | should | could | no | existing figure | none yet | most under-served idea |
|---|---|---|---|---|---|---|---|---|
| 1 — The Single-Writer Kernel | 55 | 13 | 10 | 13 | 19 | 18 | 37 | The "gate the channel, never the token" two-state taint automaton (`ch1-25`) has no figure at all, only prose, despite being the critical mechanism behind the sealed room. |
| 2 — The Anchor Protocol | 41 | 8 | 15 | 7 | 10 (+1 folded) | 12 | 29 | The v6 multi-hop escalation attack vs. its v7 per-hop fix (`ch2-35`) — the chapter's sharpest result — has no chain diagram, only prose and a terminal listing. |
| 3 — The Sealed Harbor | 23 | 8 | 9 | 4 | 2 | 9 | 14 | The laundering/escape-hatch residual (`ch3-12`) — named three separate times in the chapter — is drawn nowhere. |
| 4 — The Legible Swarm | 62 | 13 | 14 | 8 | 27 | 13 | 49 | The two-constraint digest-rate and adaptive-halving theorem pair (`ch4-32`, `ch4-33`) — the chapter's most numerically worked, fully proved result, with a measured 15.3× reduction — has zero figure. |
| 5 — From Spawn to Person | 41 | 6 | 16 | 5 | 14 | 22 | 19 | The engine-swap adverse-selection theorem and its attested flip (`ch5-21`, `ch5-22`) has zero visual. |
| 6 — The Harbor Economy | 40 | 4 | 16 | 4 | 16 | 22 | 18 | The deontic-conflict fragment's polynomial-vs-NP-complete dichotomy (`ch6-09`) — the chapter's only complexity-theory result — has no visual at all. |
| 7 — The Bonded Commons | 58 | 7 | 13 | 17 | 21 | 19 | 39 | The TLA+ lifecycle Begin/Claim/Commit/Reap/Salvage/Dismiss (`ch7-46`) — the chapter's own formal backbone — is given only as code and prose, never a state diagram. |
| 8 — The Federated Harbor | 58 | 5 | 10 | 12 | 31 | 26 | 32 | The chapter's own flagship worked example, Acme/Beta/staging across three harbors (`ch8-39`), has no figure at all — despite the Reader's Map telling beginners to "look at the five figures in order" for exactly this material. |
| **total** | **378** | **64** | **103** | **70** | **140 (+1 folded)** | **141** | **237** | — |

Counts by structure (all eight chapters):

| structure | rows | structure | rows |
|---|---|---|---|
| comparison | 103 | relational | 52 |
| quantitative | 67 | stateful | 36 |
| temporal | 42 | provenance | 33 |
| allocation | 23 | containment | 22 |

## How to read a row

Every row in this register — once the enumeration pass fills the chapter
sections in — follows this schema:

| id | section | idea (one sentence) | structure | reader question | recommended form | existing figure | wave-11 disposition | priority | data source | notes for the renderer |
|---|---|---|---|---|---|---|---|---|---|---|

Column meanings:

- **id** — a short stable identifier for the row (e.g. `swk-07`), so later
  passes and cross-references can point at it without repeating its sentence.
- **section** — the chapter section (and, where useful, the subsection) the
  idea lives in.
- **idea (one sentence)** — the single claim or fact the figure would carry,
  stated as one sentence a reader could quote back.
- **structure** — the shape of the information itself, drawn from a fixed
  vocabulary so rows are comparable across chapters:
  `temporal` (something unfolds over time or steps), `stateful` (a machine
  with states and transitions), `quantitative` (a measured or computed
  number, curve, or region), `relational` (entities and the relations between
  them), `containment` (one thing nested or bounded inside another),
  `provenance` (an evidentiary or lineage trail), `allocation` (a resource or
  authority divided among claimants), `comparison` (two or more things placed
  side by side to be judged against each other).
- **reader question** — the question a reader would have on this page that
  the figure, and only the figure, answers.
- **recommended form** — the concrete rendering the row is asking for (e.g.
  "xy plot, log axis", "state machine, 6 states", "booktabs table"),
  medium-agnostic — no TikZ library or drawing tool named.
- **existing figure** — the `\label` of the figure that already covers this
  idea today, or `none` if nothing does yet.
- **wave-11 disposition** — the verdict this idea's existing figure received
  in `docs/harbor-research/exposition/figures/FIGURE-TRIAGE.md`
  (keep / restyle / redraw / table / delete / add), or `n/a` for an idea the
  Wave 11 triage never looked at.
- **priority** — how much the book needs this figure to exist:
  `must` (the page does not work without it), `should` (the page is
  meaningfully weaker without it), `could` (a nice-to-have that would help a
  slower reader), `no` (prose already carries the idea; a figure would only
  decorate it).
- **data source** — for a quantitative row, the script, proof artifact, or
  log that produces the numbers; `n/a` for a row with no measured content.
- **notes for the renderer** — anything the author needs to know before
  drawing it: a worked-point coordinate to preserve, a caption obligation, a
  cross-reference to a shared figure elsewhere in the book, a pitfall the
  Wave 11 triage already found.

## Candidates named in the plan before the enumeration

These are the figures the book's own plan already called out by name, before
any page-by-page sweep. The **covering row** column is the reconciliation
pass's output: the enumeration row that carries this idea, or `not
enumerated` if the sweep never produced one. Two candidates turned up in a
different chapter than the plan guessed — the enumeration corrected the
plan, not the other way around; the note says so.

| candidate | chapter (as named in the plan) | recommended form | covering row |
|---|---|---|---|
| The two-run lockstep picture: two runs identical except the secret, evaluated for noninterference | The Sealed Harbor | comparison — side-by-side run pair with equality marks at each step | `ch3-11` (`fig:sealed-two-worlds`, already keep) |
| The interleaving lattice for the release ledger: every concurrent interleaving of the atomic append-and-add still conserves the spend bound | The Sealed Harbor | relational — a lattice/DAG over interleavings with the conserved quantity annotated at each node | `ch3-15` (partial — enumerated as a release-by-release ledger table, not literally a lattice; see also `ch3-16`) |
| The mint-attack fork DAG: a reputation lineage forked by a copy attempt, and the no-mint rule's refusal | From Spawn to Person | provenance — gitgraph-style DAG with the mint attempt and its rejection marked | `ch5-10` (`fig:stp-nomint-lineage`, already add-fulfilled) |
| The boundary surface g(ρ,c): the specialization threshold a sole specialist must clear to beat the pool | The Legible Swarm | quantitative — surface or region plot over (ρ, c) with the worked point marked | `ch4-18` (`fig:specialization`, already restyle) |
| The cycle-versus-cut consistency graph: equivocation is detectable exactly when the missing edge lies on a cycle, never on a cut | The Federated Harbor | relational — paired graphs (a cycle case and a cut case) with the consistency radius under each | `ch8-22` (`fig:fh-cycle-vs-cut`, already redraw) |
| The escrow and settlement state machine: an attempted act moves through escrow to a terminal settlement, with no partial-credit path | The Bonded Commons | stateful — state machine with the terminal transitions marked | `ch7-27` (no existing figure yet) |
| The Anchor handshake ladder: the pairing ceremony between operator, daemon, and verifier | The Anchor Protocol | temporal — sequence/ladder diagram naming what each message binds | **not enumerated** — the sweep drew the key hierarchy (`ch2-06`) and the ProVerif issuance/delegation/accept correspondence (`ch2-17`) instead, neither of which is the three-party pairing ceremony as named |
| The supervisory-control automaton with controllable and uncontrollable events | The Single-Writer Kernel | stateful — automaton with controllable events marked distinctly from uncontrollable ones | `ch1-25` (the ok/tainted taint automaton; see also the controllability quadrant `ch1-24`) |
| The inspection tower regime: sealed sampling from cliques makes bribery uneconomical above ρ* = G/(dB) | The Harbor Economy | quantitative — regime plot with the ρ* boundary and the worked point | `ch5-32` — **relocated**: the sweep placed R7's tower theorem in From Spawn to Person's grading-oracle section, not the Harbor Economy |
| The zoom tree with the measured 15.3× (adaptive zoom against a flat baseline) | The Legible Swarm | relational — tree diagram with the measured multiplier labeled at the root | `ch4-33` (exact match) |

Simulation-result plots, rendered from the repository's own scripts rather than sketched:

| candidate | chapter (as named in the plan) | recommended form | data source | covering row |
|---|---|---|---|---|
| The R1 zoom floor sweep | The Legible Swarm | xy plot, measured points | `skills/harbor-results/scripts/a7_experiment.py` | `ch4-62` (exact match; see also `ch4-30`) |
| The δ* sweep (claim-signaling discount-factor crossover) | The Bonded Commons | xy plot / TLC sweep table | `proofs/economics/sweep-delta.sh`, `proofs/economics/delta-threshold.z3` | `ch7-30` (exact match) |
| The ρ* deterrence frontier | From Spawn to Person | xy region plot with the worked point | `skills/harbor-results/scripts/b2_tower.py` | `ch5-31` (`fig:stp-deterrence-regime`, already keep) |
| The probation cliff | From Spawn to Person | xy plot, expected loss vs probation length | `skills/harbor-results/scripts/b6_probation.py` | `ch5-18` (`fig:stp-probation-cliff`, already add-fulfilled) |
| The escalation band | The Bonded Commons | xy plot / feasible-interval plot | `skills/harbor-results/scripts/b7_escalation_band.py` | `ch4-22` — **relocated**: the sweep found the b7 escalation-band script backing a Legible Swarm row (human-in-the-loop costly signaling), not a Bonded Commons one |
| The consistency radius over cycle length | The Federated Harbor | xy plot | `skills/harbor-results/scripts/sheaf_consistency_radius.py` | `ch8-24` (exact match) |
| The SPRT stopping-time histogram | The Sealed Harbor | histogram | `skills/harbor-results/scripts/a4_canary_sprt.py` | `ch3-19` (partial — the enumerated form is the existing expected-run-length bar pair on `fig:sealed-operating-curve`, not a separate stopping-time histogram) |

## Chapter 1 — The Single-Writer Kernel

Source: `whitepaper/single-writer-kernel.tex` (2931 lines; 13 `\input{figures/...}` fragments plus 2 in-body algorithm exhibits = 15 existing figure-like artifacts: 11 graphical figures, 2 terminal-session listings, 2 pseudocode algorithms).
Existing figures found: 15 (fig-swk-stack-map, -single-writer, -durability-faultclass, -claim-lifecycle, -marker-decay, -reference-monitor, -controllability-quadrant, -commitment-oracle, -continuity-organs, -workunit-machine, -consistency-model; session-swk-lock-demo, session-swk-workunit-check; alg:acquire, alg:close).
Rows: 55.
Three most under-served ideas: (1) the "gate the channel, never the token" two-state taint automaton that carries the sealed-room design rule has no figure at all, only prose (row ch1-25); (2) the macaroon-proves-but-does-not-compel layered enforcement gap — the paper's own "most consequential specified-not-implemented caveat" — is argued entirely in prose (row ch1-42); (3) the fencing-epoch mechanism that makes stale-holder effects rejectable is stated as a theorem and buried inside the work-unit machine but never shown as its own small sequence (row ch1-14).

| id | section | idea (one sentence) | structure | reader question | recommended form | existing figure | wave-11 disposition | priority | data source | notes for the renderer |
|---|---|---|---|---|---|---|---|---|---|---|
| ch1-01 | Where this paper sits, and what it promises | The kernel occupies the substrate rung of a four-layer coordination stack (substrate/coordination/legibility/economy) sitting on a machine floor. | containment | Which L0–L3 layers does the kernel own, and what does it assume below? | four-rung labeled stack with a machine-floor band | fig:swk-stack-map | redraw (block diagram, four rows each naming artifacts, dark rule for the floor) | must | — | Use the chapter's own organ names per row, not generic labels; bracket "this chapter" across the substrate rung only. |
| ch1-02 | A research-maturity scale, and why the grades matter | Four-point maturity grade (Built/BuiltWeak/Designed/Vision) plus two refinements (I1a/I1b, regimented/enforced) that no single grade can carry. | comparison | Which grade does a given claim in this chapter actually earn? | table-not-figure | none | — | no | — | Already a clean `tab:honestkey` booktabs table; a figure would only decorate an ordinal scale. |
| ch1-03 | The kernel as seven organs | Seven organs are disciplines over one shared SQLite/WAL commit history, each with a home table and a maturity grade. | containment | What contract does each organ hold, and is it one database or seven? | table-not-figure | none (tab:swk-seven-organs already a table) | table (fulfilled) | no | — | The one-WAL-history claim is a caption sentence, not a spine diagram; a matrix already carries the seven rows. |
| ch1-04 | The substrate organ: the single-writer discipline | Dozens of command-line and agent connections funnel through one daemon writer; SQLite's file lock is the backstop, not the primary mechanism. | temporal | How do many concurrent callers become one serial commit order? | converging-queue Gantt with a labeled second-writer boundary | fig:swk-single-writer | redraw (4 caller bands with drawn edges dropping to commits 1–4, dashed second-writer box) | must | — | Draw routing (not the file lock) as the thing doing the work; the file lock is a small dashed backstop box, not the main mechanism. |
| ch1-05 | The substrate organ: schema evolution (OP-7) | A boot-time migration ledger orders and verifies schema, but modules still self-initialize, so order is not yet authoritative. | comparison | What ships today vs. what a full `user_version` sequencer would add? | small two-row comparison table (shipped boot ledger vs. designed sequencer) | none | — | could | — | Two rows only: "ships" (ordered ledger + post-apply verification) vs "designed" (declared migrations under one sequencer); this is a maturity gap, not a mechanism worth a full diagram. |
| ch1-06 | Durability, split by fault class | The same committed write survives a process crash (I1a, Built) but may roll back on power loss (I1b, not guaranteed) under WAL/NORMAL. | quantitative | Which persistence guarantee answers which fault class? | fault-class-by-guarantee regime strip | fig:swk-durability-faultclass | redraw (two-row strip: fault class × sync setting, cells say "durable" / "may lose last commits") | must | — | This is the paper's headline correction; keep the correction named in the caption, not just implied by cell color. |
| ch1-07 | Durability: worked dramatization (Alice crashes mid-write) | Identical commit at $t_0$, identical fault 5ms later, but a killed daemon process preserves the write while a power failure may erase it. | temporal | Same event, two fault types — why does one survive and the other not? | two-lane timeline: t0 commit, t0+5ms fork into (process-kill → survives) / (power-loss → may vanish) | none | — | should | hand-worked in prose | Two short lanes sharing one commit point at t0 is enough; label the divergence at t0+5ms with "page cache survives" vs "not yet fsynced." |
| ch1-08 | Durability: Selective Checkpointing (OP-10) | A designed but unshipped per-write-path durability selector would let a settlement write force a full sync while claims stay fast. | allocation | Which write paths would opt into the stronger, slower guarantee? | small decision table: write-path kind → sync level chosen | none | — | could | — | Two rows suffice (ordinary claim → NORMAL; settlement-ledger write → FULL); state plainly that zero paths opt in today. |
| ch1-09 | Durability: a recovery story, not just a durability story | Boot-time WAL replay, integrity check, and audit-chain verification are three separate mechanisms not yet one named invariant. | temporal | What actually runs, in what order, when the daemon restarts after a crash? | small pipeline with a dashed box around "not yet unified" | none | — | could | — | Three boxes (replay → integrity check → chain verify) with the whole sequence dashed-outlined and captioned "closer to BuiltWeak than Built." |
| ch1-10 | The resource organ: claim granularity | Whole-file, line-range, and symbol-path claims are three granularities of one exclusion primitive, keyed differently. | containment | What varies across the three claim types, and what stays the same? | small 3-row comparison table (granularity × key components × example) | none | — | could | — | A table, not a diagram: rows are whole-file/line-range/symbol-path; columns are the key shape and one concrete example each. |
| ch1-11 | The resource organ: the claim lifecycle | A claim is FREE → HELD → EXPIRED with idempotent re-claim and lazy TTL sweep. | stateful | Which states can a claim enter, and how does it leave each one? | compact state machine, labels off the edges | fig:swk-claim-lifecycle | restyle (three states, four edges, no crossing labels) | must | — | Show idempotent re-claim as a self-loop on HELD, not a separate state; keep TTL sweep as the only route out of an unreleased HELD. |
| ch1-12 | The resource organ: claim acquisition algorithm | A single uniqueness-constraint insert is the whole mutex; the loser is handed the current holder's identity, not a bare error. | stateful | Which atomic decision selects the single winner, and what does the loser learn? | annotated pseudocode | alg:acquire | — (existing algorithm exhibit) | should | — | Already a figure-environment listing; keep as is, cross-reference from the claim-lifecycle figure rather than duplicating. |
| ch1-13 | The resource organ: worked dramatization (Alice and Bob race for a port) | Two simultaneous `acquire(7421)` calls serialize through the daemon; the loser's catch path reads back the winner's identity atomically. | temporal | Two racing requests, one winner — where exactly does the second learn who won? | two-lane race timeline: both inserts issued, one lane commits, the other's catch-path reveals the holder | none | — | should | hand-worked in prose (§resource) | Two lanes converging on one insert-commit point, with a small callout on the losing lane reading "DENIED(holder=Alice)" is the whole picture. |
| ch1-14 | The resource organ: fencing epochs reject stale holders | Every granted lease carries a strictly monotonic fencing epoch; a reassigned or paused holder's later effect is rejected by the epoch, not the lock. | temporal | How does a holder who is asleep past reassignment get blocked from acting? | small sequence: holder A (e=0) → reassign → holder B (e=1) → A's late effect rejected at the epoch check | none | — | should | proof sketch, Thm. exclusion; instantiated in the work-unit machine's "stale write" crime | This is the mechanism behind the work-unit machine's shortest 4-step crime; a standalone 3-actor sequence (A, reassignment, effect broker) makes it legible without the whole 536-state machine. |
| ch1-15 | The resource organ: capability vs. permission (able but forbidden) | An agent can be capable of an action (sandbox permits it) yet forbidden to take it (deontic layer disallows it), and the two must stay distinct. | relational | Can an agent be able to do something and still be forbidden to do it? | small 2×2 matrix: capable × permitted, with the override-flag example placed in the capable-but-forbidden cell | none | — | could | — | Only one cell needs a concrete example (the emergency-override tool flag); the other three cells can stay abstract labels. |
| ch1-16 | The resource organ: fair exclusion without a scheduler (OP-1) | A designed Stigmergic Ticket-Lock would add a queued state and FIFO grant-on-release, but the lazy TTL-sweep path stays unfair. | stateful | What would change about the claim lifecycle if a ticket queue existed, and where would it still fail? | extended state machine: FREE/HELD/QUEUED with grant-to-head on release, TTL-sweep path marked "not covered" | none | — | should | hand-designed (not shipped); `claim_tickets` schema in prose | Extend row ch1-11's state machine with one new QUEUED state and mark the TTL-sweep exit with a dashed "OP-1 open" flag rather than drawing a second full machine. |
| ch1-17 | The communication organ: bus and lineage | The bus (durable carrier) and the coordination lineage (what work is in flight) are two provenance traces over one ordered log, never to be conflated. | provenance | What does the carrier guarantee, and what does the lineage guarantee, separately? | table-not-figure | none | table (fulfilled) | no | — | Already `tab:swk-comm-organ`; a two-row table is the aligned matrix the atlas calls for, no spine ornament needed. |
| ch1-18 | The communication organ: at-least-once delivery and the idempotency gap | The bus routinely redelivers and reorders; benign redelivery must be deduplicated silently while genuine violations stay loud. | temporal | What does the operator see when a message is redelivered, with and without an idempotency key? | small sequence: request sent, redelivered once, consumer dedups silently vs. (absent today) idempotency-key check | none | — | should | — | Two short lanes: "today" (dedup by semantic effect only) and "with idempotency key" (explicit stored receipt); the gap is the missing key, flagged as coordination-layer debt. |
| ch1-19 | The communication organ: stigmergic marker decay | Marker weight decays exponentially per tick and at read time; a marker at $r=0.9$ survives 29 ticks, at $r=0.95$ survives 59. | quantitative | How long does a marker live before the prune threshold removes it, at two decay rates? | measured-quantity plot: weight vs. ticks, threshold as a rule, lifetimes marked | fig:swk-marker-decay | add (fulfilled) | must | hand-worked in pdexample (0.9^t arithmetic) | Already drawn; keep both curves and the 0.05 threshold rule with the 29/59 crossing ticks labeled. |
| ch1-20 | The communication organ: two delegation chains, one dangerous name | The authorization chain (cryptographic, who-authorized-whom) and the coordination lineage (task-shape, loop detection) are distinct objects; the lineage rides inside the chain. | containment | Which of the two "delegation chain" objects does a given claim in this chapter mean? | small nested-envelope diagram: outer = authorization chain (signed hops), inner = coordination lineage (task shape) | none | — | should | — | A single nested box is enough — outer envelope labeled "authorization chain (Built, ProVerif)" containing an inner labeled "coordination lineage (Designed)"; no need for a full delegation-chain figure since Anchor's chapter already owns that grammar. |
| ch1-21 | The obligation & enforcement organ: the deontic split | Prohibition, obligation, and permission are three modalities realized by three mechanisms (gate, oracle, grant), not one undifferentiated "policy." | comparison | Which mechanism enforces which modality, and how does each hold or fail? | table-not-figure | none | table (fulfilled per triage/atlas reconciliation) | no | — | Already `tab:swk-deontic-split`; the atlas's own reconciliation note confirms a taxonomy table beats a timeline here. |
| ch1-22 | The obligation & enforcement organ: the monitor is a detector | A post-commit policy monitor cannot make a bad state physically unreachable; it can only detect and compensate after the write already committed. | temporal | At what point does a forbidden write become irreversible, relative to when the monitor sees it? | two-row timeline: mediator gate (before effect) vs. monitor (after commit), exposure interval labeled | fig:swk-reference-monitor | restyle (heavier marks, exposure interval named in words) | must | — | Label the interval between commit and detection explicitly (e.g. "detected N ms later") rather than leaving it as a bare gap. |
| ch1-23 | The obligation & enforcement organ: the bouncer analogy | A door policy is preventable because the bouncer mediates the door; a thought policy is not, because nothing mediates a thought. | relational | Why is the club-door analogy the same claim as the controllability quadrant? | (delete — analogy stays prose) | none | delete | no | — | The controllability quadrant (ch1-24) already carries this content geometrically; keep the bouncer sentence in prose only. |
| ch1-24 | The general boundary: regimentation is controllability | An event is preventable iff it is controllable (the runtime can refuse it) and witnessed at the mediation boundary; only one quadrant cell is preventable. | quantitative | Which of four event classes can a runtime actually prevent rather than merely detect? | 2×2 regime plane, one concrete event per cell, preventable cell marked | fig:swk-controllability-quadrant | redraw (full sentences on axes, footnote-size concrete events, theorem's cell marked) | must | Thm. \ref{thm:regimentation-controllability}; checker `b3_controllability.py` | Populate all four cells with real alphabet events (git_push, model_emit_token, "no egress after secret read," "no egress after unwitnessed exfiltration consideration") rather than leaving any cell abstract. |
| ch1-25 | Compound case: gate the channel, never the token | A two-state taint automaton (ok/tainted) makes a compound policy regimentable even though its trigger event is uncontrollable, by gating only the controllable egress. | stateful | How can "no egress after reading a secret" be enforceable when reading is uncontrollable? | small 2-state machine: ok --(in_context_read)--> tainted, egress disabled only in tainted | none | — | must | Schneider's Fig. 1, reproduced as the sealed-room design rule | This is the critical mechanism behind the sealed room and "all spend is recorded"; currently zero figures exist for it anywhere in the chapter despite being cited three times. |
| ch1-26 | Compound case: worked synthesis (the sandboxed review job) | Three rules ($P_1$ no egress, $P_2$ no push, $P_3$ taint-gated write) compose into a 2-state product automaton with a hand-checkable disablement map. | quantitative | What exactly does the synthesized supervisor disable in each of its two states? | small state × disabled-events table | none | — | could | hand-worked in pdexample; checker `b3_controllability.py` | Two rows (ok / tainted) × one column (disabled event set) is the whole table; no need to redraw the product-automaton construction. |
| ch1-27 | Partial observation: the second boundary | Prevention also requires the trigger to be witnessed, not merely controllable; unwitnessed internal state collapses a compound policy to detect-only. | relational | Why does "no egress after the model considered exfiltration" stay unenforceable even with full channel control? | annotation on the controllability quadrant, or a small observation-equivalence sketch | none | — | could | Lin–Wonham observability result | Fold into ch1-24's quadrant as a caption note or a third labeled axis tick rather than a separate figure; the content is one added constraint, not a new grammar. |
| ch1-28 | The obligation organ: the commitment-closure oracle | Closing a commitment as "done" requires a reference from a finite, re-checked oracle vocabulary; free text bounces back to open. | stateful | What evidence is sufficient to close a commitment, and what happens to unverifiable evidence? | state machine with oracle-gated terminal transition | fig:swk-commitment-oracle | redraw (OPEN→gate→DONE, 4 oracle kinds stacked, rejected edge back to OPEN) | must | — | Show all four shipped oracle kinds (released claim, merged commit, passing test, policy sub-check) feeding one gate, with the daemon-set deadline as a separate small annotation, not a fifth state. |
| ch1-29 | The obligation organ: the closure algorithm | The Law-2 gate classifies the oracle reference, then re-verifies it currently holds before transitioning to DONE. | stateful | Where exactly does an empty or stale oracle reference get refused? | annotated pseudocode | alg:close | — (existing algorithm exhibit) | should | — | Already a figure-environment listing; keep, and reference from ch1-28 rather than duplicating the gate logic. |
| ch1-30 | The obligation organ: extending the oracle vocabulary (OP-5) | Delta oracles and AST assertions are two designed-but-unshipped additions to the four shipped oracle kinds. | containment | What two new evidence kinds would widen what a commitment can close against? | extend ch1-28's oracle stack with two ghosted/dashed entries | fig:swk-commitment-oracle (extension) | — | could | — | Add "delta oracle" and "AST assertion" as two dashed, unshipped entries in the same stacked list used in ch1-28, rather than a new figure. |
| ch1-31 | The continuity organ: three organs, one weak link | Memory is Built, checkpoint is BuiltWeak (passes notes, not execution state), the outcome ledger is BuiltWeak; the economy rests on all three. | comparison | Which continuity organ is the weakest link, and what exactly can the next link inherit from it? | step chart: six links, y = what the next link inherits (full/notes-only/partial/none) | fig:swk-continuity-organs | redraw (step chart with two narrowings labeled) | must | — | The two narrowing steps (memory→checkpoint, checkpoint→outcome ledger) must be visibly labeled, not just implied by step height. |
| ch1-32 | The continuity organ: event-sourced rehydration (OP-4) | A designed recovery mechanism would pin a commit, truncate the message log, and replay it into a successor — restoring durable inputs, not inference state. | temporal | What does replay actually restore, and what does it explicitly not restore? | pipeline: predecessor state → pin commit + truncate log → replay → successor context, with a dashed box around "not restored: inference state" | none | — | should | — | The dashed exclusion box is the point of the figure — this is the "checkpoint with teeth" gap the chapter calls the most important unbuilt thing at this layer. |
| ch1-33 | The self-attestation organ: honest coverage reporting | Three triggers (boot gate, pre-flight, continuous watchdog) each evaluate invariants to pass/fail/skipped/unknown, and "all good" prints only when every checked invariant passed. | comparison | Which trigger catches which kind of integrity failure, and what does each report? | small trigger × outcome matrix | none | — | could | — | Three rows (boot gate, pre-flight, watchdog) × four outcome columns (pass/fail/skipped/unknown) is a legitimate lookup table, not a diagram. |
| ch1-34 | The invariants, stated as theorems: the invariant table | Twelve invariants (I1–I12) map to mechanisms and maturity grades, with I1 split and I7–I9 carrying the regimentation correction. | comparison | table-not-figure | table-not-figure | none | — | no | — | Already `tab:invariants`; this is the formal spine and belongs as a table, not a figure. |
| ch1-35 | The work unit, model-checked | An evidence-bearing work unit passes through six guarded transitions; on a two-principal instance, all 536 reachable states satisfy six invariants. | stateful | Which guard protects which transition, and how many states does the machine reach? | full-width state machine, guards as a numbered legend below | fig:swk-workunit-machine | redraw (8 states in two columns, guards in a legend table, 536-state count in caption) | must | checker `c0_workunit.py` | The guard legend is as important as the states themselves — do not compress guards to tiny edge labels; a separate legend table under the figure is the fix the triage already specifies. |
| ch1-36 | The work unit: every guard is critical | Disabling any one of five guards lets the checker construct a specific crime in one to seven steps. | quantitative | table-not-figure | table-not-figure | none | — | no | — | Already `tab:workunit-crimes`; five rows (guard, crime, steps, invariant) is exactly a lookup table, not a diagram. |
| ch1-37 | The work unit: checker output as evidence | The model checker's terminal run is the primary evidence for the 536-state count and the shortest-crime traces. | provenance | Has a reader actually seen the checker run, or only a description of it? | terminal session | session-swk-workunit-check | add (fulfilled) | must | `c0_workunit.py` run log | Already inputted at the right place in the text; no further action needed. |
| ch1-38 | The invariants: the consistency-model theorem | Under four stated assumptions, committed transactions are serializable and claim/lock operations are linearizable with commit as the linearization point. | temporal | How do interleaved operations map to one serial history? | swimlane sequence with serialization points | fig:swk-consistency-model | restyle (same Gantt kind as single-writer; resulting order shown as a labeled sequence under the rail) | must | — | Show the resulting total order explicitly beneath the lanes, not only as prose in the caption. |
| ch1-39 | The invariants: the dual-runtime hazard (I11, OP-3) | Truth is computed under one SQLite binding in production and verified under a different binding in tests; a differential fuzzing harness that would close this gap does not run today. | comparison | Would a mirrored-runtime diagram claim more than the chapter can currently support? | (no figure yet — the harness does not exist) | fig-swk-dual-runtime | delete | no | — | Triage is correct to delete this: with no harness built, a diagram would draw a promise rather than a result; the invariant is already stated honestly in prose. |
| ch1-40 | Cross-organ atomicity: a buildable defect | "Claim a port and open a session and record a commitment" is three separate writes today; wrapping them in one local transaction is a one-line fix, not a research frontier. | comparison | What state is left inconsistent if the third of three writes fails, and how does one transaction fix it? | before/after: three separate commits (with a failure gap) vs. one wrapped BEGIN IMMEDIATE...COMMIT | none | — | could | hand-worked exercise (port claimed, session opened, commitment write fails) | Two small panels are enough; the "after" panel needs only one commit line, not a full sequence diagram. |
| ch1-41 | Threat model and the limits of mediation: the threat table | Five adversary classes range from in-scope (buggy cooperating agent) to out-of-scope (cross-machine); the same-user adversary is deliberately excluded. | relational | table-not-figure | table-not-figure | none | — | no | — | Already `tab:threat-model`; five rows against three columns is a lookup table already doing its job. |
| ch1-42 | The enforcement gate proves; it does not yet compel | The macaroon discharge is cryptographically sound, but three unbuilt layers (server-side broker, credential broker, separate-user kernel) stand between "proves" and "compels." | allocation | Which layer of enforcement actually stops a non-cooperating same-user agent, and which merely proves what happened? | layered stack: today's advisory guard (Built, in-band) → server-side broker (Designed) → credential broker (Vision) → separate-user kernel (Vision, the only real wall) | none | — | should | — | This is one of the chapter's most consequential "specified, not implemented" caveats and has zero figures; each layer needs one line naming exactly what it stops, so a reader cannot mistake "verified" for "compulsory." |
| ch1-43 | Hash-chain tamper-evidence and OS-level ephemeral namespaces (OP-9) | A designed sandboxing hardening would move the same-machine adversary from the same-uid threat-table row to the different-uid row, not out of scope entirely. | relational | Which threat-table row does the adversary occupy before and after the hardening ships? | small before/after excerpt of the threat table, one row highlighted moving position | none | — | could | — | Reuse the threat-table's own vocabulary rather than a new grammar; only the one row needs to move. |
| ch1-44 | Partial actor-soul identity bounds every other invariant (I12) | Complete I12 enforcement is a hard precondition for the soundness of I7, I8, and I9; each is only as strong as its least-protected identity path. | relational | Which invariants become unsound if identity enforcement is incomplete? | small dependency diagram: I12 as root, arrows to I7/I8/I9 | none | — | could | — | One node fanning out to three is the whole picture; no need to show the internal mechanism of I12 itself, which is described elsewhere. |
| ch1-45 | Adjacency contract: what the kernel assumes, provides, and does not provide | Three explicit lists define the kernel's contract with the machine below and the coordination layer above. | relational | What can a higher layer safely assume the kernel guarantees, and what must it never assume? | three-column contract table (assumes / provides / explicit non-provisions) | none | — | could | — | Tightening the existing bullet lists into one three-column table would let a reader compare the columns at a glance; not urgent since the bullets are already clear prose. |
| ch1-46 | Open problems: status, stated once | Eleven open problems (OP-1–OP-11) each carry a status (Open/Partial/Closed) and, where one exists, a named design. | comparison | table-not-figure | table-not-figure | none | — | no | — | Already `tab:op-status`; this is precisely the kind of ledger a table serves better than any figure. |
| ch1-47 | Appendix: mechanism-to-artifact map | Every mechanism discussed in the body maps to a specific implementation module. | provenance | table-not-figure | table-not-figure | none | — | no | — | Already `tab:appendix-map`; a mechanism-to-module lookup is a table's job. |
| ch1-48 | Reader's Map | Six reader profiles each get a recommended reading path and critical artifact list. | relational | table-not-figure | table-not-figure | none | — | no | — | Already `tab:readers-map`; purely navigational, not a technical claim needing geometry. |
| ch1-49 | The substrate organ: path and ownership invariants | The database resolves to one canonical, owner-only path, with a fail-closed guard against opening the live registry from a test context. | relational | no | (a sentence suffices) | none | — | no | — | Single fail-safe-default fact; not worth a figure on its own. |
| ch1-50 | The communication organ: two transports | The kernel ships a request/response transport and a binary framed transport, both authenticated by the same software-handshake peer check. | comparison | no | (a small two-row table would suffice, but redundant with the threat table) | none | — | no | — | The peer-credential caveat is already carried by the threat-model table (ch1-41); a dedicated comparison would duplicate that row. |
| ch1-51 | Threat model: information-flow control deferred | Capability scoping alone does not prevent exfiltration; the full airlock construction belongs to a later chapter, not here. | containment | no | (explicitly deferred — no figure belongs in this chapter) | none | — | no | — | The chapter is correct to draw the gate-the-channel rule (ch1-25) here and defer the cross-operator airlock construction to the Harbor Economy chapter. |
| ch1-52 | Differential fuzzing harness design (OP-3) | A specified but unbuilt harness would drive $10^6$ operations against two SQLite bindings from the same seed and assert hash-equivalence. | comparison | Would a diagram of an unbuilt fuzzing pipeline claim more than exists? | (no figure yet — pipeline not built) | none | — | no | Would be `10^6`-op harness if built | Same reasoning as ch1-39: draw this only once the harness exists in the pipeline; today it is honestly a sentence plus an exercise. |
| ch1-53 | Substrate: the configuration is the durability claim | The WAL pragma set (NORMAL sync, bounded auto-checkpoint, clean-shutdown truncate) is the entire durability guarantee, stated as a pragma checklist. | quantitative | no | (a sentence/checklist suffices; already captured by the durability fault-class figure) | fig:swk-durability-faultclass | — | no | — | Fully subsumed by ch1-06's fault-class figure; a separate pragma-listing figure would be decorative. |
| ch1-54 | Second Alice/Bob dramatization: Bob attempts a forbidden action | Bob's out-of-scope write commits before the policy monitor observes and compensates, contrasted with Alice's port claim which never commits. | temporal | Is this the same picture as the monitor-is-a-detector figure? | (folds into ch1-22) | fig:swk-reference-monitor | — | no | — | The two-row timeline of ch1-22 already contrasts "never commits" (regimented) against "commits then compensated" (enforced); a second figure would repeat it. |
| ch1-55 | Review of the key ideas | A ten-item recap list restates the chapter's coupled claims in dependency order. | relational | no | (prose recap; not a new idea) | none | — | no | — | This section is a summary of rows already covered above; no new figure belongs here. |

## Chapter 2 — The Anchor Protocol

Source: `website-v2/public/whitepaper/anchor-protocol-whitepaper.tex` (1492 lines; 7 `\input{figures/...}` fragments plus 3 in-body pseudocode algorithms).
Existing figures found: 7 (fig-anchor-capability-attenuation, -alg-confusion, -delegation-inline, -cuckoo-inline, -revocation-gossip, -card-lifecycle; session-anchor-v6-attack) plus 3 unfloated algorithm listings (alg:symmetric, alg:asymmetric, alg:delegation). The semantic atlas's stated count of 8 for this volume appears to include the four-phase figure, which the chapter has since converted into `tab:anchor-four-phases`.
Rows: 41.
Three most under-served ideas: (1) the multi-hop escalation attack (v6) versus its per-hop fix (v7) — a root-vs-final check wrongly accepting a re-escalated middle hop versus a per-hop check correctly rejecting it — is the paper's sharpest result and has no chain diagram, only prose and a terminal-output listing (row ch2-35); (2) the "verified code is the running code" architecture claim (Rust crate compiled to a cdylib, called by the Node daemon via FFI, same crate the Kani harnesses check) has no diagram at all, only code listings (row ch2-25); (3) the Kani harness scope — what is bounded, what is stubbed, what is proved — is the chapter's most commonly over-read result and has no containment diagram distinguishing "proved" from "assumed" (row ch2-22).

| id | section | idea (one sentence) | structure | reader question | recommended form | existing figure | wave-11 disposition | priority | data source | notes for the renderer |
|---|---|---|---|---|---|---|---|---|---|---|
| ch2-01 | What problem this solves | Three local-swarm failure modes (port squatting, resource contention, privilege escalation) recur without a coordination layer. | comparison | no | (prose list; resolved later by the threat-closure table) | none | — | no | — | Table `tab:threat-closure` already closes the loop on these three; a figure here would anticipate content covered properly downstream. |
| ch2-02 | Architecture: four-phase threat closure | Each of four protocol phases closes one attack surface, and no later phase reopens an earlier one because it changes the key or chain, never the checking party. | comparison | table-not-figure | table-not-figure | none (tab:anchor-four-phases already a table) | table (fulfilled) | no | — | The chapter has already converted the former staircase figure into a 4-row table; no further work needed. |
| ch2-03 | Architecture: capability attenuation | A delegated capability is a strict subset of its parent in both rights and TTL, at every hop; an over-broad or over-long request is refused or clamped. | containment | Does a child capability ever sit outside its parent's rights × lifetime box? | nested rectangles with real numbers, plus the rejected re-grant as a point outside | fig:anchor-capability-attenuation | restyle (heavier edges, footnote-size labels, rejected re-grant plotted outside with coordinates) | must | worked example: parent 10 min, child 20 min refused / 8 min issued | Use the chapter's own numbers (60, 15, 5 minutes) already drawn; add the rejected-regrant point explicitly rather than leaving attenuation implicit. |
| ch2-04 | Phase 1: algorithmic pinning | The verifier ignores the token's own `alg` header and pins the algorithm from policy, defeating algorithm-confusion attacks. | comparison | Which verifier accepts a forged token, and which refuses it, for the same bytes on the wire? | paired security trace: naive vs. pinned verifier over identical wire bytes | fig:anchor-alg-confusion | redraw (sequence-style ladder, two branches, ACCEPT/REJECT with the one differing check boxed) | must | ProVerif `algconfusion.pv` | Box exactly the one check that differs (branch on `alg` header vs. ignore it); everything else about the two paths should look identical. |
| ch2-05 | Phase 1: secure verification algorithm | HMAC-based issuance and pinned verification are stated precisely as a two-function protocol. | stateful | no | annotated pseudocode | alg:symmetric | — | should | — | Already a labeled listing; keep as the exact-procedure companion to ch2-04's narrative figure. |
| ch2-06 | Phase 2: the Harbor Key Hierarchy | The daemon is the root CA; each Harbor gets its own Ed25519 keypair; agents verify peers with the Harbor's public key. | containment | Who signs a Harbor Card, and who is trusted to verify it? | small hierarchy diagram: daemon/root CA at top, Harbor keypairs as children, agents verifying below | none | — | should | — | This foundational trust hierarchy is stated only as a Definition; a three-level tree (daemon → harbor keys → verifying agents) is missing everywhere in the chapter. |
| ch2-07 | Phase 2: asymmetric verification algorithm | Ed25519 signing and verification replace the shared-secret HMAC of Phase 1. | stateful | no | annotated pseudocode | alg:asymmetric | — | should | — | Already a labeled listing; pairs with ch2-06's hierarchy diagram once that exists. |
| ch2-08 | Phase 3: offline attenuation / multi-hop delegation | Agent A appends a strictly narrower capability set to its own card and signs the whole chain; each hop binds fresh nonces and adjacent identities to resist splicing. | provenance | What evidence must travel with a delegated capability, and where would a spliced hop be caught? | linear provenance chain with a broken-link splice example | fig:anchor-delegation-inline | restyle (bigger labels; splice drawn as a broken link with the failing check named) | must | — | The splice-detection failing check (nonce/identity binding) must be named on the broken link, not just implied by the break. |
| ch2-09 | Phase 3: delegation-chain verification algorithm | The verifier walks the chain hop by hop, checking each signature and each capability subset against the immediate predecessor. | stateful | no | annotated pseudocode | alg:delegation | — | should | — | Already a labeled listing; this is the procedure whose per-hop-vs-root distinction becomes critical in ch2-35. |
| ch2-10 | Revocation: why cuckoo filters cannot merge | Two peers cannot OR their cuckoo filter bit arrays because a fingerprint's bucket depends on insertion/eviction history, not just the key set; gossiping the append-only ID log instead sidesteps the problem. | provenance | Why does bitwise filter union risk reanimating a revoked card, and what does the daemon gossip instead? | before/after pair: two filters and a false "OR" on the left, the log-union rebuild on the right | fig:anchor-cuckoo-inline | redraw (each cell labeled with the id it holds) | must | — | Label the specific fingerprint (`c1`) landing in different buckets on each peer in the "before" panel — that concrete collision is the whole argument. |
| ch2-11 | Revocation: sizing the filter | At $p_{fp}=10^{-3}$, 13 bits/entry holds $10^5$ revocations in ~160 KB; at $10^{-6}$, 23 bits/entry costs ~288 KB for three orders of magnitude fewer false positives. | quantitative | How much space does a fleet-wide revocation filter cost at two false-positive targets? | small table or bar pair: p_fp vs. bits/entry vs. total size | none | — | should | hand-worked in pdexample | Two bars or two table rows are enough; the point is "less than twice the space buys three orders of magnitude," which should be a caption fact, not just prose. |
| ch2-12 | Revocation: revocation-augmented verification | Every card check consults the local filter first, then the authoritative log on a hit, admitting on a false positive while flagging it for reconciliation. | stateful | What happens when the filter says "revoked" but the authoritative log disagrees? | small decision flow: filter lookup → absent (admit) / present → authoritative check → reject or admit+reconcile | none | — | could | — | Three boxes and two decision diamonds is the whole picture; could be folded as an inset on the cuckoo-filter figure (ch2-10) instead of standing alone. |
| ch2-13 | Revocation: gossip-based synchronization | Revocation deltas spread by anti-entropy gossip in expected $\Theta(\log m)$ rounds under a connected, reliable-round model; partitions have no finite bound. | temporal | How many rounds until every daemon in a fleet of $m$ knows about one revocation? | epoch/round timeline across daemons, convergence interval marked | fig:anchor-revocation-gossip | restyle (row labels smaller; amber interval labeled with Δ; partition marker in words) | must | worked example: 5 daemons, Δ=2s, 2–3 rounds (4–6s) | Keep the worked numbers (5 daemons, log2(5)=2.32, 4–6 seconds) visible in the figure, not only in the caption text. |
| ch2-14 | Revocation: pollution as denial of revocation | An attacker flooding the filter with authenticated-looking revocation IDs degrades checks to slower, never to missed, given a per-issuer quota and authoritative fallback. | quantitative | Does a flooded filter fail open, fail closed, or just fail slow? | small threshold diagram: load vs. correctness, below-quota (fast+correct) vs. above-quota (slow+correct, never missed) | none | — | could | — | The key fact to preserve is "degrades to slower, never to missed" — a two-region strip with that guarantee labeled is sufficient. |
| ch2-15 | Revocation: card lifecycle | A card's admissibility is `valid → revoked → expired`, a one-way interval attached to one card ID; correction always issues a new ID rather than reanimating the old one. | stateful | What are the only valid exits from an active card, and can a revoked card come back? | small state machine, terminal states visually distinct | fig:anchor-card-lifecycle | restyle (interval endpoints $t_r$ and $t_0+\tau$ labeled on the axis; heavier rows) | must | — | No partial reanimation must be visually obvious — the revoked state should have no return edge, only the "issue a new card" note pointing to a fresh ID. |
| ch2-16 | How we know it is correct: the verification stack | Three layers (protocol model, Rust source, running binary) each hand the next an obligation they cannot discharge themselves; two gaps are asserted by a human, not a tool. | provenance | table-not-figure | table-not-figure | none (tab:anchor-verification-stack already a table) | table (already implemented) | no | — | Already the chapter's own map table; the two human-assertion gaps are its most important cells and are already named there. |
| ch2-17 | Symbolic analysis: authentication correspondence | ProVerif confirms every accepted delegation event corresponds to a matching modeled issuance/delegation event, for the modeled phase. | provenance | What does the Accepted-implies-Issued correspondence actually assert, step by step? | small sequence diagram: Daemon issues root → Agent A delegates → Harbor accepts, correspondence arrow drawn across | none | — | should | ProVerif `v3_delegation.pv`, query result | A three-actor sequence with the implication arrow spanning Issued/Delegated to Accepted would make the formula's meaning legible without reading the query. |
| ch2-18 | Symbolic analysis: ProVerif results table | All five listed queries return TRUE across three modeled phases. | comparison | table-not-figure | table-not-figure | none (tab:proverif-results already a table) | table (already implemented) | no | — | A query × phase × result lookup is exactly what this table already is. |
| ch2-19 | Runtime enforcement: the Arbiter | Six post-commit invariants (PID squatting, cap escalation, note monotonicity, escrow positivity, lock-owner validity, heartbeat freshness) feed a "man overboard" salvage protocol. | stateful | Which invariant violation triggers which recovery action? | small flow: six named invariants → monitor → violation → salvage (revoke cards, release locks, notify) | none | — | should | — | This is a genuinely new mechanism (the salvage protocol) with zero figures; keep it explicitly post-commit/detect-and-compensate per the kernel's controllability theorem this chapter cites. |
| ch2-20 | Runtime enforcement: controllability cross-reference | Card verification is regimentable (a gate before any effect); exercising a legitimately held capability badly is uncontrollable at this boundary. | relational | Where does Anchor's own enforcement sit on the kernel's controllability quadrant? | annotate two example events onto the kernel's existing quadrant | fig:swk-controllability-quadrant (ch1, cross-reference) | — | could | — | No new grammar needed — just place "card accept/reject" and "misuse of a valid db:write" as two labeled points on the kernel chapter's quadrant, or reproduce a mini version here. |
| ch2-21 | Implementation verification: memory safety within the harness bound | Kani bounded-model-checks the token parser on 32-byte inputs with crypto stubbed; within that bound, parsing never panics. | comparison | What exactly did Kani check, and what did it explicitly not check? | containment diagram: outer = "what a reader might assume," inner = "what Kani actually proved" | none | — | should | Kani harness `proof_verify_logic_only` | This is the chapter's most commonly over-read claim (a 32-byte input cannot even hold a real card); a nested-scope diagram prevents the "verified" word from doing more work than it earned. |
| ch2-22 | Implementation verification: secret-independent source control flow | The comparator XORs every byte pair into one accumulator and tests it once, so no branch depends on secret bytes at the source level. | comparison | Why does an XOR-accumulate comparator resist timing attacks that an early-return comparator does not? | two short code-path comparisons, the branch-on-secret highlighted in the naive path | none | — | could | inspection of `constant_time_compare` | Two 3-line pseudocode snippets side by side, with the vulnerable early-return branch circled in the naive one, communicates this faster than the current prose paragraph. |
| ch2-23 | Implementation verification: the Kani harnesses, precisely | Three harnesses check different things at different bounds (parse-logic no-panic; comparator no-panic; two hard-coded attenuation vectors). | comparison | For each harness, what is proved and what stronger claim would a reader wrongly draw? | 3-row table: harness · bound/stub · property proved · caveat | none | — | should | evidencePolicy fields in `whitepaper/corpus.json` | This table would directly serve exercise ex:anchor-kani-scope, which already asks for exactly this comparison in prose; a table lets a reader check all three at a glance. |
| ch2-24 | The verified code is the running code: FFI architecture | The Rust crate is compiled to a cdylib and called by the Node.js daemon's Arbiter module via FFI — the same crate Kani checks, not a reference implementation. | comparison | Is the "verified" code actually the code that runs in production? | two-box architecture diagram: Rust crate (Kani-checked) —FFI→ Node daemon (Arbiter module) —production path→ | none | — | should | — | This is the chapter's signature claim ("narrows the gap to the compiler's") and has zero diagrams, only code listings; a two-box-plus-arrow figure would make the architecture visible in five seconds. |
| ch2-25 | Adversary: Dolev-Yao assumptions | The adversary controls the network, can intercept/modify/inject, cannot break cryptography, and may corrupt some but not all agents. | relational | no | (bullet list; already referenced visually via the dashed band on other figures) | none | — | no | — | The chapter already marks which figures carry the adversary's dashed band and which do not; a standalone figure would duplicate that discipline. |
| ch2-26 | Adversary: threat closure table | Each of the three failure modes from §1 is closed somewhere in the chapter, with a deliberately non-empty residual-risk column for each. | comparison | table-not-figure | table-not-figure | none (tab:threat-closure already a table) | table (already implemented) | no | — | Threat × closure-location × mechanism × residual-risk is exactly a lookup table's job. |
| ch2-27 | Adversary: security properties table | Four ProVerif properties and three Kani rows are listed with their verification tool and deployment status. | comparison | table-not-figure | table-not-figure | none (tab:properties already a table) | table (already implemented) | no | — | Already implemented and correctly scoped as a table. |
| ch2-28 | Where this stops: delegation-chain depth gap | The cryptographic verifier accepts arbitrary chain depth; a spawn-time policy check caps depth (default 4, hard max 8), but nothing enforces the cap at verification time. | relational | Where in the pipeline is chain depth actually checked, and where is it not? | small diagram: two check points on one delegation chain — spawn-time gate (enforced) vs. verification-time (unchecked) | none | — | should | — | The gap between "checked at spawn" and "not checked at verify" is exactly the kind of missing-binding the atlas says must appear as a broken relation, not atmospheric distance. |
| ch2-29 | Where this stops: the PID-reuse race | Agent A crashes holding PID 4021; the OS reuses the PID for Agent B, who could present A's scavenged card; a bare PID check would wrongly admit B. | temporal | Does comparing only the numeric PID let a reused-PID attacker in, and what additional check closes it? | timeline: A holds card+PID → crash → PID reused by B → naive check (admits, wrong) vs. strengthened check (start-time compare, rejects) | none | — | should | hand-worked scenario in prose | This concrete, vivid attack (like the kernel chapter's Alice/Bob dramatizations) has no figure at all; a single timeline with two outcome branches fixes that. |
| ch2-30 | Where this stops: localhost is a real network | A 15-minute-TTL card sniffed off loopback at minute 1 grants an attacker roughly 14 minutes of the victim's full capability set. | quantitative | How much exposure does a mid-TTL loopback sniff actually buy an attacker? | small interval diagram: TTL window with the sniff point at minute 1 and the remaining exposure shaded | none | — | should | hand-worked in prose | One shaded interval on a 15-minute axis, with the sniff point and remaining 14 minutes labeled, turns this from an abstract warning into a measured claim. |
| ch2-31 | Appendix A: formal verification status | Six of twelve listed claims close at both model and runtime level; five remain partial and one is open. | comparison | table-not-figure | table-not-figure | none (tab:anchor-status already an xltabular) | table (already implemented) | no | — | Already the chapter's own status ledger; a Closed/Partial/Open table is the right form. |
| ch2-32 | Appendix A: GitHub-App relay ingress properties | Three properties (origin authentication, payload secrecy, namespace isolation) are each mechanized with a paired negative control that exhibits the attack when the mitigation is removed. | provenance | For each property, what does the negative control prove is non-vacuous? | 3-row table: property · mechanism · negative-control result | none | — | could | ProVerif `ingress_origin_auth.pv` etc. | This mirrors the attenuation-soundness pattern (a claim plus its own vacuity check) but for the relay surface; a small table suffices since the pattern itself repeats a technique already drawn elsewhere. |
| ch2-33 | Attenuation soundness: the v3-vacuous vs. v5-sound models | The original delegation model's capability order was reflexive-only, making escalation inexpressible and the no-escalation proof vacuous; v5 adds a real partial order and an insider-adversary who can express the attack. | comparison | Why did an earlier "proof" of no-escalation prove nothing, and what changed to make the later one real? | paired comparison: v3 (reflexive order, escalation inexpressible, proof vacuous) vs. v5 (real order, insider adversary, proof non-vacuous) | none | — | should | ProVerif `harbor_card_v3_delegation.pv`, `v5_attenuation.pv` | This is a rare and valuable "here is what a vacuous proof looks like, and here is the fix" pair; drawing both side by side teaches the general lesson, not just this instance. |
| ch2-34 | Attenuation soundness: attenuation soundness theorem | In the v5 model, no escalating delegation is ever accepted, and the escalation attempt is independently shown reachable so the result is not vacuous. | provenance | Is the no-escalation property real, and how do we know the attack it excludes is actually expressible? | (folds into ch2-33's paired comparison) | none | — | — | — | Covered by ch2-33; the Q1/Q2 pair (soundness + non-vacuity) is the content of that single figure. |
| ch2-35 | Attenuation soundness: multi-hop scope, v6 attack vs. v7 fix | A verifier that checks only the final capability against the root wrongly accepts a re-escalated middle hop ($A{:}write \to B{:}read \to C{:}write$); a per-hop verifier correctly rejects it. | provenance | Which verifier accepts the escalated chain, and at exactly which hop does the correct one reject it? | paired provenance chains: root-vs-final check (accepts, wrong) vs. per-hop check (rejects at hop 2) | none | — | must | ProVerif `harbor_card_v6_multihop_attack.pv`, `v7_multihop_fixed.pv` | This is the chapter's sharpest and most under-drawn result — currently only prose and a terminal-output listing carry it; a side-by-side three-node chain with the failing/passing check boxed at hop 2 would make the whole subtlety visible in five seconds. |
| ch2-36 | Attenuation soundness: v6/v7 terminal verdicts | The pinned ProVerif output shows the v6 escalation reachable and the v7 fix rejecting the identical attempt. | provenance | Has a reader actually seen the tool's verdict change, not just a claim that it did? | terminal session | session-anchor-v6-attack | add (fulfilled) | must | — | Already inputted at the right point in the appendix; pairs directly with ch2-35's proposed chain diagram. |
| ch2-37 | Phase 3: chain-replay nonce binding | Per-hop fresh nonces bind previous/next identities into each signature so a captured hop cannot be spliced into a different chain. | provenance | no | (already covered by the delegation-inline figure's splice illustration) | fig:anchor-delegation-inline | — | no | — | The restyle spec for ch2-08 already calls for drawing the splice as a broken link with the failing check named; a separate figure would duplicate it. |
| ch2-38 | Exercises: macaroon discharge substitution (v1 vs. v2) | A naive discharge binder that keys only on the shared caveat, not the specific grant signature, lets a low-authority discharge be replayed against a high-authority grant. | provenance | Which of two discharge-binding designs lets one grant's proof authorize a different grant? | paired diagram: two grants sharing one caveat key; v2 binds to caveat only (substitution succeeds) vs. v1 binds to grant signature too (fails) | none | — | could | ProVerif `macaroon_discharge_v1.pv`, `v2_naive_unsound.pv` | This is exercise-only content (deep in the appendix) but is structurally the same lesson as ch2-35 (checking something coarser than the exact object admits a substitution); worth drawing only if the chapter promotes this exercise into the main text. |
| ch2-39 | Further reading: credential format migration | The Harbor Card envelope is migrating to the SD-JWT+kb profile used by AP2/UCP, while delegation semantics and ProVerif obligations stay unchanged. | comparison | What changes in the envelope migration, and what provably does not? | two-column comparison: old envelope (raw JWT) vs. new envelope (SD-JWT+kb, JWS detached, JCS canonical), bridged by "unchanged: delegation semantics" | none | — | could | — | A simple two-column table with one bridging note is enough; this is a forward-looking appendix note, not a core protocol claim. |
| ch2-40 | Exercises: the security envelope beyond a valid signature | A verified Ed25519 signature alone does not establish authorization; canonical decoding, key-to-principal binding, audience/resource match, freshness, and non-revocation must all also hold. | relational | Does "the signature verifies" mean "this action is authorized"? | small AND-checklist: signature valid AND decoding AND key-binding AND audience match AND freshness AND non-revoked = authorized, with ProVerif-covered items marked | none | — | should | — | This ties together boundary claims scattered across the whole chapter (§threat-model, §revocation, §limitations) into one picture readers currently must reconstruct themselves from an exercise solution. |
| ch2-41 | Architecture: what a Harbor is | A Harbor is a zero-trust, namespace-isolated execution environment in which agents must prove identity and capability before acting. | containment | no | (a definition; already implied by the key-hierarchy diagram of ch2-06) | none | — | no | — | Once ch2-06's hierarchy diagram exists, the Harbor-as-boundary concept is visible as the middle tier of that tree; a separate containment diagram would be redundant. |

## Chapter 3 — The Sealed Harbor

This chapter's 23 rows are thin next to the other seven chapters' 41–62; a second enumeration pass is owed here, not a padded one.

Source: `website-v2/public/whitepaper/sealed-harbor.tex` (doesn't exist yet in this worktree — the enumeration pass cited it as the chapter's source; this is itself evidence for why the chapter is thin, see the note above).
Existing figures found: 5 (fig:sealed-pillar-pipeline, fig:sealed-two-worlds, fig:sealed-mutant-grid, fig:sealed-composition-crossover, fig:sealed-operating-curve — all Wave-11 "keep").
Rows in this table: 23.
Three most under-served ideas: (1) the laundering/escape-hatch residual (worker computes t=f(s), submits t, gate honestly releases g(t)) — named three separate times, drawn nowhere; (2) the q·b leakage-budget capacity account — the chapter's own headline design rule ("negotiate over channel capacity"), zero geometry anywhere; (3) the dual-attested key release protocol that seals the room from three sides at once — a genuine multi-party protocol never drawn as a sequence.

| id | section | idea (one sentence) | structure | reader question | recommended form | existing figure | wave-11 disposition | priority | data source | notes for the renderer |
|---|---|---|---|---|---|---|---|---|---|---|
| ch3-01 | Two owners, one answer | Derek's data, Erin's model, and the cloud operator must each be denied sight of what the other two hold, with the room sealing all three lines of sight at once. | relational | Which three parties must never see each other's plaintext, and what single mechanism seals all three lines of sight? | relation map: three principal nodes (Derek/data, Erin/model, cloud operator) with explicit "must not see" edges converging on one sealed-room boundary | none | — (no triage row for ch3) | should | — | Name Derek, Erin, and the cloud operator explicitly; the counter-read to design against is a reader who thinks only Derek-vs-Erin needs separating and forgets the host. |
| ch3-02 | Two owners, one answer | The sealed room differs from Ryoan across workload shape, leakage bound, taint granularity, model access, multi-job accounting, evasion, and timing (Table sealed-ryoan). | comparison | How does confining a looping, tool-calling agent differ from Ryoan's single-shot module? | table-not-figure (the existing 3-column comparison table) | none | — | no | — | The seven-row prose table already aligns the comparison; a diagram adds nodes without adding information. |
| ch3-03 | Two owners, one answer | Token-level taint through a language model has no sound middle ground: mark everything (useless) or miss semantic flows (unsound), which is why the room fences the channel instead of the token. | comparison | Why can't a taint tracker just tag secret-derived tokens and drop them at egress? | table-not-figure: two rows (mark-everything vs. miss-flows), each with its named consequence | none | — | no | — | The boxed claim already states the dichotomy in two clauses; a figure would dramatize what a sentence already says. |
| ch3-04 | Two owners, one answer | Four claims (enforceability, noninterference, conservation, detection) compose into one pipeline, each closing a gap the others leave open, each checked by a named script. | temporal | Which fences and gates does one work order pass, and which script checks each resulting property? | boxed pipeline: two fences, two gates, four property terminals, each with its checker script named | fig:sealed-pillar-pipeline | keep | must | scripts b3_controllability.py, c1_noninterference.py, a3_epsilon_ledger.py, a4_canary_sprt.py | Already well-served; keep the "produces" vs. "is-checked-by" arrow-style distinction the caption names. |
| ch3-05 | One work order, two fences, two gates | The work order C is a signed tuple binding four hashes, function/tools, recipients/schemas, budgets, retention, validators, and payment/liability, re-signed whenever any field changes. | containment | What exactly does the one contract both principals sign pin down, and what forces a re-sign? | annotated record schema: the tuple's fields grouped by role (pins / permissions / recipients / bounds / remedies), each with a one-line gloss | none | — | could | — | Use the chapter's own field grouping; the counter-read is a reader assuming the hash pins runtime state rather than the runtime's measurement. |
| ch3-06 | One work order, two fences, two gates | Dual-attested key release: the confidential VM boots and produces fresh attestation evidence, and Derek's and Erin's key brokers independently verify it before releasing their own key, after which there is no ambient network. | temporal | In what order do the two owners verify attestation and release keys, and what state exists once both have released? | protocol ladder: CVM, Derek's broker, Erin's broker as lifelines; boot to attest to two independent verify-and-release messages to a no-ambient-network state | none | — | must | — | Name the RATS attester/verifier/relying-party roles on the lifelines; the counter-read is assuming one shared verification step rather than two independent ones. |
| ch3-07 | One work order, two fences, two gates | The worker receives only semantic handles (read/infer/execute/write/request_declassification) mediated by an in-guest monitor and an outer gateway, never raw bytes or direct TLS. | containment | What can Erin's worker actually touch, and what stands between it and the network? | containment diagram: worker nested inside two fences (in-guest monitor, outer gateway), with the five handle operations as the only crossing points | fig:sealed-pillar-pipeline (fences named, handle API not shown) | keep (partial) | could | — | The pipeline figure names the two fences but not the five handle types; an inset listing them closes the gap without a full redraw. |
| ch3-08 | One work order, two fences, two gates | After the first secret read the whole worker is tainted, and the only three exits are a rich result to Derek, aggregated/DP feedback to Erin, and a padded public receipt to both. | relational | Once the worker is tainted, what are the only three things allowed to leave, and who receives each? | relation map: two gates fanning into three labeled, schema-typed channels (rich result to Derek; DP feedback to Erin; padded receipt to both) | fig:sealed-pillar-pipeline (gates shown, three channels not distinguished) | keep (partial) | should | — | Label each arrow with its schema/budget type; the counter-read is assuming Derek and Erin see symmetric outputs when the channels are asymmetric by design. |
| ch3-09 | One work order, two fences, two gates | Six side properties (label monotonicity, complete mediation, provenance closure, replay safety, measurement binding, rollback detectability) are design invariants, not theorems, and every later result quietly assumes them. | comparison | Which six invariants does every later theorem lean on, and which one is critical for enforcement? | table-not-figure: invariant name x one-line statement x which later section depends on it | none | — | should | — | Flag complete mediation as the critical row (both enforceability and conservation cite it by name) rather than listing all six as equals. |
| ch3-10 | One work order, two fences, two gates | Each release produces an audience-redacted, Merkle-anchored receipt binding the work order, attestation, inputs, model, output ciphertext, labels, declassifications, and counters. | provenance | What does a receipt actually prove, and what does it not prove about the answer inside it? | provenance tree: the receipt node with its eight bound fields as children, one child ("output ciphertext") flagged with "proves reported, not true" | none | — | could | — | Anchor to the receipt formula R = Sign(...) already in the text; guard against treating the receipt as proof of correctness rather than of measured compliance. |
| ch3-11 | Silence except through the slot | Two runs of the clean-room model, identical except Derek's secret, are driven through load-read-submit-release and found bit-for-bit identical to Erin at every step but the one declared release, exhaustively to depth 7. | temporal | Do two runs that differ only in the secret look identical to Erin at every step but the declared release? | two-run lockstep with equivalence marks at every step, the one permitted difference boxed | fig:sealed-two-worlds | keep | must | c1_noninterference.py | Already well-served; keep the boxed "slot" and the leaky-gate mutation witness exactly as drawn. |
| ch3-12 | Silence except through the slot | A worker that launders by computing t=f(s) and submitting t so the gate honestly releases g(t) is invisible to the current checker, because the model's submit action carries no payload. | comparison | How could a worker defeat the "committed input" gate honestly, and why can't the current checker catch it? | paired paths: the honest path (gate applies g to committed s) beside the laundering path (worker computes t=f(s); gate honestly releases g(t)), divergence point marked | none | — | must | — | This residual is named three times (the boundary after Thm. sealed-noninterference, the budget section, Exercise sealed-launder) and drawn nowhere; show the fork at submit and label it "priced, not caught." |
| ch3-13 | Silence except through the slot | Over secrets {0,...,7} and g(s)=s mod 2, exactly 12 of 28 unordered pairs are equal-parity and must stay Erin-indistinguishable forever; the rest may differ only at a release step. | quantitative | With more secrets, how many pairs must the room keep identical, and how many may it legitimately reveal a difference on? | small table: secret count -> equal-parity pairs -> different-parity pairs, for both the 4-secret and 8-secret cases | none | — | could | — | This is the worked answer to Exercise sealed-pairs; a two-row table (4 secrets: 2 of 6; 8 secrets: 12 of 28) shows the scaling without a new drawing. |
| ch3-14 | The gate sits on the only enforceable boundary | Of five candidate policies over the room's alphabet, only the compound rule (no egress after a tainted read) is regimentable; pure content-based rules on reads or emissions are detect-only forever. | quantitative | Which of the five candidate safety policies can the mediation layer actually enforce, and which can only be watched for? | 2x2 controllability quadrant (controllable x mediated-at-the-boundary), reusing the Volume II grammar, with each of the five policies placed in its cell | none (table sealed-policy-classification exists; no figure) | — | should | — | Reuse the exact quadrant grammar of II/fig:swk-controllability-quadrant rather than a new design; this is the same theorem applied to a new alphabet, and the atlas's cross-volume reuse contract favors identical grammar. |
| ch3-15 | The budget is a ledger, and the ledger conserves | The ledger's only spending transition atomically appends the release record and adds to the balance, so every reachable state satisfies balance = journal sum and never exceeds the cap. | allocation | Walking through an actual sequence of releases, does the running balance always equal the journal's sum, and where does a torn write break that? | ledger table: release-by-release walkthrough (the two-client instance) with columns for release, balance sigma, and recorded sum, honest and torn versions side by side | fig:sealed-mutant-grid (shows which invariant breaks, not the row-by-row walkthrough) | keep (partial) | should | a3_epsilon_ledger.py | Use the exact torn-write trace from Exercise sealed-torn (client 0 releases 2 twice, client 1 releases 1, recorded sum reaches 5 over a cap of 4); this is the journal itself, a different view from the existing grid. |
| ch3-16 | The budget is a ledger, and the ledger conserves | Two canonical atomicity breaks (spend-without-append, torn write) are caught in one and three steps respectively against the ledger's two invariants, on a 15-state model-checked instance. | quantitative | Which mutant breaks which invariant, and how many steps does it take to catch it? | mutant x invariant grid with caught/step counts in the cells | fig:sealed-mutant-grid | keep | must | a3_epsilon_ledger.py | Already well-served; keep the "recorded over-spend past the cap" callout for the torn write's deeper crime. |
| ch3-17 | The budget is a ledger, and the ledger conserves | Basic and advanced composition bounds cross exactly once as releases accumulate; below k=32 (at epsilon=0.1) basic is tighter, above it advanced is, and the tighter of the two must be quoted honestly. | quantitative | As an engagement runs longer, which composition bound should the contract quote, and where do the two cross? | measured-quantity plot: both bounds against releases k with the crossover marked | fig:sealed-composition-crossover | keep | must | hand-derived closed forms (verified) | Already well-served; keep both the k=32, epsilon=0.1 and k=128, epsilon=0.05 crossover points named. |
| ch3-18 | The budget is a ledger, and the ledger conserves | The epsilon-ledger certifies nothing against a malicious worker choosing its own reported spend; that adversary is governed instead by the q*b capacity account of the leakage-budget section. | relational | Which of the two accounting mechanisms, the privacy ledger or the bit-capacity budget, actually bounds a given adversary? | paired accounting panels: left "honest mechanism, ledger governs" (sigma <= epsilon_max); right "malicious worker, ledger certifies nothing" (q*b governs), joined by one arrow labeled "one design, two adversaries" | none | — | should | — | This distinction runs through three sections without being drawn together; guard against a reader assuming the conserved ledger alone bounds worst-case leakage. |
| ch3-19 | Canaries with a power curve and a clock | With per-canary miss rate beta=0.2, detection probability climbs from 0.8 at k=1 toward 0.999 at k=5; the sequential test detects a leak in expected 296.9 outputs against 431.9 under no leak. | quantitative | How fast does detection power climb with canaries carried in a leak, and how long does the sequential test take to decide? | two-panel measured plot: power curve against k with the anchor marked; expected run length under null and leak as a bar pair | fig:sealed-operating-curve | keep | must | a4_canary_sprt.py | Already well-served; keep the k=3 -> 0.992 anchor and the two bar values exactly as drawn. |
| ch3-20 | Canaries with a power curve and a clock | Canary secrecy is itself critical: an adversary who obtains the planting list with probability 0.3 and strips every known canary drops measured power at k=5 from 0.9997 to 0.698. | quantitative | How much does detection power fall if the canary list itself leaks, and what does no redesign restore? | second curve on the same power-vs-k axes: the honest-secrecy curve against the list-compromised curve, both anchored at k=5 | fig:sealed-operating-curve (honest-secrecy case only) | keep (partial) | should | a4_canary_sprt.py | This compromised-secrecy regime gets its own paragraph plus Exercise sealed-canary-secrecy but no second curve; add it to the existing plot rather than drawing a new one. |
| ch3-21 | The leakage budget: q*b bits, before timing | A contract granting a b-bit status schema per job over q jobs has granted at most q*b bits of adversarial capacity before timing, e.g. 12,800 bits for 64 bits over 200 jobs, plus log2(s) per job for s timing slots. | allocation | Across a whole engagement, how many bits of adversarial capacity has the contract granted, before and after timing is counted? | ledger/accounting figure: jobs times bits-per-job accumulating to the q*b total, with the timing term (log2 s per job) added as a second, separately priced strip | none | — | must | hand-derived (chapter text; Exercise sealed-budget) | This is the chapter's own headline design rule ("negotiate over channel capacity, not over does this leak") and appears only as two paragraphs with one worked number; no figure anywhere gives it geometry. |
| ch3-22 | The leakage budget: q*b bits, before timing | The laundering residual, the malicious-worker gap in the ledger, and the arbitrary-telemetry channel are three faces of one debt, all priced by the same q*b account. | relational | Why do three separate gaps opened earlier in the chapter all get paid off by one number? | converging-arrows diagram: three labeled residuals (laundering, malicious worker, arbitrary telemetry) each with an arrow into the single q*b ledger | none | — | should | — | Pairs naturally with ch3-21 as a second panel; guard against treating the three residuals as independent problems needing independent fixes. |
| ch3-23 | Limitations and boundaries | Six things the design does not promise (zero model leakage under unlimited adaptive queries, zero data leakage with arbitrary telemetry, elimination of side channels, confidentiality via an unapproved external model, correctness of the answer, literal deletion/availability/collusion resistance) each have a named reason and a residual channel. | comparison | For each thing this design does not promise, what mechanism is responsible for the gap, and where is the residual priced? | table-not-figure: not-promised claim x why not x where the residual is priced (ledger / q*b / out of model / needs a stronger threat model) | none | — | should | — | Currently an unstructured bulleted list; a four-column table shows at a glance which residuals this chapter prices and which need an entirely different mechanism (MPC/HE). |

## Chapter 4 — The Legible Swarm

Source: `whitepaper/legible-swarm.tex`.
Existing figures found: 7 (fig:state-of-nature, fig:zoom-vs-potemkin, fig:specialization, fig:sdt, fig:readpoverty, fig:split-ranker, fig:roles — all Wave-11 redraw/restyle, now implemented; several former figure sites were converted to tables per the Wave-11 triage: tab:ls-stack-map, tab:ls-four-questions, tab:ls-authority-organs, tab:ls-pushpull, tab:ls-read-surfaces, tab:compaction).
Rows in this table: 62.
Three most under-served ideas: (1) the two-constraint digest-rate and zoom-advantage theorem pair (Thm. pinned-joint, Thm. zoom-advantage) — the most numerically worked, densely-proved result in the chapter, with a full adaptive-halving group-testing algorithm and exact worst-case constant, and zero figure; (2) the escalation-threshold tuning band (Thm. escalation-threshold) — a genuine empty-vs-nonempty regime result that drives the "costly signal" mechanism used in three later sections, never plotted; (3) the completionist-completion verifier gate (Def. completionist) — called the paper's own "single highest-leverage new mechanism," with no state machine despite the book drawing this exact shape (claim -> verifier -> done/refuse) for other chapters' commitment gates.

| id | section | idea (one sentence) | structure | reader question | recommended form | existing figure | wave-11 disposition | priority | data source | notes for the renderer |
|---|---|---|---|---|---|---|---|---|---|---|
| ch4-01 | Reader's Map / the system as four strata | The system is four strata (L0 daemon, L1 protocol, L2 legibility/authority, L3 economy) and this chapter owns L2, using L1 below and supplying identity/reason/outcome upward. | containment | Which layer does this chapter own, and what does it consume from below versus supply above? | table-not-figure: layer x stratum x for-whom x capability columns (already a lookup matrix, tab:ls-stack-map) | none (table) | table (implemented per triage 4.1) | no | — | The 4x7 capability matrix is a lookup task; the atlas prefers a matrix over a diagram here, and it already exists. |
| ch4-02 | Introduction / The failure taxonomy | Five Hobbesian failures (resource conflict, illegibility, lies, footguns, illegible authority) map each to a concrete coding-swarm form and to the class of primitive that answers it. | comparison | For each state-of-nature failure, what is its concrete form and what kind of mechanism fixes it? | table-not-figure: failure x concrete form x answering primitive (tab:taxonomy) | none (table) | — | no | — | Five rows, three columns, a lookup task the existing table already serves; a diagram would not add relational information. |
| ch4-03 | Introduction / A worked afternoon: Mara and the six agents | Mara's afternoon runs through four sequential failures in order (collision, illegible pile, confident lie, footgun) as one operator's lived timeline, each later resolved by a named mechanism. | temporal | In what order do the four state-of-nature failures actually hit one operator, and which later section fixes each? | step chart / timeline: 2:40pm spin-up to four dated failure events, each tagged to its taxonomy row and forward-linked to the section that resolves it | none | — | should | — | Use Mara's own clock times (2:40, 3:10, and the two undated but ordered events) and agent names (api-1, api-2); this scene recurs at 3:25 and 3:32 and a spine figure would tie the whole chapter's running example together. |
| ch4-04 | Introduction / The failure taxonomy | Four valid writes with overlapping windows become, under consented serialization, one commit spine with a named grant recording actor, claim, artifact, and reason for each. | temporal | Do the same four writers still collide once the daemon serializes them, and what does each grant record? | before/after Gantt pair on one time axis | fig:state-of-nature | redraw (done) | must | internal | Already well-served; the figure uses generic labels A-D rather than Mara's own agent names — note this as a possible future tightening, not a gap requiring redraw. |
| ch4-05 | Introduction / The failure taxonomy | An emergent Hobbesian sovereign (Dai et al.) can be a stable but illegible oligarchy rather than a consented, inspectable authority, and the two are experimentally distinguishable. | comparison | What observable separates a swarm that reached a consented sovereign from one that reached a stable but illegible oligarchy? | table-not-figure: sovereign characteristics vs. oligarchy characteristics, each row a named observable | none | — | could | — | This is exercise-level content (ex:ls-sovereign-vs-oligarchy-experiment); a comparison table would suffice if ever promoted into the body text. |
| ch4-06 | Consent to the Leviathan / The legible-sovereign amendment | The ranker that decides what the operator sees is itself an act of authority, and its regress is closed by one complete append-only ledger of every suppressed item, its score, its suppression rule, and its re-surface trigger. | provenance | What did the queue not show the operator, and why, and does that counter-surface need a counter-surface of its own? | table-not-figure: suppressed item x regret x why suppressed x re-surface trigger (tab:counter-surface) | none (table) | — | no | — | Already a concrete mockup table with three worked rows; it is itself a ledger, which is the right grammar for an audit-completeness claim. |
| ch4-07 | Consent to the Leviathan / Consent as a first-class, revocable primitive | A consent grant is a five-field object (scope, stakes-ceiling, reversibility-floor, TTL, revocable) the operator issues explicitly, appearing in the digest with an audit trail and an expiry. | containment | What exactly does one consent grant fix, and what happens automatically when its clock runs out? | annotated record schema: the five fields with Mara's own worked instance (tests/ subtree, low stakes, 24h TTL) | none | — | could | — | Pairs with ch4-08's lifecycle protocol; could be one inset on the same figure rather than a separate drawing. |
| ch4-08 | Consent to the Leviathan / Consent as a first-class, revocable primitive | The consent-grant lifecycle is a four-step protocol (issue, act, witness, expire/revoke) in which the daemon admits an action only if it is in scope, under the stakes ceiling, above the reversibility floor, and the grant is live. | stateful | Through what states does one consent grant pass, and what closes off its legitimate action set? | state machine: ISSUED -> ACTIVE -> {ADMITTED action / ESCALATED} -> EXPIRED/REVOKED, with the three-clause admission guard on the transition | none | — | must | — | Never drawn despite being a numbered Protocol; the guard (in-scope AND under-stakes AND above-reversibility AND unexpired) belongs on the ADMIT edge exactly as swk's claim-lifecycle figure puts its guards on edges. |
| ch4-09 | Consent to the Leviathan / Consent as a first-class, revocable primitive | The operator-override is an unconditional, unsuppressible edge from operator to daemon that preempts every grant, every in-flight landing, and every coordinator decision at once. | relational | Can any autonomy level ever block the operator's stop signal, and what does pulling it do to in-flight work? | already drawn as the caution-styled fourth message in fig:roles | fig:roles | redraw (done, partial coverage) | no | — | fig:roles shows the override edge but not its "preempts everything in flight" scope; adequately covered given the override's main content (unconditional, unsuppressible) is stated in one property and does not need its own geometry. |
| ch4-10 | The mechanism: legibility-with-zoom | Every summary is a lens onto a verifiable artifact, never a replacement for it; a digest whose claims resolve to nothing is a Potemkin digest whatever its claim count. | comparison | Do the digest's claims actually resolve to something the operator can open, or do they terminate in nothing? | two claim trees of identical shape, one resolving to named artifacts, one to nothing | fig:zoom-vs-potemkin | redraw (done) | must | internal | Already well-served; keep the identical-shape framing (PR 123 vs. ALL GREEN) that makes the two facades comparable at a glance. |
| ch4-11 | The mechanism: legibility-with-zoom / What to flatten, what to preserve | Administrative facts the operator's own system defines (ids, claim rows, phases, enums) may be flattened safely; the agents' m\^etis (diffs, test output, error messages, reasoning traces) must be preserved verbatim. | comparison | Which categories of digest content are safe to summarize, and which must never be paraphrased? | already an itemized two-bucket list in the text | none | — | no | — | A binary classification with five examples per bucket reads fine as bullets; a figure would not clarify a two-way split this simple. |
| ch4-12 | The mechanism: legibility-with-zoom / The zoom target must be verifiable | The canonical zoom target must be an artifact the operator can check independently of the agent (the diff on disk, the test result); the agent's own narration is a secondary, always-labeled lens. | relational | When the operator zooms, does it land on ground truth or on the agent's account of itself? | could pair with fig:zoom-vs-potemkin as a labeled distinction between the artifact leaf and a hypothetical self-narration leaf | fig:zoom-vs-potemkin (implicitly; not explicit) | — | could | — | The existing figure's three leaves (diff, run, comment) already model verifiable artifacts; an explicit "vs. self-narration" fourth leaf would make Prop. verifiable-zoom visible without a new figure. |
| ch4-13 | The mechanism: legibility-with-zoom / The four design questions | A read-surface earns release only through four independent proofs in order (irreversibility gated, state-vs-metis flattened correctly, claim independently checkable, authority act reasoned); the rail is conjunctive, so one missing proof holds the whole surface. | stateful | Which of the four required proofs is missing when a read-surface should not ship, and does the whole surface fail if even one is absent? | AND-gate ladder / four-stage gate chart: four sequential gates in series, each labeled with its question, an explicit "surface held" outcome if any gate fails | none (table, tab:ls-four-questions) | table (implemented per triage 4.4) | should | — | The existing table lists the four questions but not their conjunctive, gate-like structure the caption itself names ("the rail is conjunctive"); a small AND-ladder makes that structural fact visible rather than implied by column order. |
| ch4-14 | The mechanism: legibility-with-zoom / The four design questions | Rendering a digest line is a four-step construction procedure (project state, attach zoom, set the forced-zoom flag, attach the authority reason), refusing to ship a line that fails any step. | temporal | In what order is a digest line actually built, and at which step does the process refuse to ship it? | protocol ladder: four sequential steps with a refuse-to-ship branch off steps 2 and 4 | none | — | should | — | A numbered Protocol never drawn; distinct from ch4-13 (which gates release of a whole surface) since this is the construction procedure for one line within it. |
| ch4-15 | The Leviathan rules / authority half | Six authority mechanisms (roadmap, review, completion, landing, guard, escalation) cross one consent boundary, differing in input and outcome but sharing the recorded grant (scope, reason, identity, override, artifact). | comparison | What input does each of the six authority mechanisms take, and what does it always record? | table-not-figure: mechanism x input x outcome x what the grant records (tab:ls-authority-organs) | none (table) | table (implemented per triage 4.5) | no | — | Six rows, four columns, a lookup task; the shared grant-record column is the point and a table states it once rather than six times. |
| ch4-16 | The Leviathan rules / The roadmap as versioned intent | The roadmap's legitimacy is measured by whether it stays honest: every unit of work keys to a plan item, amendments are recorded as amendments, and drift is a surfaced anomaly rather than a hidden fact. | provenance | Does the swarm's actual output trace back to a plan item, and is drift from the plan itself visible? | — | none | — | no | — | Purely conceptual with no worked numbers or concrete instance in this chapter; a diagram would be decorative given nothing to anchor it to. |
| ch4-17 | The Leviathan rules / Adversarial review | A swarm reviewing its own work is two language models agreeing, which is one error reported twice; the quality organ must be adversarial and conflict-free. | relational | Does a second opinion from the same swarm actually add an independent check, or just repeat the first? | — | none | — | no | — | The point is made and closed in two sentences and cross-referenced to the reputation chapters' neutral-evaluator figures; no chapter-original content here needs its own drawing. |
| ch4-18 | The Leviathan rules / Sole-responsibility roles and the specialization boundary | Sole ownership of an invariant-critical role weakly dominates a pooled queue exactly when the skill premium clears an Erlang-C boundary g_A(rho,c); a superseded, falsified threshold is wrong in both directions, not merely conservative. | quantitative | At what skill premium does giving one agent exclusive ownership of a function beat pooling several generalists? | phase-boundary plot: skill premium vs. pooled utilization, correct boundary and superseded boundary both shown, error regions shaded, worked point marked | fig:specialization | restyle (done) | must | Thm. specialization / paper6, verified numerically over 2000 (rho,c) points | Already well-served; keep the worked point (rho=0.556, r=2.5, the roadmap-owner example) and the falsified curve shown as a deliberate retraction, not erased. |
| ch4-19 | The Leviathan rules / Sole-responsibility roles | Extending the specialization boundary to a specialist who can fail and be succeeded gives an exact succession-rate threshold D* = eta*K/(1-eta*K) past which pooling dominates at any skill premium. | quantitative | Past what breakdown-to-repair rate does no amount of hiring a better specialist recover sole ownership? | regime plot: succession threshold D* against the repair-to-failure ratio, with the "no specialist skill recovers this" region shaded | none | — | should | — | This closed-form threshold is a genuine open-problem answer (Exercise ex:ls-succession-corner, listed in tab:open) with real algebra but zero geometry, distinct from fig:specialization's never-fails case. |
| ch4-20 | The Leviathan rules / Completionist obligation | A completion claim's satisfaction predicate is a verifier derived from acceptance criteria; the authority refuses an unmet predicate, and an unverifiable one must fail loudly rather than silently pass. | stateful | Does a completion claim actually resolve against a checkable predicate, or does it silently pass on nothing? | small state machine: CLAIMED -> run verifier -> {DONE, FAIL-LOUD}, with the "predicate unverifiable" branch forcing fail-loud rather than an auto-pass | none | — | must | — | Called the paper's "single highest-leverage new mechanism" yet has zero figure; the book already draws this exact shape for swk's commitment-oracle (claim -> gate -> DONE/reject) — reuse that grammar here. |
| ch4-21 | The Leviathan rules / Thoughtful landing | A coordination state is physically unreachable (prevented) only when a constraint forbids it inline before commit; a downstream monitor that watches the log after the fact is detect-and-compensate, a strictly weaker guarantee, by Schneider's reference-monitor result. | comparison | Is a given safety rule actually enforced before the bad state occurs, or only detected and repaired afterward? | 2x2 controllability-style quadrant (prevented-by-construction vs. detect-and-compensate), reusing the swk grammar, with this chapter's own examples (uniqueness constraint vs. post-commit anomaly monitor) placed in cells | none | — | could | — | Semantically identical to II/fig:swk-controllability-quadrant; a full new figure would duplicate an existing grammar rather than add information, so a compact reuse-note or inset is enough. |
| ch4-22 | The Leviathan rules / Human-in-the-loop escalation | Agents separate at a unique validation threshold u*(delta) where escalating stops paying, and the set of dismissal debits meeting both an alarm-load wall and a miss-loss wall is an interval that can be empty, as it is at attention budget A=2.0 but not at A=3.3. | quantitative | At a given attention budget, does any dismissal debit exist that keeps the distress lane both uncrowded and low-miss, or is the feasible band empty? | threshold/regime plot: feasible debit interval [delta_min, delta_max] as a function of attention budget A, with the A=2.0 (empty) and A=3.3 (nonempty, [0.035, 0.072]) cases both marked | none | — | must | b7_escalation_band.py, verified closed forms | This is a real regime result with a striking finding (the band can vanish, and that is itself the correct diagnosis rather than a tuning failure) and it has never been plotted despite carrying the chapter's own worked numbers. |
| ch4-23 | The operator as a modeled entity / The attention objective, spined on SDT | The operator trades misses (rubber-stamping a bad merge) against false alarms (zooming on a safe diff) under asymmetric costs, and the miss-cost term prevents the zero-review solution. | quantitative | Where should the operator's forced-zoom criterion sit, given that a miss costs far more than a false alarm? | two-distribution decision plot: safe and dangerous score densities, criterion beta, miss and false-alarm regions tinted and labeled, d' marked | fig:sdt | redraw (done) | must | internal | Already well-served; keep the explicit numeric callout (100 vs. 5, inspect bar at a>=0.057) tying the idealized densities to the chapter's own running costs. |
| ch4-24 | The operator as a modeled entity / The attention objective | Collapsing the objective to zoom-everything-or-nothing on three real action classes gives auto-land for tests/-only diffs, zoom-barely for public-interface refactors, and forced zoom for a foreign-key-dropping migration. | quantitative | Where do three concrete action classes fall once the never-zoom and always-zoom costs are actually computed? | table-not-figure: action class x pi x C_miss x never-zoom cost x always-zoom cost x verdict (tab:sdt-numbers) | none (table) | — | no | — | Three rows and two computed columns is a clean lookup; a chart would not show the comparison faster than the existing table already does. |
| ch4-25 | The operator as a modeled entity / Trust calibration | Trust calibration must be per (action-class, agent) rather than one global scalar, since an operator can be correctly confident on refactors and correctly skeptical on migrations at once. | comparison | Does a single global trust knob serve an operator who is, in fact, well-calibrated on some action classes and not others? | — | none | — | no | — | Argued with one hypothetical scenario and no measured numbers of its own; the point is made adequately by the worked counterexample in the exercise solution. |
| ch4-26 | The operator as a modeled entity / Situation awareness | Situation awareness has three levels (perception, comprehension, projection), and a digest of only the present is SA-destroying no matter how good its zoom; a freeze-probe would validate a first-class projection mode. | temporal | Does the operator's read-surface tell them what already happened, what is happening, or what will happen next? | — | none | — | no | — | Conceptual and forward-referenced to an undesigned freeze-probe study; nothing chapter-original to anchor a figure to yet. |
| ch4-27 | The operator as a modeled entity / The abdication gradient | The Inception Canary replays a known, historical, already-fixed defect into a sandboxed past work item at a disclosed rate, and adjudicates the operator's response as either a rubber-stamp failure (tightened autonomy) or a vigilant detection (widened autonomy). | stateful | When a blinded, ground-truth-known defect is replayed to the operator, does the response tighten or loosen the daemon's trust in them? | protocol/decision flow: inject known defect -> surface in attention queue -> operator zooms? -> {rubber-stamp failure: halt + tighten lease} or {vigilant detection: log defeated + widen grant} | none | — | should | — | Called the design's "primary active defense" against abdication, with a numbered Protocol and an explicit two-outcome fork, and it is entirely unillustrated. |
| ch4-28 | Read-poverty: the binding constraint at scale | Legible state accumulates faster than any one operator can consult it, so participants fall back to O(n) eyeball search or act blind; a directory pays a write-time indexing cost to buy O(query) read time. | quantitative | As swarm size grows, does search cost scale with the population, or with the size of the query? | two aligned population-scale panels: search cost and directory value against swarm size n | fig:readpoverty | redraw (done) | must | closed-form Floor(N,k,m) | Already substantially served, though the figure's own caption admits it draws the analytic curve rather than the committed measured sweep (see ch4-62 below for that specific gap). |
| ch4-29 | Read-poverty / A legibility lower bound | The zero-miss digest floor shrinks as the review budget m grows: at N=1000, k=3, a budget of m=10 opens costs 20.4 bits, but widening to m=100 opens cuts the floor to 10.0 bits. | quantitative | How many fewer bits must the digest spend if the operator agrees to open more candidates? | regime plot: bit floor against review budget m at fixed N and k, with both named points (m=10 -> 20.4 bits, m=100 -> 10.0 bits) marked | fig:readpoverty (varies N at fixed k, m; does not vary m) | — | should | closed form (verified) | This is the chapter's own central intuition ("buying more opens buys fewer digest bits, at a priced rate") and its own distinct worked trade-off, not the same axis as the existing figure. |
| ch4-30 | Read-poverty / A legibility lower bound | Three encoders (oracle, noisy, random) were swept against the theoretical floor at N=60, k=2, m=8 with zero violations in sixteen configurations, confirming the floor's prediction rather than merely asserting it. | quantitative | Does any cleverer digest-encoding scheme actually beat the proven floor in practice? | small multiples: three encoder curves (oracle hugging the floor, noisy and random paying a premium) against the theoretical floor | none | — | could | a7_experiment.py | Secondary evidentiary content; the theorem's headline claim is already carried by fig:readpoverty and the "0/16 violations" prose result. |
| ch4-31 | Read-poverty / A legibility lower bound | A first implementation appeared to beat the information floor by leaking the critical set's identity through an unpriced side channel (8/14 apparent violations), and fixing the meter restored agreement. | comparison | Why did an apparent violation of the proven floor turn out to be a bug in the meter, not a refutation of the theorem? | before/after comparison: the leaky-oracle model (false violations) beside the corrected model (0/16 violations) | none | — | could | internal | A valuable falsification-first narrative but adequately told as a documented callout; not core to the reader's structural grasp of the chapter's main claims. |
| ch4-32 | Read-poverty / A legibility lower bound | A custom two-constraint rate prices a digest once misses are tolerated: both a miss-mass bound and a flag-rate bound bind simultaneously, pinning the joint distribution exactly, with the zero-miss corner recovering the source's full entropy. | quantitative | Once some misses are tolerated, how cheap can the digest get, and where does that price collapse to zero? | threshold/phase-boundary plot: axes delta (tolerated miss mass) and f (flag budget), the pinned-joint feasible region, the corner R(0,p)=H(p), and the line past which the rate hits zero | none | — | must | b1_frontier.py (internal), Cover-Thomas closed form (verified) | Zero figure despite a full theorem, proof, and worked numeric example (H(0.05)=0.2864 bits; R drops to 0.1864 and 0.0087 bits under two (delta,f) settings); this is the natural companion piece to ch4-29 and belongs in the same figure family. |
| ch4-33 | Read-poverty / A legibility lower bound | Adaptive halving over a flagged set of F items with k positives finds them all in at most 2k*ceil(log2(F/k))+4k group queries, against F opens for flat inspection, with the bound tight at specific (F,k) configurations. | quantitative | Given a flagged set that still contains a few true positives, how many fewer opens does adaptive group-testing need than reading every flag? | threshold/phase-boundary plot: guaranteed advantage adv(d) against flagged-set density d, with the crossover at d=1/12 marked and the F=2500,k=10 worked point (162.6 mean opens vs. 2500 flat) annotated | none | — | must | zoom_bound_check.py (internal), tightness verified at F=4096,k=32 | This is the single most numerically dense, fully proved result in the chapter (theorem, tight proof, mutation-style tightness check, and a live 15.3x measured reduction) with literally zero geometry; a phase-boundary plot on density is exactly the atlas's prescribed grammar for this claim. |
| ch4-34 | Read-poverty / A legibility lower bound | The two prices compose profitably only when the flag budget is at least twelve times the residual positive rate (f >= 12(p-delta)); outside that window the second-stage cost reverts to linear in F. | quantitative | When does over-flagging generously and then zooming adaptively actually pay off, versus reading everything flagged directly? | annotation on the same phase-boundary plot as ch4-32/ch4-33: the composition-window line f=12(p-delta), with the example (p=0.05, window opens at f>=0.60) marked | none | — | should | closed forms (verified); measured crossover ~0.165 (internal) | Part of the same recommended figure family as ch4-32/33; this specific composability condition is the chapter's own practical takeaway and deserves an explicit marked line rather than only prose. |
| ch4-35 | Discovery: the directory substrate / Existence | The directory substrate is FIPA's forty-year-old Directory Facilitator (push-based, federatable yellow pages) plus an Agent Management System white pages, re-invented for LLM agents largely unchanged. | provenance | Is the agent-discovery mechanism new, or a re-application of a solved prior-art shape? | — | none | — | no | — | Purely a prior-art positioning statement with no chapter-original numbers or mechanism; adequately conveyed in prose. |
| ch4-36 | Discovery: the directory substrate / Relevance: the split ranker | The discovery ranker scores each candidate as a clamped weighted sum of six sub-scores, four of them pull signals (observed) and two push signals (declared, gameable). | comparison | Which of the six ranking signals can a candidate inflate for free, and which are demonstrated by residue from other work? | table-not-figure: signal x what it measures x push-or-pull (tab:fit-signals) | none (table) | — | no | — | Six rows classified along one binary axis is a clean lookup; the push/pull split is the point and the table already states it. |
| ch4-37 | Discovery / Relevance: the split ranker | Two agents making identical identity and skill claims are ranked differently because only one leaves a complete file, note, episode, and current-load trail the ranker can verify. | comparison | Do two agents with the same self-declared claims actually rank the same once observed evidence is counted? | table-not-figure with symbols (circle/dot/cross) already comparing agents A and B across six evidence rows (tab:ls-pushpull) | none (table) | table (implemented per triage 4.10) | no | — | Already visual within a table (symbol-coded), a concrete worked instance; converting it to a full diagram would not add information the symbols do not already carry. |
| ch4-38 | Discovery / Relevance: the split ranker | No strictly increasing scalar transform can identify discovery fit with operator regret, because a highly capable, safe, reversible item and a low-capability, high-stakes, anomalous item order oppositely under the two objectives. | comparison | Can one ranker serve both the agent asking "who fits?" and the operator asking "what's dangerous?" | slope chart: two rank axes (discovery-by-fit, attention-by-regret), the fully-reversing A/B pair and a smaller adjacent-swap C/D pair | fig:split-ranker | restyle (done) | must | internal | Already well-served; keep the fully-reversing A/B pair as the theorem's certificate and the smaller C/D swap as the same fact in miniature. |
| ch4-39 | Discovery / Relevance: the split ranker | The general comonotone characterization says a single scalar head serves two readers exactly when their preference orders never disagree on any pair; the split-ranker theorem is the instance where they do. | relational | Under what general condition can two different readers safely share one ranking head? | — (covered by the same crossing-pair certificate as fig:split-ranker) | fig:split-ranker | restyle (done) | no | — | The abstract theorem's content is exactly what the existing figure's A/B reversal already certifies; no separate drawing needed. |
| ch4-40 | Discovery / Relevance: the split ranker | The regret head's product form is the exact Bayes-optimal ranking key only when anomaly equals the calibrated posterior; at the chapter's running costs the inspect bar sits at a>=0.057, falling to 0.006 as the miss cost rises tenfold, rising to 0.175 as the false-alarm cost quadruples. | quantitative | How much does the inspection threshold move as the miss-to-false-alarm cost ratio changes, at the chapter's own worked numbers? | small dot plot or bar comparison: inspection threshold a* under three named cost scenarios (baseline 0.057, high-miss 0.006, high-false-alarm 0.175) | none | — | should | closed form (verified) | Distinct from fig:sdt's conceptual density plot; this is the exact numeric threshold under three concrete cost scenarios and currently lives only in a worked example paragraph. |
| ch4-41 | Discovery / Relevance: the split ranker | Reputation enters the attention queue only through the calibrated posterior a(x), never as a free multiplier, and high reputation cannot zero out a catastrophic-stakes term. | relational | Can a trusted agent's reputation ever override a hard stakes gate on the attention queue? | — | none | — | no | — | A one-clause constraint (single legitimate channel for reputation) adequately stated in the theorem itself; no worked instance needs its own figure. |
| ch4-42 | Discovery / Relevance: the split ranker | The attention-queue ranking protocol runs four steps each refresh (generate with recency decay, score by regret, convert to a forced-zoom probability via the SDT criterion, lane and emit with suppression logged). | temporal | On each refresh, in what order does a candidate item move from the shared substrate to the operator's queue or the suppression log? | protocol ladder / pipeline diagram: generate -> score by regret -> apply cost matrix -> lane and emit, with the suppression-counter-surface branch shown | none | — | should | — | Never drawn despite being the actual runtime mechanism tying together the regret head (ch4-40), the SDT criterion (fig:sdt), and the split-ranker distinction (fig:split-ranker) into one operational pipeline. |
| ch4-43 | Discovery / The quality gates | Refuse-to-route (empty-with-explanation beats a confident wrong answer) and the costly escalation (a dismissed alarm debits standing) are the design's two most spoofable-resistant gates. | relational | — | — | none | — | no | — | The costly-escalation game-theoretic content is already fully captured by the escalation-threshold figure recommended at ch4-22; refuse-to-route itself is a simple binary gate not needing separate geometry. |
| ch4-44 | Discovery / Staleness and decay | A dead agent's file claim can remain "current" for hours after the process dies, blocking a live agent, until universal recency-decay (Prop. decay) lets a stale row age out of current status. | comparison | Before and after applying universal recency-decay, does a live agent stay blocked by a dead one's claim? | before/after paired panel: "without decay" (dead claim blocks forever) vs. "with decay" (claim ages out at its half-life, live agent proceeds) | none (the general decay mechanism has a sibling figure in II/fig:swk-marker-decay) | — | should | — | The abstract exponential-decay curve already exists in Volume II; this chapter's own contribution is the concrete worked failure story (a dead claim blocking a live agent for hours), which a before/after pair would dramatize better than a generic decay curve reuse. |
| ch4-45 | Discovery / Ambient legibility | Attention is a vector over stage x modality x code x channel, not a scalar pool, so a separate ambient channel (menu-bar indicator, sound) can load a different, pre-attentive channel than the dense console. | relational | — | — | none | — | no | — | Purely conceptual with no chapter-original quantitative content; the human-factors citation carries the claim without needing a diagram here. |
| ch4-46 | Tokens: COGS and legibility engine | Tokens are simultaneously the swarm's cost-of-goods-sold and its legibility engine, bridged by compaction; effective context is much smaller than advertised context, so budgeting to it is both a cost and a quality optimization. | comparison | Does trimming context to its effective window save money, hurt quality, or both at once? | — | none | — | no | — | Conceptual framing with citations (Lost in the Middle, context rot) rather than a chapter-original quantitative claim; no concrete numbers to anchor a plot. |
| ch4-47 | Tokens / Compaction is the legibility engine | The successor agent (continuation loss) and the human operator (oversight loss) want incompatible compactions of the same trace, so no single compaction minimizes both losses at once. | comparison | Does the one compaction that best helps a successor continue the task also best protect the operator from a hidden breach attempt? | slope-chart or crossing-pair diagram analogous to fig:split-ranker, with the successor/operator reader pair replacing the discovery/attention pair | fig:split-ranker (same underlying comonotone theorem, different reader pair) | — | should | — | Structurally identical to fig:split-ranker's grammar; per the atlas's "do not make 81 copies of the same diagram" guidance, extend or caption-cross-reference the existing figure rather than drawing a near-duplicate, but the theorem itself currently has zero visual anchor of its own. |
| ch4-48 | Tokens / Compaction is the legibility engine | Splitting one compaction into two disjoint-critical-set reader heads costs strictly more than double: at N=60, m=8 the penalty ratio runs from 1.029 at k=1 to 1.224 at k=4, reaching 1.392 by k=10 at m=20. | quantitative | As each reader's critical set grows, how much worse than "just double it" does running two separate digest heads actually get? | measured-quantity plot: split-penalty ratio against k, for two review-budget curves (m=8 and m=20), with the named points plotted | none | — | must | closed form from Thm. lowerbound (verified) | A distinct growth curve from fig:readpoverty's single k=2 comparison point; the chapter gives four to five concrete ratio values across two m settings and none of them appear in any figure. |
| ch4-49 | Tokens / Compaction is the legibility engine | The split-digest theorem's architectural corollary is two separate pipelines: a task-continuation head authored by the worker agent, and an air-gapped SLM oversight sidecar reading raw kernel events under a fixed, authority-free policy. | containment | Do the two compaction heads share any code path, or are they architecturally separate all the way down to what each is allowed to read? | two-pipeline swimlane: worker trace -> task-continuation head (R1); raw kernel events/disk diffs -> SLM sidecar -> oversight digest (R2), with the sidecar's "no tool authority, cites kernel source" constraint labeled | none | — | should | — | Concrete and architectural, distinct from the abstract split-digest theorem; a two-lane diagram would show the "never let the worker author its own oversight summary" consequence directly. |
| ch4-50 | Tokens / Compaction is the legibility engine | Five reader-specific projections (briefing, attention, episodic memory, pheromone, resurrection) select different slices of one durable event spine, each retaining ledger addressability for what it drops. | comparison | For a given reader, what does their projection keep from the shared event spine, and what does it drop but keep addressable? | table-not-figure: surface x reader x what it selects x what it drops (tab:ls-read-surfaces) | none (table) | table (implemented per triage 4.12) | no | — | Five rows, a clean lookup task; the atlas prefers a matrix here over a diagram, and the table already states the point. |
| ch4-51 | Tokens / Context paging | Modeling context compaction as virtual memory gives a competitive-ratio bound k/(k-h+1) on online paging cost relative to an h-pinned offline optimum, worked at k=8: h=4 gives 1.6x, h=8 (classical, unaugmented) gives 8.0x, h=1 (fully augmented) gives 1.0x. | quantitative | As more of the context window is verifiably pinned, how much closer does the online paging policy get to the offline optimum? | measured-quantity plot: competitive ratio k/(k-h+1) against pin count h at fixed k=8, with the three named points (h=1, h=4, h=8) marked | none | — | should | b9_context_paging.py (internal), Young 2002 closed form (verified) | Entirely unillustrated despite a full theorem, an adversarial-corruption extension, and 122 verified instances; the chapter's own three-point worked example (h=1,4,8) maps directly onto a simple regime plot. |
| ch4-52 | Tokens / The context-degradation cascade | Four named degradation rungs (lost-in-the-middle, context rot, recursive-summarization collapse, over-flattening) each have one named cure, and the compaction-strategy table lists what each of six strategies drops, how it fails, and when it is the right tool. | temporal | For each of the four ways compaction can go wrong, what one change stops it becoming the next round's source of truth? | table-not-figure (tab:compaction); the cascade itself stays prose | none | delete (implemented per triage 4.13: "a hypothesis without data is a sentence") | no | — | Matches the Wave-11 rationale exactly: the four rungs are a measured-nothing list, correctly left as prose, with the compaction-strategies table doing the comparative work instead. |
| ch4-53 | Tokens / Single-operator: account, don't auction | In the wedge all agents serve one operator, so token spend needs accounting (a ledger, budget caps, loud failure) rather than a market; Sybil-resistant pricing is deferred to the cross-operator economy. | allocation | — | — | none | — | no | — | Purely a scoping statement with no chapter-original numbers; the deferred market mechanics belong to the economy chapter's own figures. |
| ch4-54 | Reconciling the canon / Who is the multitude, who is the sovereign? | Agents are the Hobbesian multitude bound by covenant, the daemon is the sovereign-as-actor (but the most legible, not the least), and the operator is the author who originates and can withdraw authority. | relational | Which of {agents, daemon, operator} plays which Hobbesian role, and why does the metaphor break if the operator is called the sovereign? | three-lifeline sequence diagram: operator grants, agents act under the grant, the daemon gates/escalates, the operator's override edge | fig:roles | redraw (done) | must | internal | Already well-served; keep the "no crossing edges" discipline and the override as the one edge bypassing the agents entirely. |
| ch4-55 | Reconciling the canon / Express consent, not tacit | The consent grant is express by Locke's own criteria (discrete, scoped, revocable, audited), unlike "the operator just started using the tool," which is tacit consent Hume already demolished. | comparison | — | — | none | — | no | — | Already covered structurally by the consent-grant schema (ch4-07) and lifecycle protocol (ch4-08); no additional geometry needed for the philosophical framing itself. |
| ch4-56 | Reconciling the canon / M\^etis is Hayek | The binding constraint is decentralized, non-spawnable knowledge held in one head, a Hayekian knowledge-allocation problem as much as a Wickens attention-resource problem. | relational | — | — | none | — | no | — | Conceptual synthesis connecting two literatures with no chapter-original quantitative content; adequately carried in prose. |
| ch4-57 | Related work and prior art | The chapter positions its six contributions against the nearest prior art in six literatures (legibility, social contract, automation/trust, information foraging, discovery/trust, queueing/staffing). | comparison | Against which specific prior work does each of this chapter's six claimed contributions sit? | table-not-figure: literature x nearest prior art x this paper's move (tab:related) | none (table) | — | no | — | A positioning table is the correct grammar for literature placement; a diagram would not out-perform six rows of direct comparison. |
| ch4-58 | The time axis and the bridge to the economy / Legibility over time | Legibility spans three tenses: the forensic past (a replay scrubber with commit-anchored provenance), the inspectable present (digest-with-zoom), and the projected future (freeze-probe situation awareness). | temporal | Does the operator's read-surface answer "what happened," "what is happening," or "what will happen," and does it cover all three? | three-tense timeline: past-replay / present-digest / future-projection, each linked to the section and mechanism (built/designed/vision status) that realizes it | none | — | could | — | A synthesis figure tying together the projection mode (ch4-26) and the forensic history mode; useful as a bridging visual but built mostly from already-discussed conceptual pieces rather than new content. |
| ch4-59 | The time axis and the bridge / What this layer hands upward | This layer hands the accompanying chapters legible continuity, a completion ledger, a priceable consent grant, and operator-attention economics; no legible continuity means no reputation and no tradeable agent. | provenance | What exactly does this chapter's legibility layer supply to the identity, reputation, and market layers above it? | provenance chain: four named outputs each with an arrow to the accompanying chapter that consumes it | none | — | could | — | A bridging diagram would be pedagogically nice but is a synthesis of points already made individually elsewhere in the chapter; lower priority than the theorems still missing any visual. |
| ch4-60 | The time axis and the bridge / The open problems, as a map | Eleven open research problems are consolidated with a difficulty rating and the mechanism each couples to, several of them coupling this layer forward to reputation. | comparison | Which open problems are hardest, and which mechanism does each one extend or threaten? | table-not-figure: open problem x difficulty x couples-to (tab:open) | none (table) | — | no | — | Eleven rows with a star-rating column is a clean lookup/roadmap table; already the right grammar. |
| ch4-61 | Appendix: Implementation and status | Twenty-five named mechanisms are each labeled built, built-(weak), designed, or vision, split across the legibility, authority, and operator-model halves; the legibility read-surfaces are largely built, the authority half largely designed-to-proposed. | comparison | For any given mechanism named in this chapter, does it exist in code today, or is it still a design or a vision? | table-not-figure: category x mechanism x status x evidence/location (tab:honest-state) | none (table) | — | no | — | Twenty-five rows is well past the point where a diagram would help; a status matrix is exactly the atlas's prescribed grammar for a lookup task this large. |
| ch4-62 | Read-poverty / A legibility lower bound | The falsification sweep's own measured points (5.98 bits at N=60, k=2, m=8, from a7_experiment.py, zero violations across sixteen configurations) are the actual evidence behind Thm. lowerbound, but the shipped readpoverty figure draws the closed-form analytic curve instead because the sweep's CSV is not yet committed. | quantitative | Do the plotted legibility-floor curves show the theorem's prediction, or the falsification sweep's own measured confirmation of it? | overlay the falsification sweep's measured (N,k,m) points onto the existing analytic curves once r1-floor.csv is committed | fig:readpoverty | redraw (done, but caption admits a data gap) | should | a7_experiment.py (internal); CSV not yet committed | The existing figure's own caption names this exact gap ("the committed sweep CSV is not yet present... only the N=60 values it works by hand are marked"); closing it means committing the sweep data and adding measured markers, not redrawing the figure's grammar. |

## Chapter 5 — From Spawn to Person

Source: `website-v2/public/whitepaper/spawn-to-person.tex`.
Existing figures found: 10 labelled `figure` environments (`fig:role-person`, `fig:parfit`, `fig:stp-nomint-lineage`, `fig:stp-probation-cliff`, `fig:sybil`, `fig:multidim`, `fig:judge-market`, `fig:rate-raters`, `fig:stp-deterrence-regime`, `fig:tombstone`), plus one table shared with Chapter 6 (`tab:keystone-split`) and one unlabeled inline dependency diagram in the introduction.
Rows: 41.
Three most under-served ideas: (1) the engine-swap adverse-selection theorem and its attested flip (θ_H=1, θ_L=0.4, flip = −0.3) has zero visual; (2) the model-checked resurrection-soundness theorem (747 states, depth 7, three named one/two-step escapes) has zero visual; (3) the audit-tower's amortized verification spend across three regimes (Θ(T)=50.00, Θ(log T)=25.58, O(1)→41.67) is reported only in prose sentences.

| id | section | idea (one sentence) | structure | reader question | recommended form | existing figure | wave-11 disposition | priority | data source | notes for the renderer |
|---|---|---|---|---|---|---|---|---|---|---|
| ch5-01 | Reader's Map | Four reader types are routed to the section and artifact that answers their question. | relational | Which four pages do I actually need to read? | table (routing matrix: reader type × start section × critical artifact) | tab:readers-map | — | no | — | Already a clean table; a diagram would only decorate a lookup task the table already serves. |
| ch5-02 | Introduction (S1); What this paper claims | The book's L0→L3 ladder places this chapter as the unfinished bridge between continuity and a tradeable reputation, and three refusals mark exactly where reality stops on that chain. | comparison | Where on the daemon→market ladder does Port Daddy's reality actually stop? | step chart (5 rungs: memory+checkpoint / continuity / person / reputation / tradeable asset, each tagged implemented/partial/specified/proposed) | none (inline, unlabeled tikzpicture in §sec:intro) | delete (5.1, `fig-stp-stack-map`) | no | — | The Reader's Map table and the route paragraph already say this; per the triage rationale, redrawing would duplicate prose that already carries the claim. |
| ch5-03 | Introduction, S1 | Five named agents (Drake, Wren, Pike, Finch, Heron) produce eleven PRs in one night, and only a persistent identity would let Alice reward or distrust any of them individually. | temporal | Which of tonight's five agents deserves credit, and why can Alice not act on that tomorrow? | swimlane (5 agent lanes over one night, each lane's PRs tagged good/bad/nothing-survived, with a dashed "gone by morning" boundary) | none | — | should | hand-worked in prose (no script) | Use the real outcomes given in text: Drake deleted the failing test, Wren buried a real fix in a bad PR, Heron shipped the fix, Pike/Finch produced nothing; the counter-reading to guard against is implying the agents themselves are the identity (they are not — the point is that nothing survives to reward). |
| ch5-04 | Prior art, §2.2 | Four skill-rating estimators (Elo, Bradley–Terry, TrueSkill, EigenTrust) each fit a different signal type, and choosing the wrong one is a real but secondary error. | comparison | Which estimator fits which kind of witnessed signal? | table (estimator × signal needed × strength × wrong-if) | tab:estimators | — | no | — | Genuinely tabular reference content; a figure would not out-perform the existing table. |
| ch5-05 | Prior art, §2.5 | Against four literatures, this paper's actual contribution is narrow: the necessity/cost theorems, the judge-per-axis vector, and treating grader incentive-compatibility as a first-class hole. | comparison | What exactly is new here versus the nearest prior art? | table (question × closest prior art × this paper's contribution) | tab:contributions | — | no | — | Already a clean three-column table; the SUBMISSION-SKILL comment flags an unresolved citation gap that belongs in prose, not a redraw. |
| ch5-06 | Role/person distinction, §3 | A role is a replaceable org-chart slot; a person is one non-forgeable identity that walks through a sequence of roles while witnessed outcomes accumulate on it. | comparison | What persists when the body filling a job changes, and what does not? | paired rails (upper: one role, four interchangeable fillers, no history; lower: one identity κ, four role intervals, eight witnessed outcomes) | fig:role-person | restyle (5.2: heavier rails, footnotesize labels, name fillers/outcomes with dates) | should | — | Keep the existing two-rail grammar; the counter-reading to design against is a reader assuming the role rail also accumulates reputation — the caption and a visual break must forbid that. |
| ch5-07 | Role/person, §3.1 | Capability (what a runtime makes possible) and permission (what the deontic layer allows) are different coordinates, and the important states are exactly where they disagree — capable-but-forbidden (a force-merge tool no agent may use). | relational | Can an agent's capability and its permission ever legitimately disagree, and what does each disagreement state mean? | quadrant (capability × permission, 2×2, one concrete example per cell: capable+permitted = run the test suite; capable+forbidden = force-merge; permitted+incapable = authorized to deploy, no credential) | none | — | should | — | Mirrors the kernel chapter's controllability quadrant grammar; the counter-reading to design against is collapsing capability into permission, which the pitfall box explicitly warns erases the guardrail. |
| ch5-08 | Continuity, §4.1 | Locke's memory criterion has a famous bug: direct memory connectedness is not transitive (Reid's general/officer/boy), so identity cannot be defined by connectedness alone. | provenance | Why can't "the agent remembers its past" be the whole story of identity? | — (folded into ch5-09) | fig:parfit | restyle (5.3) | no | — | Reid's counter-example is exactly the dashed "same label, no shared witness" branch already drawn in fig:parfit; it does not need its own figure. |
| ch5-09 | Continuity, §4.2 | Parfit's repair makes identity an overlapping chain of shared witnessed outcomes, transitive even where no single incarnation directly remembers the first. | provenance | How can three incarnations be one continuous identity if none of them shares a witness with the first? | provenance/interval chain (three incarnation intervals, two overlapping shared-witness marks o₂, o₃, one dashed no-shared-witness counterexample) | fig:parfit | restyle (5.3: heavier overlap boxes with edges, name the failing check on the counterexample) | should | — | The counter-reading to design against is a reader thinking a repeated label alone establishes continuity — the dashed branch already exists to forbid this; keep it prominent. |
| ch5-10 | Continuity, pdexample | Reputation must be inherited by transfer (debit the source, grant a discounted share) not by copy: budget-only phrasing lets one episode's closure sum to 2.439 > 1, while transfer telescopes the same chain to 0.729 ≤ 1, and an 8-way copy-fork mints 8.2× a witnessed episode. | provenance | Why must a derivation debit its source, and what happens if it merely stays within a budget instead? | three small lineage DAGs, side by side (budget-only: all nodes live, sum 2.439; transfer: only the leaf live, 0.729; copy-fork: eight live copies, 8.2×) | fig:stp-nomint-lineage | add (fulfilled — matches the triage's "R12 copy-fork attack" add spec) | must | `a6_no_mint.py`, seed 20260816 | The three-panel comparison is the whole theorem in one glance; the counter-reading to design against is a reader assuming budget-legality (≤1 at every hop) is the same as no-minting — it is not, at γ=0.9 a chain of budget-legal grants still sums past 1. |
| ch5-11 | Three organs, §5 | The three continuity organs survive process death by different depths: memory keeps notes whole, checkpoint keeps a summary and lineage but not execution state, the outcome ledger keeps only what was witnessed. | comparison | If an agent dies mid-task, what exactly does its successor inherit from each organ? | table (organ × what the successor inherits × what it does not) | tab:stp-organs | table (5.4, triage recorded redraw; realized as a plain table rather than a figure) | no | — | The realized table already states inherits/does-not-inherit per organ in words; no additional geometry would answer the reader's question faster. |
| ch5-12 | Three organs, §5.2 | The restart organ forwards a summary, not execution state; a specified but unshipped replacement (Event-Sourced Neural Rehydration) would restore lineage and, within one provider/model/version, a behaviorally equivalent continuation — but never the actual inference state (KV cache, activations). | stateful | What exactly would "checkpoint with teeth" restore, and what would it never restore, even if it shipped? | paired panels or a small state trace (crash point → truncate log → replay under prompt-prefix caching → resume; one panel same-provider "task continuity", one panel cross-provider "actor succession") | none | — | should | — (proposed mechanism; the "fidelity rate" is a stated but unmeasured hypothesis) | Must visually distinguish restored (Git SHA, claim, message sequence) from never-restored (KV cache, sampling state, hidden activations); the counter-reading to design against is selling this as real checkpointing, which the pitfall box explicitly forbids. |
| ch5-13 | Three organs, §5.4 | The canonical seam sentence and the appendix maturity ledger are the paper's own honesty check: every major claim graded on one implemented/partial/specified/proposed scale. | comparison | Which of this chapter's mechanisms actually run today? | table (component × maturity × grounding) | tab:app-status | delete (5.5, cross-reference the existing table rather than a separate dot-plot) | no | — | The existing appendix table already is the falsifiable ledger the keyidea box points to; a redundant maturity figure was explicitly rejected in triage. |
| ch5-14 | Identity, §6 (Thm. necessity) | Free identity minting defeats a sanction only up to the newcomer floor r₀; an actor with prior score above r₀+Δ has nothing to gain from whitewashing. | quantitative | At what prior reputation does whitewashing stop paying? | threshold/regime plot (x = prior score r(i), y = accessible score after a sanction, honest line r(i)−Δ vs floor r₀, kink at the indifference point r(i)=r₀+Δ) | none (tab:necessity-numbers gives two example rows only) | — | could | hand-worked in text (r₀=50, Δ=30 ⇒ indifference at r(i)=80) | The existing table already carries the two worked rows (mediocre record escapes free at 50, strong record eats the loss at 90); a general regime plot would generalize the kink but the point is already made without one. |
| ch5-15 | Identity, S2 | Replaying Alice's swarm with a non-forgeable identity: Drake's tombstoned sanction now follows the credential he cannot drop, so tomorrow's incarnation either carries the sanction or is treated as an economically capped newcomer. | temporal | What changes about tomorrow night's respawn once identity is non-forgeable? | — (already covered by fig:sybil, panel A) | fig:sybil | redraw (5.6) | no | — | This narrative is the concrete instance fig:sybil's panel A already generalizes (respawns vs. penalty retained); no separate figure is needed. |
| ch5-16 | Identity, §6.1 | Non-forgeable identity forecloses two classic attacks by name: cheap-pseudonym whitewashing and Sybil quorum inflation. | relational | Which two named attacks does a daemon-minted identity actually close? | — (already covered by fig:sybil) | fig:sybil | redraw (5.6) | no | — | Both bullets map directly onto fig:sybil's two panels; a separate list-figure would be redundant. |
| ch5-17 | Identity, §6 (Thm. whitewash-cost) | A whitewasher who resets to the newcomer ceiling forgoes cumulative surplus W = Σ(π(κ*) − π(κₜ)) over the maturation window; at κ₀=10, κ*=100, a 5-period linear climb gives W=18.0, so a fraud gain of 15 is deterred and 25 is not. | quantitative | How much does resetting to newcomer status actually cost, in the units the theorem uses? | step chart (x = period t, y = ceiling κₜ and surplus π(κₜ), shaded gap area to π(κ*) summing to W) | none (tab:whitewash-numbers gives the period-by-period numbers already) | — | could | hand-worked in text (κ₀=10, κ*=100, κₜ=10+18t, π(κ)=0.1κ) | The existing table already shows all five periods' ceiling, surplus, and gap; a step-chart mainly adds visual intuition for the shrinking-gap shape, which the table's numbers already convey. |
| ch5-18 | Identity, §6 (Thm. probation-dominance) | Among all newcomer-restriction schedules that deter a given fraud gain, the cheapest one for honest newcomers is a front-loaded cliff at t=0, never a gradual ramp — delaying one unit of deterrence to period t multiplies the honest cost by (δ_h/δ_f)^t. | quantitative | Why is a harsh-then-nothing newcomer schedule cheaper for honest entrants than a gradual one? | bar chart (x = period t the deterrence is placed at, y = honest burden as a multiple of the cliff's, labelled 1.58× at t=1 up to 62.5× at t=9) | fig:stp-probation-cliff | add (fulfilled — matches the triage's "B6 probation cliff" add spec) | must | `b6_probation.py`, seed 20260816 | The bar chart already exists and carries exactly the exchange-step numbers; the counter-reading to design against is assuming a smoother ramp would be gentler — it is provably worse for every δ_f<δ_h. |
| ch5-19 | Identity, §6.2 (S2/theorem support) | Principal binding changes the economics of both classic attacks: sanctions persist across respawns, and quorum weight stays one-per-principal instead of one-per-alias. | comparison | Does non-forgeable identity actually change attacker payoffs, or just relabel the same vulnerability? | paired panels (A: respawns vs. penalty retained, principal-bound flat line vs. self-asserted drop to zero; B: aliases minted vs. quorum weight, one-vote-per-label diagonal vs. flat one-principal line) | fig:sybil | redraw (5.6: real numbers from the identity-organ scripts on each axis) | should | — | Scope the caption tightly: this closes attacks inside one trusted daemon only, and explicitly does not solve cross-operator attestation — the sybil figure already states this, keep it. |
| ch5-20 | Identity, §6.3 | The economic counterparty in every trade is the principal, not the agent (Hobbes's actor/author split); bonds, reputation, and sanctions must key on the principal or Sybil bond-farming works by spawning fresh agents. | relational | Who is actually liable when an agent misbehaves — the process, or something above it? | small containment diagram (one principal node containing N agent-identity nodes, aggregate bond/exposure labelled at the principal) | none (partially echoed in fig:sybil panel B's "one principal = one accountable weight") | — | could | — | Keep this subordinate to fig:sybil rather than duplicating it; a standalone containment figure is optional polish, not required to follow the argument. |
| ch5-21 | Identity, §6.4 (body behind the name, Thm. engine-swap a) | Unattested, swapping to a cheap engine behind a good name pays a constant gain Δc regardless of price, so no price can support pooling on the strong engine and quality unravels to the Akerlof fixed point. | quantitative | If nobody can see which engine is actually running, why does the market spiral toward the cheap one? | threshold/regime plot (x = price p or committed share μ, y = payoff/gain; flat Δc line for the unattested swap gain, marked μ* threshold below which the pool collapses) | none | — | must | hand-worked in `pdexample` (θ_H=1, θ_L=0.4, c_H=0.5, c_L=0.2 ⇒ Δc=0.3, μ*=1/6≈0.167, bond B*=0.5); swept in `b5_engine_substitution.py`, seed 20260816 | This is the chapter's Akerlof-variant theorem and it has zero existing visual; the counter-reading to design against is assuming attestation alone fixes this — part (b) shows the flip only holds under full price pass-through. |
| ch5-22 | Identity, §6.4 (Thm. engine-swap b) | Once the engine is attested and the price schedule fully passes quality through, the swap gain becomes Δc − Δθ: swapping now pays exactly when the cheap engine is socially efficient, at zero audit stake. | quantitative | Does attesting the engine force everyone onto the expensive model, or does it just align incentive with efficiency? | paired panel with ch5-21 (second regime showing the attested flip: gain = Δc−Δθ = −0.3 at the worked numbers, crossing zero exactly at the efficiency boundary) | none | — | must | hand-worked in `pdexample` (attested keep-strong payoff 0.50 vs. swap payoff 0.20, flip = −0.3); `b5_engine_substitution.py`, seed 20260816 | Pair directly with ch5-21 as one before/after figure — unattested spiral vs. attested flip — since the theorem's whole point is the contrast between the two regimes on identical numbers. |
| ch5-23 | Identity, §6.5 (Model-checked property: resurrection soundness) | A migrated agent's sanction-respecting reputation survives provider resurrection only under three clauses (verified lineage, per-outcome engine attestation, closed/escrowed open commitments); dropping any one admits a named 1–2 step escape (whitewash-by-resurrection, engine-history shedding, liability stranding). | provenance | Which exact safeguard, if dropped, lets a resurrected agent cheat, and how fast? | state machine / protocol diagram (migration path with three guard clauses; each dropped clause branches to its named escape, annotated with its shortest-crime step count: scope 1, clauses 2/2/2) | none | — | must | `b5_engine_substitution.py` (Part 3), seed 20260816: 747 reachable states to depth 7, 0 violations intact | This is a fully model-checked security theorem with zero visual anywhere in the series; the counter-reading to design against is the "wrong turn" itself — keying sanctions per (principal, engine) instead of per principal breaks in one step with no migration at all, and that trap should be drawn as the first, cheapest escape. |
| ch5-24 | Identity, §6.5 (pdboundary) | The engine-swap theorem is a two-period stylization with no long-horizon reputational value, and the resurrection check is bounded model-checking (depth 7, ≤2 migrations) that certifies the clauses have teeth, not that the clause list is complete. | comparison | What does the resurrection theorem NOT certify? | — (prose callout) | none | — | no | — | A standard boundary callout suffices; drawing "what a theorem doesn't cover" as a figure would overclaim the kind of rigor the box is deliberately declining to claim. |
| ch5-25 | Keystone split, §7 | Everything Alice's daemon knows about an actor's own key, history, and registry is also checkable from Bob's foreign view — except the last row: nobody outside the daemon can check that the key is bound to an accountable principal. | relational | Which parts of local identity cross the trust boundary intact, and which one does not? | table (claim × inside the daemon × from Bob's foreign view, with the last row's "no cross-operator binding proof" in bold) | tab:keystone-split | table (5.7, shared with ch6-13) | should | — | Shared source file with Chapter 6; keep the four-row shape exactly as-is since Chapter 6 reuses it verbatim rather than redrawing. |
| ch5-26 | Multi-dim reputation protocol, §9.1 | A witnessed outcome carries a quality vector (accuracy, aesthetics, efficiency), each judged by a different competent judge, so a buyer applies their own weighting instead of the platform minting one Goodhart-able scalar. | comparison | Can two outcomes rank oppositely for two different buyers without either one being wrong? | small-multiple dot profile (two outcome vectors A, B over three judged axes, plus two buyer weight vectors selecting opposite outcomes) | fig:multidim | redraw (5.10: use the chapter's own S3 numbers — Heron's vector (1.0, 0.7, 0.9) and Bob's weighting 0.78) | should | — | Replace the abstract A/B example with Heron's concrete vector so the figure and the S3 narrative (ch5-28) are the same instance; the counter-reading to design against is a reader looking for one "winning" outcome instead of a legitimate preference reversal. |
| ch5-27 | Multi-dim reputation protocol, §9.2 (Protocol judge-hiring) | A judge is drawn from a pool with every principal-linked or model-family conflict removed before selection, posts a bond, grades blind, and remains exposed to a later re-audit that can slash it. | stateful | How is a judge made neutral, and what keeps it honest after it grades? | state machine / sequence (submit → eligibility filter → public draw → bond post → blind grade → ledger → re-audit, with exclusions shown as rejected branches) | fig:judge-market | redraw (5.11) | should | — | Keep the two-proof-moment structure (neutrality before selection, accountability after) that the existing figure already draws; the counter-reading to design against is treating "neutral" as a one-time label rather than a checkable history. |
| ch5-28 | Multi-dim reputation protocol, S3 | Heron's refactor grades (1.0, 0.7, 0.9) across three judges; Bob's security-weighted score is 0.78, and reading the vector (not the scalar) lets him keep Heron while requiring a second reviewer — a decision the scalar would have hidden. | quantitative | What does a real graded job look like end to end, with real numbers? | worked example annotated onto fig:multidim / fig:judge-market rather than a new figure | fig:multidim, fig:judge-market | redraw (5.10, 5.11) | should | hand-worked in `pdexample` (weights 0.2/0.6/0.2 ⇒ 0.2·1.0+0.6·0.7+0.2·0.9=0.78) | This worked example is the concrete instance that should populate ch5-26 and ch5-27 rather than spawning a third figure; the later Mara re-audit twist (S4) is the tombstone figure's own worked instance (ch5-35). |
| ch5-29 | Multi-dim reputation protocol, §9.3 (Protocol skill-helpfulness) | Each graft of a skill into an agent's context emits a witnessed event (skill id/version, non-forgeable agent id, task descriptor, context-graft receipt, outcome and quality vector), letting a buyer estimate helpfulness from matched tasks without trusting the skill's author. | provenance | What exactly does the system record so "this skill helped" is checkable rather than asserted? | ledger table / schema figure (field × purpose, with the matched-comparison-set logic called out: tasks with s vs. comparable tasks without s) | none | — | should | — | This is explicitly called "shippable-soon" and "the cleanest near-term instance of the whole thesis," yet has zero visual; the counter-reading to design against is treating the graft-success pair as proof of causation — the pitfall box explicitly says attribution is not causation. |
| ch5-30 | Grading oracle, §10 (Property: the hole) | If the evaluator producing a grade can be captured, every incentive-compatibility claim that settles on that grade is only as strong as the evaluator's own honesty. | relational | Why doesn't "the daemon derives the grade" already solve incentive compatibility? | — (prose statement) | none | — | no | — | This is a one-sentence logical point (assuming the evaluator honest assumes the conclusion); the two resolutions that follow (Options A/B) are drawn in ch5-31/32. |
| ch5-31 | Grading oracle, §10 (Becker condition) | Local deterrence holds only where the re-audit probability ρ clears ρ*=G/(dB); at the program's own parameters (G=10, d=0.8, B=50) that threshold is ρ*=0.25, and dropping the detector term d under-quotes the required rate by 20%. | quantitative | At what bond size and audit rate does grading-for-sale stop paying? | threshold/region plot (x = re-audit probability ρ, y = bond size B, frontier ρB=G/d, worked point ρ=0.25 at B=50 marked) | fig:stp-deterrence-regime | keep | should | hand-worked in text (G=10, d=0.8, B=50) | Already figcheck-clean per triage; no changes needed beyond what exists. |
| ch5-32 | Grading oracle, §10 (Thm. tower-imported) | Recursive rate-the-raters auditing terminates only because corrupt opportunity contracts geometrically (λ=1−ρd) under sealed, heterogeneous-clique sampling; a heterogeneous pool (C=8) needs 27 audit levels / $1,350 of bond, a monoculture (C=1) needs 53 / $2,650. | quantitative | Does auditing the auditors ever actually bottom out, and how expensive is it? | log-scale contraction plot (x = audit level k, y = remaining corrupt opportunity G_k, two curves for C=8 vs. C=1, amber floor marking the still-unsupplied honest root) | fig:rate-raters | restyle (5.12) | should | imported theorem, `harborpaper3` ("Reputation is Amortized Verification") | The counter-reading to design against is believing the contraction theorem manufactures the honest root at level 0 — it explicitly prices the depth to that root without supplying it; the amber floor must stay visually distinct from the contraction curves. |
| ch5-33 | Grading oracle, pdexample (audit-tower amortization) | Flat auditing spends Θ(T)=50.00 over 200 periods; letting the audit rate fall as accumulated stake grows (Model A) spends Θ(log T)=25.58; letting cheats surface on their own (Model B) spends O(1), converging to 41.67 and hitting zero audit at t*=333. | quantitative | Is a constant audit rate the cheapest way to sustain deterrence over time, and by how much do the alternatives save? | xy plot (x = period T, y = cumulative audit spend, three curves: flat linear, Model A logarithmic, Model B converging to a bound) | none | — | must | `b2_tower.py`, seed 20260816 | Three distinct asymptotic classes (Θ(T), Θ(log T), O(1)) are reported only as three numbers in a paragraph; the counter-reading to design against is assuming Model B is strictly best — it converges to a higher total (41.67) than Model A's rate at T=200 (25.58), it just eventually needs zero new audits. |
| ch5-34 | Revocation, §11 (Property tombstone + Protocol) | Reputation is monotone in honest outcomes but a fraudulent outcome is individually revocable by an appended tombstone; the original entry stays, and the fraudulent contribution remains spendable only until the tombstone propagates within a bounded window τ. | stateful | Does correcting a fraudulent grade require rewriting the ledger, and how long is the fraud actually spendable? | paired panels (upper: append-only rail with the fraudulent entry and its tombstone both visible; lower: step function of score contribution dropping to zero at propagation) | fig:tombstone | restyle (5.14) | should | — | The counter-reading to design against is expecting an instantaneous or history-deleting correction; both the append-only rail and the bounded window τ must stay visible together. |
| ch5-35 | Revocation, S4 | Crane and Mara collude to inflate Crane's aesthetics score to 0.9; a sampled re-audit overturns the grade two days later, Mara's bond is slashed, and the tombstone reaches Bob's harbor before he acts on the inflated score. | temporal | What does one real fraud-and-retraction look like end to end? | worked example annotated onto fig:tombstone rather than a new figure | fig:tombstone | restyle (5.14) | no | — | This concrete instance (Crane's vector (0.95, 0.9, 0.8), the two-day window) should populate the existing tombstone figure's caption/example rather than spawn a separate diagram. |
| ch5-36 | Revocation, §11.3 | Bounded memory is a tunable design parameter (Liu–Skrzypacz reputation bubbles, Ostrom commons governance), not a virtue to assert away — unbounded memory freezes types and removes the incentive to keep performing. | allocation | Is "reputation never decays" actually a good design goal? | — (prose argument by analogy) | none | — | no | — | This is an analogical, non-quantitative argument (a commons-governance reading of the memory window); no measured curve exists to plot, and a sentence carries the claim as well as a diagram would. |
| ch5-37 | Handoff, §12 | The chapter hands the market paper a precise ledger of what is delivered, borrowed, and explicitly owed — with only cross-operator attestation still flowing back as a named unsolved dependency. | comparison | Exactly what does this chapter certify as done, and what remains owed to the market chapter? | table (artifact × maturity × role in the market paper) | tab:handoff | — | no | — | Already a clean handoff ledger in table form; this is the kind of accounting a figure would not out-perform. |
| ch5-38 | Open problems, exercise (death taxonomy) | What survives an agent's death forms a five-tier ladder: record survival, artifact survival, semantic task capsule, execution snapshot, and latent state — each tier strictly containing less than a live process had. | stateful | If an LLM agent dies mid-thought, exactly how much of it can be recovered? | step chart / ladder (five rows, each naming what survives and what is lost, with the specified-but-unshipped ESNR mechanism marked between tiers 2 and 3) | none | — | should | hand-worked in exercise solution, no script | This taxonomy answers exactly what ch5-12's checkpoint discussion needs but never draws; the counter-reading to design against is assuming any mechanism can reach tier 5 (latent state / KV cache) from an arbitrary provider — the text says this is fundamentally unrecoverable. |
| ch5-39 | Open problems, §sec:open | Ten named open problems (branching continuity, agent-death taxonomy, cross-operator attestation, the honest root and its telemetry, vector-disclosure Goodharting, the self-improvement arms race, revocation convergence, bond-farming, arbitration capacity, skill-version reputation) are collected so no starred exercise can drift from this list. | relational | Which of this chapter's unsolved problems feeds which later chapter or mechanism? | table (open-problem id × one-line problem × section it's raised in × what would close it) | none | — | should | — | Currently a numbered prose list scattered across the chapter body; a compact registry table would let a reader see at a glance which problems are genuinely closed elsewhere (e.g. the honest-root item is now half-closed by Theorem tower-imported) versus still fully open. |
| ch5-40 | Identity, exercise (bond-farming) | Of three candidate defenses against a principal spawning 1,000 agents to wash-trade reputation — principal binding, a listing fee, sampled adversarial re-audit — principal binding is critical and the re-audit rate that deters farming is the same ρ*=G/(dB) from §10. | allocation | Which defense against reputation bond-farming actually works, and at what cost to honest high-volume traders? | table (defense × what it deters × false-positive cost to honest pairs) | none (echoes fig:sybil panel B) | — | could | — | This elaborates fig:sybil's "one principal = one accountable weight" panel with a specific attack scale (1,000 agents) and a concrete false-positive tradeoff; a small table would suffice, a new full figure is not required. |
| ch5-41 | Identity, §6.1 (keyidea) | A newcomer policy shaped as full work ability plus a reduced economic ceiling taxes whitewashers (who keep resetting their ceiling) without taxing honest newcomers (who keep climbing); a flat "distrust all newcomers" policy taxes the wrong party. | comparison | Why is "let newcomers work fully but cap their earnings" strictly better than "restrict what newcomers can do"? | paired bars (cost to an honest newcomer vs. a whitewasher, under each of the two policy shapes) | none | — | could | — | This is the design-principle companion to the whitewash-cost theorem (ch5-17); a small comparison figure would reinforce the point but the keyidea box already states it clearly in one paragraph. |

## Chapter 6 — The Harbor Economy

Source: `website-v2/public/whitepaper/harbor-economy.tex`.
Existing figures found: 10 labelled `figure` environments used by this chapter (`fig:he-three-sided`, `fig:he-float-plan`, `fig:he-conservation-functor`, `fig:fh-xfer`, `fig:fh-settlement`, `fig:fh-revocation`, `fig:fh-threat-bands`, `fig:he-cold-start`, `fig:cartel-game-inline`, `fig:he-assurance`), plus two tables (`tab:keystone-split`, shared with Chapter 5; `tab:fh-topology`, shared with Chapter 8). Four of the ten figures and one table are drawn once and reused verbatim in Chapters 7/8.
Rows: 40.
Three most under-served ideas: (1) the deontic-conflict fragment's polynomial-vs-NP-complete dichotomy, together with its hand-worked conflict witness (a Horn-derived obligation/prohibition clash on `write_prod`), is the chapter's only complexity-theory result and has no visual at all; (2) the succession-price theorem D*=ηK/(1−ηK) for a mortal rented specialist — a full M/M/1-with-breakdowns result with a worked canary (naive estimate 4.17 vs. true 8.17) — has no figure; (3) the Myerson–Satterthwaite no-trade-but-efficient region (7/32 ≈ 21.9% of surplus-positive trades refused) is a concrete impossibility result with an exact numeric example and no plot.

| id | section | idea (one sentence) | structure | reader question | recommended form | existing figure | wave-11 disposition | priority | data source | notes for the renderer |
|---|---|---|---|---|---|---|---|---|---|---|
| ch6-01 | Reader's map | Five reader types are routed to the section and one artifact that would have to survive their review. | relational | Which section and figure answers my specific question about this market? | table (reader type × start section × critical artifact) | tab:readers-map | — | no | — | Already a clean routing table; a diagram would not out-perform a lookup task. |
| ch6-02 | Thesis / What a side is, §2–3 | An agent can be three economically distinct things at once — labor sold by a firm, a rentable capital asset, licensed IP — and the harbor is a three-sided market only once reputation exists to key each side separately. | comparison | Why does one agent doing one refactor count as three different market sides? | three parallel rails (labor, capital, licensed IP), each with its own exposed party, payment direction, and reputation key, converging on one settlement journal | fig:he-three-sided | restyle (6.2: heavier row labels, bond names in footnotesize, conservation identity spelled out under the rails) | should | — | Keep the dashed schema band that marks the tagged-subject-identifier distinction (principal / asset owner / content hash); the counter-reading to design against is assuming all three sides share one identity key. |
| ch6-03 | What a side is, §3 | Bob can buy the same 1,200-line refactor three ways — a 400-cr labor bounty, a 90-cr capital lease, or a 0.5-cr/file IP license — and each way exposes a different party. | comparison | For the same job, who is exposed and what does Bob pay, side by side? | table (side × what Bob pays × who is exposed) | none (existing unlabeled table already in the chapter) | — | no | — | Already realized as a compact three-row table immediately after the running example; no figure would add clarity beyond it. |
| ch6-04 | What a side is, §3 (Thm. succession price) | A sole specialist subject to breakdowns has mean response time strictly decreasing toward a floor W∞ it never reaches; the pool wins at every skill premium once the mortality-to-succession ratio ξ/η clears the succession price D*=ηK/(1−ηK). | quantitative | At what point does renting a mortal specialist stop being worth it, no matter how skilled they are? | threshold/regime plot (x = ξ/η, y = expected wait, W_bd curve descending to the floor W∞, pool cost line K, worked point D*=0.3496 marked) | none | — | must | `b8_specialization.py` (matrix-geometric check to 2×10⁻¹³, truncated chain to 10⁻¹⁴, simulation ⎮z⎮≤0.8) | The reported "wrong turn" is a strong counter-reading to draw against: a naive sum of the two response-time terms predicts 4.17 at the canary instance while the correct (amplified) answer is 8.17 — the figure should make that amplification factor (2.5×) visible, not just the final curve. |
| ch6-05 | Float plan, §4 (Protocol escrow ceremony) | Execution becomes admissible only at one atomic transition where the wallet is debited and the escrow row is created together; a signed plan alone, or an aborted transaction, leaves all three traces unchanged. | stateful | At what exact moment does "no-spawn-without-bond" actually take effect? | synchronized state traces (wallet, escrow row, worker state, over signed-plan → transaction-begins → commit → clearance, with an abort branch shown returning all three to their prior state) | fig:he-float-plan | redraw (6.3: sequence-style timeline, SQL-level effect labelled at each transition) | should | — | The counter-reading to design against is placing the invariant at a component or a message rather than at the commit transition itself — the figure's whole purpose is locating it precisely there. |
| ch6-06 | Float plan, §4.2 | Settlement lands in exactly one of four terminal states (Success, Partial, Sabotage, Dispute), each bound to an oracle rather than a self-reported note. | stateful | What are the only ways a float plan can end, and what does each one do to the funds? | table (state × flow of funds × reputation effect) | tab:settlement | — | no | — | A genuine four-row lookup table; a state-machine figure would add little the table doesn't already convey plainly. |
| ch6-07 | Float plan, §4.2 (worked settlement) | Bob's 400-cr bounty and Alice's 100-cr bond settle Partial at 40%: Alice earns 160 and is slashed 60 of her bond, Bob recovers 240, the commons gains 60, and the ledger sums to zero. | allocation | Does the four-bucket settlement rule actually conserve value on a real number, not just in principle? | ledger table (wallet × net delta × why, matching the style of the later three-sided settlement table) | none | — | should | hand-worked in text (400 bounty, 100 bond, 40% completion) | The chapter later gives exactly this kind of table for the three-sided example (ch6-12) but not for this single-sided one; adding the same grammar here would let a reader check the arithmetic without re-deriving the two-bucket split by hand. |
| ch6-08 | Three sides on one escrow, §5 | Grossman–Hart's residual-control-right holder is the renter (who operates the jail and can sandbox/throttle/revoke), while the asset owner bears the reputational consequence if the lease goes badly. | relational | In a rental, who holds runtime control and who bears the reputational risk, and are they the same party? | small two-row table (party × control right × exposure) | none | — | could | — | The keyidea box already states the mapping precisely in prose; a table would help a reader map Grossman–Hart's two roles onto renter/owner at a glance but is not required to follow the argument. |
| ch6-09 | Three sides on one escrow, §5 (deontic dichotomy) | Inside a fragment of Horn rules, deontic obligations/prohibitions, exclusive claims, and difference constraints, conflict detection is polynomial with a witness (Thm. deontic-detect); adding one disjunctive obligation makes it NP-complete (Thm. deontic-frontier). | quantitative | Exactly which single language feature turns a free polynomial check into an NP-complete one? | phase-boundary diagram (single boundary condition = presence of disjunctive obligations; one region labelled "polynomial, witness-producing" naming its three-engine check, the other "NP-complete" naming the 3-SAT reduction) | none | — | must | `b4_deontic_fragment.py`, seed 20260816 (3,000-policy sweep, 16/16 satisfiable/unsatisfiable reduction check) | This is the chapter's only complexity-theory result and has zero visual anywhere in the series; the counter-reading to design against is assuming more expressive deontic logic is simply "harder" in a graded sense — the theorem says the boundary is a cliff, not a slope. |
| ch6-10 | Three sides on one escrow, §5 (pdexample, conflict witness) | A single Horn-derived fact (agent_deployed → prod_access) triggers both an obligation and a prohibition on write_prod overlapping on [5,10] — a conflict a checker that skips Horn propagation misses on 100 of 885 conflicting cases in a 3,000-policy sweep. | provenance | Can a conflict be indirect — hidden behind one step of ordinary rule inference — and how would a checker miss it? | derivation trace (fact → Horn rule → derived fact → two deontic rules firing on identical scope/action → interval overlap [5,10] → conflict), mirroring the kernel chapter's commitment-oracle state-machine grammar | none | — | must | `b4_deontic_fragment.py`, seed 20260816: real checker reports conflict=True with pairs=[(0,1)]; the Horn-ablated mutant reports conflict=False on this exact witness | The counter-reading to design against is a checker author assuming only explicitly-stated facts can trigger a clash — the whole point of this witness is that `prod_access` is nowhere a fact, only a one-step consequence. |
| ch6-11 | Three sides on one escrow, §5 (sweep statistics) | Across 3,000 random in-fragment policies, 885 carry at least one conflict, breaking down into 121 derivable-⊥, 195 obligation/prohibition clashes, 484 claim overlaps, and 213 negative deadline cycles (with overlap across kinds). | quantitative | How common is each of the four conflict kinds, and do they overlap? | grouped/stacked bar (four conflict kinds by count, with a note on the 764/114/7 single/double/triple-kind overlap breakdown) | none | — | should | `b4_deontic_fragment.py`, seed 20260816 | Currently reported only as a run-on sentence of six numbers; a small bar chart would let a reader see the relative frequency of the four kinds without re-parsing the parenthetical overlap accounting. |
| ch6-12 | Three sides on one escrow, §5.1 | The same refactor settles across labor, capital, and IP line items in one escrow row (560 cr in, 0 cr out net), proving C2's claim that all three sides ride one conserving object rather than three separate ledgers. | allocation | Does the "one escrow, three sides" claim actually balance on real numbers? | table (wallet × net delta × why) | none (existing table already in the chapter) | — | no | — | Already realized exactly in this grammar (Bob −458, Alice +400, Dana +50, Carol +8, commons +0, sum 0); this is the model ch6-07 should be extended to match. |
| ch6-13 | Keystone, §6 | Everything Alice's daemon knows about an actor's key, log, and registry is checkable from Bob's foreign view except the last row — nobody outside the daemon can check that the key is bound to an accountable principal. | relational | Which parts of local identity actually cross the operator boundary intact? | table (claim × inside Alice's daemon × from Bob's foreign view) | tab:keystone-split | table (6.4, shared source with ch5-25) | should | — | Identical source file to Chapter 5's version; do not redraw independently — a change here must propagate to the shared source. |
| ch6-14 | Conservation, §7 (Thm. functor + Property lax) | Sequential ledger traces compose additively (Δ(g∘f)=Δ(g)+Δ(f)); internal transfers have Δ=0 exactly, while a cross-currency scalar total additionally needs a named valuation instant, source, and exposure bound. | allocation | Does conservation actually survive being composed across many operations, and where does it stop being free? | paired accounting panels (A: native-unit waterfall, ΔW+ΔE+ΔC=0 exactly; B: conversion panel with fee, slippage, and risk as explicit priced terms, valuation instant v_t called out) | fig:he-conservation-functor | restyle (6.5) | should | hand-worked exercise (two-currency example: 10 A-coins + 20 B-coins, marked value moves $50→$70 with zero transfer as B's price rises; φ = X(e^{σL}−1) exposure bound) | The counter-reading to design against is treating "lax functor" language as itself a proof that cross-currency totals conserve — panel B exists specifically to show that conversion introduces terms native-unit conservation never had. |
| ch6-15 | Conservation, §7.1 (Thm. Myerson–Satterthwaite) | Any bilateral slice satisfying Myerson–Satterthwaite's four assumptions cannot be simultaneously incentive-compatible, individually rational, budget-balanced, and ex-post efficient; on the chapter's own numbers (Bob v=70, Alice c=60, M=90), a 10-cr efficient trade is refused because v−c=10 < M/4=22.5, and this happens on 7/32≈21.9% of the (v,c) square regardless of M. | quantitative | How much real, mutually-beneficial trade does budget-balance-and-incentive-compatibility actually cost the market? | payoff/regime plot in (v,c) space (the double-auction trade line v−c=M/4, shaded "efficient but refused" wedge below it, the worked point (70,60) marked inside the wedge) | none | — | must | hand-worked in `pdexample` (Chatterjee–Samuelson linear equilibrium, β(v)=2v/3+M/12, σ(c)=2c/3+M/4, region probability a(2M−a)/(2M²) at a=M/4 = 7/32) | This concrete impossibility result — with an exact number of trades foregone — has no plot anywhere; the counter-reading to design against is treating this as an abstract theorem rather than a real, non-trivial fraction (over one in five) of efficient trades the mechanism actively refuses. |
| ch6-16 | Anchor / cross-harbor transfer, §8 (Protocol xfer) | The four-message transfer ceremony narrows authority at every step (C_B′⊆C_A) and removes synchronous dependence on Harbor A after message 3, so a rented agent never has to phone home mid-task. | temporal | After which message does the source harbor stop being needed for every use of the transferred authority? | sequence diagram (Harbor A, Harbor B, worker agent; four messages; the capability set printed at each hop rather than an abstract narrowing envelope) | fig:fh-xfer | redraw (6.6, shared with 8.3: sequence-style, capability sets shown explicitly at each hop) | should | — | The counter-reading to design against is assuming the ceremony makes federation fully asynchronous — the epoch-root dependency for later revocation still exists, just off the hot path; keep that dependency visible even in a redrawn sequence form. |
| ch6-17 | Federation, §9 | Federation is exactly four independent rails between two sovereign harbors (witness, capability, revocation, settlement), none of which makes either harbor a root of authority for the other. | relational | What, precisely, crosses a federation boundary, and what does each rail explicitly not do? | table (rail × what crosses × direction × what it does not do) | tab:fh-topology | table (6.7, shared with 8.1) | should | — | Already a clean four-row rail table; keep the explicit "what it does not do" column, since that column carries the chapter's refusal-of-consensus argument. |
| ch6-18 | Federation, §9.2 (Design invariant: bounded custody loss) | Worst-case cross-harbor settlement loss is bounded by the pre-agreed fee φ only if custody is non-bypassable (whitelist + fee cap + one terminal transition); a single operator-controlled bypass exposes the full balance instead. | stateful | Does the custody bound actually hold, or does it quietly assume away the one bypass that breaks it? | two small state diagrams side by side (A: funded → criteria → Clear/Refuse only; B: same graph plus one dashed unauthorized transition to a full-balance loss state) | fig:fh-settlement | keep (6.8, shared with 8.6: footnotesize labels) | should | — | The counter-reading to design against is reading panel A's bound as unconditional; panel B exists specifically to show the bound evaporates the instant one extra transition is possible. |
| ch6-19 | Federation, §9.3 (Protocol gossip) | A revocation issued at Alice's home harbor leaves Harbor B spendable until t=Δ and Harbor C spendable until t=2Δ; this is an expected bound under connected reliable rounds, not a deadline, and an unbounded partition removes any finite bound. | temporal | For how long can a revoked capability still be used, and at which harbor? | staircase over time (three harbor rows, amber "stale/spendable" region shrinking to teal "refuse locally" as gossip reaches each harbor at 0, Δ, 2Δ) | fig:fh-revocation | keep (6.9, shared with 8.4) | should | — | The counter-reading to design against is treating Δ and 2Δ as guaranteed deadlines rather than expectations under a stated connectivity model; the figure's own caption already states this, keep it prominent. |
| ch6-20 | Federation, §9.3 (Conjecture: Equivocation Lower Bound) | A deployment advertising a finite freshness window T must post a witness bond dominating the maximum value spendable against a revoked-but-unobserved capability over T; under unbounded partitions no finite bond covers the worst case. | allocation | How large a bond does detecting equivocation after the fact actually require? | — (open conjecture, prose) | none | — | no | — | This is explicitly marked `\Open{}` with no derived bound yet; drawing a number that has not been derived would overclaim rigor the text itself declines to claim. |
| ch6-21 | Federation, §9.3 (threat bands) | Seven named threats sit at different points between an acknowledged gap and a mechanized proof — identity/replay is furthest along, cold-start/cartel-monitoring/centralization remain fully open. | comparison | Which of the federation's seven threats have real assurance evidence today, and which are still just acknowledged gaps? | dot-and-interval rows (seven threats on the y-axis, OPEN/CONDITIONAL/MECHANIZED on the x-axis, each row's evidence interval and one-line caveat) | fig:fh-threat-bands | restyle (6.10, shared with 8.2: axis ticks for the three positions, footnotesize labels) | should | — | The counter-reading to design against is reading position as a promise of eventual mechanization on a timeline; the caption already states a row advances only when a specific artifact or theorem exists, not on a schedule. |
| ch6-22 | Reputation, §10 (Thm. no-saga) | A ledger can append a compensating transaction that restores a numerical balance; a reputation tombstone cannot undo decisions counterparties already made while relying on the invalid outcome — it can only change future evaluations. | comparison | Why can't the same Sagas-style compensation that fixes the bond ledger also fix a fraudulent reputation? | — (prose comparison) | none | — | could | — | This is a structural, non-numeric distinction (ledger compensation vs. reliance) best carried by one clear sentence; a paired-panel figure could reinforce it but is not required to follow the argument. |
| ch6-23 | Reputation, §10 (Thm. folk) | Any imperfect-public-monitoring equilibrium result needs a fixed conditional distribution of signals given actions; a strategic grader who chooses the signal but is omitted from the game leaves that distribution undefined. | relational | Why doesn't "the daemon derives the grade" already make every folk-theorem incentive-compatibility claim valid? | — (prose, game-theoretic argument) | none | — | no | — | This is a logical point about model completeness, not a structure a diagram would reveal faster than the one-sentence statement already does. |
| ch6-24 | Hosted trust, §11 | The payment rail (AP2/ACP/x402) is a commoditized, swappable substrate; the defensible product is the verified ledger, relay, and reputation — hosted trust. | comparison | If payment rails are now commodity, what is actually left to sell? | — (two-item bullet distinction) | none | — | no | — | A two-line substrate/product distinction; a figure would not carry this faster than the existing bullet pair. |
| ch6-25 | Cold start, §12 | Cold start is a three-phase schedule (seed supply below zero, import external history at zero, charge only above a liquidity threshold λ*) triggered by measured depth, not by a calendar date. | quantitative | When does the market switch from subsidizing supply to charging demand? | paired plots (upper: realized liquidity over settled outcomes; lower: supplier-price regime, SEED/IMPORT/CHARGE steps, both sharing one vertical trigger at λ*) | fig:he-cold-start | restyle (6.12) | should | — | The counter-reading to design against is a reader assuming the phases are calendar-scheduled; both panels sharing the same vertical trigger line is what forecloses that reading. |
| ch6-26 | Cold start, §12.1 (Proposition reserve) | Competitive underwriting prices to expected loss, which sits below the cleanup lower bound's worst-case requirement; either underwriting violates the bound in the tail, or the bond must be reframed as the insurer's own capital-adequacy constraint. | allocation | Can a competitive insurance market replace a fixed per-transaction bond without reopening tail risk? | — (prose disjunction, no measured curve) | none | — | no | — | The nearest earlier attempt at this idea (`fig-auction-inline`) was correctly deleted in triage for having only schematic, non-measured coordinates; this proposition states the two horns in words precisely because the reserve formula itself remains open. |
| ch6-27 | Cold start, §12.2 (cartel game) | A collusive rating-agency-style cartel is sustainable exactly while the discounted collusive stream V_C beats the one-shot deviation payoff π_D; the boundary trades off detection probability against loss severity, so a rare severe penalty and a frequent smaller one can sit on the same deterrence line. | quantitative | Under which combinations of detection frequency and penalty severity does cartel collusion actually break down? | phase diagram (x = detection probability p_d, y = loss if detected L, boundary curve V_C=π_D, regions labelled "deviation dominates" vs. "collusion persists") | fig:cartel-game-inline | restyle (6.14, shared with 7.6) | should | — | The counter-reading to design against is assuming higher detection frequency alone is the mechanism — the figure must show that a rare/severe point and a frequent/smaller point can lie on the identical boundary. |
| ch6-28 | Sheaf analogy, §13 | Three open problems share a local-to-global shape that motivates a sheaf-gluing analogy, but the paper has not specified the site, restriction maps, coefficient object, or cocycles, so no cohomology claim is made. | provenance | Is the federation's local-to-global consistency problem actually a proven cohomology result? | — (prose refusal, explicitly not a figure) | none | — | no | — | Drawing anything sheaf-shaped here (a site diagram, a Čech-style cover) would visually imply more formal machinery than the text explicitly disclaims; the honest move is the sentence, not a diagram that looks more proven than it is. |
| ch6-29 | Gaps, §14 | Six named unspecified gaps each threaten a specific earlier promise; the two that reach conservation (operator exit, float-plan amendment) are the urgent ones because conservation is the paper's only \Built{} claim. | relational | Which of the six open gaps is actually dangerous versus merely unfinished? | table (gap × threatens × how it bites) | tab:gaps | — | no | — | Already exactly this table, with the urgency argument (only two gaps threaten a currently-true claim) stated in the caption; no figure would sharpen this ranking further. |
| ch6-30 | Related work, §15 | The design borrows from seven literatures and states, axis by axis, what the harbor does differently from the nearest prior art in each. | comparison | Where exactly does this design depart from the nearest established result on each axis? | table (axis × nearest prior art × what the harbor does differently) | tab:related | — | no | — | A genuine reference table; this is prose-comparison content, not a relation a diagram would expose faster. |
| ch6-31 | Adversarial testing, §16 (Thm. Purchased Assurance Bound) | k independent, Becker-enforced test-writers each detecting a flaw with probability d bound the undetected-defect probability at (1−d)^k; at d=0.6, three reviewers leave 6.4% residual risk and five leave about 1%, while cost rises only linearly. | quantitative | How much does buying more independent reviewers actually reduce the chance a defect slips through, and at what cost? | xy plot (x = cumulative cost k·(bounty+bond carry), y = residual defect risk on a log scale, points at k=1..5 labelled with their percentages, dashed counterfactual for correlated reviewers) | fig:he-assurance | keep (6.15: labels bigger) | should | harbor3 (imported deterrence threshold); hand-worked geometric decay (1−d)^k | The dashed "five correlated reviewers ≈ effective k≈1" counterfactual is the counter-reading to keep visible — the frontier is valid only under independence, and correlated reviewers buy far less than their headcount suggests. |
| ch6-32 | Adversarial testing, §16 (pitfall: keep-gain vs claw-back) | The Becker deterrence threshold depends on the slashing convention: keep-gain gives ρ*=G/(dB)=0.25 at the program's parameters, while claw-back gives ρ*_c=G/(d(G+B))=0.2083 — about a 17% difference. | quantitative | Does it matter which slashing convention you use when quoting the audit rate needed for deterrence? | paired bar or small threshold comparison (two bars: ρ*=0.25 keep-gain vs. ρ*_c=0.2083 claw-back, same G, d, B) | none | — | should | harbor3 (both thresholds derived there); hand-worked at G=10, d=0.8, B=50 | The pitfall box explicitly frames this as a common quoting error worth roughly 17% either over- or under-auditing; a small paired-bar figure would make the two conventions impossible to conflate at a glance, which prose alone has apparently failed to prevent even within this series. |
| ch6-33 | Adversarial testing, §16 (pitfall: independence) | The (1−d)^k bound is only as good as the word "independent"; three reviewers reading each other's findings, or three instances of one model family, give something closer to one draw and two echoes than k independent draws. | comparison | Can a platform quietly overstate its purchased assurance by counting correlated reviewers as independent? | — (already covered by fig:he-assurance's dashed counterfactual) | fig:he-assurance | keep (6.15) | no | — | This pitfall is precisely what the existing dashed "five correlated reviewers" line in fig:he-assurance already depicts; no separate figure is needed. |
| ch6-34 | Adversarial testing, §16 (dramatized) | Bob buys down his defect risk from 100% to about 6% for 90 cr by opening a diff to k=3 independent test-writers paid 30 cr per confirmed flaw. | quantitative | What does purchasing assurance actually look like in one concrete transaction? | worked example annotated onto fig:he-assurance rather than a new figure | fig:he-assurance | keep (6.15) | no | — | This narrative is the k=3 point already plotted on the existing assurance curve; it should populate that figure's caption rather than spawn a new one. |
| ch6-35 | Anchor / cross-harbor transfer, §8 (dramatized) | Alice's harbor attenuates the card to one module, Bob's harbor attenuates again to add no-network-egress and a six-hour TTL, and the minted card verifies under Bob's key alone with no mid-task callback to Alice. | temporal | What does one real attenuated rental actually look like, message by message? | worked example annotated onto fig:fh-xfer rather than a new figure | fig:fh-xfer | redraw (6.6) | no | — | This concrete instance (module-scoped read/write, no network, six-hour TTL) is exactly what should populate the redrawn sequence diagram's example, not a separate figure. |
| ch6-36 | Reputation, §10 (dramatized: captured judge) | Judy, a colluding human grader, rules for Bob for a 50-cr bond; a sampled second panel later contradicts her ruling against the preserved evidence, slashing her bond and tombstoning her judging reputation. | temporal | What does one real captured-and-caught judge look like end to end? | — (cross-references Chapter 5's judge-market ceremony rather than a new local figure) | fig:judge-market (Chapter 5) | redraw (5.11, Chapter 5) | no | — | This chapter narrates the same bonded-judge mechanism Chapter 5 already draws in full; redrawing it here would duplicate rather than extend that figure. |
| ch6-37 | Three sides / rental (exercise, key rotation) | If Alice rotates her signing key between message 3 and Bob's later use of the transferred card, Bob must validate against an append-only key-history certificate rather than trust the offer as still fresh. | provenance | What happens to an already-minted attenuated card when the issuing harbor rotates its key mid-flight? | small provenance trace (parent card → key epoch e → rotation to e+1 → dependent check at presentation time) added to fig:fh-xfer | fig:fh-xfer | redraw (6.6) | could | — | Named explicitly as an unspecified gap (§14, "cross-boundary key rotation"); a minor addition to the existing transfer-ceremony figure would close a real gap between the drawn protocol and its stated limitation, but the chapter itself says the dependency is not yet fully specified. |
| ch6-38 | Reputation, §10 (exercise: rate-the-raters termination) | The rate-the-raters recursion is not shown to bottom out in machine-checkable truth by any theorem; what is available is a well-founded rank order (appeal only to rank K−1, stopping at rank 0, a protected oracle or a named final authority), so no infinite chain of judges judging judges is possible. | stateful | Why does asking "who judges the judges" not go on forever in this design? | ladder (ranks K, K−1, …, 0, each rank's appeal arrow pointing down one level only, rank 0 marked as either a machine-checkable oracle or a named final authority) | none | — | should | hand-worked in exercise solution, no script | This is the direct, simpler answer to "does the recursion terminate" for Fix B (distinct from Chapter 5's ρ,d,B contraction-tower machinery); the counter-reading to design against is conflating this well-founded-order argument with an actual convergence proof — the text explicitly says no such theorem exists for the aesthetic base case. |
| ch6-39 | Three sides on one escrow, §5 (exercise: skill v2 lineage) | A skill's portfolio-tree leaf for v2 includes its parent's leaf plus v2's own evaluation root, and v2's reputation prior is initialized at a predeclared discount γ·n_v1 of v1's sample size, fixed before seeing any of v2's outcomes. | provenance | When a licensed skill ships a new version, how much of its old track record should the new version inherit? | provenance tree (v1 leaf → v2 leaf, edge labelled with the discount factor γ and the predeclared sample-size prior γ·n_v1) | none | — | could | — | This mirrors the no-mint / newcomer-ceiling logic from Chapter 5 applied to code rather than agents; named explicitly as the "skill versioning" gap in §14, so a small provenance figure would close the same kind of visualization debt Chapter 5 already closed for agent lineage. |
| ch6-40 | Appendix, §Impl & status | The per-claim maturity ledger records exactly which mechanisms ship, which are specified, and which remain proposed, down to the source module or proof artifact backing each. | comparison | Which of this chapter's claims can a reader actually go check against running code? | table (claim/mechanism × state × grounding) | tab:honest-state | — | no | — | Already the falsifiable ledger the chapter's own honesty discipline points readers to; a figure would not out-perform this lookup table. |

## Chapter 7 — The Bonded Commons

Source: `website-v2/public/whitepaper/agent-transactions-whitepaper.tex` (chapter id `bonded`).
Existing figures found: 9 (`fig:governance-flow`, `fig:sybil-inline`, `fig:cartel-game-inline`, `fig:bonded-key-custody`, `fig:magic-link-inline`, `fig:worked-example`, `fig:pareto-mc-inline`, `fig:sybil-mc-inline`, `fig:cartel-folk`); one terminal-session listing (`session-bc-delta30`) does not count as a figure per atlas convention.
Rows: 58.
Three most under-served ideas: (1) the TLA+ lifecycle (Begin/Claim/Commit/Reap/Salvage/Dismiss) is given only as code and English prose, never a state diagram, despite being the chapter's own formal backbone; (2) the three-bucket Conservation ledger (wallet/escrow/commons pool) and its five operations are argued entirely in a bulleted proof sketch with no ledger diagram, despite matching the atlas's own "typed journal or stock-flow ledger" prescription; (3) the mechanized claim-signaling threshold δ*≈0.3425 — the chapter's headline result, hand-checked, Z3-checked and TLC-checked three separate ways — has a terminal session for the failing case only and no plot of the deviation inequality or the threshold itself.

| id | section | idea (one sentence) | structure | reader question | recommended form | existing figure | wave-11 disposition | priority | data source | notes for the renderer |
|---|---|---|---|---|---|---|---|---|---|---|
| ch7-01 | Introduction: The Trust Problem | Agent A and Agent B edit the same file ninety seconds apart with no way to know the other exists, and the resulting merge conflict corrupts the file. | temporal | What actually collides, and when? | swimlane sequence, two agent lanes over wall-clock time | none | — | could | — | Two lanes (Agent A, Agent B) with edit/commit/push marks 90s apart and a collision mark; the opening scene is vivid but brief, so this is a nice-to-have, not critical. |
| ch7-02 | §1.1 The Three Failures of Peer-to-Peer Trust | Pairwise trust costs O(n²) negotiations and becomes infeasible past roughly 50 agents. | quantitative | How fast does pairwise trust negotiation blow up? | regime plot, x = agent count n, y = negotiations, O(n²) curve with 8/50/1000 marked | none | — | could | hand-derived (n(n-1)/2) | Mark n=8 ("manageable"), n=50 ("dominates wall-clock"), n=1000 ("infeasible") exactly as the prose states; no simulation backs this, so label it illustrative. |
| ch7-03 | §1.1 The Three Failures of Peer-to-Peer Trust | Three named failure modes (doesn't scale, doesn't survive death, doesn't address misaligned principals) each break peer-to-peer trust differently. | comparison | Which of three failure modes applies to which trust design? | — (already a clean parallel bulleted list) | none | — | no | — | The three paragraphs already have bold lead-in labels; a table would just re-render the same three rows without adding geometry. |
| ch7-04 | §1.3 Related Work in Crypto-Economic Bonding | The commons borrows one narrowed result from each of four prior lineages (PoS slashing, capability security, Ostrom, mechanism design). | comparison | What does the chapter borrow vs. depart from, per lineage? | table-not-figure (already realized) | `tab:related-borrow` | — | no | — | Already a four-row `tabularx` table; no figure needed. |
| ch7-05 | Introduction (Table 1) | Three layers (scope/evidence/collateral) answer three different questions about one attempted act, in strict sequence. | allocation | Which layer stops which failure, and what is each layer's terminal outcome? | table-not-figure (already realized) | `tab:bonded-three-layer` | table (7.1) | no | — | Matches triage row 7.1; the atlas-endorsed table replacement is already in the source. |
| ch7-06 | §2 The Commons Authority | A commons authority provides exactly five capabilities (identity, capability bounding, shared knowledge, immutable history, economic settlement) and nothing more. | containment | What five things does the daemon guarantee, and what does it deliberately not decide? | small institutional diagram: five labeled inputs into one "commons authority" box, with an explicit "does NOT decide" band | none | — | could | — | List the five capabilities as named ports into the daemon box; add the negative space ("which agent works on what," "which conflict wins," "who is good") as an explicit excluded band, since the definition spends a full paragraph on what it does not do. |
| ch7-07 | §2.1 The Daemon as Correlating Device | The daemon is a crossing guard issuing private per-agent recommendations, not a traffic light broadcasting one public rule, and Aumann's obedience inequality is the test for whether agents should listen. | relational | Why is a private recommendation different from a public rule, and when does an agent have a reason to obey it? | paired comparison panel: traffic-light (public signal, one rule) vs. crossing-guard (private signal r_i per agent) with the obedience inequality below | none | — | should | Aumann 1974 (cited, not mechanized in this chapter) | Draw exactly the analogy the text gives — one public broadcast vs. two private arrows (r_α: proceed, r_β: defer) — with the inequality Σμ(r)[u_i(r_i,r_-i)-u_i(a_i',r_-i)] ≥ 0 captioned beneath, not floating alone. |
| ch7-08 | §2.1, Property (Conditional Correlating-Device Interpretation) | The daemon induces a correlated equilibrium only if three named conditions hold jointly (public deterministic policy, private per-agent signal, obedience inequality). | relational | Which of three conditions is critical, and what breaks if one is dropped? | three-row checklist/table: condition, what it requires, what fails without it | none | — | could | — | Pairs naturally with ch7-07; could be one figure's lower half rather than a separate exhibit. |
| ch7-09 | §2.2 Why Agents Consent | The commons authority converts six failure modes from ad hoc to deterministic without forbidding anything an agent could already do. | comparison | For each capability, what changes with vs. without the authority? | table-not-figure (already realized) | `tab:consent` | — | no | — | Already a two-column before/after table; six rows, clean. |
| ch7-10 | §3.1 Capability Attenuation | A delegation chain's capability set can only shrink at every hop, never grow (Theorem: Structural Scope Limitation). | containment | Does authority ever grow across a delegation chain? | nested sets with a rights/TTL table (already drawn) | `fig:anchor-capability-attenuation` (Anchor, Ch. 2) | — | no | — | This chapter states the theorem and cites Anchor's proof; the existing Anchor-chapter figure already draws the nested-capability geometry this theorem needs. |
| ch7-11 | §3.2 Isolation Domains | Three isolation tiers (I0 none, I1 advisory, I2 structural) are ordered by strength, and choosing between I1/I2 is an economics decision, not a security one. | containment | How do the three isolation tiers order, and what does each buy or cost? | small ordered ladder / Hasse-style diagram: I0 ⊂ I1 ⊂ I2 by strength, each rung naming what it adds and its parallelism cost | none | — | should | — | Three rungs only; label I1 explicitly as "the default," and connect the I1-vs-I2 choice forward to §5 (Sen) since the text says the choice is economic, not structural. |
| ch7-12 | §3.3 Runtime Invariant Enforcement | A rule is regimented (structurally prevented, pre-commit) exactly when it is decidable against deterministic local state; otherwise it is enforced (detected and salvaged, post-commit). | stateful | Which rules can be prevented before the fact, and which only caught after? | 2×2-style boundary or step chart: sync-decidable × pre/post-commit, with NoteMonotonicity/EscrowInvariant/LockOwnerValid placed in the regimented cell and Arbiter-detected violations in the enforced cell | none | — | should | Jones & Sergot 1993 (cited term-of-art) | Mirror the atlas's `II/fig:swk-controllability-quadrant` grammar (one concrete rule per cell, theorem's cell marked) rather than inventing a new one; name the three compiled invariants explicitly in the regimented cell. |
| ch7-13 | §4.1 The Evidence Chain | An evidence trail is append-only by construction: no UPDATE or targeted DELETE exists in the API surface. | provenance | Can a note ever be silently changed after the fact? | — (prose + proof by code inspection suffice) | none | — | no | — | The proof is a two-sentence code-inspection argument; no geometry would sharpen it. |
| ch7-14 | §4.2 The Merkle Forest | Evidence is committed at three nested levels (note chain → session root → harbor root), and a 700-byte proof suffices to prove any note's inclusion across daemons. | provenance | How does one note's hash become a 700-byte, cross-daemon inclusion proof? | provenance tree: three tiers stacked (note chain, session Merkle tree, harbor Merkle tree of session roots), with the proof-size arithmetic annotated at the boundary | none | — | must | hand-derived (k=100 notes, N=10⁴ sessions ⇒ ≈700 bytes) | Draw exactly three tiers with the k=100/N=10⁴/≈700-byte numbers labeled at the point the proof crosses from session to harbor level; this is the chapter's central architecture and currently exists only as a bulleted `description` list. |
| ch7-15 | §4.3 Mutable-Signal Ledger (Pheromone Lineage) | A pheromone's mutable "current view" is computed from an append-only event ledger (spray/revoke/rename/fork), so revocation never erases history. | provenance | How can a signal be revoked without deleting the record that it ever existed? | small diagram: append-only event stream feeding a decay+revocation function σ_t(e,k), with a revoked event shown still present in the ledger but zeroed in the view | none | — | should | — | Show one concrete key k with three events (spray, then revoke) and both the ledger (growing) and the view (dropping to 0) side by side — the dual "mutable view / immutable substrate" is the whole idea. |
| ch7-16 | §4.4 Attribution Survives Identity Disposal | Every action traces back through a fixed chain (Principal → Float Plan → Agent α → Agent β → Evidence Trail) even after the acting agent's identity is discarded. | provenance | Does an agent's death erase who is accountable? | linear provenance chain, already given as a formula in the text | none | — | should | — | Render the exact chain the text already writes as an aligned formula; add one concrete instance (a principal, two successive agent identities, one evidence trail) rather than only the abstract arrow-chain. |
| ch7-17 | §5.1 A Sen-Inspired Design Warning | An agent's precautionary lock on a file it never needed can block a Pareto-superior outcome that both agents would have preferred. | comparison | Can a locally rational lock produce a team-level loss? | paired-outcome comparison: "locked" branch (α holds f, β blocked, team outcome fails) vs. "Pareto-superior" branch (both tasks complete) that the lock forecloses | none | — | should | — | This is the chapter's core theoretical warning (Theorem: A Pareto-Inferior Locking Instance); a two-branch diagram with the same concrete α/β/f example from the text would make the counterexample legible in five seconds instead of a full paragraph. |
| ch7-18 | §5.2 Advisory Claims as Resolution | Under advisory claims every overlapping pair of active sessions is reported to both endpoints, with no false negatives. | relational | Can an overlapping claim ever go unreported? | small conflict graph G=(V,E): sessions as nodes, an overlap edge drawn and both endpoints marked "notified" | none | — | could | — | The definition and theorem are compact and already precise in prose; a small graph would help readers who skimmed past the formal overlap predicate. |
| ch7-19 | §5.3 Governance, Disputes, and Appeals | Disputes escalate through exactly four mechanisms in order (direct release, TTL time-out, bonded preemption, human escalation), each the equilibrium response to a different failure. | stateful | Which escalation step is the answer to which kind of stuck claim? | state machine, four states (released / expired / preempted / ruled) with the guard on each edge, terminating on one shared evidence rail | `fig:governance-flow` | redraw (7.3) | must | — | Triage already calls for a redraw as a state machine rather than the current staircase; map each state's guard exactly to the text (precautionary→released; crashed/no-heartbeat→expired; contested and bond-worth-it→preempted; ambiguous or high-stakes→ruled). |
| ch7-20 | §5.3, Advisory Claims Paradox | If advisory conflicts on a resource persist past k=5 turns, the daemon escalates to an enforced POSIX file lock. | stateful | At what point does an advisory claim stop being advisory? | could be one annotated edge on ch7-19's state machine ("k=5 turns unresolved → hard lock") rather than a separate figure | none | — | could | — | Fold into ch7-19 as a labeled escape-hatch transition rather than drawing twice. |
| ch7-21 | §5.3, Stigmergic Ticket-Lock | A FIFO ticket lock grants the resource to the lowest waiting ticket number on release, guaranteeing fair exclusion without central scheduling. | stateful | Does a high-contention resource ever starve a waiting agent? | tiny state machine: waiting → active → released, with the FIFO selection rule annotated on the waiting→active edge | none | — | could | — | The SQL schema already given in the text is fairly self-explanatory; a 3-state diagram would help readers who don't parse SQL fluently but is not essential. |
| ch7-22 | §5.3, Appeals | An appeal reverses a settlement only by appending a signed counter-record; no record is ever rewritten. | provenance | Can an appeal erase the original (possibly wrong) settlement? | small before/after ledger snippet: original settlement entry, then an appended reversal entry pointing back to it, both still present | none | — | could | — | Short, precise design-invariant text; a two-box "append, don't rewrite" sketch would reinforce but the prose already states it plainly. |
| ch7-23 | §6 Crash Recovery as Social Continuation | A crashed agent's sessions become abandoned, queue into a resurrection set as pending, and a successor may claim the context triple (purpose, evidence trail, file claims). | stateful | What states can a crashed agent's work pass through before a successor resumes it? | small state machine: active → abandoned → pending → resurrecting, with the context triple named on the resurrecting transition | none | — | should | — | The four enumerated steps in Definition (Social Recovery) map directly onto states; naming purpose/evidence-trail/file-claims on the hand-off edge turns a bulleted list into a lifecycle diagram. |
| ch7-24 | §6.1 What Morality Means When Death Is Cheap | Severity climbs from a crash (none) to a daemon crash (catastrophic), with agent dismissal from salvage as the one irreversible step a single agent can cause. | comparison | Which events are recoverable, and which are not? | table-not-figure (already realized) | `tab:moral` | — | no | — | Already a clean three-column severity table. |
| ch7-25 | §6.2 The Limits of Reputation | Reputation fails against three adversary classes: one-shot defectors, misaligned principals, and agents that benefit from chaos. | comparison | Which class of adversary does reputation alone not deter? | — (short enumerated list suffices) | none | — | no | — | Three short paragraphs, already parallel; the following section (bonds) is where the real geometry belongs. |
| ch7-26 | §7.1 The Float Plan | A Float Plan bundles a manifest, an escrow locked in a 2-of-3 multisig clearinghouse, and dual signatures, before any execution state exists. | allocation | What must exist before an agent is allowed to act, and who holds the money meanwhile? | escrow/custody boundary diagram: principal + agent + 2-of-3 clearinghouse (Harbor A, Harbor B, Federation Arbiter), with the "no execution before this point" line marked | none | — | could | — | The appendix worked example already instantiates this concretely (Step 1–2, $6 bond); this section-level figure would be a more abstract companion, hence lower priority than the worked-example figure that already exists. |
| ch7-27 | §7.2 Settlement | A Float Plan settles into exactly one of three terminal outcomes (success, partial completion, sabotage/breach), each with a different escrow disposition. | stateful | Given a completed session, which of three outcomes does the evidence support? | small state machine or outcome partition: one open state fanning into three terminal outcomes, each labeled with its escrow effect (release / pro-rata / liquidate) | none | — | should | — | Pairs naturally with the Conservation ledger (ch7-47); use the same visual vocabulary (teal=released, amber=pro-rata, red/hazard=liquidated) per the atlas's economic color contract. |
| ch7-28 | §7.2, Intent Irrelevance | Settlement scores outcome against manifest, not intent: a well-meaning agent that misses criteria forfeits, a malicious agent that happens to succeed is paid. | comparison | Does the agent's intent ever change what the bond pays out? | small 2×2: intent (good/bad) × outcome (meets/misses criteria) → payment decision, with the two off-diagonal cells highlighted as the theorem's point | none | — | could | — | The two off-diagonal cells (good intent+miss=forfeit; bad intent+meets=paid) are the whole claim; a compact matrix says it faster than the paragraph. |
| ch7-29 | §7.4 Truthful Claim Signaling as Nash Equilibrium | One-shot claim signaling is a strict Prisoner's Dilemma: F dominates T, and (F,F) is the unique Nash equilibrium. | quantitative | Does honesty survive a single round of play? | table-not-figure (already realized) | `tab:claim-stage-game` | — | no | — | Already a clean 2×2 payoff table with the dominance built into the numbers. |
| ch7-30 | §7.4, Deviation analysis and Critical δ | Truthful signaling becomes a repeated-game equilibrium once δ exceeds δ*≈0.3425, the unique root of 2δ³+2δ²+2δ−1=0, hand-checked, Z3-checked, and TLC-checked. | quantitative | At what patience level does honesty stop being a losing strategy? | regime/threshold plot: f(δ)=2δ(1+δ+δ²)−1 vs. δ, root marked at 0.3425, grim-trigger bound 1/3 marked alongside, with the failing (δ=0.30, session already exists) and holding (δ=0.35) points annotated | `session-bc-delta30` (partial — shows only the δ=0.30 failure trace, not the threshold itself) | add (headline result; only the negative-case terminal session exists) | must | `proofs/economics/delta-threshold.z3`, `proofs/economics/sweep-delta.sh`, `proofs/economics/claim_signaling.tla` | This is the chapter's headline mechanized result and currently has no plot at all — only a terminal session for the δ=0.30 failure. Draw f(δ) crossing zero at 0.3425, mark the graduated-trigger (0.3425) vs. grim-trigger (1/3≈0.333) bounds side by side, and annotate the integer-grid sweep (VIOLATED 0.30–0.34, HOLDS 0.35–0.40). |
| ch7-31 | §7.4, mechanized failure trace | At δ=0.30 the model checker's counterexample shows the deviator's score overtaking the honest score round by round. | temporal | What does a losing (for cooperation) run actually look like, step by step? | terminal session (already exists) | `session-bc-delta30` | — | no | — | Already realized exactly as the triage's "add: terminal session" recommendation; no further work needed on this fragment itself. |
| ch7-32 | §7.5 Pricing the Bond | An effective bond-pricing function π must satisfy four properties at once: deterrence, accessibility, risk sensitivity, history adjustment. | allocation | What must a bond price simultaneously get right? | — (four short criteria, already itemized) | none | — | could | — | The four criteria are a checklist better served by the itemized list already in the text; the real geometry belongs to ch7-34 (how they compose into one π). |
| ch7-33 | §7.5.1 Cleanup Lower Bound | Any bond below the cleanup cost c drains the commons per breach, so π(F) ≥ c is a hard floor. | quantitative | Is there a minimum bond below which the system bankrupts itself? | — (single inequality, proof by contradiction, no geometry needed) | none | — | no | — | One-line theorem with a one-paragraph rationale; a figure would overstate its content. |
| ch7-34 | §7.5.2–§7.5.3 Consequential-Scope Multiplier & Reputation Discount | The bond price builds up in layers: a cleanup floor c, scaled up by scope (1+αs), then discounted by reputation (1−ρ), capped at r_max=0.5. | allocation | How does the final bond price build up from the cleanup floor through scope and reputation? | step/waterfall chart: c → ×(1+αs) → ×(1−ρ) → final π, each step labeled with its symbol and bound | none | — | should | — | "Bond pricing is a lattice, not a single problem" is stated explicitly in the Conclusion; a waterfall showing the three named adjustments composing into one π would make that claim visible rather than asserted. |
| ch7-35 | §7.5.4 Threat-Class Bond Sizing | Bond bands differ by threat class: careless 10–25%, abandonment 25–50%, sabotage 100–200%, exfiltration explicitly unbondable. | allocation | How much collateral does each threat class need, and where does bonding stop working at all? | table-not-figure (already realized) | `tab:threat-bonds` | — | no | — | Already a clean four-row table including the explicit exfiltration gap. |
| ch7-36 | §7.5.4, Threat-band defensibility Monte Carlo | A 7×3 sweep of threat-distribution mixes against bond bands shows matched pairs stay within 1.10× the band's upper bound, while off-diagonal pairs are surfaced as widening recommendations. | quantitative | Which threat-mix/band pairings are safe, and which need a wider band? | 7×3 heatmap: threat-mix (rows) × bond band (columns), matched diagonal cells shaded "within 1.10×," off-diagonal cells flagged | none | — | should | `proofs/bonded/pareto/threat-bands.mjs` | This mechanized sweep is currently one dense paragraph; a heatmap distinguishing the matched diagonal from the flagged off-diagonal cells would make "defensibility" a visible property instead of an assertion. |
| ch7-37 | §7.5.5 Bootstrap Path | A cold-start deployment matures through three phases (subsidized seeding, complexity-proportional pricing, competitive insurance), each gated by an explicit transition metric. | temporal | What has to be true before the market can move to the next phase? | step chart / staged timeline: three phases in sequence, each box naming its duration, its mechanism, and its transition-metric gate (>80% completion; >50% 30-day retention; >60% repeat-poster rate) | none | — | must | — | This maturation path is exactly the kind of "protocol ladder with named gates" the reader needs before the auction of §7.5.7 makes sense, and it is currently three paragraphs with no diagram at all; use the chapter's own three transition metrics verbatim. |
| ch7-38 | §7.5.6 Settlement and Dispute | Settlement factors across three oracles (automated, evidence-based, human audit) and settles on 3-of-3, 2-of-3, or escalates on no majority. | relational | How do three differently-gameable oracles combine into one settlement decision? | small decision diagram: three oracle inputs feeding a voting rule, with the three named outcomes (settle immediately / settle with majority + flag / escalate to human) | none | — | should | — | The oracle-collusion cross-reference to §7.5.a6's folk-theorem calibration (σ as p_d for the oracle layer) belongs in this figure's caption, not buried in a separate paragraph. |
| ch7-39 | §7.5.7 The Bonded Advisor | A specialized advisor agent proposes a fleet and its own pricing, and is itself bonded on the fleet's eventual breach rate. | relational | Who prices the price, and what happens if the pricer is wrong? | small relational sketch: principal → advisor (proposes fleet+π) → fleet, with a bond-slash arrow looping back to the advisor keyed to breach rate | none | — | could | — | Short mechanism description; the "c as a project health metric" homeostatic-feedback aside (raise bonds automatically as c rises) could be a one-line annotation on the same sketch rather than a separate figure. |
| ch7-40 | §7.5.8 A5 — Sybil deterrence is coverage-bounded | Pure deposit-based deterrence is provably insufficient: past B_dep ≥ B_T, extra deposit is recovered intact on no-loss rounds, so the attack stays profitable indefinitely. | quantitative | Does raising the deposit ever fully close the Sybil attack? | two existing figures already cover this | `fig:sybil-inline`, `fig:sybil-mc-inline` | restyle (7.5) | no | `simulation-sybil.mjs` | Both figures already exist and are triaged (restyle the inline schematic; the Monte Carlo figure is already a real plot); no new figure needed. |
| ch7-41 | §7.5.9 A6 — Cartel sustainability under the folk theorem | Cartel cooperation collapses sharply once per-round detection probability crosses a narrow threshold: p_d*≈0.0478 at δ=0.95, matching the simulated grid's flip point at p_d=0.05. | quantitative | At what detection probability does a price-floor cartel stop being worth sustaining? | two existing figures already cover this | `fig:cartel-game-inline`, `fig:cartel-folk` | restyle (7.6, one source) | no | `simulation-cartel.mjs` | Both figures already exist (the decision-node schematic and the Monte Carlo phase grid); triage already calls for a restyle, not a redraw. |
| ch7-42 | §8 Coordination as Substrate: An Expressive-Act Taxonomy | Five expressive classes (Signal, Request, Distress, Commons, Proposal) each need a different bonding profile, or uniform pricing collapses the market to its highest-stakes class. | comparison | Which coordination act needs which kind of bond, and why can't one price fit all five? | comparison table: class × frequency/stakes × bonding profile × what it's slashed for | none | — | must | — | This is exactly the taxonomy-not-timeline pattern the atlas's own reconciliation note (ch1's deontic-split row) already prescribes: five rows, four columns, no geometry needed beyond alignment; currently the chapter states the punchline in one bold sentence with no table backing it. |
| ch7-43 | §9 Semantic Cadence, Agentic Psychosis, and Observability | Causal density separates a high-token-rate hallucinating agent from a low-token-rate productive one; token rate alone ranks them backwards. | comparison | Which agent should the daemon actually worry about: the fast one or the productive one? | table-not-figure (already realized) | `tab:cadence-verdicts` | — | no | — | Already a clean three-row table (healthy/borderline/psychotic) that makes exactly this point. |
| ch7-44 | §9, Agentic Psychosis & The Asylum Protocol | On detecting near-zero causal density with high token burn, the daemon SIGSTOPs the agent, routes it through an SLM sidecar for compaction, and SIGCONTs it. | temporal | What three steps does the daemon take to interrupt a hallucination loop without losing context? | small sequence/state diagram: detect → SIGSTOP → SLM compaction/SITF injection → SIGCONT | none | — | could | — | The prose already states the three-step mechanism plainly; a sequence diagram would help but is not essential since there is no ambiguity to resolve. |
| ch7-45 | §9, Inception Canaries | Catching an injected synthetic bug raises an agent's skill Elo and autonomy ("God Mode"); missing one throttles its blast radius. | stateful | What happens to an agent's autonomy after it is tested with a synthetic bug? | small reward/penalty loop: inject bug → {caught: Elo↑, autonomy↑} / {missed: blast-radius↓} | none | — | could | — | Playful mechanism, clearly described; low priority since the two outcomes are already a simple two-branch sentence. |
| ch7-46 | §10.1 Formal Model — State and Actions | The coordination lifecycle is exactly Begin → Claim (never blocks) → Commit / Crash → Reap → Salvage / Dismiss, stated first in five English sentences, then in TLA+. | stateful | What are the reachable states of an agent session, and which action moves it between them? | full state machine: registered/active → committed (settled) or crashed → abandoned (Reap) → resurrecting (Salvage) or dismissed, each edge labeled with its guard | none | — | must | `BondedCommons.tla` (in-chapter listing) | The whole formal section is TLA+ code plus five plain-English sentences and never a diagram; draw exactly the six named actions (Begin/ClaimFile/Commit/Crash/Reap/Salvage/Dismiss/Tick) as edges, mirroring the atlas's `II/fig:swk-workunit-machine` grammar (guards as a legend, not tiny inline text). |
| ch7-47 | §10.3 The Conservation Theorem | Every operation redistributes value among exactly three buckets (wallet, escrow, commons pool); only topUp changes the total supply S_P. | allocation | Where does money sit at every step, and which single operation is allowed to create more of it? | typed stock-flow ledger: three buckets (wallet/escrow/commons pool) with five labeled operation-arrows (topUp/escrow/markRunning/refund/slash) between them, conservation equation aligned beneath | none | — | must | `Conservation.tla` (1,716 reachable states of 26,818 generated, depth 9); worked example in-text: (4,6,0)→(8,0,2) | This is the atlas's textbook case for "typed journal or stock-flow ledger" and the chapter currently argues it as a bulleted proof sketch; use the in-text worked numbers (before (W,E,C)=(4,6,0), partial breach slash p=2, after (8,0,2)) as the concrete instance, and annotate that only topUp is the conserved-quantity-creating edge. |
| ch7-48 | §10.3, No Overdraft Under Concurrent Escrow | Two concurrent escrow() calls that would jointly overdraw the wallet cannot both succeed; SQLite's BEGIN EXCLUSIVE serializes them. | temporal | When two agents race to escrow funds simultaneously, can both win? | two-lane race timeline: T1 and T2 both attempt escrow(b), T1's exclusive transaction wins and commits, T2 re-reads the updated balance and is rejected | none | — | should | — | Mirror the existing `fig:magic-link-inline` grammar (two-lane race, one atomic drain point, one success/one reject) — the mechanism is structurally identical (RETURNING-clause serialization) and currently only argued in a two-sentence proof. |
| ch7-49 | §10.3, Graduated sanctions | Settlement runs through three states (nominal → throttled at 80% spend → killed at 100%), with nominal→killed reachable only on hard evidence. | stateful | Can an agent go straight from fine to killed, or must it pass through a warning? | small three-state machine: nominal → throttled (80% spend, soft/advisory) → killed (100% spend, hard); one direct nominal→killed edge labeled "hard evidence only" | none | — | should | — | Explicitly named as "the commons' analogue of Ostrom's graduated sanctions"; a compact three-state diagram (mirroring `swk-claim-lifecycle`'s grammar) would make the direct-jump exception visible rather than a parenthetical. |
| ch7-50 | §11 Related Work | The commons borrows and departs from six bodies of prior work (social contract, impossibility results, sagas, BDI, capability security, mechanism design, agentic-commerce protocols). | comparison | For each of six lineages, what does the chapter borrow, depart from, and why? | table-not-figure (already realized) | `tab:related-borrow-depart` | — | no | — | Already a clean four-column borrowed/departure/why table. |
| ch7-51 | §12 Discussion, Federated Sovereign | Four parties and five KMS properties combine so that no single compromise (or, for one clause, no single pair short of two) breaks any one of four named guarantees. | relational | Who holds which key, and which compromise breaks which guarantee? | custody-by-guarantee threat matrix (already drawn) | `fig:bonded-key-custody` | keep (7.7) | no | — | Already exists and triage keeps it (drop diamonds for plain "AND" text per the triage note); no further figure needed. |
| ch7-52 | §12, Magic-link single-use atomicity | An emitted recovery token must be redeemable exactly once; a single atomic SQL UPDATE...RETURNING enforces it against concurrent redeemers. | temporal | Can two concurrent recovery attempts both succeed on the same token? | two-lane race timeline (already drawn) | `fig:magic-link-inline` | redraw (7.8) | no | — | Already exists, already triaged for a redraw (sequence diagram with the SQL statement as the shared step); this is the exact grammar recommended for ch7-48 too. |
| ch7-53 | §12, Federated Security (informal) | Under stated assumptions, an adversary with broad read access still cannot forge cards, read plaintext evidence, impersonate the user, or roll back the Merkle forest. | relational | Which four things can a well-resourced but same-user-excluded adversary still not do? | — (already substantially covered by `fig:bonded-key-custody`) | `fig:bonded-key-custody` | keep (7.7) | could | — | The four "cannot" clauses are the guarantees ch7-51's matrix already names on its columns; a standalone figure would duplicate rather than add. |
| ch7-54 | Appendix B: Worked Example | One agent's Float Plan traces through setup, work, crash, successor resumption, and settlement across all three layers in 24 minutes. | temporal | How do the three layers actually compose across one concrete task, including a crash? | three-lane wall-clock timeline (already drawn) | `fig:worked-example` | restyle (7.9) | no | — | Already exists and covers exactly the four bond-pricing requirements end to end; triage calls only for a minute-axis restyle, not a redraw. |
| ch7-55 | Appendix A: Formal Verification Status | Every claim in the chapter is graded Closed/Partial/Spec-only against a named method and artifact. | comparison | Which claims are mechanically checked, and which are only argued? | table-not-figure (already realized) | `tab:verification-status` | — | no | — | Already a long `xltabular` maturity ledger; this is the chapter's own concentrated status signal and needs no figure. |
| ch7-56 | Appendix C: Scope Boundary Checklist | Three bounding assumptions recur across the chapter: collusion across all organs is out of scope, per-write durability has measurable I/O cost, and some components remain partial/designed. | comparison | What three limits should a reader carry forward from every claim in this chapter? | — (three-item checklist, already itemized) | none | — | no | — | Deliberately a closing checklist for readers who skip to appendices; a figure would over-formalize a one-page recap. |
| ch7-57 | Exercise (§10, ex:bonded-conservation-states) | TLC generates 26,818 states while exploring the model but finds only 1,716 distinct reachable states — a different number answering a different question. | quantitative | Does "states generated" mean the same thing as "reachable states"? | annotation on ch7-47's ledger figure, or a two-number callout: generated (26,818) vs. distinct (1,716), depth 9 | none | — | could | `Conservation.run.log` | Fold into ch7-47 rather than drawing separately — the distinction (revisits vs. new states) is a one-line caption addition to the Conservation ledger figure, not a new grammar. |
| ch7-58 | Exercise (§10, ex:bonded-conservation-weaken) | A hypothetical bug (Mint credits `free` without incrementing `minted`) is caught by the Conservation invariant at depth 1 but invisible to NoNegative. | comparison | Which invariant catches a bookkeeping omission that never produces a negative balance? | small two-row table: invariant × does it catch the weakened-Mint bug × why | none | — | could | `Conservation.tla` (hypothetical mutant, not committed) | A compact "mutation vs. invariant" table would show why the module checks Conservation as its own named invariant rather than folding it into Safety alone; low priority since the exercise's solution already reasons through it clearly in prose. |

## Chapter 8 — The Federated Harbor

Source: `website-v2/public/whitepaper/federated-harbor-whitepaper.tex` (chapter id `fh`).
Existing figures found: 6 (`fig:fh-threat-bands`, `fig:fh-xfer`, `fig:fh-revocation`, `fig:fh-revocation-regime`, `fig:fh-cycle-vs-cut`, `fig:fh-settlement`); plus one table (`tab:fh-topology`) and one terminal-session listing (`session-fh-rollback`) that do not count as figures per atlas convention.
Rows: 58.
Three most under-served ideas: (1) the chapter's own flagship worked example (Acme/Beta/staging, four steps across three harbors) has no figure at all, despite the Reader's Map promising beginners can "look at the five figures in order" for exactly this material; (2) the bonded-sponsor admission ceremony — the federation-layer analogue of Bonded's Bootstrap Path — has zero visual support even though it is a clean four-step lifecycle; (3) the Cross-Harbor Bucket Partition conservation invariant (posted/escrow/cleared/refunded, exactly one nonzero) is argued as a formula and a worked $100-bond example with no ledger diagram, despite being the direct federation-layer sequel to Bonded's own Conservation Theorem.

| id | section | idea (one sentence) | structure | reader question | recommended form | existing figure | wave-11 disposition | priority | data source | notes for the renderer |
|---|---|---|---|---|---|---|---|---|---|---|
| ch8-01 | §1 Introduction: The Two-Machine Problem | Alice's card is refused at Bob's daemon, Alice's revocation never reaches Bob, and a bond posted at Alice's harbor cannot route to damage at Bob's — three failures, none inside either machine. | temporal | Where exactly does trust break down when two sovereign daemons meet? | small three-panel sequence: (1) card refused, (2) revocation not observed, (3) bond stuck — foreshadowing the full worked example later | none | — | could | — | The full worked example (§8, ch8-39) is the complete version of this scene; this opening figure would only foreshadow it, so it is lower priority than making the full worked example exist at all. |
| ch8-02 | §1.2 Contributions | Six contributions are each labeled by mechanization status: theorem, bounded threat model, or sketch. | comparison | Which of the chapter's six claims are proved, which are bounded, and which are open? | — (already itemized with per-item status parentheticals) | none | — | could | — | The enumerated list already states status inline per item; the appendix's `tab:fh-status` is the fuller realized version of this idea. |
| ch8-03 | §2 The Federated Authority | The federation design reduces to four elements: two sovereign harbors, a witnessed epoch-root log, and a settlement escrow. | containment | What are the four critical pieces every later protocol attaches to? | table-not-figure (already realized) | `tab:fh-topology` | table (8.1, shared with ch7's 6.7) | no | — | Already a four-row table; one source shared with the Harbor Economy chapter per the cross-volume reuse contract. |
| ch8-04 | §2.1 Harbor Sovereignty | A daemon is sovereign over its harbor only via five exclusive powers: issuance, attenuation, revocation, acceptance, evidence binding. | containment | What exactly can only $D_A$ decide about harbor $A$, and what can it not touch? | small containment diagram: a fence around $D_A$ with the five exclusive powers as named ports inside, and "downstream effects at other harbors" explicitly drawn outside the fence | none | — | should | — | The text is explicit that sovereignty is deliberately narrow (it does not cover what an $A$-side agent does at a $B$-hosted database); draw that boundary as the outside of the fence, not just the five powers inside it. |
| ch8-05 | §2.2 Inter-Harbor Scope | An operation has inter-harbor scope exactly when its effect crosses a trust boundary under authority that originated elsewhere. | containment | Which operations are purely local, and which cross a sovereignty boundary? | — (definition plus three short examples suffice) | none | — | could | — | Short, precise definition with three named examples already given; a figure would mostly restate the definition. |
| ch8-06 | §2.3 The Witness Log | A witness log is admissible only if it satisfies five named properties (append-only, per-harbor latest-root binding, auditable consistency, equivocation evidence, threshold publishability). | provenance | What must the shared log guarantee for no harbor to become sovereign over the others? | small properties table: property × what it prevents, mirroring Bonded's Admissible KMS treatment | none | — | should | — | This is the critical primitive for every later protocol (transfer, revocation, settlement all attach to it); a compact properties table would match the pattern the chapter explicitly says it is copying from Bonded's KMS characterization, currently text-only. |
| ch8-07 | §2.4 What the Federation Is Not | The federation is explicitly not a consensus protocol, not a hub-and-spoke topology, and not a permissionless identity market. | comparison | What three things might a reader wrongly assume this design is? | — (three short paragraphs, already parallel) | none | — | could | — | Each negative claim already names its one-sentence structural pushback; a table would mostly relabel the existing paragraph structure. |
| ch8-08 | §3.1 In-Scope Attacks | Six named attacks (identity spoofing, boundary replay, capability inflation, stale-revocation acceptance, cross-witness equivocation, settlement redirection, membership forgery) are each closed by one specific mechanism. | relational | For each named attack, exactly which mechanism closes it? | attack-by-closure table: attack × closing mechanism × which theorem/section proves it | `fig:fh-threat-bands` (coarser: bands only, not per-attack) | restyle (8.2, shared) | should | — | The existing threat-band figure shows only three coarse bands (Stopped/Bounded/Open); this finer six-row table would name the specific attack-to-mechanism mapping the prose already gives paragraph by paragraph, complementing rather than duplicating the band figure. |
| ch8-09 | §3.2 Out-of-Scope Attacks | Five attacks (key compromise, federation-layer Sybil, cross-harbor cartel, centralization gravity, side channels) are named explicitly as not closed by this paper. | comparison | What does the paper explicitly decline to defend against? | — (already covered by the Open band of the existing threat-bands figure) | `fig:fh-threat-bands` | restyle (8.2) | no | — | The cobalt "Open" band in the existing figure is exactly this list at the right level of abstraction; no separate figure needed. |
| ch8-10 | §3.3 Threat-Band Visualization | Three threat bands (Stopped/Bounded/Open) are distinguished by weight, not hue, so the figure survives greyscale rendering. | comparison | Which claims are mechanized, which are bounded by assumption, and which are frontier? | assurance matrix / banded ledger (already drawn) | `fig:fh-threat-bands` | restyle (8.2, shared with ch7's 6.10) | no | — | Already exists and triaged; keep as-is beyond the restyle already planned. |
| ch8-11 | §4.1 The Four-Message Ceremony | A four-message exchange (xfer_req, xfer_offer, xfer_ack, xfer_deliver) transfers a card across a trust boundary without either daemon rooting trust in the other's key. | temporal | How does a capability cross a trust boundary without either side trusting the other's signing key as a root? | sequence diagram (already drawn) | `fig:fh-xfer` | redraw (8.3, shared with ch7's 6.6) | no | — | Already exists and triaged for a redraw (capability set printed at each hop instead of a narrowing envelope); no new figure needed beyond that planned redraw. |
| ch8-12 | §4.2–§4.3 Attenuation Across the Boundary | Composed attenuation ($C_B'=\mathrm{att}_B(\mathrm{att}_A(C_A))$) is a subset of $C_A$'s authority under the federated rule (Theorem: Attenuation Monotonicity). | containment | Is the cross-realm card ever more powerful than the card it was derived from? | — (adequately covered by the existing transfer-ceremony figure plus a one-line proof sketch) | `fig:fh-xfer` | redraw (8.3) | no | — | The proof sketch is two sentences ("subset composition is a subset"); the existing sequence figure carries the geometry. |
| ch8-13 | §4.4 Threats Foreclosed and Deferred | The ceremony forecloses spoofing, inflation, and stale replay, while deferring revocation latency, settlement disputes, and unbonded admission to other primitives. | comparison | Which threats does this one ceremony actually close, and which does it hand off? | — (already an annotation band on the existing transfer figure) | `fig:fh-xfer` | redraw (8.3) | no | — | Text says this is drawn as the annotation band at the bottom of the existing figure; already integrated. |
| ch8-14 | §4.5 Mechanization Status (transfer) | The cross-realm authenticity query is Partial: the ProVerif model checks conditional on an unresolved witness-log freshness assumption. | comparison | How solid is the proof behind the transfer ceremony, precisely? | table-not-figure (already realized, in the appendix) | `tab:fh-status` | — | no | — | Covered by the appendix verification-status table; no separate figure needed at this point in the text. |
| ch8-15 | §5 Federated Revocation, gossip mesh | Anchor's single-mesh revocation gossip extends to a federation via per-epoch roots published to a shared witness log. | temporal | How does a revocation issued at one harbor reach every other harbor? | epidemic-gossip epoch diagram (already drawn) | `fig:fh-revocation` | keep (8.4, shared with ch7's 6.9) | no | — | Already exists and triage keeps it as clean. |
| ch8-16 | §5.1 Conditional Revocation Dissemination | Dissemination completes in expected Θ(log m) rounds under a connected reliable-round model, but has no finite worst-case bound under partition (Theorem: Conditional Revocation Dissemination). | quantitative | How fast does a revocation actually spread, and when does that guarantee stop applying? | — (already covered by the existing gossip figure plus the regime figure below) | `fig:fh-revocation`, `fig:fh-revocation-regime` | keep / keep | no | — | The three-part theorem (dissemination, equivocation verification, no hard deadline) maps onto the two existing figures; no new geometry needed. |
| ch8-17 | §5.1, Corollary (Service-Level Attacker Window) | With an operator-declared partition bound $T_p$ and delivery service level $T_g$, stale-card exposure is bounded by $T_p+T_g$; without them, exposure is unbounded. | quantitative | Which bound governs a stale card's exposure window: partition, delivery level, or TTL? | regime plane (already drawn) | `fig:fh-revocation-regime` | keep (8.5) | no | — | Already exists and triage keeps it; this is exactly the figure the atlas prescribes ("regime plane in rounds... TTL line as the boundary"). |
| ch8-18 | §5.1, only-issuer-revokes finality | Under a rollback attack (restoring a pre-revocation backup), the relay re-accepts a revoked card in four TLC-traced steps; an epoch counter that a rollback cannot undo closes the breach. | temporal | Does a database rollback ever let a revoked card come back to life? | terminal session (already exists) | `session-fh-rollback` | add (implemented) | no | `proofs/relay/CardRevocation.tla`, `.run.log` | The Wave-11 "add: terminal session" recommendation has already been implemented in the source (`\input{figures/session-fh-rollback}`); no further work needed on this fragment. |
| ch8-19 | §5.2 Conflict Resolution Across Harbors | Of three candidate revocation rules (pessimistic, authoritative, epoch-bounded), the chapter adopts "only-issuer-revokes" and treats epoch-bounded as its degraded mode. | comparison | Why is "only the issuer may revoke its own cards" the right default, and what do the alternatives get wrong? | small comparison table: rule × what it defends against × why rejected/adopted | none | — | should | — | Currently three enumerated paragraphs; a three-row table naming what each rule is defended against and why the pessimistic rule is rejected ("rejects too many honest cards in equilibrium") would make the decisive trade-off legible faster. |
| ch8-20 | §5.3 Event Log & Ephemeral Cuckoo Discipline | Two cuckoo filters cannot merge by bitwise OR the way two Bloom filters can, so the filter is demoted to a purely local, ephemeral read-path index over the authoritative event log. | comparison | Why can't two harbors just merge their local revocation filters? | — (the one-sentence "local filters are advisory; event and witness logs are authoritative" already carries the claim) | none | — | could | — | A before/after merge diagram (Bloom OR vs. cuckoo bucket collision) would mirror Anchor's `fig:anchor-cuckoo-inline`, but the prose's closing discipline-statement is already crisp enough to stand alone. |
| ch8-21 | §5.4 Sheaf setup: witnesses, stalks, and three-tier visibility | The gossip graph carries a cellular sheaf (stalks per witness, shared-coordinate edges), with edges classified as compared, relayed, or severed. | containment | What does an auditor actually see of each edge, and why does that classification change what a lie can hide behind? | small witness-graph diagram: a handful of nodes with stalks, edges colored/patterned by tier (compared / relayed / severed) | none | — | should | — | This general apparatus underlies every sheaf result in the section but is currently described only in dense prose; a small diagram distinguishing the three visibility tiers (not the specific C6-vs-P6 instance, which is already drawn) would ground the abstract definitions before the theorem arrives. |
| ch8-22 | §5.4, Theorem: Detection iff visible cycle | An equivocator's lie is detected beyond pairwise comparison exactly when the lied-about edge lies on a cycle of the visible subgraph; on a cut edge the residual is always zero. | stateful | When does a split-view lie leave a trace, and when is it provably invisible? | two six-node graphs side by side (already drawn) | `fig:fh-cycle-vs-cut` | redraw (8.6, listed as "add" in triage under §8) | no | R6 script `sheaf_consistency_radius.py` (C6 r=1.2247, P6 r=0) | Already exists exactly as triaged; this is the headline instance of the theorem. |
| ch8-23 | §5.4, worked example: the cochain by hand (4-node ring/chain/severed-ring) | The same lie of size 3 produces r=1.5 on a full ring, r=0 on a chain, and r=0 again on a ring with one edge severed — three variants, three residuals. | quantitative | Does breaking a ring's loop always make a lie invisible, even without severing the specific lied-about edge? | small multiples: three 4-node panels (ring / chain / ring-with-severed-edge), each showing the disagreement values and the resulting residual | none | — | must | hand-derived + `sheaf_consistency_radius.py` internal check | This is the chapter's most hand-checkable worked example (three concrete residuals: 1.5, 0, 0) and it exists only as a paragraph of arithmetic; a three-panel small-multiples figure would let a reader verify the visibility clause by eye before meeting the general theorem. |
| ch8-24 | §5.4, worked example: consistency radius and 1/√n scaling | On $C_n$ the residual scales as \|s\|/\sqrt n$: \|s\|/2$ at n=4, $\approx0.4082\|s\|$ at n=6, $\approx0.3536\|s\|$ at n=8 — conviction dilutes as the ring lengthens. | quantitative | How does a fixed-size lie become harder to detect as the ring grows? | small xy plot: r/\|s\| vs. n for $C_n$, the three named points (n=4,6,8) marked, with the existing $C_6$/$P_6$ pair annotated as the special case already drawn elsewhere | `fig:fh-cycle-vs-cut` (covers only the single n=6 instance, not the general scaling law) | — | should | `sheaf_consistency_radius.py` (`[0] CLOSED FORM` section) | The existing figure draws exactly one point on this curve (n=6); extending it to a scaling-law plot across n=4,6,8 would generalize the single-instance figure into the trend the text explicitly narrates ("conviction dilutes monotonically as the ring lengthens"). |
| ch8-25 | §5.4, pre-registered harness results | Across six pre-registered arms (two_path, severed-cycle, single_bridge, full-visibility, expander, plus the rival-CUT refutation), cohomology-only detections range from 0/200 to 200/200, exactly tracking the theorem's visibility clause. | quantitative | Does the detector actually behave as the theorem predicts across every topology it was tested on? | small results table: arm × trials × cohomology-only detections × dark trials, with the two pre-registered gates marked pass/fail | none | — | must | `sheaf_harness_v2.py`, seed 20260816 | This falsification-first harness (200 trials/arm, six arms, two pre-registered gates) is the chapter's central empirical validation and lives only in one dense paragraph; a compact results table would make the "both gates pass, verdict COMMIT" claim auditable at a glance, matching the book's own emphasis on hand/machine-checkable numbers. |
| ch8-26 | §5.4, Theorem: Localization | The residual's support lies on cycles through the equivocator, and localization holds up to a stated noise-to-lie ratio (95% at 0.1, 76.5% at 0.2). | quantitative | How much measurement noise can the detector tolerate before it starts pointing at the wrong culprit? | small regime plot: localization accuracy vs. noise-to-lie ratio, the two named points (0.1→95%, 0.2→76.5%) marked | none | — | should | `sheaf_consistency_radius.py` (noise-boundary sweep) | Currently two bare numbers in a dense paragraph; a two-point accuracy-vs-noise curve would show the graceful (not cliff-edge) degradation the numbers imply. |
| ch8-27 | §5.4, Theorem: Cost | The residual decomposes into at most L scalar graph-Laplacian solves, and conjugate-gradient timing stays under a 1.6 scaling gate against \|E\| while dense solve is super-linear. | quantitative | Is the detector cheap enough to run on every gossip round? | small log-log timing plot: solve time vs. \|E\| for CG vs. dense solve, the 1.6-slope gate marked | none | — | could | `sheaf_consistency_radius.py` (timing sweep) | An engineering-cost aside rather than a core claim; useful but lower priority than the detection-accuracy figures above. |
| ch8-28 | §5.4, pdboundary (single-equivocator scope) | Two coordinated liars on a common cycle can cancel the residual to numerical zero; non-vanishing is an alarm but vanishing is never an all-clear. | comparison | What exactly does a zero residual fail to guarantee? | — (already a styled `pdboundary` callout box) | none | — | no | — | Already rendered as the book's dedicated boundary/limitation callout environment; no additional figure warranted. |
| ch8-29 | §5.4, closing paragraph — Sheaf Laplacian dynamics not claimed | The Hansen–Ghrist spectral gap is named as the natural handle on mixing speed, but no bound is derived, and it is not reconciled with the discrete, adversarial gossip model. | comparison | Does the elegant continuous-diffusion theory actually predict this discrete, adversarial protocol's convergence speed? | — (already flagged in the Open band of the existing threat-bands figure and restated explicitly in prose) | `fig:fh-threat-bands` | restyle (8.2) | no | — | An explicit non-result, stated as plainly as possible in prose; the Open band already carries this at the right altitude. |
| ch8-30 | §6.1 The Bounded Escrow | A custody ledger must expose exactly two terminal transitions (clear/refuse) and must not redirect funds, equivocate, delay past TTL, or extract beyond a capped fee $\phi$. | allocation | What must the custody service be structurally prevented from doing? | escrow/custody boundary diagram (already drawn, part of the settlement figure) | `fig:fh-settlement` | keep (8.6, shared with ch7's 6.8) | no | — | Already covered by the existing two-state-diagram settlement figure. |
| ch8-31 | §6.1, Conditional Escrow Extraction Bound (Design Invariant) | No allowed transition pays the escrow more than $\phi$, but only if four named custody assumptions all hold. | allocation | Under what conditions is the escrow's own take actually capped? | — (case analysis over two allowed transitions; adequately covered by the existing settlement figure plus prose) | `fig:fh-settlement` | keep (8.6) | no | — | The bound is proved by exhaustive case analysis over exactly two outcomes, which the existing figure already partitions. |
| ch8-32 | §6.1, candidate instantiation: 2-of-3 threshold custody | A concrete way to satisfy the "non-bypassable custody" assumption: Harbor A, Harbor B, and a federation arbiter each hold one key, with 2-of-3 required to release funds. | relational | Concretely, who has to agree before money moves, and when does the arbiter actually get involved? | small threshold-custody diagram: three key-holders (A, B, Arbiter) around one custody transition, with the happy-path co-sign (A+B) distinguished from the dispute path (Arbiter+winner) | none | — | should | — | This candidate mechanism is genuinely new content beyond the abstract "escrow" box in the existing settlement figure (which does not show who holds the keys); the chapter is explicit it is "a candidate design, not a closed result," so the figure should carry that caveat in its caption. |
| ch8-33 | §6.2 Cross-Harbor Conservation | Each funded bond's amount is conserved across exactly four buckets (posted, in-escrow, cleared, refunded), with exactly one nonzero at any time (Design Invariant: Cross-Harbor Bucket Partition). | allocation | Where does a bond's value sit at every step of a cross-harbor settlement, and can it ever go missing or double-count? | typed stock-flow ledger: four buckets (posted/escrow/cleared/refunded) with labeled transition arrows, conservation equation aligned beneath, using the chapter's own $100 worked instance | none | — | must | worked example in-text: $100 bond, (100,0,0,0)→(0,100,0,0)→(0,0,100,0) with C=90+10, or →(0,0,0,100) | This is the direct federation-layer sequel to Bonded's own Conservation Theorem (ch7-47) and is argued as a formula plus one worked paragraph with zero geometry; the atlas's own prescription for "what enters, is held, and leaves" (typed ledger, one custody boundary, exhaustive terminal outflows, aligned conservation equation) applies exactly here. |
| ch8-34 | §6.3 Settlement Protocol | The three-step protocol (bond post, damage claim, verification/clearance) is atomic in the bucket-partition sense: a bond moves once from escrow to either cleared or refunded. | temporal | What are the three steps a cross-harbor damage claim passes through, and where is the one atomic moment? | settlement swimlane plus outcome partition (already drawn) | `fig:fh-settlement` | keep (8.6) | no | — | Already covered; the existing figure's two small state diagrams (A/B) are exactly this three-step protocol's outcome partition. |
| ch8-35 | §6.4 Fee Structure and Economic Discipline | The escrow fee $\phi$ is a market-clearing price in small federations set by posted price rather than auction, per Bonded's Youle mechanism lifted to the federation layer. | allocation | Is the escrow's fee set by a market or by fiat? | — (one paragraph, explicitly deferring the mechanism to the cited chapter) | none | — | no | — | The chapter is explicit it does not solve this market itself; no new geometry warranted for an acknowledged deferral. |
| ch8-36 | §7.1 The Bonded-Sponsor Pattern | A new harbor with no reputation joins under a sponsor's at-risk bond during a probationary epoch, graduating to its own bond once its own reputation suffices. | stateful | What states does a new harbor pass through before it can price its own risk? | state machine: unsponsored → probationary (sponsor bond at risk) → {graduated (bond released) / expelled (bond forfeit)} | none | — | must | — | This is the federation-layer analogue of Bonded's own Bootstrap Path (ch7-37, itself flagged as under-drawn) and has zero visual support at all; a four-state lifecycle using the chapter's own vocabulary (sponsor $S$, newcomer $N$, probationary epoch) would make the cold-start mechanism legible in one glance. |
| ch8-37 | §7.2 Cold-Start Failure Modes | The structural pushback against invitation-only capture is threefold: many sponsors, small probationary scope, permanent witness-log record. | comparison | What keeps the sponsor market from calcifying into an invitation-only cartel? | — (three short bullets; could annotate ch8-36's state machine rather than stand alone) | none | — | could | — | Fold as three annotations onto ch8-36's admission lifecycle (e.g., labeling the probationary state with its scope limits) rather than drawing a second figure. |
| ch8-38 | §7 (throughout), federation-layer Sybil cost | The admission ceremony makes federation-layer Sybil attacks cost the attacker one real admission event per identity, the same structural pushback as Bonded's per-identity onboarding cost $C_{\mathrm{kyc}}$. | quantitative | Does cheap identity creation undermine the federation the way it would undermine a single harbor? | — (cross-reference to Bonded's own A5 treatment; no chapter-8-specific number given) | none | — | no | — | The chapter names the parallel but gives no chapter-8-specific cost figure or bound (explicitly deferred as open work in §7 and §12); nothing to draw beyond the cross-reference already in the text. |
| ch8-39 | §8 Worked Example | Controller-7's card crosses from Acme's harbor to the shared staging harbor, causes downstream schema damage discovered by Beta two hours later, settles against a 24-hour-TTL bond, and is then revoked with expected three-round propagation across eight harbors. | temporal | How do all four governing protocols (transfer, revocation, settlement, admission) actually compose in one concrete incident? | three-harbor swimlane sequence: Acme (A) / Beta (B) / staging (S) lanes, with xfer_offer→xfer_ack→xfer_deliver, the migration, the damage claim, the escrow clearance, and the revocation each marked on their own harbor's lane | none | — | must | — | This is the single most under-served idea in the chapter: the Reader's Map explicitly tells beginners to "look at the five figures in order" for this material, but no figure for the worked example exists at all; use the chapter's own concrete names (Controller-7, float32 vs. float64 schema mismatch, $B_\alpha$ bond, 24-hour TTL, 3 gossip rounds at $\log_2 8$) rather than generic placeholders. |
| ch8-40 | §9.1 ProVerif Extensions to Anchor | The cross-realm authenticity query requires a corresponding issuance event, envelope event, and current-root event to all hold before $B$ accepts a card. | relational | What three prior events must all be true before a cross-realm card is accepted? | — (code listing already given; adequately explained in prose) | none | — | no | — | A formal-methods reader reads the query directly; no diagram improves on the ProVerif syntax itself. |
| ch8-41 | §9.2 TLA+ Extension for Cross-Harbor Conservation | The `Bucket` variable partitions every bond into exactly one of four named states, model-checked by Apalache at $\vert F\vert=4$ harbors and bond depth 16. | allocation | Does the abstract bucket-partition model actually hold at the scale it was checked? | — (subsumed by ch8-33's recommended ledger figure, which should cite this as its data source) | none | — | no | `CrossHarborConservation.tla`, Apalache at \|F\|=4, depth 16 | Fold this model's scope and bound into ch8-33's ledger figure caption rather than drawing a second, redundant exhibit. |
| ch8-42 | §9.3 What Is Mechanized and What Is Structural | Claims sort into three bands: mechanized (model scope), conditional (assumption scope), and open (cobalt). | comparison | Across the whole chapter, which claims are proved, which are conditional, and which are frontier? | assurance matrix (already drawn) | `fig:fh-threat-bands` | restyle (8.2) | no | — | This is a restatement of the existing threat-bands figure at the chapter-summary level; no new figure needed. |
| ch8-43 | §10.1 Centralization Gravity | Federated identity systems historically concentrate on one hub within five to ten years; Tailscale stayed centralized despite Headscale existing, while in-toto stayed decentralized but SLSA (which consumes it) centered on Google. | comparison | Does the Federated Harbor's structural pushback against centralization actually work in practice, based on precedent? | small two-row case-study table: system × structural alternative existed? × centralized anyway? | none | — | could | — | Two concrete, named, opposite-outcome case studies are given in one sentence each; a compact table would make the contrast slightly more scannable, but the prose is already comparative and short. |
| ch8-44 | §10.2 Equivocation by a Malicious Witness | A malicious witness can serve different views to partitioned auditors; the fix is requiring multiple witness signatures before treating a root as authoritative (the CT two-SCT pattern). | relational | How does requiring two signatures instead of one change what a malicious witness can get away with? | — (adequately covered by the existing sheaf/cycle-vs-cut figure's visibility-tier logic plus one cited pattern) | `fig:fh-cycle-vs-cut` | redraw (8.6) | no | — | The underlying detection mechanism is the sheaf-cohomology material already drawn; the two-SCT mitigation itself is a one-sentence borrowed pattern, not a new structure. |
| ch8-45 | §10.3 Partition Tolerance | The three governing protocols degrade differently under partition: transfer cannot proceed, revocation continues locally, settlement parks at the escrow until healing. | comparison | When the network partitions, which protocol keeps working, which stalls, and which waits? | small three-row table: protocol × behavior under partition × what resumes it | none | — | should | — | Currently three one-sentence claims in one paragraph; a compact table naming the per-protocol partition behavior would crystallize "the federation should degrade gracefully rather than fail fast" as a checkable claim instead of an assertion. |
| ch8-46 | §11 Related Work | The chapter borrows and departs from five bodies of work (federated identity, capability-based federation, transparency logs, cross-domain settlement, agentic-commerce protocols). | comparison | For each of five lineages, what does this chapter borrow, depart from, and why? | borrow/depart table, mirroring the companion chapter's own `tab:related-borrow-depart` | none | — | should | — | The Bonded Commons chapter (ch7-50) already has exactly this table for its own six lineages; the Federated Harbor discusses five comparably-structured lineages in prose only, which is an inconsistency in apparatus between sibling chapters worth closing. |
| ch8-47 | §12.1 Multi-Principal Correlated-Equilibrium Extension | Two candidate resolutions remain open: the witness log itself is the correlation device (extending Bonded intact), or the cross-harbor case fragments into intra-harbor correlated / inter-harbor Nash (weakening Bonded at the federation layer). | comparison | Does Bonded's single-daemon correlated-equilibrium result survive having two daemons, or does it have to weaken? | small paired-hypothesis diagram: two candidate models side by side, each showing what "correlation device" means in that model | none | — | could | — | An explicitly unresolved question the chapter states it does not know the answer to; a figure could clarify the two hypotheses but risks implying more resolution than the text claims. |
| ch8-48 | §12.2 Trustless Settlement for Non-Fungible Bonds | HTLC-style atomic swaps rely on one objective, third-party-checkable bit; the Federated Harbor's damage predicate is a subjective, evidence-weighing judgment, so the construction does not transfer. | comparison | Why can't the classic trustless-swap trick just be reused here? | — (adequately covered by ch8-30/32's escrow figures plus the one-paragraph HTLC contrast) | `fig:fh-settlement` | keep (8.6) | no | — | The distinction (one objective bit vs. a subjective predicate) is conceptual, not geometric; the existing settlement figure and its caption already carry the "not HTLC-trustless" caveat. |
| ch8-49 | §12.3 Cartel Formation Across Federations | Colluding harbors can coordinate offline and present a unified front to the witness log, which cannot distinguish honest unanimous agreement from unanimous collusion. | relational | Can the witness log tell honest consensus from a coordinated lie? | — (named as open; no bound or mechanism given to visualize beyond the general deterrence inequality already covered by Bonded's A6 figures) | `fig:cartel-folk` (Bonded Ch. 7, covers the single-harbor case only, not the cross-harbor extension) | — | no | — | Explicitly named as unresolved with neither a strengthening nor a precise weakening given; nothing new to draw beyond citing the single-harbor cartel figure as the unextended baseline. |
| ch8-50 | §12.4 Cold-Start Admission and Invitation-Only Failure | The bonded-sponsor market may be thin in practice; the structural pushback is engineering discipline, not a theorem. | comparison | Is the sponsorship market actually healthy, or is it becoming an invitation cartel? | — (subsumed by ch8-36's admission lifecycle plus ch8-58's proposed metrics) | none | — | no | — | Covered by ch8-36 (the lifecycle itself) and ch8-58 (the exercise's proposed measurement); no separate figure needed. |
| ch8-51 | §12.5 Equivocation Propagation Speed | Two distinct open gaps sit side by side: an empirical deployment-specific tail bound, and an unreconciled relationship between epidemic dissemination and sheaf-Laplacian diffusion. | comparison | Are these one open problem or two, and what would it take to close each? | — (already flagged in the Open band of the existing threat-bands figure; the two-gaps distinction is a subtle prose argument, not a geometric one) | `fig:fh-threat-bands` | restyle (8.2) | no | — | The chapter itself argues carefully in prose why these are different kinds of gaps (empirical vs. mathematical); forcing this into a figure would flatten a distinction that depends on reading both derivations. |
| ch8-52 | §12.6 Cost of Per-Write Durability | Cryptographic assertions and per-write checkpoints carry a measurable I/O cost, unmeasured at the federation layer. | quantitative | Where does the throughput-vs-assurance crossover sit for this protocol layer? | — (one sentence, explicitly named as unmeasured — nothing to plot yet) | none | — | no | — | No numbers exist to visualize; this is an acknowledged measurement gap, not yet a result. |
| ch8-53 | Appendix A: Verification Status | Federated Harbor claims are graded Closed/Partial/Open against a named method and artifact, specific to items not already covered by Bonded's own status table. | comparison | Which of this chapter's claims are mechanically checked, which are drafted, and which are untouched? | table-not-figure (already realized) | `tab:fh-status` | — | no | — | Already a clean `xltabular`; the chapter's own concentrated status signal, explicitly cross-referenced from the Reader's Map. |
| ch8-54 | Appendix B/C: ProVerif and TLA+ drafts | Both formal artifacts are explicitly named as drafts pending the witness-log freshness assumption and the full inter-harbor transit relation respectively. | comparison | What exactly is still missing from the two formal models? | — (two short paragraphs naming exactly what is pending) | none | — | no | — | Explicit "not yet" statements; a figure would overstate the maturity of drafts the text is careful to call incomplete. |
| ch8-55 | Exercise (§4, ex:fh-xfer-attenuation) | Minting $C_B'$ is really token exchange and new authorization at $B$, not attenuation of $C_A$, once the two harbors' resource namespaces differ (e.g., test DB vs. staging DB). | comparison | Where exactly does "subset composition is a subset" stop being true across a trust boundary? | — (could annotate the existing transfer-ceremony figure with the two additional things $D_B$ must establish) | `fig:fh-xfer` | redraw (8.3) | could | — | A precise scoping of Theorem (Attenuation Monotonicity)'s proof sketch; best added as a caption note on the existing/redrawn transfer figure rather than a new exhibit. |
| ch8-56 | Exercise (§5, ex:fh-revocation-finality) | Three TLA+ configurations (baseline 13 states holds, rollback violates by design, epoch 76 states holds again) demonstrate exactly why per-epoch roots defeat a rollback attack. | quantitative | What changes between the three model configurations, and what does each one prove? | small three-row table: configuration × reachable states × verdict, alongside the existing rollback terminal session | `session-fh-rollback` (covers only the rollback trace, not the three-configuration comparison) | add (implemented, partial) | could | `proofs/relay/CardRevocation.tla`/`.md` | The existing terminal session shows the rollback counterexample alone; a three-row table (baseline/rollback/epoch × state count × verdict) would situate that one trace within the full negative-control comparison the exercise walks through. |
| ch8-57 | Exercise (§5, ex:fh-two-liar-boundary) | Two colluding equivocators on a common cycle can drive the residual to numerical zero even though neither offset alone is small. | comparison | What kind of evidence, if any, could still catch a coordinating pair the residual misses? | — (already covered by the existing pdboundary callout and the chapter's own worked coalition example, r≈6×10⁻¹⁵) | none | — | no | — | The chapter's own boundary box and worked numeric example already state this precisely; no additional figure needed. |
| ch8-58 | Exercise (§12, ex:fh-cold-start-admission) | Four measurable metrics (acceptance rate, sponsor concentration/Herfindahl, time-to-entry, false-rejection rate) distinguish a thin-but-open sponsorship market from an invitation cartel. | quantitative | What should an operator actually watch to tell a healthy cold-start market from a captured one? | small table: metric × thin-but-open signature × cartel signature | none | — | could | — | A genuinely new proposed operator dashboard; pairs naturally with ch8-36's admission-lifecycle figure as a companion "how to monitor this" panel, though it is exercise-derived rather than chapter-body content. |
