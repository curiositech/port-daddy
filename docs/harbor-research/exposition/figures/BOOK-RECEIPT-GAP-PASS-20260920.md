# Receipt-gap figure and heading polish — 20 September 2026

## Accepted slice

The parent replaced the old successful-redelivery lanes in
`whitepaper/figures/fig-swk-idempotency-gap.tex` with an aligned two-history
crash witness and the complete 12-case observed matrix. Two folded-paper
journal records look identical despite different remote effects. The matrix
distinguishes duplicate work, unresolved work and the tested atomic-receiver
contract. Time is explicitly order-only; this is local synthetic SQLite
evidence, not Port Daddy runtime certification or statistical sampling.

The existing label and Figure 1.9 number are retained. SWK prose now separates
the TubeEnvelope's conversation metadata and separate message replay guards
from an operation key bound to an external effect and receipt. Source checks
used `lib/tube.ts` and `lib/event-envelope.ts`; no runtime was started.

Descartes protected three neighboring Spawn-to-Person heading words from
hyphenation (Liability, under, Verification) and removed the manually duplicated
proof square. Claim text, mathematics and counters are unchanged. The original
annotation's requested bold `Kind N (Title).` style remains covered by the
11-page heading fixture and full-volume tests.

## Exact proof

- PDF: `.cache/book-receipt-gap-20260920/coordination-papers-mega-volume.pdf`
- PDF SHA256: `8fe8a5fad2bf38d678ea26b89858bd745763d01f48d87ebb534cf482f04bb832`
- AUX SHA256: `6a571b1a51485bd8fbcefceaecdbbe9ec572eeb9762724ff326dc6de5e6cf37c`
- 693 pages (two more than the preceding accepted 691-page proof).
- All 1,364 registered margins placed; zero registered margin issues.
- 232 numbered captions; zero placement failures; 192 floating-owner adjacency checks.
- All 2,170 auxiliary number/anchor records unchanged against the immediately
  preceding worked-example proof. Page locations may move; that is not a
  numbering exception.
- 45 whitespace candidates remain a review queue, not 45 established defects.
- The full compile still contains 42 overfull warnings, the same count and
  normalized warning categories as the predecessor. No clean-whole-Book claim.

## Tests and visual review

The Book and experiment suites passed: seven receipt-figure tests; 19 packaged-experiment
tests; 44 existing block/typography/quantitative/worked-example/migration tests;
70 combined generator tests. The receipt tests reject changed cells, missing
cases, forged receipts and conflated pending/done states. Experiment tests
retain deliberate broken variants and restored controls. Counts are separate
scopes, not a claim of comprehensive correctness.

The parent read the complete packaged runner and test suite, reran the bounded
offline reproduction, and viewed the native fragment and actual Book pages
54–56. The diagram is readable at native size; event ownership, crash marks,
both slips, the complete matrix and the margin caption fit without scaling.
The preceding exposition and figure form a facing-page spread. The following
stigmergy passage and bibliography remain visible and unoccluded. Margin
baseline alignment remains a separate Book-wide editorial review, not something
the collision check proves.

Review extract: `.cache/book-receipt-gap-20260920/review/review.pdf`.
The figure-system and TikZ skills governed the written semantic brief, rejection
of an unsupported probability plot, real Book-preamble proof and actual-page
review. Passing their mechanical gates is not author approval.

Descartes independently inspected final physical pages 339, 343, 346, 347 and
372 at native size. Liability, under and Verification are whole; page 346
has exactly one proof square and the complete binding-probation example.
The parent additionally viewed pages 339, 345, 346 and 371. Two justified
headings retain expanded word spacing (underfull warnings), not clipping.
Scoped specialist evidence is in
`book-block-style-lab-20260920/stp-heading-polish/receipt-gap-review/REVIEW.md`.

## Experiment provenance

The archive in `docs/harbor-research/experiments/receipt-gap-20260920/` preserves
original run-02 byte-for-byte. The parent fresh run is
`.cache/receipt-gap-tests/reproduction-9ln8fo9n/matrix/experiment-results.json`,
SHA256 `e0d93a0061c1ed6b0d2fe127f2e246e6b152c49d20ffcd5736dcc959b94af216`.
It passed 22 gates with the same observed records as run-02. Receiver atomicity
is a mock SQLite transaction; observer separation is an interface contract,
not an OS sandbox. Arbitrary tools, key expiry, concurrency and power loss
remain excluded. No paid model calls, payment transfer or Port Daddy use.

## Still open

Atlas coverage is not whole-Book acceptance. The receipt-gap row reduced the
missing-identity queue to 52. Parent source/prose review subsequently accepted
three contracts: rate regime, auction refusal band and inclusion check. For
the last, the parent rejected the specialist's single-path grammar and blanket
key-icon exclusion: the approved diagram has independent recomputation and
authentication paths converging at a conjunction, with useful paper/key role
pictograms. This leaves 49 proposed rows requiring source-by-source review.
These contracts do not imply fresh rendered approval of those three figures.
Live coverage now reads 126 source identities, 77 atlas identities and 49
missing, with no stale/duplicate/incomplete rows, root drift or reuse issues.
The coverage checker remains failed. Its separate 25-test suite has 24 passes
and one failure: the live-corpus test still pins the old 66-figure inventory.
It fails at 126 versus 66 before reaching the also-unsatisfied complete-coverage
assertion. Neither the test nor the checker was weakened to hide the backlog.
The Split-Digest theorem and corollary are
unchanged pending the operator's answer. The isolated definition footnote,
broader heading/citation polish, remaining figures, experiment corpus, plates,
chapter cards and publication gates remain on the completion board. Nothing
was staged, committed, pushed, rebased or published in this batch.
