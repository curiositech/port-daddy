# binder-aor-log: 2026-08-22 — restart entry

**Run type:** AoR restart (out-of-cadence; last run 2026-07-14, last surviving
chapter content 2026-07-10). **Author lane:** helmsman planning session.
**Companion PR:** the pd-helmsman planning slice (ADR-0131..0134).

## 0. Why this file exists (the ledger's new home)

Ch16 §Ledger requires an append-only `binder-aor-log:` entry per run. The
2026-07-10 entry was written as a `pd note` and **lost to daemon DB churn**;
the 2026-07-14 run's findings survive only inside two roadmap `summaryMd`
strings (`binder-baseline-ambition-archaeology`,
`binder-mechanical-truth-fixes`). A ledger that a database can eat is not a
ledger.

**Declaration:** from this entry forward, the AoR ledger lives in this
committed directory — `docs/architecture/agent-harbor-technical-binder/
aor-log/YYYY-MM-DD-<label>.md`, append-only, one file per run. A `pd note`
may mirror an entry; it never substitutes for one. An empty run still writes
its entry — absence is itself a finding (ch16). This adopts the mirroring
rule that `binder-mechanical-truth-fixes` proposed and makes the committed
file the primary, not the mirror.

## 1. Tier-3 contradiction note — M10 re-entered without a focus receipt

Ch00/ch18 park cloud billing, marketplace, hosted swarms, and mobile as
"Not now." ADR-0116 (fleet monetization, pricing, Stripe) and ADR-0117
(fleet v2 execution, adversarial testing, AI gateway) — plus the shipped
`apps/fleet-executor`, `apps/relay`, and `apps/github-app-fleet` — re-entered
that list on 2026-07-15 with **no AoR contradiction note and no focus-receipt
revision**. This is exactly the Tier-3 "changes what Port Daddy is"
escalation ch16 exists to catch, recorded here ~5 weeks late.

**Disposition (recorded, not resolved):** the binder's M10 chapter text is
now **contradicted-by-later-decision**. The next scheduled run must either
(a) amend ch00/ch18's Not-now list to admit the fleet/monetization program
with rationale, or (b) record the operator's decision to hold the binder's
ordering and classify the fleet program as a sanctioned sidequest. Owner:
Cartographer wearing `architecture-binder-of-record`, next run.

## 2. Re-record: the 2026-07-14 contradiction register (CR-5..CR-12)

Recovered verbatim-in-substance from the two roadmap summaries so the
register survives in the binder's own ledger:

- **CR-5** — ch03-vs-ch09 event-schema fork.
- **CR-6** — spawn-dissolve vs spawn-live inconsistency.
- **CR-7** — surface-triad ambiguity. A D2 recommendation exists
  (`operator-surface-triad-binder` slug): the triad is
  Scout/FleetBar/pd-console; Fleet Control Center is **not** a fourth surface
  but FleetBar's deep-window face. Awaiting operator ratification; ch01+ch20
  edits pending.
- **CR-8** — C2-Governed same-UID containment overclaim.
- **CR-9** — undefined "Antigravity" body kind in ch07 (the code knows it in
  `lib/transcript-compliance.ts`; the binder never defines it).
- **CR-10** — false verified path `apps/pd-console` in ch19; the real path is
  `core/pd-console`.
- **CR-11** — ch18 says "Shipped" where ch07 is silent (no status field).
- **CR-12** — console control socket absent.

Still-open from 2026-07-10: **CR-1** (control buttons with no
`control_commands` counterparty; audit FAIL 56/100 on re-audit), **CR-2**
(lease-less fire-and-forget live interrupt), **CR-3** (no single roadmap
system of record — now *worse*: 279 snapshot items vs the 151/127 divergence
CR-3 originally cited). **CR-4** remains the only closed entry.

**Movement in this PR:** ADR-0134 is the accepted resolution path for CR-1 +
CR-2 (ingress + leased lifecycle + honest per-backend matrix, aligned with
the `control-contract-wirer` target contract; first slice ships
interrupt+steer with everything else rendered disabled-with-reason). CR-3's
resolution path is the pd-helmsman roadmap-enrichment program
(`roadmap-schema-wiring` fixes the snapshot overwrite + harbor divergence).
CR-5/6/8/9/10/11/12 and the 18-family archaeology sweep remain owned by the
existing slugs `binder-baseline-ambition-archaeology` and
`binder-mechanical-truth-fixes` — not duplicated here.

## 3. Coverage axis for this run: execution truth

One rotating axis per run (restart discipline — 20 minutes, not a full
audit). This run's axis: **does the binder's milestone map match shipped
code?** Verdict from the 2026-08-22 archaeology pass: ~35% of the prescribed
foundation exists as code, ~15% is proven by the binder's own gates; the
dominant pattern is **built, tested, unwired** (ch27's own M6 phrase,
confirmed repo-wide: the contract layer landed — F0 schemas, C1 ledger,
C-routes, C8 doctor — while the enforcement layer has zero production
importers). IT-001..IT-014 exist only as prose. The full per-milestone
scorecard lives in the pd-helmsman planning record; the queryable fix
(`binder-m0`..`binder-m10` roadmap rows with `source_refs: binder:*`) is the
`binder-milestone-slugs` item spawned by this PR.

## 4. Handover

- **Next run:** within 30 days (monthly cadence restarts now), fired by a
  fleet lane, executed by Cartographer wearing
  `architecture-binder-of-record`.
- **Next run's agenda, in order:** the M10 disposition (§1); ratify or reject
  the CR-7 D2 recommendation; one coverage axis (suggest: ownership — the
  ownerless work-packets); write its entry here.
- **Standing rule reaffirmed:** an empty run writes an entry saying so.
