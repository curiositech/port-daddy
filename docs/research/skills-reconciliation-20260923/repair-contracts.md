# Follow-up contract decisions

The operator asked for all supplied skills and then explicitly requested repair
of the inherited CI failure. The following decisions reconcile that request
with repository authority; they are not blanket test exemptions.

- **One Port Daddy operating manual.** The imported duplicate's still-current
  graph, memory, and ideas guidance is merged into `port-daddy-agent-skill`.
  Nine supporting files survive byte-for-byte in its historical reference
  subtree. The obsolete entrypoint is removed; its original bytes remain in
  the initial import commit. The canonical-authority test remains unchanged.
- **Portable source paths.** Illegal filename punctuation and entrypoint case
  are repaired with exact reference updates. `source-path-mappings.json`
  records each destination; the coverage verifier rejects missing source files,
  stale mappings, collisions, traversal, and root symlinks. A semantic merge
  records changed bytes explicitly rather than claiming byte identity.
- **Complete requested catalog.** The earlier platform-exclusion test encoded
  a narrower import decision from PR #8908. This request supersedes that choice.
  Its replacement checks that both platform bundles have named, described
  entrypoints alongside the planning bundles. Catalog presence grants no
  platform execution or dependency-installation authority.
- **Preserve source attribution.** Imported skill names, source URLs, and the
  requested skill-graft evidence retain accurate WinDAGs attribution. The
  repository-wide retired-name test now requires a reviewed SHA-256 receipt
  for each exact source/research file, rather than falsely relabeling upstream
  work. Eligibility is limited to requested source bundles and this research
  report. Native Port Daddy/Jury-rig skills, harness configuration, hooks, and
  external symlink targets cannot obtain an exception. Native bootstrap and
  discovery-authority tests remain intact. Negative cases reject new, changed,
  missing, duplicate, and out-of-scope attribution entries.
- **Keep validation honest.** The attribution receipt is a byte/provenance
  check, not a semantic safety certificate for imported scripts. Files are
  scanned incrementally. Refresh it only after reviewing changed source
  attribution with `node scripts/update-skill-source-attribution.mjs`;
  `--check` verifies the committed receipt without changing it.
- **Research metadata and sealing.** The existing sheaf repair fixture is
  declared in the corpus and actually added to the hosted proof loop. Paper 8
  joins the research program; its standard definition and lemma join the
  library index. The Drydock convention seal is regenerated after reviewed
  skill changes so it describes current bytes, not the original smaller import.

No local runtime or installed skill link is changed. Source receipts do not
constitute a new roadmap registration, execution authorization, or deployment.
