# Publication authorization and Register coordination

On 23 September 2026, after the prepared local commit and PR material were presented, the operator explicitly approved using the existing `erichowens` GitHub login solely to push `codex/skills-substrate-research-20260923` and open its PR in `curiositech/port-daddy`. A read-only account lookup confirmed `erichowens` (type `User`). The exception does not authorize merging, credential export, runtime activation, or mutations in another repository. It is personal-account publication and must not be represented as App/Fleetbot publication.

The source checkpoint presented for approval was `562c1c316d13e4659330048c0e675f55a1506243`. The operator also supplied existing WinDAGs skill-graft evidence for inclusion before publication; the final successor commit will contain that correction. The final head, GitHub PR URL, exact provider readback, and portable Git bundle are retained in the task's persistent external handoff directory.

## Register is a separate capability

The operator approved the task name `codex:skillapalooza` for `curiositech/port-daddy`. The one-use pairing code was exchanged successfully at `/v1/register/exchange`. The resulting credential grants only `register:read` and `register:write`, is bound to that repository and task, and is held privately outside Git. No credential value is recorded here.

The authenticated availability read and claim readback succeeded. The claim is `skillapalooza`, held by `codex:skillapalooza`, owner `@erichowens`, branch `codex/skills-substrate-research-20260923`. Its provenance is **proposed**: the server reports no roadmap mirror, so this is a coordination claim rather than evidence of registered/scheduled work. A durable note records the source checkpoint, validation, runtime halt, current evidence update, and publication exception.

Register access cannot push Git branches or create a PR. GitHub publication uses the separately authorized existing login. Local Port Daddy remains halted, and the primary checkout remains read-only.
