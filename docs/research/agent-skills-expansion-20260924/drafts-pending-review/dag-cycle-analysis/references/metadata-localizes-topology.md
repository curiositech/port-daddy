# Orientation metadata and metrics

Map each selected undirected cycle using its explicit `O` ordering data, then
report source/sink pairs, antichains, length, height, stretch, and overlap as
descriptive values. No portable threshold for edge participation, eigenvalue,
or balance follows from the paper's ER/Price experiments. A metric identifies a
question for review; it does not diagnose a workflow or authorize a change.
