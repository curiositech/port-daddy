# B06 three-skill source review notes

These are root observations of the existing skill text, not completed Luna research or acceptance. All three bundles contain only SKILL.md; exact preimages and headings are in planning-B06-three-preimages.json. Read planning-B06.md and AUTHORING-ACCEPTANCE.md before drafting.

- dag-planner: preserve the five decisions, five failure diagnoses, three worked examples and typed dependency method. Replace universal node/skill/time thresholds with declared workload policy. A cycle diagnosis must return a witness; deleting a supposedly weakest resource/authority/data edge is not a valid general repair. Parallel work requiring the same invariant can conflict across different files. The presentation-before-approval example may allow a local draft while an approval-gated effect stays blocked, but must never assume approval. Preserve human-gate criteria, unresolved requirements, and actual input/output contracts.
- dag-dynamic-replanner: a timeout leaves effect status unresolved and cannot automatically authorize retry. Bypassing a failed producer does not supply its required output. Schema equality is not equivalent data provenance, freshness or meaning. Distinguish graph revision rollback from external compensation. The memory example contradicts 8GB available with 1.6GB free; replace with a single explicit capacity/accounting snapshot. Splitting an arbitrary training task does not preserve semantics automatically. A running task retains its immutable plan snapshot; define cancel/drain/reconcile and version-compare at revision admission. Multiple roots can be valid; reachability from a single root is not universal.
- dag-task-scheduler: original research example names eight tasks but lists seven, omits gather duration from its deadline sum, and asserts a critical path without all edges/durations. Restore a fully explicit small task table, both schedule and resource-time trace. Work-conserving readiness differs from whole-wave barriers. Preemption requires a task-specific resume contract; budgets and hard capacities are not priority preferences. Do not discard tasks merely because a wave exceeds twice a concurrency limit. Remove universal optimality/zero-waste and arbitrary utilization/reschedule/failure guarantees. Retain starvation, priority inversion and deadline diagnostics as competing explanations.

Next Luna task: retrieve primary/official material for topological readiness and cycle witnesses, workflow version/replay constraints, resource scheduling assumptions and a suitable named precedence-scheduling result. Verify source versions and exact assumptions; do not import guarantees into the proposed local agent workflow. Next Terra task: use those research findings to restore useful procedures/examples, add references and at least two diagrams per bundle, recursively convert genuine ASCII, preserve all heading dispositions, and hand-check deterministic examples offline. No provider or Port Daddy runtime launches.

Potential Book link: typed plan revision is already candidate 4 in BOOK-PLACEMENT-REVIEW.md; schedule versus execution is an existing teaching distinction. New candidate status requires a specific additional result or experiment, not a renamed principle.
