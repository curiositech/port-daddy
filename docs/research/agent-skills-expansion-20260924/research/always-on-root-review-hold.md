# Root review: further safety repair required

2026-09-24. Draft not accepted. Read the entire SKILL.md and all three references. Rich sections are retained, but the following concrete issues survive:

1. CostGuard claims prevention but only checks mutable counters: no reservation, concurrency exclusion, settlement, retries, unknown outcome, monthly check or counter updates; request limit warns rather than refuses. Estimated output is not a spend ceiling. Replace with an explicitly illustrative reservation/settlement protocol and runnable pure fixture or a properly delimited admission algorithm; do not assert actual provider hard cap. Keep useful pricing arithmetic and optimization choices.
2. Meeting YAML says always capture own screen/speech, permits meeting audio with mere disclosure, and summarizes before deletion without independently justified derivative retention. Own screen/speech can contain others' data; disclosure is not a blanket capture grant. Make capture default off until channel/purpose/authority is configured and enforce it before capture, not OCR afterward. Separate pause, collection stop, deletion pending, verified deletion, and restricted legal retention.
3. pgcrypto is listed as an encrypted PostgreSQL database without scope; local-only bind/keychain/hash checks overclaim if unqualified. Hash stored beside data does not authenticate provenance or prevent an attacker replacing both. Use concrete threat boundaries and official sources for product claims, or replace named capabilities with neutral control requirements.
4. Anthropomorphism section and vulnerable-population bullets make unsupported psychological/clinical generalizations. Retain interaction-design hypotheses, user controls and target-population evaluation without diagnostic/stereotyped assertions. Functional naming/third-person wording has no established protection here.
5. Prompt injection persistence/cascade are threat scenarios, not inevitable outcomes. Need relevant primary methods (memory injection, isolated authorization) and precise tests, not a NIST landing-page citation for every mechanism.
6. Containment state diagram goes directly from detection to contained after a stop request. Distinguish requested, partial/unknown and independently verified effect fencing, plus in-flight/remote effects and safe restart. Sequence reference always fences even for continue policy result; add policy branches.
7. Approval table/checklist imposes unconditional approvals despite standing delegation. Respect existing authority; sensitive/high-impact unsupported scope needs approval, ordinary already-authorized reversible actions do not need repeated permission.
8. Audit logs must be purpose-scoped/minimized with retention; ambient_capture_enabled is not proof of authority for a specific third-party record. No all-operations raw-data logging.

Need Luna primary-method deepening, then bounded Terra repair, then source/visual review. Do not discard the original know-how or convert diagram decisions solely to prose. W stays read only, local PD halted.
