# B07 remaining four: Book placement review

Planning decision, 2026-09-24. The four accepted offline skill bundles suggest **three compact teaching additions and one fold-in to an existing proposal**, with **zero new figure ports**. This is a placement and evidence review, not manuscript integration, a new theorem, runtime proof, or an originality claim. No Book prose, canonical figure, or frozen bundle was changed.

## Evidence boundary

- Read worktree `/Users/erichowens/coding/tmp/agent-skills-expansion-20260924`: linked Git root and branch `codex/agent-skills-expansion-20260924`, HEAD `00ab2c9ab2197ff97e85edc173370b7446fdb2ef`, gitdir `/Users/erichowens/coding/port-daddy/.git/worktrees/agent-skills-expansion-20260924`, clean when inspected. The primary checkout `/Users/erichowens/coding/port-daddy` supplied read-only manuscript bytes. This note lives only in the assigned handoff folder.
- All eight primary chapter SHA256 values were recalculated and matched `../manuscript-snapshot.json`. Relevant exact bytes: `whitepaper/legible-swarm.tex` `ca2b4dd5b9d91698ee5550b908a0fa8a4bd0d80ea86f938cb2c98289ef27c8a6`; `whitepaper/single-writer-kernel.tex` `666473b5ad3db54a40d8386a923ead47a574e55b1a96132dbda8dcae701e6eea`; `website-v2/public/whitepaper/spawn-to-person.tex` `8b629d39d9a24e312ad1c17b1e0bdbcda01b0194fcabb081f7671d4130536684`. Other five matching hashes remain in that snapshot. These are actual current primary working bytes, not a Git-HEAD claim.
- Every listed file in `../architecture-skill-drafts/B07/validation/remaining-four-root-final/accepted-bundle-manifest.json` matched its SHA256, and all four bundle digests recomputed under the manifest's path/NUL/raw-digest algorithm: `dag-confidence-scorer` (8 files) `6ff4ccbb9d2d725c37bda61bc4f092925a81ae622008d782634cf08d52afc01c`; `dag-dependency-resolver` (2) `48b3834c6f5e91de98c3786893a17242ef02a76a7c18ddb44cc6cad95fb0d467`; `dag-execution-tracer` (2) `4ae59c5fabd67abe88667767539bc9ce8d0ed68497221659a593fce20bb50865`; `dag-failure-analyzer` (2) `20f0728d4aaa709ce76379f60f0befd9cb138ff140bd76716ccd43bc3669a9b4`. Read all four `SKILL.md` files and their method references; for the confidence bundle also checked the manifest and its positive example/contract description. The root acceptance, corrections, and source-boundary receipts in `remaining-four-root-final/{FINAL.md,ROOT-CORRECTIONS.md,SOURCE-VERIFICATION.md}` control over older author notes. Their validation is offline method/contract acceptance, not authentic forecast, complete trace, effect, or causal proof.
- Placement comparison used the earlier `../book-B07-candidate-review/PLACEMENT-REVIEW.md`, `../BOOK-PLACEMENT-REVIEW.md`, `../ASTRA-BOOK-REVIEW.md`, and prior Book draft briefs/receipts. Applied read-only guidance from `textbook-craft`, `solely-responsible-agent`, `whitepaper-figure-system`, `tikz-figure-engineering`, and the installed `tikz-diagram-craft` skill. Existing standalone ports and current chapter figure atlas were checked for duplication. No new external prior-art search was performed in this pass; hence no novelty claim follows.

## Placement decisions

| Bundle | Already in current Book and earlier plan | Bounded insertion with exact anchor | Epistemic boundary and test |
| --- | --- | --- | --- |
| `dag-confidence-scorer` | Chapter 4 already requires a calibrated bad-event posterior for the regret head (`whitepaper/legible-swarm.tex:2084–2110`), distinguishes action-class calibration (`:1344–1361`), and names Brier score in the freeze-probe study answer (`:2810–2823`). Chapter 5 has witnessed task outcomes and versioned skill helpfulness, with an observational-causation warning (`spawn-to-person.tex:1973–2018`). None of these passages walks a *prospective, resolvable* probability through independent resolution and a held-out calibration check. | **Add a Check/Trace exercise to Chapter 4's operator exercise group**, immediately after the freeze-probe exercise at `legible-swarm.tex:2810–2823`, with a short pointer near the regret-head theorem at `:2092–2110`. Give a named artifact/event and three prospective probabilities `(0.8,0.8,0.2)` resolved `(1,0,0)`. Have the reader compute mean Brier `0.24`, identify the forecast IDs and independent resolution sources, and explain why three cases do not establish calibration. Ask why a declined/null forecast cannot enter the score and why a fitted map must be evaluated on disjoint forecast IDs. Keep action authority separate: a good score does not approve publication. This teaches the missing operational link without recasting the Chapter 4 posterior theorem as an empirically proven model. | Constructed arithmetic and protocol discipline. A real claim needs immutable pre-outcome forecasts, exact event/evaluator/version/clock/cohort identities, independent outcomes, disjoint fit/evaluation strata, uncertainty and drift checks. Compare scores against declared baselines and report both calibration and decision utility by action class. The bundle's validator checks consistency, not forecast truth or source authenticity. |
| `dag-dependency-resolver` | Chapter 4 already defines a versioned roadmap with dependency (`legible-swarm.tex:934–947`). Earlier candidate 4 in `BOOK-PLACEMENT-REVIEW.md:23–37` already proposes typed receipt invalidation with an independent oracle; `book-drafts/judges-revisions-conflicts/BRIEF.md` and its receipt already port that relation as a Book-style invalidation table. The current text does not teach the hand step between a declared graph and a valid readiness order. | **Fold into candidate 4 rather than create another candidate.** At the proposed three-task plan-revision example after `legible-swarm.tex:934–947`, add a short Check/Trace inset: `fetch→parse→report`, `lookup→report` produces synchronous generations `{fetch,lookup}`, `{parse}`, `{report}`. Contrast a streaming Kahn queue after `fetch` (where `parse` can enter while `lookup` waits) with a generation barrier. Add a resource conflict between ready `fetch` and `lookup` and a separate publication approval; neither becomes a semantic prerequisite edge. A `report→parse` change yields a directed cycle witness and owner hold. Then show producer-v1→v2 consumer closure for revalidation, with authority/effect status still handled by candidate 4's typed receipt logic. | Existing graph/build-system ideas and a constructed hand check, not a new scheduling or minimal-invalidation theorem. A ready antichain is not a resource-safe batch; an SCC witness calls for semantic repair by the named plan owner, not automatic edge deletion. Test fixture correctness by hand or graph oracle; test proposed typed invalidation separately against independent acceptance/effect oracles as the earlier plan says. |
| `dag-execution-tracer` | Chapter 1's work-unit machine already records effect and idempotency journals (`single-writer-kernel.tex:1767–1838`), Chapter 1's recovery discussion flags half-applied operations (`:715–727`), and `book-drafts/BRIEF.md` already supplies a three-edition standalone **unknown-effect reconciliation** figure with a source receipt. Chapter 5's three-organ continuation exercise names tool receipts and idempotency journal (`spawn-to-person.tex:2589–2604`). Missing is a compact trace **coverage** and attempt-identity reading rule: a missing span or cancellation request is not a terminal receipt. | **Add an evidence-table exercise in Chapter 1's continuity exercise group** at `single-writer-kernel.tex:2501–2521`, keyed back to the recovery/work-unit discussion at `:715–727` and `:1767–1838`. One run `r7`, graph revision, node C, attempt 1 times out; the export says one span dropped and target state is unknown. Ask the reader to label observation ID, operation key and attempt ID separately, report coverage as partial/unknown, and hold attempt 2. A second row gives an authoritative operation-bound terminal-absence receipt plus fence and current retry authority (or validated same-operation dedup plus authority); only then may attempt 2 start. Keep attempted, applied, cancelled and unknown distinct. Cross-reference the existing unknown-effect port rather than redescribe its three branches. | Instrumentation and structured evidence, not complete execution or exactly-once effects. Parent/span links can express provenance but cross-machine wall-clock order alone is not causal. A cancellation request without acknowledgement remains unknown. To claim operational benefit, inject export loss, crash and late commit; reconcile exact target receipts and measure duplicate effects, false closure, trace coverage, storage/overhead and recovery latency. |
| `dag-failure-analyzer` | Chapter 4 already teaches independent review and fail-loud completion (`legible-swarm.tex:948–951`, `:1074–1095`). Chapter 1 already distinguishes fault classes and recovery (`single-writer-kernel.tex:715–727`) and has fault-class exercises (`:2399` onward). Earlier unknown-effect planning already covers a timeout action gate. The Book does not yet teach a reader to distinguish a DAG propagation path from a tested causal mechanism in a small incident. | **Add one Trace case to Chapter 4's grouped authority-rule exercises** near `legible-swarm.tex:2717–2780`, following the completionist reference: observable top event “report R missed its declared deadline.” Give C's queued 80 seconds, B's three-second completion, a target throttle response, a preceding configuration change, and incomplete target logs. Have the reader make a timeline with clock sources; list queue saturation, target throttling, and config regression as competing hypotheses; name one check that could weaken each; mark which descendants merely *could* inherit C's failure. A permission-denial variant asks why retry or broader scope is not justified and who owns the next decision. Close with a conditional corrective action and re-observation predicate. | Constructed investigation, not automated root-cause detection. A timeout string, temporal precedence, and DAG reachability cannot establish cause. No numerical confidence absent a defined calibrated cohort. Any possibly effectful retry inherits the tracer's exact operation, fence/dedup and authority gate; compensation needs confirmed effect and separate authority. Evaluate later on blinded seeded incidents with known fault injection and missing-data variants, measuring cause identification, false certainty, unsafe recovery and time to containment. |

## Figure decision and editorial order

**No additional diagram port now.** The strongest visual relations already have standalone Book-style artifacts: `draft/typed-plan-revision` for candidate 4 and `draft/unknown-effect-reconciliation` for timeout recovery. The confidence skill's two Mermaid flows repeat forecast→resolution→score and fit→evaluate, which the proposed three-row numerical exercise makes more auditable. The dependency skill's two flowcharts repeat the typed-invalidation table and its Kahn generations fit in one line. The tracer's sequence duplicates the existing unknown-effect port; its second flow is a coverage/status table. The failure skill's two causal-flow pictures would risk depicting untested arrows as established causes; a hypothesis table with falsifiers is clearer. No source diagram was selected for port, so no new brief or rendering began. Existing standalone renders do not prove in-page integration.

The editorial sequence should first settle candidate 4's typed receipt contract and its existing figure, then add the dependency hand check; place the confidence exercise alongside Chapter 4's calibrated attention model; and let tracer coverage and incident analysis use separate answer-key tables. Each proposed addition should be labeled an exercise or design method, not a result. The earlier candidate 6/7 placement decisions remain separate and unchanged. An editor should inspect page density and cross-references before any manuscript integration.
