# Validation receipt

All six final PDF builds (three standalone and three page-context) exit 0, with no Overfull, Missing character, Undefined control sequence or LaTeX Error. build.py and renders/compile-results.json retain reproduction/results. Native grid 11.4cm wide, no scaling; page previews are 7x10 inches with 4.5-inch columns. Poppler text bounds are within 4.5 inches. PDFs embed TeX Gyre Pagella/Heros and Computer Modern math; labels use footnotesize (9pt), normal math subscripts may be smaller. Font tables and bbox receipts are retained.

Opened and inspected all three final 144-dpi full-page PNGs individually at original resolution. Claim/grammar/hierarchy/spacing/labels/caption/page-fit pass manual review: same graph drawn once, arrows terminate at circular nodes, chain/path rows align, dashed derived relation identified, two singleton paths visible, no clipped or overlapping text, no hyphenated labels. Edition accents are redundant with marks and words. Source/import/font/palette provenance follows the accepted B03/B04 wrappers, with identical local style copies. source-receipt.json records original hashes; .fls files retain actual imports. This uses the actual active inherited style vocabulary, not the newer v2 library described by the installed craft skill.

fixture-checks.json independently enumerates all antichain subsets and bipartite matchings for this five-node graph, agreeing with root's 2/3 fixture. This is exact finite-example verification, not a general algorithm proof or performance benchmark. Pure mathematical lower bounds are spelled out in PLAN.md and the figure/caption.

Limit: PyMuPDF remains unavailable in tested Python environments. figcheck/beauty_lint are not claimed passed; Poppler text bounds/font inspection plus actual image review are narrower than automated vector/text collision checks. No full chapter pagination, source integration, teaching-efficacy study, application performance, runtime, or publication claim. Primary manuscript and all earlier accepted artifacts were untouched. boundary-check.json rechecks assigned clean worktree metadata and all eight active manuscript hashes.
