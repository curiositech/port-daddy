# B06 order exercise: one distinct teaching addition

Decision: port one same-DAG comparison, minimum reachability-chain partition 2 versus vertex-disjoint direct-edge path cover 3. This is a constructed illustration of established graph theory, not a new theorem, algorithm, empirical gain or runtime claim. Do not add the schedule/level counterexamples as separate figures.

Placement: Chapter 4, legible-swarm.tex:934–945, the roadmap as a versioned statement of intent, priority and dependency. Add a future margin pointer to a Trace exercise in the authority-rules exercise group beginning :2717; the explanatory figure can accompany that worked trace. The chapter does not need a new graph-algorithm section. Reader question: what exactly is being minimized when a planning tool reports a chain count? Teach the two meanings before any operational inference. This remains a placement proposal, not manuscript text or integration.

Current coverage: :924–928 separates roadmap priority, review gate and landing order; :934–945 bounds plan intent against actual output. Sole-role queueing at :957 onward already separates service capacity assumptions. The accepted four-relations figure separates dependencies from messages, authority and resources. None of those distinctions answers whether a chain successor needs an original arc or only reachability. The earlier witness/exhaustion and MAS worksheets address quantifier/certificate scope, not this graph objective. No global claim of absence from literature or the whole Book is made: these exact relevant passages and the accepted figure designs were compared.

Hand certificate: vertices {a,c,x,b,d}, arcs ax,cx,xb,xd. Chains (a,x,b),(c,d) cover every vertex exactly once; {a,c} is an antichain, proving optimum 2. Direct paths (a,x,b),(c),(d) give 3. Every nontrivial path uses x, vertex-disjointness allows only one such path, and its length covers at most 3 vertices; the other two require singletons. Hence optimum 3. The reachability witness c→x→d uses x without assigning x to the second chain. No new edge is added to the original DAG.

Primary prior art: Dilworth, A Decomposition Theorem for Partially Ordered Sets, Annals 51(1),161–166 (1950), DOI https://doi.org/10.2307/1969503 ; Fulkerson, Note on Dilworth’s Decomposition Theorem for Partially Ordered Sets, Proc AMS 7(4),701–702 (1956). Their original-body readings are inherited from research/planning-B06-order-algorithms-deepening.md. This pass attempted the named UCSD/MPG scans, but encountered timeout/403, so does not claim to have newly read those bodies. Kritikakis and Tollis, Fast Reachability Using DAG Decomposition, SEA 2023, https://drops.dagstuhl.de/opus/volltexte/2023/18352/pdf/LIPIcs-SEA-2023-2.pdf , was freshly opened and pp1–3 read: it explicitly distinguishes successor-by-arc from successor-by-directed-path and defines vertex-disjoint covering decompositions. Its own Figure 1 already compares path and chain decomposition; our five-node fixture is not a novel distinction. No Chen implementation or optimality claim is made.

Matching interpretation (exercise follow-on, not extra drawing): make left/right copies of all five vertices. Matching on strict reachability has size3 using ax,xb,cd, producing 5−3=2 chains. Matching on original arcs has maximum size2, producing 5−2=3 paths. Do not quietly change relation between certificate and checker. Independent root exhaustive enumeration is preserved at architecture-skill-drafts/B06/validation/root-order-fixtures/exhaustive-results.json; this port is hand-checked against that receipt, not a rerun of Chen or a general theorem proof.

Companions: width-versus-weighted-schedule supports the already established order/capacity distinction. Keep its 5,20,1,1 duration example as optional Check text, not another generic diagram. Max sink-level size2 versus width3 in B→A,C→A,D→C,E→C is a useful algorithm-validation counterexample; retain in skill fixtures or an optional Trace extension. It refutes the specific level procedure, not every stratification algorithm. Partial edge filter versus exact reduction versus static index belongs in the skill’s implementation reference until a Book argument actually uses it.

Evaluation: no performance experiment is required to justify this exact finite teaching fact. A downstream implementation could use the three fixtures as differential tests against brute-force antichain/matching enumeration, with original-arc versus closure inputs explicit. A teaching study could test whether readers choose the correct relation on held-out small DAGs; no learning improvement is established here. Operational concurrency, worker assignment, effect authority, weighted makespan, index maintenance and coordination savings remain separate questions.

Figure brief: same source graph above two aligned partition witnesses; compare objective, membership and minimum certificate in five seconds. Reject a worker swimlane because it would imply execution resources; reject two different DAGs because that would hide the effect of changing only the objective. Adapt the Book paired-comparison grammar; no existing stable atlas row is claimed for this new offline candidate. Dashed arrow always means derived reachability, explained adjacent to the mark and in the caption. Source font/palette imports match the accepted B03/B04 ports; no shared-library change.

Skills: the previously read TikZ craft, engineering, figure-system and textbook-craft guidance is applied. solely-responsible-agent was explicitly read for this request but its own NOT-FOR boundary excludes one-shot agents; no persistent owner/runtime machinery is instantiated. The assigned output directory is the bounded ownership record.
