Root saw right-column labels extending beyond the page text column in the full-size Swiss PNG. PDF text boxes confirmed overflow in all editions: maritime max 416.38 pt, technical 431.96 pt, Swiss 432.46 pt, against right edge 414 pt. Shortened three labels without changing the proposed mechanism. pdffonts showed every font embedded in all three page PDFs; corrected the false maritime embedded_fonts flag. Final renders and text bounds are rechecked separately. System Python is used for XML parsing because Homebrew Python expat import fails on this host.
