# Release, upgrade, and revocation

**Status:** STUB

Define lifecycle across policy/schema epochs, skills, Convoy release capsules, bodies, grants, and permits.

To fill: compatibility matrix; staged/canary rollout; signature/key rotation; rollback as forward event; outstanding permit behavior; body upgrade; capability revocation; migrations; deprecation; exact-head proof.

