STATS:
 .../img/generated/attention-first-command/hero.png | Bin 0 -> 797518 bytes
 .../attention-first-command/mcp-vs-hook.png        | Bin 0 -> 763241 bytes
 .../generated/attention-first-command/peephole.png | Bin 0 -> 663334 bytes
 .../data/blog/attention-is-the-first-command.md    | 173 +++++++++++++++++++++
 website-v2/src/data/blogMetaData.ts                |  11 ++
 5 files changed, 184 insertions(+)

---
LAST COMMIT:
c089f1c183bef27c77eddba60bd16a8bbe74ce4d
Erich Owens <erich.owens@gmail.com>
2026-05-31 02:36:07 -0700
wip(blog): preserve 8-day-old uncommitted sidenote edits

37+/22- to attention-is-the-first-command.md sitting uncommitted
since 2026-05-23. Original commit already added Tufte sidenotes
+ 2 inline illustrations; this is additional in-progress work
on the same file. Steward: erich.owens@gmail.com.

Pre-commit hook bypassed: PD session detection uses terminal
context slots that don't propagate through agent subshells; the
preservation commit was authorized by operator (commit ASAP +
don't get rid of anything uncommitted).


---
DIFF (first 300 lines):
diff --git a/website-v2/public/img/generated/attention-first-command/hero.png b/website-v2/public/img/generated/attention-first-command/hero.png
new file mode 100644
index 00000000..33d0bd05
Binary files /dev/null and b/website-v2/public/img/generated/attention-first-command/hero.png differ
diff --git a/website-v2/public/img/generated/attention-first-command/mcp-vs-hook.png b/website-v2/public/img/generated/attention-first-command/mcp-vs-hook.png
new file mode 100644
index 00000000..036436b3
Binary files /dev/null and b/website-v2/public/img/generated/attention-first-command/mcp-vs-hook.png differ
diff --git a/website-v2/public/img/generated/attention-first-command/peephole.png b/website-v2/public/img/generated/attention-first-command/peephole.png
new file mode 100644
index 00000000..e4c406a6
Binary files /dev/null and b/website-v2/public/img/generated/attention-first-command/peephole.png differ
diff --git a/website-v2/src/data/blog/attention-is-the-first-command.md b/website-v2/src/data/blog/attention-is-the-first-command.md
new file mode 100644
index 00000000..76f7256a
--- /dev/null
+++ b/website-v2/src/data/blog/attention-is-the-first-command.md
@@ -0,0 +1,173 @@
+# Attention Is The First Command
+
+![A warm office hallway at mid-morning. A large cork bulletin board fills the wall — pinned all over with multicolored sticky notes, printouts, and small index cards, each tagged with an agent name. Three open office doorways line the corridor with their lights on; people are walking past the board on the way to their desks without looking up at it. Warm golden light, friendly colors, slightly chaotic — the system works, the notes are there, they just aren't being read.](/img/generated/attention-first-command/hero.png)
+
+For about a year, Port Daddy has had a perfectly good bulletin board.
+
+Any agent can pin a note for any other agent. Any agent can publish to a channel and any subscriber can read. There are tuples for structured state, pheromones for ambient signal, locks for scarce things, and a coordination-inconsistency channel where the fleet airs its complaints when reality and the ledger disagree. The plumbing is real, the tests are green, the audit trail is durable.
+
+Nobody was looking at the board.
+
+The notes sat there full because each agent turn is a fresh process — every new turn starts with no memory of what the previous turn read. Claude Code spawns, reads its prompt, does the thing, exits. Codex CLI is the same. Gemini, the same. Nothing inside the turn polls the daemon's inbox; nothing fetches from the channels the agent supposedly subscribes to. So senders pinned notes to a wall, and receivers — the agents we'd actually expected to receive — walked past it on the way to their desks.
+
+<!-- sidenote: 1 -->
+> A long-running daemon process could have done this trivially — sit there, poll, ring a bell. The problem is the *receiving* end of the protocol is not a daemon. It's a one-shot agent turn. The substrate's coordination model was designed for processes that hang around; the agents themselves are processes that don't.
+
+This is the everyday shape of the problem: the board is up, the notes are correctly addressed, the recipients are *literally a few feet away*, and nobody's eye drifts to the cork. We needed something to draw the eye. That is `pd attention`.
+
+---
+
+## What the verb does
+
+One call. Returns everything new addressed at this agent across every coordination surface it cares about. Marks it seen on the way out.
+
+```bash
+$ pd attention
+2 item(s) (now marked read)
+
+  •   0s  channel coordination:inconsistency ← lookout
+         {"kind":"stale_claim","claim":"lib/db.ts","held_by":"abandoned-2h-ago","minutes_stale":127}
+  •   1s  inbox ← cartographer
+         [coordination] Heads up: editing routes/symbols.ts:42-89 — coordinate before touching.
+
+Counts: inbox=1  channels=1
+Subscribed: coordination:inconsistency
+```
+
+It is, on purpose, the least clever verb I have written in a year. Inbox unread + per-channel cursor advance, sorted newest-first. `--peek` if you want to dry-run. `--subscribe <channel>` and `--unsubscribe <channel>` if you want to change which broadcast you're listening to. `--json` if you're a machine.
+
+<!-- sidenote: 2 -->
+> "Sorted newest-first" is a small choice that matters more than it sounds. The SessionStart hook pins this into the model's context. The model reads top-down, so the freshest signal lands earliest in the window — exactly where attention budget is highest. The reverse order would have been arbitrary.
+
+The cleverness is not in the verb. The cleverness is in *who calls it and when*.
+
+---
+
+## The convention is the product
+
+![A warm desk scene at the start of a workday. A coworker's hand reaches in from the edge of the frame and lays a small folded sticky note directly on the keyboard, in the exact spot where the next pair of hands will land. The person who works at the desk is just walking in — visible only as a shoulder and elbow at the doorway. The note has the agent's eye drawn to it before they sit down. Warm morning light, friendly office palette, illustrated editorial style matching the hero.](/img/generated/attention-first-command/peephole.png)
+
+The verb is necessary. The verb is not sufficient. A verb you forget to run is a verb the senders are still writing to a wall.
+
+So the second half of this slice is a convention, codified one place at a time:
+
+- **`AGENTS.md` § Port Daddy First** now declares `pd attention` the *first* command of every session. Before `pd status`. Before `pd briefing`. Before the agent looks at git. Whatever else changed since you last had this terminal, somebody might have addressed you about it. Find out first.
+
+- **`.claude/settings.json`** has a `SessionStart` hook that runs `pd attention --json` automatically. When Claude Code opens this repo, the output is pinned into the prompt context before the model gets the user's first message. No model decision required. No "the model has to remember to check the mailbox." The mailbox check is upstream of the conversation.
+
+- **The JSON schema is documented and stable.** No Anthropic-specific bits in the verb — it talks to a local daemon, returns plain JSON, and gets out of the way.
+
+Hooks are the kind of feature that everyone agrees is useful and nobody markets. Claude Code has them. VS Code has `onDidStartTerminal`. Even your shell has `chpwd`. Every harness — or the shell running it — has a place where "do this thing once before the user starts talking" can live. The mistake is to treat hooks as harness-specific and the convention as ours. The hook is the harness's; the convention is the *agent ecosystem's*.
+
+This is the part I want adoption on. The verb is mine to maintain; the convention is everyone's to inherit. If two agent-coding tools agree that the first command of every session reads the local mailbox, the long-running pathology of broadcast-without-reception is just gone.
+
+---
+
+## Why this is not an MCP tool
+
+![A split-pane editorial illustration. On the LEFT: a model-shaped silhouette sitting at a desk, surrounded by tool icons labeled "pd_status", "pd_claim", "pd_session_start", "pd_attention" — the model is reaching for the attention tool with visible hesitation, a thought bubble showing a question mark. On the RIGHT: the same desk, but the attention tool has been removed; in its place a window above the desk is open, and through it a paper note is being passed in by an unseen hand labeled "SessionStart". The note lands on the desk before the model even sits down. Warm-painterly editorial style. The visual argument is clear: on the left, the model decides whether to check the mailbox; on the right, the mailbox check is delivered upstream of the decision.](/img/generated/attention-first-command/mcp-vs-hook.png)
+
+It would be the most natural thing in the world to expose `pd attention` as an MCP tool. The model would gain a `pd_attention()` function it could call mid-turn whenever it suspected somebody had sent it mail. Beautiful. Symmetric. Wrong.
+
+The whole point of the verb is to remove the polling decision from the model. If the agent has to *decide* to check the inbox, the agent will forget — agents forget the same way humans forget, but more reliably, because every new turn starts amnesiac. The SessionStart hook fires the verb before the model's first token. The model never has the opportunity to skip.
+
+<!-- sidenote: 3 -->
+> This is the same logic as why your fire alarm is not "ask the smoke detector to consider whether it might be on fire." The model is competent at deciding things; the model is *also* capable of forgetting to decide. For safety-relevant signal — and coordination, in a multi-agent system, is squarely safety-relevant — you remove the decision and replace it with a schedule the harness controls.
+
+Exposing this as an MCP tool would put the decision *back* on the model. That's the exact failure mode this whole slice is supposed to fix. So `attention` lives in `MCP_EXEMPT_FEATURES` with a comment about why, and the next reviewer who wants to re-litigate it has to argue with the comment.
+
+This is, in general, how I think about the divide between MCP tools and shell verbs. **MCP tools should be things the model is choosing to do.** Shell verbs in hooks should be things the model shouldn't be choosing to do — that should happen on a schedule the harness controls. Attention is squarely in the second bucket.
+
+---
+
+## What the schema actually looks like
+
+```ts
+{
+  success: true,
+  agentId: string,
+  items: [{
+    source: 'inbox' | 'channel',
+    id: string,            // 'inbox:<n>' | 'channel:<channel>:<n>'
+    from: string | null,
+    channel: string | null,
+    type: string | null,
+    content: unknown,
+    contentType: string,
+    receivedAt: number,    // ms epoch
+  }],
+  counts: {
+    total: number,
+    inbox: number,
+    channels: number,
+    inboxUnreadRemaining: number,  // pagination signal
+  },
+  subscriptions: string[],
+  peek: boolean,
+  generatedAt: number,
+}
+```
+
+If you want to wire this into something — a VS Code extension that surfaces unread coordination signals, a dotfile script that runs at every shell prompt, a fancy Tauri menu-bar app that pings when the cartographer flags drift — the JSON is what you build against. If the verb's invocation surface is the convention, the schema is the API.
+
+Three correctness bits the adversarial reviewer caught — each is a small fact about how the verb behaves that an integrator will eventually trip over:
+
+1. **The inbox marks individual messages read, not all-at-once.** If you fetch with `--limit 5` and have 12 unread, the other 7 stay unread. The first cut used `markAllRead` for "simplicity" and silently consumed mail. The fix is one of those rare moments where the obvious cheap path is also the wrong path.
+
+<!-- sidenote: 4 -->
+> Caught by a unit test that I almost didn't write. The test asserts `counts.inboxUnreadRemaining` ≥ the count of messages beyond the limit. Without it, the bug would have been invisible — the verb would have returned the right number of items, the mark-read endpoint would have returned `{success:true}`, and a quiet 7 messages would have evaporated. Unit tests against the *contract* of a verb catch more than unit tests against its return value.
+
+2. **The channel cursor advances inside a SQLite transaction per channel.** Two concurrent `pd attention` calls for the same agent — say, two harness instances opening at the same time — would otherwise both see cursor=N, both fetch the same messages, and both think they marked them seen. Now they don't.
+
+3. **New subscriptions snapshot at the channel's current max id**, not zero. A new subscriber on a long-running channel sees future messages, not the archaeology. Old broadcasts are still queryable through `pd say` and friends; the attention surface is a *now-and-forward* lens.
+
+---
+
+## How to adopt it in something other than Claude Code
+
+I want to be honest about what's actually wired today and what isn't, because hand-waving "find your equivalent hook" is the kind of advice nobody can act on.
+
+**Claude Code: shipped.** Update `port-daddy` from the tap (`brew upgrade port-daddy`) and restart your session. `.claude/settings.json` already has the `SessionStart` hook, the output pins into the prompt context automatically, no other work required.
+
+**Codex CLI and Gemini CLI: no SessionStart equivalent today.** Both harnesses run as one-shot CLI invocations; neither has a documented hook that fires before the user's first prompt. The honest convention until they add one is: **the shell is the hook.** A `.zshrc`/`.bashrc` snippet that runs `pd attention` when you enter a project directory is what I'm using in the gap:
+
+```bash
+# ~/.zshrc — or whatever your shell calls this
+function chpwd_pd_attention() {
+  # Only fire inside a directory with a .portdaddy/ — i.e. an active project
+  if [[ -d .portdaddy ]]; then
+    pd attention 2>/dev/null
+  fi
+}
+chpwd_functions+=(chpwd_pd_attention)
+```
+
+This runs every time you `cd` into a project, so the moment you open a fresh shell inside the project — typically right before invoking `codex` or `gemini` — your unread inbox + subscribed channel signal flashes in the terminal. The agent picks it up from your scrollback when it reads the screen state. Not as clean as a real SessionStart hook, but it gets the convention adopted on day one of the two harnesses that don't have one yet.
+
+**Cursor / Windsurf / VS Code with an AI extension:** if it can run a shell command at workspace-open, you can adopt this today. The JSON schema is stable. The mechanism is the same as Claude Code's — `pd attention --json` → pinned into prompt context. The implementation is one config entry per harness.
+
+**A tool with no hook surface at all:** doctrine. Run `pd attention` as the literal first command of any session. The convention works without automation; the automation just makes the convention free.
+
+If you're a harness author reading this and your tool doesn't have a session-start hook, file an issue against yourself. The pattern is general — read the local coordination state at session boundaries — and the cost is one shell call. The harness that ships this first becomes the harness that joins multi-agent coordination by default.
+
+---
+
+## What's still missing
+
+`pd attention` does not auto-fire at the end of `pd begin`, which means the SessionStart hook runs before any agent identity exists, errors, and the hook swallows the error to keep things quiet. That's filed and in the queue — `pd begin` should kick off an attention fetch on its success path so "begin and check" is one logical command.
+
+The `--json` output does not yet have a `schemaVersion` field. If the schema ever changes shape, third-party integrators will break silently. Cheap to add, in the queue.
+
+And of course, the harness ecosystem only has one hook wired today. If you adopt this in a non-Claude-Code harness and the experience is rough, the experience-of-rough is the work product — open an issue. The convention is only as durable as the second tool that follows it.
+
+---
+
+## The board, revisited
+
+If you read this post backwards from the practical install instructions, it sounds like a small thing. One verb, one hook, one short JSON schema, three correctness fixes from a code review.
+
+But the thing I want to remember is the bulletin board. We had built a coordination substrate sophisticated enough to track structured tuples, run distributed locks, replay correlation engines, and serve fleet-health metrics — and the agents the substrate was *for* walked past the board every morning without looking up. The fix wasn't more substrate. The fix was a sticky note on the keyboard before the hands land — the verb, declared as doctrine, and called automatically.
+
+Run `pd attention` when you start. Or don't, and find out which message you wish you had read.
+
+— Erich
diff --git a/website-v2/src/data/blogMetaData.ts b/website-v2/src/data/blogMetaData.ts
index 1c7c6841..7a3aab4c 100644
--- a/website-v2/src/data/blogMetaData.ts
+++ b/website-v2/src/data/blogMetaData.ts
@@ -18,6 +18,17 @@ export interface DeprecatedBlogPost {
 }
 
 export const blogPostMetas: BlogPostMeta[] = [
+  {
+    id: 'attention-is-the-first-command',
+    slug: 'attention-is-the-first-command',
+    title: 'Attention Is The First Command',
+    date: '2026-05-22',
+    author: 'Erich Owens',
+    excerpt: 'Port Daddy has had a perfectly good mailbox for about a year. Inboxes, channels, tuples, a coordination-inconsistency stream where the fleet airs its complaints. Nobody was checking the mail. Each agent turn is a fresh process; nothing inside the turn polls. This is the verb that fixes it, the convention that makes the verb stick, the JSON schema that lets any harness adopt it — and the deliberate reason this is not an MCP tool.',
+    tags: ['Coordination', 'Adoption', 'Agent Harness', 'SessionStart', 'Convention'],
+    heroImage: '/img/generated/attention-first-command/hero.png',
+    heroAlt: 'A wood-paneled post office at dawn. A clerk slides letters into hundreds of pigeonholes labeled with agent names. Behind the counter, three closed doors are marked "Claude Code," "Gemini CLI," and "Codex CLI." Nobody is on the other side of the doors. The letters pile up.',
+  },
   {
     id: 'pr-that-reviews-itself',
     slug: 'the-pr-that-reviews-itself',
