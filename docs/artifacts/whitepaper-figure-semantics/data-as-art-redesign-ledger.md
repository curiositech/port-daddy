# Coordination Papers: data-as-art redesign ledger

This ledger is the pre-render contract for the analytical figures. An existing
figure is never a candidate grammar. Each entry starts from the reader question
in the surrounding argument, records a second plausible grammar, and names the
false reading that the selected geometry must prevent.

## Volume I — The Legible Swarm

### I.1 — system strata (`fig:stack-map`)

- Reader question: what does this chapter own, assume, and provide?
- Selected grammar: stratigraphic section with aligned capability marks.
- Rejected alternative: concentric rings; they imply containment and centrality,
  neither of which the chapter claims.
- False reading to prevent: higher layers replace lower layers. The aligned marks
  instead show accumulation and reuse.
- Color job: teal identifies the owned L2 stratum only.

### I.2 — consented order (`fig:state-of-nature`)

- Reader question: what exactly changes when the same actors consent to one
  sequencer?
- Selected grammar: paired interval traces on one time scale.
- Rejected alternative: actor-node topology; it hides overlap duration and makes
  the daemon look like an ornamental hub.
- False reading to prevent: consent changes membership. It changes admission
  order while preserving all arrivals.
- Color job: amber is the collision interval; teal is the commit spine.

### I.3 — digest reachability (`fig:zoom-vs-potemkin`)

- Reader question: can every summary claim reach independently inspectable work?
- Selected grammar: reachability field with an aperture and material evidence
  glyphs.
- Rejected alternative: two prose tiles; they force the caption’s conclusion to
  do work the geometry should do.
- False reading to prevent: the bad surface has no evidence. It has evidence but
  no path to it.
- Color job: teal carries resolvable paths; amber marks the opaque boundary.

### I.4 — four release proofs (`fig:four-questions`)

- Reader question: which independent conditions must all hold before release?
- Selected grammar: conjunctive proof rail with four distinct marks.
- Rejected alternative: checklist table; rows make the conditions look optional
  and unordered.
- False reading to prevent: three of four is sufficient. The separate broken-rail
  inset makes failure conjunctive.
- Color job: teal marks passed proof stations; amber appears only on the broken
  counterexample.

### I.5 — authority instruments (`fig:authority-organs`)

- Reader question: how can six different acts share one authority contract?
- Selected grammar: repeated instrument traces crossing one consent boundary.
- Rejected alternative: six cards around a central daemon; it equates distinct
  mechanisms and falsely centralizes the actor.
- False reading to prevent: the mechanisms are interchangeable. Inputs and
  returns stay distinct while the grant fields align.
- Color job: teal is the common recorded boundary; mechanism glyphs remain ink.

### I.6 — specialization boundary (`fig:specialization`)

- Reader question: where does a specialist beat a pooled queue, and where did the
  superseded rule decide differently?
- Selected grammar: two-boundary regime plot with a hatched disagreement set.
- Rejected alternative: worked-example decision tree; one example cannot reveal
  the two directions of error.
- False reading to prevent: hatching is confidence or uncertainty. The caption
  and direct label state that it is deterministic disagreement.
- Color job: teal is the verified boundary; amber is the falsified proposal.

### I.7 — forced zoom (`fig:sdt`)

- Reader question: what does moving the inspection criterion trade?
- Selected grammar: overlapping signal distributions with directly encoded error
  regions.
- Rejected alternative: confusion matrix; it hides criterion movement and
  sensitivity.
- False reading to prevent: more zooms improve classifier sensitivity. They move
  the criterion; legibility changes separation.
- Color job: amber is dangerous-action evidence and misses; teal is the policy
  criterion.

### I.8 — abdication (`fig:abdication`)

- Reader question: how is the canary hypothesized to arrest loss of supervisory
  practice?
- Selected grammar: explicitly schematic autonomy trajectory plus practice floor.
- Rejected alternative: four role cards with arrows; it cannot show a continuous
  loss or distinguish arrest from recovery.
- False reading to prevent: the curve is measured and the canary restores prior
  awareness. The on-canvas status says schematic; the curve only floors.
- Color job: teal is the proposed intervention, not empirical success.

### I.9 — read-poverty (`fig:readpoverty`)

- Reader question: when does indexing change read cost, and when does L2 have
  value?
- Selected grammar: aligned scaling small multiples.
- Rejected alternative: one dual-axis plot; unrelated units would invite false
  quantitative comparison.
- False reading to prevent: the discovery onset is universally measured at a
  fixed swarm size. It is shown and labeled as a design regime.
- Color job: teal carries the indexed path and value curve; amber only marks the
  unmeasured onset.

### I.10 — push versus pull (`fig:pushpull`)

- Reader question: which evidence distinguishes two identical capability claims?
- Selected grammar: provenance signal field with hollow declaration marks and
  filled residue marks.
- Rejected alternative: candidate-by-signal prose matrix; it turns evidence
  strength into reading volume.
- False reading to prevent: declarations are ignored. They remain present but
  carry less verified weight.
- Color job: amber is declared evidence or a missing episode; teal is independent
  work residue.

### I.11 — split ranker (`fig:split-ranker`)

- Reader question: can one monotone score serve discovery and attention?
- Selected grammar: slopegraph over one candidate pool and two rank axes.
- Rejected alternative: two formulas in adjacent cards; formulas state but do not
  expose the order reversal.
- False reading to prevent: the two heads merely use different weights. A and B
  reverse order across the same domain.
- Color job: teal is discovery fit; amber is regret if ignored.

### I.12 — reader-specific compaction (`fig:read-surfaces`)

- Reader question: how do five readers select different views while retaining
  source coordinates?
- Selected grammar: selection masks aligned over one raw event spine.
- Rejected alternative: source-to-five Sankey; curved fan-out becomes twisty and
  obscures exact event addresses.
- False reading to prevent: each surface owns a separate summary corpus. All
  selections share one durable spine.
- Color job: teal is retained structure; amber is a sparse hazardous event.

### I.13 — degradation cascade (`fig:cascade`)

- Reader question: where can the causal loss of recoverable truth be interrupted?
- Selected grammar: schematic degradation trajectory with transverse cuts.
- Rejected alternative: diagonal prose ladder; cards make the sequence chunky and
  hide the common fidelity dimension.
- False reading to prevent: the curve is empirical. The on-canvas note identifies
  it as a causal ordering only.
- Color job: amber is degradation; teal is an intervention that cuts propagation.

### I.14 — institutional roles (`fig:roles`)

- Reader question: who authors authority, who exercises it, and who covenants
  with whom?
- Selected grammar: typed institutional routes over three role stations.
- Rejected alternative: circles or nested ellipses; they imply hierarchy,
  containment, and the “sand-dollar” form explicitly rejected in review.
- False reading to prevent: the daemon is a peer party to the agents’ covenant.
  The covenant route stays on the agent station while enforcement descends from
  the daemon.
- Color job: teal is bounded exercised authority; amber is the retained override.

## Volume II — The Single-Writer Kernel

### II.1 — stack ownership (`fig:swk-stack-map`)

- Reader question: exactly which architectural depth does Chapter II own?
- Selected grammar: architectural section with a measured ownership bracket.
- Rejected alternative: four prose slabs with arrows; it made responsibility a
  reading task and confused chapter order with layer order.
- False reading to prevent: the chapter owns coordination semantics or the
  operator and market layers. Its bracket stops at the carrier half.
- Color job: teal is the owned architectural depth; the dark floor is the narrow
  machine dependency boundary.

### II.2 — seven kernel contracts (`fig:swk-seven-organs`)

- Reader question: how do seven different contracts share one source of truth?
- Selected grammar: synchronized contract score over one commit pulse.
- Rejected alternative: a 3-by-2 organ card grid over a database slab; it was a
  feature catalogue rather than a relationship.
- False reading to prevent: the organs are separate services or stores. All seven
  traces align to the same WAL history.
- Color job: teal is implemented contract activity; amber is the partial
  obligation/outcome trace; neutral marks expose self-asserted identity.

### II.3 — single-writer serialization (`fig:swk-single-writer`)

- Reader question: how do overlapping callers become one observable history?
- Selected grammar: concurrent arrival intervals aligned to a commit rail.
- Rejected alternative: queue-order table; it showed a result but not the overlap
  that serialization resolves.
- False reading to prevent: the model tolerates a second daemon writer. A separate
  amber boundary mark keeps it outside the rail.
- Color job: teal is the sole commit rail; amber marks the model boundary.

### II.4 — durability boundary (`fig:swk-durability-faultclass`)

- Reader question: how far does a returned-success guarantee survive as a fault
  reaches deeper into the machine?
- Selected grammar: fault-reach profile with two guarantee extents.
- Rejected alternative: fault-by-guarantee table; it made a continuous boundary
  look like unrelated checklist rows.
- False reading to prevent: either default WAL or full sync means immortal data.
  Both profiles end at an explicit boundary.
- Color job: teal is the default earned guarantee; amber/hatching is unsupported
  fault reach, with line extent redundantly carrying the claim.

### II.5 — claim lifecycle (`fig:swk-claim-lifecycle`)

- Reader question: which transitions are guarded, and which fairness state is
  absent?
- Selected grammar: guarded state cycle with an interrupted queue rail.
- Rejected alternative: transition prose around oversized state pills; it buried
  the missing state in the caption.
- False reading to prevent: contention is queued fairly. The absent queue is
  visible as a literal gap.
- Color job: teal is a valid held state and retry path; amber marks contention and
  missing bounded wait.

### II.6 — communication organ (`fig:swk-comm-organ`)

- Reader question: what lives inside the carrier, and which provenance questions
  remain independent?
- Selected grammar: message cross-section over a durable cursor with two traces.
- Rejected alternative: carrier/act/cursor/verification table; it conflated fields,
  records, and questions.
- False reading to prevent: authorization ancestry and work lineage are one
  delegation chain. They stay on separate traces.
- Color job: teal is typed carrier content and authority; amber is coordination
  lineage, redundantly separated by vertical position.

### II.7 — deontic split (`fig:swk-deontic-split`)

- Reader question: why can prohibition, obligation, and permission not be one
  generic policy mechanism?
- Selected grammar: three temporal mechanism traces.
- Rejected alternative: three prose columns; it classified terms but showed no
  operational difference.
- False reading to prevent: technical ability implies permission, or post-commit
  compensation implies prevention.
- Color job: teal is admitted/evidenced/allowed state; amber is refusal, deadline,
  detection, or the able-but-forbidden interval.

### II.8 — reference-monitor boundary (`fig:swk-reference-monitor`)

- Reader question: what causal fact distinguishes prevention from detection?
- Selected grammar: paired pre-effect and post-commit counterfactual traces.
- Rejected alternative: agent-gate-WAL-monitor architecture diagram; it located
  components but hid the exposure interval.
- False reading to prevent: the subscriber policy monitor makes forbidden states
  unreachable. The lower trace begins compensation only after commit.
- Color job: teal is admissible progression/compensation; amber is the exposed
  interval that cannot be erased from history.

### II.9 — commitment oracle (`fig:swk-commitment-oracle`)

- Reader question: which evidence can actually close a promise?
- Selected grammar: typed evidence trajectories through a narrow closure aperture.
- Rejected alternative: lifecycle diamond with an oracle vocabulary box; it made
  the finite evidence boundary peripheral.
- False reading to prevent: free text is weak evidence that the daemon interprets.
  It is deflected and the commitment stays open.
- Color job: teal is mechanically re-checkable evidence; amber is refused free
  text and the daemon-derived deadline.

### II.10 — continuity foundation (`fig:swk-continuity-organs`)

- Reader question: where does evidence support narrow before identity and market
  claims inherit it?
- Selected grammar: dependency profile whose width narrows at weak links.
- Rejected alternative: three continuity cards joined by arrows; it could not show
  how downstream claims inherit upstream weakness.
- False reading to prevent: durable notes are an execution checkpoint, or the
  present ledger is already reputation-grade.
- Color job: teal is carried support; amber diamonds identify the two narrowing
  evidence boundaries.

### II.11 — consistency model (`fig:swk-consistency-model`)

- Reader question: how can overlapping real-time operations still have one
  authoritative order?
- Selected grammar: interval history aligned to an extracted commit rail.
- Rejected alternative: a prose theorem strip; it stated the order without showing
  which calls overlapped.
- False reading to prevent: linearizability survives a bypass, uncommitted reads,
  or replies before commit. The caption states these conditions.
- Color job: teal is the linearization rail; neutral intervals carry caller time.

### II.12 — dual-runtime parity (`fig:swk-dual-runtime`)

- Reader question: what evidence would make test and deployment runtime parity an
  earned invariant?
- Selected grammar: mirrored operation trajectories with an explicit comparison
  at observable state.
- Rejected alternative: two runtime boxes under a shared interface; architecture
  proximity is not behavioral parity.
- False reading to prevent: a normalization shim or successful build proves
  identical locking and pragma semantics.
- Color job: teal is the verified test trajectory and final comparison; amber
  hatching is the unproved binding-divergence surface.

## Corpus-wide controls

- Neutral ink carries structure. Teal means verified, selected, conserved, or
  valid. Amber means hazard, missingness, superseded logic, or retained override.
  Cobalt is reserved and is not a default accent.
- Color is always redundant with position, shape, weight, dash, or pattern.
- Canvas prose is limited to direct labels. Explanation and caveats live in the
  caption unless a status label is necessary to prevent an empirical overclaim.
- No maritime, pirate, parchment, antique-book, or scenic metaphor may function
  as an analytical substrate.
- Generated art begins with zero entitlement to a page. It must survive a
  separate editorial-plate review and can never substitute for analytical marks.
