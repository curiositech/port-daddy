# mockups-ch20 — the converged renders (parley punch items 1+2)

These seven files are the ch. 20 (Story Linework) evolution of the surface set and
supersede the corresponding files in `../mockups/` (whose skin is retired; their
content patterns were ported here). Base structures follow the PR #658 spec pixels
where they existed (FleetBar, console detail, Scout).

Operator fork rulings applied throughout (from the #679 arbitration page, decided
2026-07-05): **dark substrate = palette v2 near-black `#101216`** (warm ebony
retired); **display face = IBM Plex + Recursive CASL-1** (settled); **flag→state
map = work states** — Hotel=claim-active, Foxtrot=awaiting-human,
Bravo=burning-cash, Victor=conflict, Delta=blocked, Yankee=claim-stale, Mike=idle,
Quebec=newcomer — with flag geometry per Pub. 102 (the ICOS skill is the referee)
and colors palette-locked (red→rust-slab, yellow→lime, blue→cobalt-slab,
white→`--flag-white`).

FleetBar demonstrates the parley's state-dependent stacking: the gate queue does
not render when empty; when armed it renders above the composer as the view's one
color zone.
