{
  "name": "@port-daddy/github-app-receiver",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker — GitHub App webhook receiver and cloud fleet executor (Workers AI, no tunnel).",
  "type": "module",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "wrangler": "^3.60.0"
  }
}
