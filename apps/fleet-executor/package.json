{
  "name": "@port-daddy/fleet-executor",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker \u2014 cloud fleet executor. Queue consumer that runs Port Daddy fleet ships on Workers AI and posts PR check runs / comments, with per-ship blocking verdict semantics.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.0",
    "@cloudflare/workers-types": "^4.20260507.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.4.0",
    "vitest": "^3.2.0",
    "wrangler": "4.99.0"
  },
  "dependencies": {
    "@cloudflare/sandbox": "^0.12.4",
    "yaml": "^2.9.0"
  }
}
