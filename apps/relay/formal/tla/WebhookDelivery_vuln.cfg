\* Negative control: no continuity guard — daemon dispatches anything the relay
\* delivers. AtMostOnce MUST fail (relay duplicates a delivery -> double dispatch).
CONSTANTS
    MaxSeq = 3
    Dedup = FALSE
INIT Init
NEXT Next
INVARIANTS
    TypeOK
    AtMostOnce
