type: fixed

- **The standalone Pilot installer can bind its exact source before installation.** An explicit source directory takes precedence over Homebrew discovery, with SHA-256 provenance for the captured prompt/config bytes and paired expected digests. Invalid source selections, pin mismatches and malformed arguments stop before target cleanup or writes; package-first defaults and the five runtime formats remain unchanged.
