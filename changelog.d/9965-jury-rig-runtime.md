type: changed

- **Jury-rig now owns native skill discovery and Fleet guidance.** `pd jury-rig`, `jury_rig: true`, and `jury_rig_status()` replace the WinDAGs-coupled live surfaces while preserving BM25 plus Tool2Vec semantic ranking, checkpointed warming, and guarded skill-owned reference reads. `pd jury-rig bootstrap plan|status|apply|rollback` adds a fail-closed, receipt-producing post-merge cutover for machine-level instruction and skill projections; apply verifies the merged and installed replacement, imports third-party skills as non-executable references, and retains encrypted exact rollback state.
