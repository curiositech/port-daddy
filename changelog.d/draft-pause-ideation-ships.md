type: changed

- **The four ideation ships (`spark`, `spider`, `lookout`, `snipe`) are paused in `pd-fleet.yml`.** Across a single pull request they produced 23 advisory proposals of which 0 were actionable: restatements of features shipped in the diff under review, one proposal targeting the wrong layer, and — in a second round — proposals citing phrases from the review comments as evidence that the capability they described was missing. All four are `class: ideation` and never gated a merge, so this removes proposals rather than review; the blocking path (`qa`, `code-reviewer`, `red-team`, `test-author`, `tautology-sniffer`) is untouched and still runs on every PR. The stated precondition to re-enable is one mechanical gate: a proposal must cite an evidence path that exists, and must be rejected when the identifier it claims is missing already appears in the diff.
