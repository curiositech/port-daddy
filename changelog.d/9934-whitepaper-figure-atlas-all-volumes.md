type: added

- **Every canonical whitepaper figure now has an explicit semantic design contract.** The new `skills/whitepaper-figure-system` atlas covers all 81 figure and algorithm-exhibit environments across Volumes I--VII, maps each claim to a professional representation family, rejects known-bad maritime art and generic node-box defaults, defines reuse contracts for repeated mechanisms, and ships a tested source scanner that fails when TeX figures and atlas prescriptions drift.
