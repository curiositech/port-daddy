type: fixed

- **Fleetbot can resolve an exact PR review thread with readback.** The bounded `pull-request.resolve-review-thread` operation verifies the repository, pull request, and head before and after mutation, then requires GitHub to report the thread resolved.
