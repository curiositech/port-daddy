type: changed

- **Porthole now proves the live harness instead of replaying terminal theater.** Six reproducible primary scenes cover isolated installation, decoded model context, two-agent collision refusal, a real elapsed-time jump cut, `pd up` service identity/readiness/discovery, and a four-turn Parley; scene changes restart at time zero, retired observers are disconnected, `pd squid tap` labels the exact context delivered to the model, and the product contract now places replaceable terminal engines beneath a proposed privacy-safe evidence, continuity, search, receipt, and controlled-successor layer without presenting those future joins as shipped.
