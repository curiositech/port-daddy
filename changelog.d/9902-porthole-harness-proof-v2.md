type: changed

- **Porthole now proves the live harness instead of replaying terminal theater.** Six reproducible casts cover isolated installation, decoded model context, two-agent collision refusal, a declared real-time jump cut, `pd up` service readiness, and a four-turn Parley; scene changes and restart begin at time zero, compact terminals shed provenance text before clipping, `pd squid tap` labels the exact context delivered to the model, and session-anchor fields carry distinct semantic colors.
