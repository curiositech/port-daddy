type: changed

- **Porthole now proves the live harness instead of replaying terminal theater.** Six reproducible primary scenes cover isolated installation, decoded model context, two-agent collision refusal, a real elapsed-time jump cut, configured service identity/readiness/discovery, and a six-turn Parley executed in three distinct tmux sessions beside a read-only Port Daddy witness. The proof room and main Harness page explain the agent, harness, Port Daddy, Porthole, and video evidence in plain language; color commentary follows each participant's public explanation without claiming private chain of thought, while a separate historical board preserves exact shared-read chronology, receipt gaps, and an unresolved outcome instead of fabricating settlement. The recorder now captures and hash-binds each real tmux pane's available history before teardown, so Nora, Milo, Aya, and the witness have independently focusable, selectable scrollback with explicit history limits and truncation state. The page also distinguishes today's manual, all-live fixture from the proposed daemon-convened Parley contract: detect nearness automatically, preserve durable summons across shifts, allow only consent-leased verified reentry or a named successor, keep silence and dissent visible, and authorize effects separately. Scene changes restart at time zero, retired observers are disconnected, `pd squid tap` labels the exact context delivered to the model, trigger-inclusive SQLite mutation counts can no longer falsely reject valid multi-recipient Parley turns, and the product contract places replaceable terminal engines beneath a proposed privacy-safe evidence, continuity, search, receipt, and controlled-successor layer without presenting those future joins as shipped.
