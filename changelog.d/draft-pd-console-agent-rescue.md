type: fixed

- **Mission now keeps each conversation attached to its actual execution.** pd-console restores explicit timestamps, wraps and expands long turns and Mission goals, refuses to splice an unrelated Lane agent into a WorkIntent, recognizes killed and timed-out transcript endings, shows the exact terminal receipt, and releases the composer instead of displaying an endless working state.
