type: added

- **The native console now has a deterministic AST and suggestibility proof voyage.** `demos/ast-suggestibility/` seeds explicitly labeled fixture daemon state, drives the GPUI Harbor pane through baseline, active, blocked, gate, and receipt states, and emits fail-closed per-artifact provenance manifests plus repeatability evidence for CI and authorized macOS captures.
