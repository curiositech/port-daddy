type: changed

- **A few more "load-bearing" holdouts are gone.** The brand style guide's border-hierarchy bullet and two research figure scripts that draw the phrase directly into a published PNG (`r1_figures.py`, `r8_figures.py`, plus the `a7_experiment.py`/`b1_experiment.py` wrong-turn write-ups that feed them) now say "critical" instead, matching the wording the Book and papers pass already landed. Code comments, tests, and Rust source/proofs that also use the phrase are intentionally out of scope for this pass.
