type: removed

- **Removed the daily newcomer identity-count cap.** Daemon registration and `/sugar/begin` no longer reject a fleet merely because it has minted 25 newcomers; the SDK and shared fixtures no longer send the retired caller-selected `project` field. Credential verification, reserved-alias protections, provenance/replay safeguards, and the shared newcomer spend ceiling remain enforced.
