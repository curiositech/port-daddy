type: added

- **Jury-rig skill selection works without Port Daddy running.** The standalone `jury-rig` executable searches metadata, explicitly grafts bounded bodies, reuses compatible semantic centroids without a live generator, and never enters daemon startup or CLI freshness loops; discovery also starts a detached loopback-only Tool2Vec reconciler that prefers the installed `qwen3-embedding:8b` model and binds mutable Ollama tags to exact digests, while `seamanship search` delegates to the same hybrid retrieval path instead of substring matching.
