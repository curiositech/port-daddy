type: added

- **Jury-rig skill selection works without Port Daddy running.** The standalone `jury-rig` executable searches metadata, explicitly grafts bounded bodies, reuses compatible semantic centroids without a live generator, and never enters daemon startup or CLI freshness loops; `seamanship search` now delegates to the same hybrid retrieval path instead of substring matching.
