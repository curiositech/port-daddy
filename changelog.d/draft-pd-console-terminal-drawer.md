type: fixed

- **pd-console's CLI drawer is now a real resizable, scrollable terminal.** Its named drag edge changes the native PTY row count with the visible height, wheel and high-resolution trackpad deltas traverse retained history without leaking into the pane underneath, full-screen alternate-buffer programs receive wheel navigation, and a visible history offset can return to the live prompt without destroying the shell process.
