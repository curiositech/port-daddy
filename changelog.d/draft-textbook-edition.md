type: changed

- **The Collected Volume becomes the Textbook Edition of the coordination papers, ordered by what each chapter stands on.** One data source, `whitepaper/textbook.json`, now fixes chapter order, Arabic numbering, four parts (Ground Truth, The Cost of Seeing, What Survives the Restart, Trade Between Strangers), edition metadata, and each chapter's question and epigraph; the LaTeX generator, the standalone chapters' locator maps, the website's `/library` and `/whitepaper` pages, and their tests all read it. Every chapter opens on a page of its own (title, epigraph, plate, the question it answers) and begins with exposition; abstracts, keyword lists, reading-time notes and locator boxes stay in the standalone papers only. Live colored-quiet hyperlinks, three-level PDF bookmarks, and "Cited on pages" back-references return to every PDF. The reading-order DAG, nesting diagram, reading paths and the explain/prove split are retired in favour of one table of contents; the story palette v2 (re-derived indigo, violet and rust, a new gold for the economy) ships in the design tokens and as the Book's `pd*` color system, kept in lockstep by the figure-palette guard. A new `library-checks` workflow runs the citation and propagated-correction checkers, the textbook source-of-record checks, and the palette lockstep in CI, and the metadata drift guard now covers the seven research-paper PDFs too.
- **The Book gains its pedagogy, its first folded chapter, its proof estate, and its plates.** A shared pedagogy block (`figures/pd-pedagogy.tex`) gives every chapter claim boxes labeled by kind, boundary and example boxes, recitations, and rated Check/Trace/Open exercises whose solutions collect at the end of a standalone chapter and in the Book's solutions section, linked both ways by page. The Single-Writer Kernel chapter folds in the controllability theorem (regimentation is Ramadge–Wonham controllability, with the nine-policy table checked by hand and the supervisor synthesised for the sandboxed review job) and the model-checked work-unit machine (six invariants over 536 states, the shortest crimes reconstructed by hand), and its ten inline exercise blocks become 34 exercise and solution pairs. The proof estate is wired: Kani's fast harnesses on every PR and the parser harness nightly, every ProVerif model checked against its committed results, the relay TLA specs with their attack configurations, one manifest (`whitepaper/corpus.json`) in which every formal artifact is WIRED or RETIRED. The `harbor-chartwork` skill brings mechanical figure QA. The interior plates carry the cover's faded watercolor register: one colossal unearthly presence over one small everyday thing, part pages opening full-bleed like the cover, chapter washes bled into the page stock.

