type: fixed

- Native Porthole closes local frame delivery immediately on Stop or Quit, bounds stream and movie finalization with one deadline, and prevents late callbacks from publishing success receipts after failure or revocation. Synthetic writer and shutdown regressions require no operator capture permission.
- A non-capturing native visual-proof command renders the actual Stage in light and dark appearances, with generated cooperative pointers and motion, decoded movie checks, and SHA-256 artifact manifests that explicitly distinguish UI proof from capture permission or production release evidence.
