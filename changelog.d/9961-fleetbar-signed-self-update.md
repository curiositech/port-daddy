type: fixed

- **FleetBar updates itself instead of abandoning the operator on a website.** The version-skew card installs the exact daemon-matching GitHub release only after its named SHA-256, Curiositech Developer ID, bundle identity, version, and Apple notarization pass; replacement keeps a recoverable backup and relaunches through launchd, while `pd setup` now installs that same verified release rather than compiling an ambient checkout.
