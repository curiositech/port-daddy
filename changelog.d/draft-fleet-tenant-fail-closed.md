## Changed

- Activate immutable tenant-bound Fleet queue envelopes: Relay onboarding now creates an inert exact-repository proposal, webhook admission has no legacy fallback, and the executor refuses credentials or model work until tenant, accepted configuration, served roster, entitlement, and stop-loss checks all pass.
