type: added

- **The Harbor Figure Desk's data is generated from the repository instead of hand-maintained, and every figure is scored against a named, citable rubric.** `build_figure_desk.py` derives the desk's chapters from `whitepaper/textbook.json`, its figure set from the chapter sources' own `\input`/`\includegraphics` sites, page numbers from the committed Book PDF, the Wave 11 rows from `FIGURE-TRIAGE.md`, the pixel judgment from `PIXEL-JUDGMENT.md`, mechanical-gate state from `blockers.json` and the per-chapter register from `FIGURE-REGISTER.md`; `--check` regenerates and diffs, failing on a stale copy, in the idiom of `render_figure_audit.py`. The three fields nothing can derive — the three lenses' prose, the diagram-research voice, and the pinned render pages and undrawn ids the author's saved rulings are keyed under — are named as curated in the generator's own docstring and checked exactly like the derived ones. Enumerating figures from the Book rather than from a fragment glob finds the three imported matplotlib PDFs in chapter 7 that the hand-written list had missed. `figure_doctrine.py` adds fifteen criteria from the Tufte, dataviz, textbook-craft and expository-writer doctrines, each carrying its source and, where contested, who objects; nine are computed from committed sources, two are mapped from the pixel judgment's prose with the sentence quoted, and four are reported as unscored rather than guessed. Two are counter-checks the repository's own gates structurally cannot produce, because they measure a harmful absence rather than the presence of a mark: `over-erased`, which finds the 22 figures whose regions print with no drawn edge because one Swiss style file sets `draw=none`, and `caption-carries-the-fact`, which takes `readers_eye.py` from 2 findings to 5 by expanding the `\input` sites it otherwise never opens.
