type: fixed

- **Book statement IDs no longer collide across the attestation and Float Plan chapters.** Give three definitions distinct raw IDs, update their references and library index, and run duplicate-statement CI without exceptions. Regression cases cover CLI failure reports, selective repeated allowances, and the real-corpus entry point.
