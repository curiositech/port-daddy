type: fixed

- **`From Spawn to Person` (Chapter III of the Book) is brought up to the `textbook-craft` standard.** The chapter now states its own route in one paragraph, per the skill's chapter-template; the top-level Reader's Map it duplicated stays in the source, because the Book already strips a starred reader's-map heading when it collates the chapter (the front matter owns the one book-wide map) while the standalone submission paper, which has no front matter, has no other routing. All eleven previously untagged claim-like environments (eight definitions, three properties) now carry an explicit epistemic-kind tag distinguishing a formal theorem-track claim from a design intention, stated in the claim's own body rather than only in a manifest; the chapter-end `Exercises` section is now genuinely the closing exercises section (a same-named "Open problems" section after it, sharing the word "exercises" in its old title, had been defeating the chapter-end-apparatus check); a `History and references` section collects every citation used in the chapter with a page/theorem/script-and-seed locator; and three cosmetic `tikz`/`fill=` box macros already dead at render time (neutralized by `figures/pd-pedagogy.tex`) are deleted from the chapter's own preamble. Three worked examples are added, each reusing numbers the chapter already fixes elsewhere (the newcomer-floor/sanction arithmetic of Theorem `necessity`, and two of the chapter's own running Alice/Bob scenes promoted from narrative to a `pdexample`) rather than inventing new ones; the remaining fourteen sections without a worked example are left alone because they carry no number or mechanism a reader could check by hand.
- **The chapter's new Recall block no longer prints on Figure 5.3.** Figure 5.3 is wider than the measure, so the Book's overflow net sets it over the column and the outer margin, where its third panel reaches about 30pt into the margin column; a `pdrecitation` is a margin device raised so its last line sits on the line that issues it, and it was issued directly under that figure, so the raise carried it up into the panel and overprinted five words on Book p.242. The occupancy caps in `figures/pd-pedagogy.tex` could not catch it -- they track margin heads and earlier margin blocks, and a float that overruns its measure is neither. The figure is now drawn before the worked example that counts it rather than after, which leaves prose taller than the Recall block between the float and the block's anchor, so no page break can bring the two into contact; checked on three forced page breaks as well as the natural one.
- **`tests/purser/mega-volume-test-helpers.js` finds the real `\begin{document}`.** `injectAfterDocumentStart` took the first textual occurrence, and this PR's own chapter preamble explains itself with the words `\begin{document}` in a comment eighty lines above the real marker -- so every probe landed in the preamble, the generator's comment-skipping `documentBody` correctly left it out of the collated body, and `test-namespace-conflicts.js` failed on a label that was never in the document. The helper now skips commented markers exactly as the generator does. `tests/unit/purser-mega-volume-successor.test.js` also gets a `maxBuffer`: a failure in that suite prints the whole collated body and blew past `spawnSync`'s 1 MiB default, so both runners reported `ENOBUFS` instead of the failing test.
