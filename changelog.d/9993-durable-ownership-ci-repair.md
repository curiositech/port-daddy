type: fixed

- **Durable ownership contracts now have accurate coverage and recovery instructions.** Inventory the existing signed ownership schemas and SDK-only routes, distinguish exact SHA256 identities from model selections without excluding fixture files, and run roster admission tests in controlled linked worktrees even when CI checks out a detached commit. The legacy takeover CLI remains explicitly grant-required; no runtime authorization, stale claim, ownership epoch, or operator-presence rule is relaxed.
