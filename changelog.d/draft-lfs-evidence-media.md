type: changed

- **Evidence media now lives in Git LFS, and the PR-body URL form changed with it.** 590 files and 303 MiB of PR screenshots, capture artifacts and recordings under `docs/pr-assets/`, `docs/artifacts/`, `docs/pr-media/`, `docs/pr-artifacts/`, `website-v2/screenshots/`, `website-v2/docs/artifacts/`, `website-v2/docs/pr-artifacts/` and `core/pd-console/docs/artifacts/` are tracked by LFS from this commit forward; deployed assets under `website-v2/public/**` deliberately are not, because no workflow sets `lfs: true` on checkout and Cloudflare Pages would serve the 130-byte pointer. Embed evidence with `github.com/<repo>/blob/<sha>/<path>?raw=1`, not `raw.githubusercontent.com` — measured against a real LFS object, the former returns image bytes and the latter returns the pointer as `text/plain`. `scripts/check-binary-lfs.mjs` (CI job `binary-lfs-guard`) fails any binary of 1 MiB or more committed outside LFS on a non-allow-listed path, so the rule cannot drift; `docs/artifacts/whitepaper-figure-semantics/` and `demos/` are carved out because a test sha256-pins the former and `vhs.yml` auto-commits the latter. This is forward-only and reclaims none of the existing 5.2 GB `.git`.
