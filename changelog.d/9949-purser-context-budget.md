type: fixed

- **Purser now budgets and receipts every source-bearing review path.** STEEL-MAN uses the configured input-heavy model, complete diffs flow through when they fit, oversized diffs carry exact omission receipts and a neutral partial-coverage result, missing or malformed reusable-suite receipts force fresh authoring, every non-executed sandbox outcome is a BLOCK, and only an embedded executed-sandbox receipt can authorize a resumable checkpoint.
