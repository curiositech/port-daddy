type: changed

- **The `tufte-evidence-design` skill gets the automation its own review pass asked for, starting with the margin gloss the Book had been missing.** Both `pd-pedagogy.tex` twins now define `\pdgloss{Term}{one-line definition}`, which bolds a house term where it's first used and carries its definition as a margin note built from the same primitive `\keyidea` and `\pitfall` already use, folding back into a parenthetical outside the margin column so a standalone chapter loses nothing; no chapter calls it yet, since picking terms is the author's editorial pass, not this one's. `skills/tufte-evidence-design/scripts/margin_lint.py` checks the margin apparatus mechanically over the Book's eight chapters: a portrait (a `\pdmarginfigure` whose slug actually resolves under `plates/marginalia/`) is rationed one per section while small multiples, sparklines and regime strips in the margin aren't, two margin figures sitting close together in source get an advisory nudge toward a possible page collision, a house term can be glossed as many times as there are terms but never the same term twice, and a `\footnote` in a chapter body is reported, not failed, since there's still no sidenote macro to rewrite it into. `skills/harbor-chartwork/scripts/figcheck.py` now renders each page's own measured region and reports an advisory ink-fraction and edge-density reading via the skill's `ink_audit.py`, never touching its own pass/fail verdict. A single `tufte.py` CLI wraps the audit, the margin lint, a checklist reader, and the decision tree, and that tree now points to the exact critique-and-limits section for every branch with a known objection, so a reader meets the counterargument before applying the rule.
