type: fixed

- **Fleet reviews now have enough bounded time to reach their final blocking reviewer.** The default nine-ship roster's earlier 45- and 75-minute logical-run ceilings ignored legitimate multi-call reviewers and queue continuations; exact-head reviews still ended neutral after 82-93 minutes before their remaining Purser work. The ceiling is now three hours, with per-call, provider-retry, and Purser repair limits still bounding spend. Expired continuations also reuse already-paid checkpoints before naming the first ship that would require new model work.
- **Purser repairs monorepo-relative imports only when two trusted sources agree.** If a generated test names an ambiguous package suffix, Fleet may now rewrite it only when the repository tree and the reviewed PR's exact changed-file list identify one path; absent, conflicting, or untrusted hints still fail closed before sandbox execution.
