type: fixed

- **13 relay routes no longer 500 on a malformed URL escape.** `%ZZ` in a path segment made `decodeURIComponent` throw `URIError`, which escaped routing and hit the worker's global boundary — 500 `INTERNAL_ERROR` for what is only a bad URL. `/v1/fleet/runs/:id` (GET+DELETE), `/v1/interruptions/:id/answer`+`/ack`, `/v1/push/apns/devices/:id`, `/fleet/runs/:id`, `/skills/:id`, `/v1/skills/:id`, `/billing/balance/:id`, `/v1/quotas/:fp`, `/v1/harbors/:ns/:name...`, `/v1/config/issuers/:id`, and `/v1/cache/jwks/:id` now decode through the (generalized) `safeDecodeSegment` helper, which turns a bad escape into `''` instead of throwing — verified per route that the route's own existing check already rejects `''` the same way it rejects any other bad or unknown id, so the malformed-encoding case lands in an answer the route already gives (a 404, a 400, or an unconditional-idempotent 200) rather than a new one.
