type: fixed

- **Plan editing and completion now agree on visible checklist tasks.** `pd done` shares the `pd plan check` scanner: fenced examples, HTML comments, and prose checkboxes do not block completion, while unfinished `[ ]` and `[-]` tasks do. Earlier plan versions remain intact.
