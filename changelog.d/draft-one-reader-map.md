type: changed

- **One reader's map for the whole Book, not one per chapter.** The per-chapter `Reader's Map` sections and `abstract`s are gone from all eight chapter sources (standalone editions included), replaced by a single front-matter figure (`figures/fig-book-reader-map.tex`) tracing five reader archetypes through the Book's four parts. The figure ships as a genuine two-page spread — Parts I–II on one page, Parts III–IV on the next, row labels repeated on both — rather than one figure rescaled to fit a single page, after the single-page draft was found to render its right-hand columns illegibly small (two of its annotations fell below the 7pt legibility floor) once shrunk to fit. `scripts/generate-mega-whitepaper.test.mjs` now asserts zero per-chapter reader maps or abstracts, on both the chapter sources and the generated Book body.
