type: added

- Three chapter pages whose idea had no drawing now have one, each built from numbers the repository already fixes. The Anchor Protocol gets the issue--delegate--verify ceremony on a clock, so a reader can see that the issuer stops being spoken to after the first message. The Harbor Economy gets the succession price as a boundary in the plane the criterion is stated over, with the worked instance and its canary on one vertical read. The Bonded Commons gets the claim-signaling threshold plotted as the deviator's own payoff against the discount factor, with both punishment regimes on one axis and the model-checked discount factor marked. The two chapters that now carry a plot load `pgfplots` in their own preambles, so the submission papers still build standalone.
