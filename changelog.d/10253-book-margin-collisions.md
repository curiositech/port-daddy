type: fixed

- **Six real, rendered collisions in the Book are gone, found by measuring every page rather than by re-checking a sample.** A full 553-page PyMuPDF bounding-box scan of `coordination-papers-mega-volume.pdf` — not a spot check — found a work-order node hanging off the physical left edge of the page (Chapter 3), a monospace italic font-substitution warning (missing `ItalicFont` on the Book's Source Code Pro), an axis label bleeding into the margin column and colliding with a Recall box (Chapter 4), a margin head and a Recall block overlapping by 2.35 pt (Chapter 5), and two Federated Harbor figures' own labels overlapping each other and a Recall block (Chapter 8). Each was root-caused in its own figure source or in the shared `pd-pedagogy.tex` margin-placement macros and re-verified by rebuilding and re-measuring. Margin devices tied to a figure (`\pdgloss`, a Recall block) now default to centering on that figure's own vertical span instead of hanging from the line that issues them, falling back to top- or bottom-aligning on the figure, and only then to the original anchor, whichever clears first.
