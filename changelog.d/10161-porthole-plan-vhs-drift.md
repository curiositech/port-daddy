type: fixed

- **`demos/porthole/PLAN.md` no longer describes a CI driver that does not exist.** It still named VHS `.tape` files as the recording DSL after the VHS workflow was deleted and terminal-recording CI moved to the `porthole-recordings` job (deterministic shell drivers writing asciicast evidence, replayed by the browser's VT interpreter). The plan now says so.
