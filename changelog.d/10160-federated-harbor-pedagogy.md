type: changed

- **The Federated Harbor chapter's honesty ledger now has no untagged claims.** Harbor Sovereignty, Inter-Harbor Scope, and the Service-Level Attacker Window were a bare `definition`/`corollary` with no epistemic kind stated in their own body; all three are now `pdclaim{Design invariant}` — each is a property characterization the paper's protocols are checked against, not a theorem derived from a formal model, and the corollary's bound only becomes a maximum once an operator imposes the partition and service-level assumptions it names.
- **Six new worked examples, each using a number or trace the chapter, a proof script, or a TLC run already fixes.** The section literally titled "Worked Example" gained one (the eight/sixty-four-harbor round count from the chapter's own $\Theta(\log m)$ result); Threat Model gained the `CardRevocation.tla` rollback counterexample (13 states, then a 4-step breach, then 76 states once fixed) as a traced mechanism before the attack catalog; The Federated Authority and Cross-Harbor Capability Transfer gained concrete instantiations of their own definitions/theorem drawn from the chapter's existing Alice/Bob and test-database scenarios; Federation Admission gained the Acme/Beta six-month sponsorship the chapter's own worked example already commits to. Formal Model gained the actual terminal output of `federated.pv`, an already-mechanized ProVerif result distinct from the chapter's own draft cross-realm model. No section that lacked a real number or mechanism to trace was given an invented one.
- **The chapter's own Reader's Map is gone.** Deleted in full — the persona table and per-audience reading paths were the repeated-reader-map anti-pattern the front matter already owns; nothing in it stated a chapter-specific fact the front matter's own per-chapter guide (`whitepaper/textbook.json`) does not already carry.
- **A dead tinted-box macro left the preamble.** `\pullquote` was defined locally as a `tikz` node with a `fill=`, but `figures/pd-pedagogy.tex` already neutralizes that exact name at `\AtBeginDocument` time, so the local definition never rendered; removed rather than left as a misleading leftover.
