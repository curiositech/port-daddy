type: added

- **Claim-tree trouble is now an actionable agent nudge.** The suggestibility scan projects live claim evidence into ordered `VERIFY` through `PROCEED` states, delivers a Mermaid ego graph to `pd nudge`, and renders the same semantic state in pd-console and the Claim Tree web documentation.
