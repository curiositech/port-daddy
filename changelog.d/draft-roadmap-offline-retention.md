type: fixed

- Validate roadmap declarations offline with `--body-file` while preserving the current declaration-only admission policy. Use direct summary writes and halt-safe remediation without GitHub or daemon access. Retains the useful offline validation work from #10109.
