type: changed

- **The Book's central edition is now Swiss.** `\pdedition` defaults to `swiss` in `coordination-papers-mega-volume-preamble.tex`, so the canonical `coordination-papers-mega-volume.pdf` — the file every published link already points at — renders the Swiss character: colour blocking, grotesk display, flat marks. No path, link or download URL moved, and the change reverses by flipping one macro. The maritime and technical characters stay switchable but are no longer built: their driver roots (including a new `coordination-papers-mega-volume-maritime.tex`), their plates and their preamble branches all stay in the tree and build on demand via `scripts/build-whitepapers.sh coordination-papers-mega-volume-maritime`, but they publish nothing, so the Book's registry, its publication digests and the site's download panels now describe one PDF instead of three.
