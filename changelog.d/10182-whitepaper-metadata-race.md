type: fixed

- **A regenerated whitepaper PDF can no longer outrun the metadata that describes it.** `.github/workflows/whitepaper-build.yml` regenerated PDFs and committed them, while `.github/workflows/whitepaper-metadata.yml` separately recomputed their byte length, SHA-256 and page count into `website-v2/public/whitepaper/publication-digests.json`. Two workflows on one event, the metadata one minutes faster: on PR #10162 it healed at 10:05, the regen pushed new PDFs at 10:10, and nothing re-ran — a regen push is made with `GITHUB_TOKEN`, which starts no further workflow runs — leaving four PDFs adrift and `tests/purser/whitepaper-hashes.test.js` and `tests/unit/spawn-whitepaper-contract.test.js` red on a byte count until a person ran `--fix` by hand. The regen job now runs `website-v2/scripts/check-whitepaper-metadata.ts --fix` itself between the render and the commit, re-runs the plain check as a gate, and stages the derived files alongside the PDFs, so the artifact and the record of it move in one commit or not at all. The metadata workflow keeps healing PDFs a person committed by hand, and now also stages `website-v2/src/data/researchPapers.ts`, which `--fix` could already rewrite but it never committed.
