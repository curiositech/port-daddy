type: changed

- The Purser fleet ship is disabled. It authored adversarial tests it could not execute — its sandbox has failed before the test runner starts for weeks, an adjudicated fleet-wide fault — and then demanded those unexecuted tests be satisfied, while its retarget mechanism silently failed and left the tests unmerged beside the PR they were written for. Its entry in `pd-fleet.yml` always carried the header "DISABLED by default; uncomment to enable" while the entry itself was uncommented, so it had been running since it landed; the block is now genuinely commented out with the conditions for re-enabling written beside it. The other fleet ships are unaffected.
- A PR merged into a feature branch rather than `main` is not shipped, and GitHub says "Merged" either way. `scripts/find-stranded-prs.mjs` finds the ones that never made it the rest of the way, testing each against `main` by patch-id and by whether its added files exist there at all, and `docs/roadmap/stranded-work-rescue.md` records the current backlog with a disposition for each. AGENTS.md now requires every PR to be based on `main`, and requires review threads to be resolved rather than merely replied to.
