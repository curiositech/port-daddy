type: security

- **A daemon restart no longer changes who an agent is or strands its exact work.** Durable actors now hold bounded, revocable body credentials instead of one restart-fragile secret; a signed FleetBar operator-presence recovery can rebind one actor to one successor session, worktree, branch, context slot, and enumerated claim map without minting a replacement identity or releasing unrelated claims. Approval immediately consumes a daemon-internal one-shot grant, and lost responses reconstruct the same credential-free binding and custody receipts. Identity-bound IPC falls back to the canonical credentialed HTTP boundary, every session mutation proves both display ownership and the stamped actor, and append-only receipts expose real ledger sequence numbers with replay, expiry, generation drift, revocation, and first-use evidence. The legacy startup promotion of historical display principals and its plaintext `~/.port-daddy/actor-credentials` files are deleted; no compatibility authority remains.
