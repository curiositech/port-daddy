type: fixed

- **The Book downloads from the site again — its URL had been answering with the SPA shell.** `website-v2/scripts/prune-pages-assets.mjs` deleted `whitepaper/coordination-papers-mega-volume.pdf` out of every Pages deploy, citing Cloudflare's 25 MiB per-asset limit; the PDF is 9,740,631 bytes, 9.29 MiB, 37% of that limit, and `https://portdaddy.dev/whitepaper/coordination-papers-mega-volume.pdf` returned HTTP 200 `text/html` 4,829 bytes byte-identical to a nonexistent path, while the untouched 12,641,454-byte `collected-treatise-inside-jacket.jpg` beside it served fine. The exclusion list is now empty and the 25 MiB rule is enforced by `oversizedPagesAssets()`, which walks the built `dist`, measures every file, and fails the build naming the asset and its byte count instead of silently deleting it.
