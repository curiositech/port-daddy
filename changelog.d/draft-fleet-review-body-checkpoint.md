type: fixed

- **Fleet no longer rewrites human-owned pull request descriptions and revalidates same-head review metadata at publication boundaries.** Purser publishes its Purpose and Obligations through a checked, attributable Fleet-owned comment; exact title, body, head, base, lifecycle, file, and diff evidence remains checkpoint-bound. GitHub's non-atomic webhook-to-check and final guard-to-mutation intervals remain explicit residuals, D1 run steps remain retry-replaceable telemetry, and the App-owned check receipt is the verdict authority.
