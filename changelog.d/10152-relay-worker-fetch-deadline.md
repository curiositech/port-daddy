type: fixed

- **Relay Ship Controls can complete GitHub checks in production again.** Repository and fleet-inventory requests now use Workers-compatible deadlines and refuse to forward user authorization across redirects.
