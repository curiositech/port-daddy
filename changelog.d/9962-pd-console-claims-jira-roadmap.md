type: changed

- **pd-console Claims is now an inspectable ledger, and Planner can show the operator's real Jira roadmap.** Claims exposes sortable full-path, scope, owner, purpose, worktree, branch, lifecycle, authority, and timestamp metadata with a selected-row inspector that wraps instead of truncating. Planner labels Port Daddy's local roadmap as local authority and renders a separate read-only Jira Cloud projection from Keychain-managed FleetBar credentials; missing or failed Jira configuration never hides local work.
