type: changed

- Redrew the audit-tower figure (Chapter 5, "Numbers by hand") around the chapter's own two-phase recurrence instead of a hand-placed smooth curve, which had drawn a different shape than the closed form gives. The three closing depths (27, 36, 53 levels) now read as three lengths on one shared, log-scaled axis, each region carrying an explicit drawn edge.
