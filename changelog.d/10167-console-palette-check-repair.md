type: fixed

- **The pd-console-vs-website palette check runs again, and pd-console's dark theme is honest about its own colors.** `test:console-palette` (`website-v2/scripts/check-console-palette-tokens.mjs`) was wired into `package.json` but never invoked by any workflow, so it rotted red on main after a legitimate console restructure moved the surface/accent tokens it was pinned to. Along the way the dark theme's `engaged`/`gated`/`landed` roles had drifted onto unrelated colors — most visibly, the "needs attention" warning chip (`Tone::Gated`) rendered violet instead of the coral heat every other severity color uses — so they are restored to the website's dark `--brand-warm`/`--brand-heat`/`--brand-accent`. The check itself now parses both sides into field/token maps instead of grepping for literal hex strings, and a new `console-palette-tokens.yml` workflow runs it on every PR and push that touches the palette, the tokens, or the script.
