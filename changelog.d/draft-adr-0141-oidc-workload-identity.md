type: changed

- **Fleetbot publication now uses a policy-admitted workload identity and a revocable standing operator grant instead of a `pdu_` bearer.** Relay validates an immutable GitHub Actions repository subject plus the exact owner, repository, workflow, ref, environment and runner claims; the signed-in repository administrator can create or revoke a bounded grant; every publisher capability and receipt names the current grant epoch; and admission intersects the grant with the live GitHub App installation before opening the granting account's dedicated, refreshable credential. The old bearer-bound capability schema is refused. A protected manual workflow exercises enrolment and read-only PR inspection without starting local Port Daddy. Host workload enrolment and the broader consent/cost ladder remain explicit follow-up work.
