type: changed

- **The `/whitepaper` and `/research` pages now read as though a person wrote them.** Forty-seven copy blocks across `website-v2/src/pages/whitepaper/index.tsx`, `website-v2/src/pages/research/index.tsx`, `website-v2/src/data/researchPapers.ts`, `website-v2/src/data/whitePapers.ts` and both copies of `textbook.json` were rewritten to drop the em-dash-pivot cadence, the "not X — Y" antithesis and the aphoristic sign-off that made every panel standfirst land the same way. No claim, number, result tag, citation, file size, page count or link changed: the seven research papers keep their pull quotes and `resultTags` verbatim, the eight chapter epigraphs and `oneLine` summaries are untouched, and `whitepaper/textbook.json` stays byte-identical to `website-v2/src/data/textbook.json`. `LIBRARY_SPINE` is the one substantive rewrite — the compressed chain now says what each link buys instead of naming it.
