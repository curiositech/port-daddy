type: added

- **pd-console agents can replay sophisticated Mission interactions through stable semantic controls.** The owner-only versioned socket now discovers enabled actions, reports redacted Mission and project context, types without sending, invokes shared UI transitions, scrolls and resizes bounded workspace surfaces, waits and asserts structured state, and returns deterministic scenario receipts. Mission terminal launches rebind to the admitted WorkIntent project while preserving the retired receipt and reporting live shell cwd as explicitly unknown.
