type: changed

- **`make_copy_and_media_human` is rebuilt around measured evidence, and now passes the review it sells.** The catalog grows from 70 tells to 228 in the core families (436 counting the lanes below) with `false_positive_when`, `currency`, `family` and citations on every item; thresholds move into `skills/make_copy_and_media_human/references/catalog.json` so the rubric and `skills/make_copy_and_media_human/scripts/humanize_review.py` cannot drift apart, and `--validate` proves it. Rhythm signals (em dash, comma, contraction, sentence variance) cap at low severity unless `--baseline` supplies the author's own prior writing, because those signals are fluency proxies that overlap badly between people and models. New `skills/make_copy_and_media_human/references/fairness-and-false-positives.md` carries the base-rate argument for why a finding is an editing cue and never evidence of authorship. Two entries were wrong and are corrected: the mangled-hands image heuristic is marked obsolete in favour of provenance, and the U+202F claim is downgraded to paste evidence. Also fixes HTML injection in the report renderer, a `groq` typo that silently dropped an item from every generated reference, and detectors that counted code fences and frontmatter against the author.
- **The skill now covers the surfaces a model has no way to open.** `skills/make_copy_and_media_human/references/unopened-surfaces.md` adds 36 items across navigation and information architecture, internationalisation, documentation sites, commerce, email templates and print/PDF, and `skills/make_copy_and_media_human/references/dark-patterns-the-model-inherits.md` adds 9 for the consent banners, countdowns and activity counters the commercial web supplied. 27 new static detectors in `skills/make_copy_and_media_human/scripts/humanize_review.py` back them, each verified to fire on a failing fixture and stay silent on a matched clean one. The dispatch gains `analyze_app_surfaces`, so a plain `.js` or `.ts` file is now checked for the send call, the PDF export, the price formatter and the countdown — none of which live near the markup. Two findings that are properties of a set rather than a file are handled as such: `every-page-opens-with-in-this-guide` reports low on one page and escalates once three files in a run share the opening. Also fixes a false positive in `missing-html-lang`, which flagged every correctly internationalised root layout because `lang={locale}` did not look like a language to it.
- **Tool provenance is now answered separately from quality, because conflating them is the mistake the skill exists to avoid.** `python3 skills/make_copy_and_media_human/scripts/humanize_review.py page.html --provenance` reports which tool published a page and stops: no report, no severity. A fingerprint says what made the page, never who wrote it and never whether it is good — and Wix and Squarespace prove the point, since their AI flows seed the ordinary editor and produce markup identical to hand-built output. `skills/make_copy_and_media_human/references/tool-fingerprints.md` adds 21 items carrying that argument plus a greppable table generated from the script's own pattern list, so the two cannot drift. Seven of those fingerprints are also genuine defects and stay ordinary findings: a meta description reading “Generated by v0”, a share card that is the builder’s logo or an auto-screenshot of a preview build, a favicon that is a build tool’s logo, a palette still at a component library’s exact default values, per-character text spans that make a screen reader read “W e B u i l d”, and a generated-image filename on an author byline. For all of them the advice is to do the work the string points at rather than delete the string.
- **Accessibility now covers the part automated tooling cannot reach, and says how much that is.** axe-core's own coverage report claims 57.38% of issues by volume, but counted as distinct success criteria a machine can fully verify it is around 30% — roughly 15 of WCAG 2.1 AA's 50. `skills/make_copy_and_media_human/references/accessibility-beyond-the-checklist.md` adds 40 items in the other 70%, and `skills/make_copy_and_media_human/references/five-minute-manual-pass.md` is eleven keyboard and screen-reader steps written for someone who has never used one. A fourth `detection_type`, `assistive`, marks the checks a headless browser cannot settle because the finding is what a person hears — filing those as `rendered` would invite an assertion that passes and launders a manual check into a green tick. 17 static detectors back the decidable subset, and `scripts/render_check.py --probe-a11y` turns two more into verdicts: it applies the WCAG 1.4.12 text-spacing values and measures what stops fitting, then switches to forced colors and confirms which elements lost their only visible boundary. Most of this lane reads UNREVIEWED rather than machine-written and the entries say so; the six that are genuinely model-flavoured share one shape, which is the model producing the appearance of accessibility work.
- **Dogfooding the new reference file found a real detector gap.** `definition-after-use` fired on writing that HAD defined its terms, because a term is often defined under a near form — "ARIA is a set of attributes, all beginning `aria-`" defines `aria-` — and the check only looked for the exact token. It now accepts the bare form with separators and case stripped.
- **In-product UX writing is now covered, and it is the one lane where hard string matching is defensible.** `skills/make_copy_and_media_human/references/product-ux-writing.md` adds 35 items for the strings a logged-in user reads mid-task — errors, empty states, button labels, confirmations, notifications, field hints. Everywhere else in this skill a closed phrase list is a bad detector, but a product's string table is small, enumerable and extractable, and within it the phrase space is genuinely narrow: “Something went wrong” has no good use at all in a product that knows what went wrong. So `skills/make_copy_and_media_human/scripts/humanize_review.py` gains a UI-string pass that extracts candidate strings first — from i18n catalogs, JSX text and the render/error path — and runs 18 closed sets only over those, so it will not fire on an article quoting the same phrase. The mechanism the lane names: under uncertainty about what failed, the only clause that carries zero risk is the one about how sorry everyone is, so the copy is optimised for the writer's uncertainty rather than the reader's blockage — and the less the system knows about the failure, the warmer it gets. The headline entry is `collapsed-empty-states`, which is not a wording problem at all: three unrelated situations (never had data, filter matched nothing, fetch failed) produce the same zero count and collapse into one string, and the third is what makes users believe their data was deleted.
- **Typographic craft and design-system structure, under one governing idea.** `skills/make_copy_and_media_human/references/typographic-craft-and-tokens.md` adds 38 items covering leading, tracking, line breaking, numerals, type scale, weight, optical size, text colour and token architecture. They all express one finding: a generator emits CSS that is internally consistent and has no craft in it, because craft in typography is per-context judgement and a default is by construction context-free. So the tell is never a wrong value — it is one value where there should have been a function of context, and every check is the same computation: does this property vary with the thing it is supposed to vary with? 18 detectors back the decidable subset. Two cautions are built into them so the lane does not generate noise: read computed values rather than declaration counts (utility frameworks bundle a tightening line-height into their size scale, so a page with no explicit declaration is usually correct), and treat restraint as distinct from absence (a page declaring no font-weight at all is on defaults and is not the two-weights finding).
- **Building the lane surfaced four real bugs, three of them in code that already shipped.** The CSS `margin` shorthand was parsed wrongly: `margin: 2rem 0` is vertical-then-horizontal, so it sets top and bottom to the SAME value, and the first version read it as asymmetric while reading the correct `margin-block: 3rem 1rem` as symmetric — exactly backwards. A layout table carrying `role="presentation"` was being asked for header cells, which is the opposite of the advice for an email template. `definition-after-use` reported “never defined” on writing that defined the term in the very next clause, because a term of art is almost always code-formatted and the closing backtick sat between it and its definition; the em-dash appositive was missing from the pattern outright. And the single line-height check only matched unitless values, so the commonest generated form — a length — escaped it entirely.
- **Performance and payload, framed as a design tell and reported honestly.** `skills/make_copy_and_media_human/references/performance-as-a-design-tell.md` adds 29 items for the place where correctness-in-source and correctness-in-delivery come apart: the img tag is valid and the format is wrong, the font stack is tasteful and six weights ship, the component is correct React and it did not need to be React. 20 detectors back it. The lane's most important contribution is a refusal: **there is no published measurement that AI-generated sites are heavier.** What exists measures AI-adjacent signals — builder subdomains, the spread of the indigo palette from 0.03% to 0.54% of all sites — and explicitly does not measure page weight. So the entries say UNREVIEWED, and only the three or four that are genuinely model-flavoured claim otherwise. `skills/make_copy_and_media_human/references/performance-budget-and-folklore.md` carries eleven budget numbers a designer can check without opening a profiler, plus ten pieces of circulating folklore to drop — the Lighthouse score as a proxy for speed, the per-KB parse-time constant retired by V8 in 2019, the shared font cache killed by cache partitioning in 2020, and the weight claim above.
- **Motion is the one design property whose quality lives in time, and a generator emits it as a string it can never watch run.** `skills/make_copy_and_media_human/references/interaction-and-motion.md` adds 26 items on that mechanism: duration, easing and stagger are judgements made by watching, so what arrives is the corpus median applied uniformly — 300ms for a 2px hover tint and for a full-height sheet alike — and uniform motion is decoration by definition, because decoration is the only use of motion that needs no knowledge of what is happening. The other half is input devices: hover is the state the author sees while building, focus only exists if you put the mouse down, and on a touchscreen hover latches on tap and never releases. Two items carry published evidence rather than taste — Nielsen Norman Group found most participants at least mildly disoriented by scroll capture, several reading it as a bug, and 35.4% of US adults aged 40 and over showed vestibular dysfunction in the 2001-2004 NHANES data. The sharpest finding is `reduced-motion-honoured-in-css-ignored-in-script`: the media query is satisfied while the parallax keeps running, because the query and the animation are two separately correct answers to two separately given instructions.
- **A form is the one surface where the output is the beginning of the user's work rather than the end of it.** `skills/make_copy_and_media_human/references/forms-and-input.md` adds 25 items for the properties that only exist while somebody is operating the form — the keyboard that opens, the value autofill puts in, what survives a failed submit — none of which are in the markup. `submit-disabled-until-valid` is the headline: a clean testable sentence producing an interface where the control that would tell the user what is wrong is the control being withheld, and its worst instance, `validity-gate-misses-the-password-manager`, leaves a user with a visibly complete form and a dead button because the gate listens for keyup and a password manager's fill does not fire one. Three items are WCAG failures carrying no fairness caveat at all: missing autocomplete tokens (1.3.5, and Google reports correct tokens cutting checkout time by up to ~30%), errors not tied to their fields, and paste blocked on a password field (3.3.8).
- **Three of the new entries duplicated entries that already shipped, and were consolidated rather than added.** A new autocomplete check and a new address-shape check were both second versions of findings the catalog already had, so `missing-autofill-attributes` absorbed the better evidence and the purpose-matching detection and rose to high while the duplicate was deleted, and `name-and-address-shape-assumed` kept its two-marker detector, which has the better false-positive profile than the one that would have replaced it. A third, `label-that-only-exists-while-empty`, was firing on the no-label case, which is `input-without-label`; it now detects only the floating label that shrinks past legibility once the field has a value. Net catalog change is 486 items, not the 488 the additions would have made.
- **Building the lanes found four more bugs, and one measured source contradicted the catalog.** The reduced-motion block matcher required a newline before its closing brace, so it never fired on the single-line form, which is how the copy-pasted universal reset is usually pasted. Worse, a duration-only reduced-motion block was being counted as a guard for parallax — it is not one, since parallax is a transform and an attachment and neither is a duration, which is the entire argument of the check sitting directly above it. `required-marking-with-no-key` reported a missing legend on a form whose legend read “Fields marked * are required”, because the pattern was a fixed phrase list that did not contain the canonical phrasing; it now matches an asterisk sharing a text node with required, optional or mandatory in either order. And six items were marked `structural` with no detector behind them, which is a claim the bundle did not honour — all six are now implemented. Separately, a ranked analysis of ~3.2M Reddit posts puts mesh, blob and aurora backgrounds at roughly 0.1% of comments naming AI slop, so `stock-mesh-gradient-background` and `dark-mode-radial-glow-blobs` drop to low and say why in the entry — with the methodological limit stated, since comment share measures what irritates people rather than what predicts generation.
- **A review bot was right about the one thing that matters most in this skill: 51 of 486 items shipped with no usable `false_positive_when`.** Every generated reference prints “Every item carries a False positive when line. Read it before you act on the item”, and 51 of them answered “Not yet characterized” — which honours the sentence and misses the point, because that field is what stands between a finding and an accusation. All 51 now carry a real answer to “who legitimately writes this way?”, and several turned out to be the entries where it matters most: `deepseek-qwen-overformal-register` and `qwen-deepseek-chinglish-calque` describe genuine features of second-language English, which is exactly the population AI detectors are documented to penalise, so both now say plainly that they are never usable as an authorship signal. `apologetic-over-qualification` notes that reflexive hedging is a documented feature of the professional register of women and junior colleagues, so flagging it can amount to enforcing a narrow confident register. `parallel-overload-uniform-bullets` admits it sits closest of any entry to flagging correct writing, since parallel list structure is required by Chicago and the Microsoft Style Guide. `--validate` now fails on a placeholder or a sub-eight-word answer, which caught two further thin entries including one added in this same branch.
- **`SKILL.md` told the judge pass to handle every mechanically-decidable item the script does not implement, and gave it no way to find them.** Auditing that instruction turned up 138 of 382 such items — which is not a defect but the documented split between the two detection layers, since `detection_type` says what KIND of evidence settles an item and never that this bundle automates it. The gap was that the instruction was unactionable: an agent would have had to grep the script to find its own queue. Every generated reference entry now carries an **Automated here** line computed from the scripts themselves, so it cannot drift, and `--validate` reports the split (244 implemented, 138 for the judge pass). A parallel audit of every other catalog field came back clean — no missing or placeholder values anywhere in `description`, `detection`, `why_it_reads_ai` or `fix` — so the 51 hollow `false_positive_when` entries were an anomaly rather than a symptom of general rot.
- **A clean, hand-written LaTeX paper scored a HIGH finding, which is the exact failure this catalog exists to prevent.** `unfilled-placeholder-residue` made both braces optional in its merge-tag pattern, so a single `{word}` group matched — and `\usepackage{amsmath}` read as an unfilled template token. A confident accusation on the most ordinary possible input in its domain, on a file type nobody had pointed the skill at yet. Both braces are now required, which still catches `{{ name }}`, `%%FIELD%%`, `*|MERGE|*` and `[Job Title]` and no longer fires on LaTeX, CSS blocks or Python format strings. `--selftest` gained a clean LaTeX paper alongside its clean-prose sample and a new `analyze_file_text` helper that runs the real dispatch over in-memory text, so the guard exercises the route a user's file actually takes rather than one analyzer; re-introducing the old regex makes it fail with “FALSE POSITIVES on a clean LaTeX paper”, which was verified rather than assumed.
- **A false-positive sweep across nine clean file types found `heading-spam` accusing reference documentation, and its own caveat had said not to.** The entry has always read “Reference documentation and API docs are legitimately heading-dense, because readers arrive by search and leave immediately. Scope to narrative prose” — and the detector scoped to nothing, so a well-formed CLI reference scored medium. The caveat is now implemented rather than only printed: reference shape is recognised mechanically as 60% or more of headings being short labels AND a code block present, and both must hold before the finding is suppressed, so genuine heading-per-paragraph spam still fires. The sweep's other eight file types — BibTeX, Python, R, SQL, YAML, plain text, a design-token stylesheet whose uniformity is the whole point, and a math-heavy LaTeX paper — all came back clean, so this and the placeholder bug were isolated rather than a class. Both clean samples now sit in `--selftest`, and removing either fix was checked to make it fail.
- **Research papers: every honest check points outward, at the world, rather than inward at style.** `skills/make_copy_and_media_human/references/research-papers.md` adds 28 items on one mechanism — a paper's sentences and its checkable commitments come out of the same machinery at the same confidence, and nothing in the finished artifact marks which is which, so the failure is not that the writing sounds wrong but that the verifiable claims were never verified by anyone including the author. Checks that ask whether a DOI resolves or whether the body contains the abstract's number cost an accused author nothing when they come back clean, which matters most here because the population likeliest to leave a style marker is the one writing in its second language and the cost of being wrong is a career. The lane's most useful entry is a triage rule rather than a defect: fidelity of model-generated citations tracks the cited paper's citation count, saturating near verbatim recall above ~1,000 citations, so sort the bibliography ascending and verify from the bottom — and note the corollary that checking the famous references proves nothing.
- **LaTeX: the tell is never that it failed to compile, it is that it compiled and nobody opened the PDF.** `skills/make_copy_and_media_human/references/latex-source.md` adds 30 items and `.tex`/`.bib`/`.cls` are now routed properly rather than read as prose. Generated `.tex` reaches for LaTeX's visual layer — `\\`, `\vspace`, `\textbf`, a typed-out “Figure 1” — over its semantic layer, because the visual layer is the only one verifiable from the token stream alone: a counter has no value until TeX assigns one, so the number written is the one already visible in the draft. Ten items are `defect` and reproducible by compiling, carrying no fairness caveat. The lane makes no claim that any of it is commoner in generated than hand-written LaTeX, because the largest mined corpus of real LaTeX faults is a taxonomy of human faults with the same top categories.
- **Scientific figures: the defect is in the code, not the pixels.** `skills/make_copy_and_media_human/references/scientific-figures.md` adds 29 items, 24 of them `structural` because they are checkable in the `.py` or `.R` that drew the figure — the render sits on the other side of a step the author never watched, so nobody sees that 10pt became 2.9pt in an 89mm column or that `savefig` with no `dpi` wrote 640x480. Three things are kept apart that get conflated constantly: image integrity (2 items, predates generative models entirely and says so in capitals), generated imagery (3), and unreviewed plotting (24, carrying no misconduct implication at all). The lane refuses a detector for “is this figure generated” with numbers: against 72,965 real and 150,807 synthetic figures the best zero-shot method reached 53.68% and most sat at chance. It also corrects the canonical Frontiers case — a reviewer did raise concerns and the authors did not respond, so the failure was editorial workflow rather than reviewer inattention, which changes the fix.
- **Four existing entries INVERT for a manuscript figure and are now scoped so they cannot mislead.** Journals reject PNG/JPEG/TIFF for main figures and want vector, so `no-modern-image-format-anywhere` and `hero-image-as-full-resolution-png` do not apply; Nature asks that submitted vector retain its editing capability, so `figma-export-svg-unoptimised` is the wrong move on a submission asset; and a PDF figure's accessible description IS its caption, which is required to be long, so `narrating-alt-text`'s 125-character threshold would flag a correct one. Integrating the lanes also caught two problems in the new work itself: a banned phrase in two of the merged entries, and `review-with-no-locator` firing on this skill's own reference files, which discuss peer review without being a review — it now requires an actual recommendation to an editor, and a plural-form bug found while fixing that had briefly stopped it firing on real reviews at all.
- **A regex in documentation was also a broken markdown link, and it failed CI.** The LaTeX lane's unit-spacing entry documented its grep as a digit character class immediately followed by an alternation group — `[0-9]` and then `(ms|us|ns|...)`. That is an ordinary regex, and the two characters where they meet are also markdown link syntax, so once the generator wrote it into a reference file the repo's `doc-citation-guard` read the alternation as a relative link target and failed. The regex is now written with `\d` and a non-capturing group, which avoids the collision and is the more idiomatic form anyway. `--validate` gained a guard that refuses any catalog string containing a RELATIVE markdown-link target, mirroring the repo guard's own exclusions so a legitimate external badge image is not flagged — the first version of that guard flagged exactly such an image, and the quick fix I reached for was a zero-width space, which `invisible-unicode-artifacts` in this same catalog exists to catch. Two process lessons, both recorded because they cost a CI cycle each: the failure did not reproduce locally because `origin/main` was stale, so the changed-file set was 29 files rather than CI's 32; and the first fix failed again because this very bullet, describing the collision, contained it.
- **The skill had depth and no navigation, so an agent calling it had to read 833 lines to find out what applied.** It now routes. `skills/make_copy_and_media_human/SKILL.md` opens with a router — answer one question, what artifact is in front of you, and it names the files — followed by the three-layer pipeline as a diagram. **27 Mermaid decision diagrams** were added, one per reference file plus the router and pipeline, each validated to parse and each diffed against its source so the verdicts map to the right file. Every generated reference now also carries an index of its contents with severity, family, and whether the scripts automate the check, so finding the three items that apply is a 40-row scan rather than a 900-line read. The two oversized files were split on their natural medium boundaries: `structure-and-deck-tells.md` (1,393 lines) became `document-and-deck-structure.md` and `marketing-and-platform-tells.md`, and `generated-media-tells.md` was separated out of `visual-design-tells.md` because generated imagery is reviewed provenance-first and that is a different order of operations. SKILL.md drops from 833 to 684 lines because 220 of them were lane essays duplicating the lane files' own intros — they are now one table row each, pointing at the file that carries the argument.
- **The restructure tripped three of the skill's own checks, and each was fixed the way the skill says to.** Putting the diagrams outside the `humanize:ignore` block meant a decision diagram that legitimately QUOTES the residue it tells you to look for — one contains the literal string “Certainly, here is” — was scored as residue, and Mermaid node labels were scored as staccato sentence fragments; the ignore boundary now covers the diagram and index while leaving the intro reviewed, which is where Law 2 has teeth. Adding a heading to `fairness-and-false-positives.md` tipped its heading-to-paragraph ratio, so the diagram was folded under the existing intro instead. And the typography lane's intro really did contain five staccato fragments in a row, which that lane's own `staccato-fragment-triplet` caught; they were merged back into one sentence, which is the remedy the entry itself prescribes. A fourth guard was added after the split left the replaced file orphaned on disk, still indexed and still citable: the generator now refuses to run when a reference file exists that it no longer generates.
