type: changed

- **`make_copy_and_media_human` is rebuilt around measured evidence, and now passes the review it sells.** The catalog grows from 70 tells to 157 with `false_positive_when`, `currency`, `family` and citations on every item; thresholds move into `skills/make_copy_and_media_human/references/catalog.json` so the rubric and `skills/make_copy_and_media_human/scripts/humanize_review.py` cannot drift apart, and `--validate` proves it. Rhythm signals (em dash, comma, contraction, sentence variance) cap at low severity unless `--baseline` supplies the author's own prior writing, because those signals are fluency proxies that overlap badly between people and models. New `skills/make_copy_and_media_human/references/fairness-and-false-positives.md` carries the base-rate argument for why a finding is an editing cue and never evidence of authorship. Two entries were wrong and are corrected: the mangled-hands image heuristic is marked obsolete in favour of provenance, and the U+202F claim is downgraded to paste evidence. Also fixes HTML injection in the report renderer, a `groq` typo that silently dropped an item from every generated reference, and detectors that counted code fences and frontmatter against the author.

- **The skill now covers the surfaces a model has no way to open.** `skills/make_copy_and_media_human/references/unopened-surfaces.md` adds 36 items across navigation and information architecture, internationalisation, documentation sites, commerce, email templates and print/PDF, and `skills/make_copy_and_media_human/references/dark-patterns-the-model-inherits.md` adds 9 for the consent banners, countdowns and activity counters the commercial web supplied. 27 new static detectors in `skills/make_copy_and_media_human/scripts/humanize_review.py` back them, each verified to fire on a failing fixture and stay silent on a matched clean one. The dispatch gains `analyze_app_surfaces`, so a plain `.js` or `.ts` file is now checked for the send call, the PDF export, the price formatter and the countdown — none of which live near the markup. Two findings that are properties of a set rather than a file are handled as such: `every-page-opens-with-in-this-guide` reports low on one page and escalates once three files in a run share the opening. Also fixes a false positive in `missing-html-lang`, which flagged every correctly internationalised root layout because `lang={locale}` did not look like a language to it.

