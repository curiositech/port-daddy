type: changed

- **The `/library` page (and `/whitepaper`, which redirects to it) now describes the manuscript instead of advertising files.** It opens on the reader's situation, states the book's one claim from page one, and lists the eight questions the chapters ask in the order they have to be asked, each linked to its chapter. A "What it argues" section carries the spine sentence, the four parts, how every claim is labelled (theorem, design invariant, model-checked property, empirical hypothesis; five assurance modes), and what page one admits about what is built, modelled, and proposed. A "How the book is kept honest" section shows the four loops between the manuscript and the repository: proofs that run on every pull request, hand-worked numbers tagged by who regenerates them, questions the book cannot settle becoming pre-registered experiments (the substrate study), and the harness-experimentalism vocabulary. The seven conference-form research papers are listed with what each proves and which chapter folds it in. Per-chapter PDF buttons and page counts are gone from the page and from the table of contents; the Book itself, with its Swiss and Technical editions, remains the one download. The library changelog gains the September 7 entry.
- **The research program has its own page at `/research`** (the old `/library/research` redirects there). It carries the seven conference-form papers, the ledger of every executed result with the chapter that holds it and the paper that carries it, the mechanized estate by tool with the artifacts that are not green named, the pre-registered substrate study with its four hypotheses, the planned proof lifts, the open problems the book names for itself, and the wrong turns kept on the record. The whitepaper page and the research page link to each other in both directions.
- **"Whitepaper" and "Research" join the primary navigation**; Scout and Examples move to the "More" menu, and the footer's reference column lists both pages.
- **Updates to the harbor research program now compel updates to the website.** `docs/harbor-research/program.json` is the one file `/research` renders, mirrored byte-for-byte into the site. `scripts/harbor-research/check_research_program.py` derives its results, estate, and review-ledger sections from `library-index.json`, `whitepaper/corpus.json`, and `critique-ledger.json`, checks that every research paper, deep dive, wrong turn, and study on disk is listed, that every cited source exists, that every silence named in the Porthole decisions brief is an open problem, and that the site mirror is identical; the library-checks workflow runs it on every pull request and the site's vitest suite checks the mirror from its side. `--sync` regenerates the derived sections and the mirror.
