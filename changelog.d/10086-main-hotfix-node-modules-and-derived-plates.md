type: fixed

- **A `node_modules` symlink that reached `main` is untracked, and the plate-provenance gate now has an honest slot for a plate that is a render rather than a generation.** A merge commit picked up a `node_modules` symlink from the worktree it was made in; `.gitignore` lists `node_modules/`, but a path already tracked is not re-ignored, so every fresh clone would have found a self-referential symlink where its dependencies belong. Separately, the three edition covers on `/library` are rasterisations of page one of each CI-built PDF, not image-model output, and `check_plate_provenance.py` had no way to say so — it demanded a prompt, a model and a post-processing note of every plate, which for a render invites a fabricated prompt. An entry may now declare `derived_from` (what it was rendered from) and `render` (how) in place of those three; `derived_from` without `render` fails, and the aspect-ratio check still applies. Three tests pin all three behaviours.
