type: changed

- **The Book and the standalone chapters now print the same status word at the same call site.** `\Built`, `\BuiltWeak`, `\Designed`, `\Vision`, their capitalised aliases, `\NotGuar`, `\Closed`, `\Partial`, `\Open`, `\SpecOnly`, `\Verified`, `\Proved` and `\code` were defined in `website-v2/public/whitepaper/coordination-papers-mega-volume-preamble.tex` *and* again in individual chapter preambles, and `scripts/generate-mega-whitepaper.mjs` keeps only what lies between `\begin{document}` and `\end{document}`, so a chapter's own definition governed its standalone PDF and never reached the collected volume. `whitepaper/legible-swarm.tex` printed "built", "designed" and "vision" on its own and "implemented", "specified" and "proposed" in the Book, at the same rows of the same table. Every one of those names is now defined once, in `whitepaper/figures/pd-pedagogy.tex` and its byte-identical twin under `website-v2/public/whitepaper/figures/`, and the chapter-local copies are deleted.
- **The Legible Swarm's abstract, Reader's Map and honest-label appendix say the words its tables actually print.** The chapter adopts the series vocabulary — `implemented` / `partial` / `specified` / `proposed` — and its evidence marker `\Proposed` is renamed `\Unproved`, because it and the maturity marker `\Vision` both rendered the single word "proposed" eight lines apart in one legend while meaning different things.
- **`scripts/harbor-research/check_duplicate_macros.py` refuses a second definition of any corpus macro.** Run against the corpus as it stands before this change it names 17 duplicated macros, eight of them with bodies that disagree; run against the corpus after, it reports every name defined once.
