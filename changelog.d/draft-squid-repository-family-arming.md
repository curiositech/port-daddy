type: security

- **Giant Squid hook activation now follows verified local repository families without crossing clone or worktree privacy boundaries.** `pd squid on` installs dormant Claude, Codex, Gemini, and agy user hooks once, binds activation to a private local filesystem authority, automatically covers existing and future linked worktrees, rejects unrelated same-remote clones, and adds `pd squid off --this-worktree` as a narrower verified deny; family revocation leaves shared registrations inert, while global uninstall refuses until every family is off. This TypeScript slice provides static ancestry traversal rejection plus concurrent-swap detection, not hostile same-uid race containment; Rust dirfd-relative no-follow mutation is required before making that stronger claim.
