type: added

- **Agents and the daemon now keep a listening watch for the halt sentinel (ADR-0132 phase 3).** Every Giant Squid hook tentacle and the `pd hooks install` gate wrapper check `~/.port-daddy/HALT` before any daemon work, so a halt is heard even when the daemon is down on purpose. Under a halt the prompt tentacle injects a notice opening `SECURITE HALT` with the sentinel's own text, the pre-tool tentacle refuses `pd`/`port-daddy` (except `--help`/`--version`), `mcp__port-daddy__*` tools, `launchctl` relaunches of a Port Daddy label and `brew services start|restart port-daddy`, and each session answers `SEEN` then `COMPLIED` in `~/.port-daddy/DISTRESS`. The daemon runs a 30 s halt watch that stops the reaper/resurrection cleanup, dispatch worker, auto-merge sweep and fleet ticks, and `GET /health` reports `state: halted`. Claude Code's PreToolUse matcher now includes `Bash` and `mcp__port-daddy__.*`.
