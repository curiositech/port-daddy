type: changed

- **The Book is recut as a two-sided block, and its leading stops being LaTeX's default.** `coordination-papers-mega-volume-preamble.tex` loaded `fontsize` with a size and no leading, so `\changefontsize`'s optional argument defaulted to 1.2 and the Book set 10.5 pt on 12.6 pt — a ratio that suits a narrow column, not a 64-character measure. It now sets `\changefontsize[13.4pt]{10.5pt}`, the largest value in the 1.25–1.32 band that still fits 44 lines in the text block. The `\geometry` line moves from the one-sided `left=0.8in,right=1.7in,textwidth=4.5in` to a two-sided `inner=0.95in,outer=1.8in,textwidth=4.25in,marginparsep=0.18in,marginparwidth=1.12in`, so the gutter grows, the margin column narrows, and `\marginnote` flips to the outer edge on verso pages instead of sitting right on every leaf. `scripts/harbor-research/page_overflow.py` carries the same five numbers as a second copy and is updated in lockstep. The collected volume goes from 553 pages to 604; `website-v2/src/data/whitePapers.ts` records 604, correcting a metadata entry that read 551 while the committed PDF was already 553.
