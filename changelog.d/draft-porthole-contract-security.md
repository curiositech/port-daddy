type: security

- **Porthole's internal evidence foundation rejects mutable history and invalid sealed envelopes.** Seven strict schemas, storage-boundary validation, independent privacy/completeness verification seams, and real SQLite/blob regressions define the source-only contract. Replacement inserts abort alongside updates and deletes even with recursive triggers disabled. Capture indices bind to distinct time slots, including partial final intervals; burst samples cannot manufacture completeness. Required step-up proof gates authorized control-lease contracts. Keychain root creation distinguishes proven absence from errors and never overwrites a race winner. Capture, retrieval services, and shared blob-GC integration remain unexposed.
