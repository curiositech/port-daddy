type: fixed

- **The agent directory explains durable-session salvage holds.** Dormant entries and earlier admitted replacement attempts retain their distinct meaning; held entries cannot be dismissed or started/resumed from the directory. Distinct agent instances no longer collapse by fleet role, and ambiguous role-only controls cannot target a sibling. Exact-session evidence remains available, and no unimplemented hold-clearance action is offered.
