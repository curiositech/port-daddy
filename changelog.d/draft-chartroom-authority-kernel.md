type: added

- **Chartroom now has a remote append-only authority kernel.** Relay accepts short-lived GitHub-verified, account/team/repository/harbor/resource-scoped capabilities and harbor-signed typed events, enforces plan-version, authority-epoch, nonce, idempotency, hash-chain, payload, row, and cost bounds in D1, preserves tombstones and supersession, and returns signed exact-readback and bounded-export receipts without depending on a legacy roadmap snapshot or exposing an Oracle compatibility route.
