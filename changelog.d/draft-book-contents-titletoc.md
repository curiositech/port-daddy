type: changed

- **The Book's contents pages drop their dot leaders and pull every page number tight against its title, per edition.** `website-v2/public/whitepaper/coordination-papers-mega-volume-preamble.tex` now sets both contents pages with `titletoc`: the page number stands 0.6 em after the entry instead of at the right margin behind a run of dots, every level hangs from one left datum, and weight rather than size separates parts from chapters from sections. The generated chapter map (`scripts/generate-mega-whitepaper.mjs`, still driven entirely by `whitepaper/textbook.json`) carries a page number for the first time — `\pageref*{chap:<prefix>}` — and separates its parts with negative space instead of a tinted band. It dispatches on `\pdedition` the way `\pdchapter` and `\pdpart` already do: Swiss floods the "Part I" label with the part's own hue and sets it in the cream surface colour, technical renders the map as a drawing-sheet index with callout-numbered chapters in ochre and burnt orange, and maritime stays near monochrome with the part hue reduced to a thin rule so it does not fight the watercolour plates. The first-edition concordance keeps its `booktabs` rules and no vertical rules, and those rules now take a part hue instead of black. `titlesec` carries the same weights into the body's section heads, at article's own vertical spacing so the Book repaginates by nothing: 549 pages before and after, and `scripts/harbor-research/page_overflow.py` reports zero rows with loss in all three editions.
