type: changed

- **Release candidates now face artifact-bound whole-product journeys.** A shardable harness builds and stages the real `pd` launcher plus companion, proves isolated transport, restart, coordination, repository-family, resource, cleanup, collision, log-redaction, and bounded pressure behavior, reuses existing compiled smokes, and keeps installed macOS, Brew, Porthole, App, Relay/Chartroom, Cloudflare, and production-scale stability proof as explicit required external gates instead of fake passes.
