type: changed

- **The Harbor Economy chapter now carries a worked example in every section and an honest kind on every claim.** `website-v2/public/whitepaper/harbor-economy.tex` had the Book's worst `chapter_lint.py` ratio (18 of 21 sections with no `pdexample`); most of the fix promotes prose the chapter already had — "dramatized" settlement, ceremony, revocation-race, captured-judge, and adversarial-test-market walkthroughs — into `pdexample` blocks using numbers already fixed elsewhere in the chapter, plus two new examples (the smallest three-sided settlement in `\S`thesis, a hand-traced escrow orphaned by an operator exit in `\S`gaps) built the same way. Three untagged claims (the float-plan definition, the local-non-forgeable-identity definition, the underwriting-reserve proposition) now state their honesty-ledger kind and maturity grade in place. The "Reader's map" no longer prints in the Book: it was the one numbered Reader's Map among its siblings, so the generator read it as chapter content and printed it while stripping the unnumbered ones — it is now unnumbered like theirs, which drops it from the Book and keeps it in `harbor-economy-whitepaper.pdf`, where the five-persona routing table is the only routing a reader arriving cold at the submission paper has. Its cast-of-characters box moves into the thesis section where the cast is first used.
