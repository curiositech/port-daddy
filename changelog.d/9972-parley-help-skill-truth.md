type: fixed

- **Parley help and its agent instructions now match the shipped raw protocol and capability-gated Sugar contract.** `pd parley --help` reaches the handler-owned usage instead of global help; the README uses the real raw syntax and names CAP0's fail-closed settlement boundary; the public and contributor skills distinguish the 75 ms peer hint from the 150 ms Sugar card, preserve zero-turn failures, require roadmap rent, and remove stale migration and worktree guidance.
