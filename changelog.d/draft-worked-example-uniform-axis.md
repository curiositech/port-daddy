type: fixed

- **The Book's worked-example continuity braid (Chapter 7) now draws its minute axis to one uniform scale.** The 3-12 and 13-24 minute spans previously rendered at roughly 41-44% of the scale used for the rest of the same axis, with no break marked, so a reader measuring the drawing got the wrong duration; every coordinate in `fig-worked-example.tex` now derives from the same linear minute-to-page map as the axis ticks it prints.
