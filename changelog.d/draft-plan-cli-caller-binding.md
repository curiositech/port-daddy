type: fixed

- Bound plan writes to the selected caller’s credential without confusing display aliases with the session’s verified owner. Checklist numbers now count tasks instead of raw lines; exact labels, clear refusals, and complete append-only plan history prevent accidental edits or silent lost context.
