type: fixed

- The Anchor Protocol's issue--delegate--verify figure is redrawn to the sequence-diagram convention and its caption now claims only what the drawing shows. The root signing key is named once, as the Daemon's own `sk_daemon`/`pk_daemon`, which is what the chapter's delegation algorithm requires and what the ProVerif models and the Rust verifier implement; the chapter's undeclared `pk_root` alias is gone, and the key hierarchy definition now says plainly that the escrowed per-Harbor keypair belongs to Phase 2 and does not root delegation chains. The "offline" fact is drawn as a shaded region rather than written in a note beneath the picture, participants carry activation boxes, the verifier's own loop is a reflex self-message, every communication arrow is the same weight and colour, and message labels are structured so the caption could shrink to two sentences.
