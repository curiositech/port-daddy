type: fixed

- **Purser now budgets every source-bearing request against the exact model context.** STEEL-MAN uses the configured input-heavy model, complete diffs flow through when they fit, oversized diffs carry exact omission receipts and a neutral partial-coverage result, and missing, malformed, or ambiguous reusable-suite receipts force fresh authoring instead of inferred trust.
