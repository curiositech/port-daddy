type: changed

- **Harbor Editor recovery now advertises only its real fail-closed boundary.** The authenticated HTTP scaffold returns `503` until typed sequence-zero receipts, sealed abandonment, canonical Rust Loro validation, verified scope and file authorities, and one atomic P3 released-claim transfer exist; P2 snapshots, `/blob`, notes, generic salvage, and `apply_remote_ops` remain checkpoint/reconnect mechanisms and no CLI, MCP, or SDK alias is exposed.
