type: security

- **Session recovery and lifecycle writes now fail closed on identity ambiguity.** CLI context resolution treats environment identity as an atomic pair, reports conflicting provenances, preserves dormant durable sessions as explicit owner-bound takeover candidates, and refuses first-match completion. The daemon binds done, relink, takeover, end, abandon, phase, remove, note, file-claim, lock, and salvage mutations to verified actor credentials and exact stored owners; raw IPC and direct SQLite mutation paths plus caller-supplied operator override flags are rejected. Managed spawned agents use one private exact-session lifecycle path that waits for transcript finalization, prevents backend launch after a racing kill, abandons failed admission bindings, and applies terminal state exactly once.
