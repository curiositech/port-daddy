type: fixed

- Every sentence of the three new Book captions is now a claim that survives being checked. The claim-signaling threshold figure said its two curves cross zero "within 0.009" of each other when the gap is 0.0091747, said grim trigger leaves the frame "by δ = 0.72" when it leaves at 0.7222, and called δ = 0.30 "the one discount factor the model checker has actually been run at" when the sweep runs eleven and CI runs 0.35 — what is true of 0.30 is that its transcript is the one committed. It also called its mark a diamond, which it is under the chapter preamble and is not under the Book's, where the datum styles render as squares; the caption no longer names the shape. The succession-price figure attached the script's `mu_s = 1e8` assertion to a canary the script runs at a different point entirely, and plotted its boundary mark at 0.3497 while labelling it 0.3496; both are corrected, and the drawing now gives the hand-checkable reason no premium rescues the specialist there. Each fragment's preamble records the per-claim verification behind its caption, including that the audit is against `pd guide` as defined today, so a change to that style forces a re-run rather than inheriting a verdict it did not earn.
- `FIGURE-REGISTER.md`'s `existing figure` column is checked in both directions for the first time: every id a row names must be declared by a `\label` or a listings `label=` somewhere in the chapter sources or the figure corpora, and every drawing a chapter actually `\input`s must be named by some row. It had already drifted — one row named a fragment the Wave 11 triage deleted and which exists nowhere on disk. A row that wants a figure nobody has drawn says `none`, which is an explicit status rather than a silence the reader has to interpret.
- The Book's registry and publication digests are re-derived against the committed PDFs. The ladder redraw made the Book 555 pages and changed two PDFs' bytes after the metadata had already been carried, leaving the page-count guard and the sha256 digest pins describing a Book that no longer existed.
