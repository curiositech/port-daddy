type: changed

- **Jury-rig now searches before it grafts.** `pd jury-rig search` and the shorthand return only bounded skill metadata, while `pd jury-rig graft` is the explicit full-guidance operation. Hybrid ranking, guarded references, and the existing Fleet opt-in remain intact; the ambiguous body-loading `query` command is removed.
- **Legacy skill-system branding is gone from the tracked repository.** Imported research dossiers now describe Port Daddy directly, website skill references link to native repository sources, and a repository-wide regression catches both singular and plural spellings of the retired name.
