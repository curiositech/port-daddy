type: fixed

- Managed Claude Code, Codex, and agy launches share explicit manual, edit-only, and autonomous intent. Native permission bypass requires both requested autonomy and actual Coast Guard confinement proof; disabled or degraded confinement cannot silently authorize a worker.
- Fresh and resumed workers preserve caller guidance without inferring a selection receipt, workspace identity, capture, and primary-plus-cleanup errors. Codex always suppresses eager skill instructions and manual mode never enables automatic approval review. Claude Code and agy disable native skills without disabling hooks; only Claude aliases support requested Squid injection, after wrapper admission, failing closed on installation errors.
- Dispatch admits Claude Code, Codex, and agy only through Conductor; its semantic non-executable plans are not launch authority. Reactive rules preserve explicit intent. Gemini CLI fails closed pending policy-preserving skill suppression; unverified Groq and the incompatible Grok proxy are blocked in selection and execution. Spawn results and current-daemon history expose resolved execution intent alongside confinement evidence.
- Removed the raw dispatch adapter and ambient-account publication fallback. Local worktree creation retains Guard and verifies exact repository, linked-worktree root, and branch before reuse; failed identity checks preserve the target. Publication needs an authorized GitHub App publisher, not yet wired, and preserves completed local work with an attention receipt. Removed the permissionMode compatibility field; malformed Claude settings are preserved byte-for-byte when requested hook installation fails closed.
