type: fixed

- **The Bonded Commons chapter now carries a worked example and an honest epistemic-kind tag on every claim.** Nine of its eighteen sections gained a hand-checkable `pdexample` (capability delegation, Merkle-proof sizing, hard-lock turn counting, crash-detection timing, pairwise trust-negotiation counts, and more), all built from numbers the chapter, its proofs, or its appendix already fix. All twelve previously untagged claims now carry a kind from the honesty ledger, including relabeling the ledger Conservation claim from Theorem to Model-checked property to match what `Conservation.tla`'s bounded TLC run actually establishes. Its dead `\pullquote` tinted-box macro is deleted.
- **The chapter's Reader's Map is back, unchanged, as paper apparatus.** An earlier revision of this branch deleted it as a duplicate of the Book's front matter. That was wrong: the Reader's Map was never printed in the Book. `scripts/generate-mega-whitepaper.mjs` already strips an unnumbered Reader's Map when it assembles the volume — its own comment reads "The standalone PDFs keep all of it; those are papers" — so the section was paper-only routing, working exactly as designed. This chapter's source doubles as a standalone submission paper with no front matter, and the map was its only routing for a reader arriving at the PDF cold; deleting it removed that routing and changed nothing about the Book. The section is restored verbatim from history, in the form the generator expects: `\section*{Reader's Map}` with an `\addcontentsline`, matching the four sibling chapters that already carry that shape.
