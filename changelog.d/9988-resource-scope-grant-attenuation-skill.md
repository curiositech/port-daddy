type: security

- **ResourceScope delegation now has a reusable hostile-chain review skill.** `resource-scope-grant-attenuation` drives the merged policy kernel through multi-hop narrowing, revocation, expiry, repository/world isolation, read-only federation, malformed caveats, and every Harbor-envelope dimension without creating a second authorization implementation.
