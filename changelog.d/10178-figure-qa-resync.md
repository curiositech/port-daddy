type: fixed

- **The Book's figure-QA record set is re-derived against the corpus it actually ships, and can no longer drift from it unnoticed.** `docs/harbor-research/exposition/figures/figcheck/` held 79 geometry records for a live corpus of 56 drawings, overlapping on 52: 27 records described fragments no chapter `\input`s any more (`fig-swk-seven-organs`, `fig-anchor-four-phases`, `fig-he-stack-map`, `legible-swarm-stack-map` and 23 others) and 4 live drawings (`fig-fh-cycle-vs-cut`, `fig-stp-nomint-lineage`, `fig-stp-probation-cliff`, `fig-swk-marker-decay`) had never been checked at all — while `render_figure_audit.py --check` and `check_figure_register.py` both stayed green, because each validates the shape of a record and neither asks whether the record is about anything. All 56 fragments were recompiled under their own chapter's preamble and re-checked (35 clean, 21 with a T1-T5/T8 finding; 22 verdicts on the surviving 52 had changed since their record was written, all but one of them an improvement), and the blocker ledger fell from 61 entries to 21. The new `scripts/harbor-research/check_figcheck_corpus.py` fails when the record set and the live corpus disagree in either direction, deriving that corpus mechanically rather than from a skip list — a fragment counts when a chapter source named in `whitepaper/textbook.json` `\input`s it from its own `figures/` directory and it opens a `tikzpicture`, which is what separates the 56 drawings from the shared `pd-*` macro includes, the `session-*` transcripts, the `tab-*` tabulars, and 12 files on disk no chapter reaches. It runs in `.github/workflows/library-checks.yml` ahead of the render check, is covered by `tests/harbor-research/test_check_figcheck_corpus.py`, and was mutation-tested both ways including the case a count comparison would miss (one record deleted and one junk record added, 56 against 56, still caught). `docs/harbor-research/exposition/figures/INK-AUDIT.md` records the re-derived Tufte ink reading of the same 56 figures.
