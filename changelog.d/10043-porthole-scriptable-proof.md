type: added

- **Agents can now drive Porthole without weakening its picker authority.** An owner-only local socket and stdlib driver expose approved-source lifecycle control, exact-window stills, finite recordings, bounded assertions, and batch scenarios with deterministic structured receipts; no command enumerates unapproved sources, injects input, broadens scope, or overwrites artifacts.
- Concurrent clients keep status responsive during idle connections and recordings. Manual and scripted approvals reject app-wide persistence, source cards reflect the active persistence gate, and still/recording admission rechecks the current picker and signed-launch binding.
