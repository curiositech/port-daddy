type: added

- **The Critical Analysis of 2026-09-08 is now in the critique ledger, and two of its findings are guards.** The thirty-page outside review of the Book was adjudicated the same day in `docs/harbor-research/exposition/critique/ADJUDICATION-2026-09-08.md`, whose own work list ordered a fold into the ledger that never happened — `CA-` matched nothing in `docs/harbor-research/CRITIQUE-LEDGER.md` or `docs/harbor-research/critique-ledger.json` and nothing in the tree linked to the adjudication, so the one question the ledger exists to answer could not be asked of it. Eighty-four rows now carry the review, ids matching the adjudication's, each `Status` set against the tree rather than against the verdict: 6 done with a commit, 20 declined with a refutation or a reason, 58 open with the file and line that show they are open. The review's sharpest structural find turned out to be half-fixed in a way that hid the other half: the shared *drawing* of the four-message cross-harbor transfer moved into `\ifpdbook` and both duplicate checks went green, while the ceremony stayed written out twice — a labelled Protocol at `website-v2/public/whitepaper/harbor-economy.tex:1243` and a hand-written enumerate at `website-v2/public/whitepaper/federated-harbor-whitepaper.tex:415` — which neither sibling can see, since there is no shared `\input` and no shared `\label`. `scripts/harbor-research/check_duplicate_protocols.py` reads the Book branch of each chapter and fails when three or more message names introduce a step in two chapters; `scripts/harbor-research/check_absent_data_captions.py` fails when a figure caption tells the reader its own data is absent, which Figure 4.5's does. Both run in `.github/workflows/library-checks.yml` with the one standing exception recorded by name, both are mutation-tested in `tests/harbor-research/test_check_duplicate_protocols.py` and `tests/harbor-research/test_check_absent_data_captions.py`, and the suite floor that job asserts moves from 212 to the 321 the suite actually has.
