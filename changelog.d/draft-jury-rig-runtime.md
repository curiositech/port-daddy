type: changed

- **Jury-rig now owns native skill discovery and Fleet guidance.** `pd jury-rig`, `jury_rig: true`, and `jury_rig_status()` replace the WinDAGs-coupled live surfaces while preserving BM25 plus Tool2Vec semantic ranking, checkpointed warming, and guarded skill-owned reference reads.
