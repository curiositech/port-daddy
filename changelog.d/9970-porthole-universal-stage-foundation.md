type: added

- **Defined Porthole's universal cooperative-stage and encrypted-evidence foundation.** Body-neutral Agent Harbor contracts cover arbitrary GUI, browser, terminal, editor, operator, and agent perspectives; sealed target descriptors, pre-capture schedule commitments, attributable stage events, revocable control leases, signed completeness and disclosure receipts, and provenance-bound regression receipts replace the Parley-specific framing. An unexposed storage proof seals evidence with the parity-gated `pd-vault` XChaCha20-Poly1305 path before blob persistence, keeps its ledger append-only, publishes no plaintext equality or byte-length correlation fields, and fails closed when exact-target capture, privacy receipts, ciphertext durability, or key custody cannot be verified.
