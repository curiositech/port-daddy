type: added

- **Sugar now turns grounded coordination overlap into a bounded human choice.** Interactive `pd begin` and `pd attention` can show Work separately, Send note, or Resolve together when vector-reviewed semantic relevance and an exact active claim overlap agree; bounded Parleys carry distinct hook context and require unanimous typed settlement before releasing only the evidenced claims, updating plans, and suppressing settled reminders.
