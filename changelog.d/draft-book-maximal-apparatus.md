type: changed

- **The Book's margin column is now used, and the figure set it sits beside is complete.** The margin is 1.3 in of a 7 in trim — 21 per cent of the page width — and on `main` 44.6 per cent of 531 body pages left it entirely empty, with a median of 35 characters on the pages that used it at all. This lands five in-flight branches on one base (`#10197` captions, `#10198` and `#10190` figures, `#10191` figure typography, `#10184` the stack map, `#10192` the manuscript critique) and then fills the column: 93 new `\pdgloss` calls take the Book from 16 glosses to 109, one per term of art at its first use, each definition drawn from the sentence in that chapter that introduces the term. The corpus is committed at `docs/harbor-research/exposition/gloss-corpus.json` and the placer at `scripts/harbor-research/place_glosses.py`, which enforces first-use-only, never places inside a float or a heading's moving argument, and keeps eight source lines between margin devices; every term it declines is reported with the rule that declined it rather than dropped.
- **The Swiss edition's `draw=none` override is on the Book's build path, and it does not make any fill invisible.** `coordination-papers-mega-volume-preamble.tex` sets `\providecommand{\pdedition}{swiss}`, so `figures/pd-figure-language-swiss.tex` loads for the canonical `coordination-papers-mega-volume.pdf` and its `draw=none` on `pd focus fill`, `pd caution fill`, `pd neutral fill`, `pd state` and `pd hatch` is live. Two reviews flagged at HIGH that new fills might therefore render invisible; measured against the `hhpaper` ground (`#FBF7EF`), `pd neutral fill` reads at ΔE 12.9, `pd focus fill` at 21.2 and `pd hatch` at 9.5 — the override removes the region's drawn edge, not the region. The one genuinely soft pair is `pd hatch` against `pd neutral fill` at ΔE 9.0, which is recorded here rather than left for the next reader to rediscover.
