type: added

- **Three pages of the Book that argued a shape in prose and never drew it now carry a figure.** `figures/fig-bc-graduated-trigger.tex` and `figures/fig-bc-oracle-audit-rate.tex` plot the graduated trigger's threshold and the oracle-collusion audit rate in §7.7.4-5, and `figures/tab-bc-settlement-rule.tex` sets the settlement rule as a table rather than a chart because it is a look-up rather than a shape. Each series is read from a committed CSV under `whitepaper/figures/data/` - `graduated-trigger-threshold.csv`, `oracle-audit-threshold.csv` and `audit-tower-depth.csv` - emitted by `whitepaper/figures/data/make-figure-data.py`, and each caption names the file it was drawn from, so the numbers on the printed page can be regenerated instead of trusted. `docs/harbor-research/exposition/figures/PIXEL-JUDGMENT.md` records the per-figure verdicts the drawings came out of.
