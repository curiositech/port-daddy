% TEMPLATE: block / stack diagram (layers, with what each layer owes the next).
% Full-width bands of equal height, heaviest at the layer the chapter is about,
% and a right-hand column saying what each layer provides. No arrows: the stack
% IS the relation, and an arrow between adjacent bands would say nothing.
\begin{figure}[H]
\centering
\pdfigurehue{pdcobalt}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\w{9.95}  \def\h{0.98}
  \foreach \i/\name/\gives/\s in {%
    3/{economy and federation}/{settlement, reputation, cross-machine trust}/{pd neutral fill},
    2/{legibility}/{digest with zoom, review, operator surface}/{pd neutral fill},
    1/{coordination}/{typed acts, commitments, delegation}/{pd focus fill},
    0/{substrate}/{one writer, claims, sessions, bus, memory}/{pd neutral fill}}{
      \draw[\s] (0,{\i*\h}) rectangle (\w,{\i*\h+\h});
      \node[pd title,anchor=west] at (0.22,{\i*\h+\h/2}) {\name};
      \node[pd label,anchor=east,text width=5.6cm,align=right]
        at ({\w-.22},{\i*\h+\h/2}) {\gives};}
  % the floor: not a layer of the system, a dependency the system assumes
  \fill[pd hatch,draw=pdink,line width=.9pt] (0,-\h) rectangle (\w,0);
  \node[pd title,anchor=west] at (0.22,{-\h/2}) {machine floor};
  \node[pd label,anchor=east,text width=5.6cm,align=right] at ({\w-.22},{-\h/2})
    {a file lock, a clock, a socket, a supervisor};
  \draw[pd focus rule,decorate,decoration={brace,amplitude=4pt,mirror,raise=3pt}]
    ({\w+.12},{1*\h}) -- ({\w+.12},{2*\h});
  \node[pd label,rotate=90,anchor=center] at ({\w+.56},{1.5*\h}) {this chapter};
\end{tikzpicture}
\caption{An architectural section through the four-layer coordination stack.
Each band names a layer and what that layer hands the one above it; the shaded
band is this chapter's. The hatched floor is not a layer of the system but the
narrow dependency boundary the substrate assumes: a file lock, a clock, a
socket and a supervisor. [internal]}
\label{fig:tpl-block-stack}
\end{figure}
