% TEMPLATE: nested sets (containment, attenuation, "strictly inside").
% Concentric edged rectangles on a common origin, each labelled at its own top
% edge; the containment IS the claim, so nothing else competes for attention.
\begin{figure}[H]
\centering
\pdfigurehue{pdviolet}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\ox{1.30}   \def\oy{0.30}
  \foreach \w/\h/\s/\name in {%
    8.20/4.20/{pd focus fill}/{root grant},
    5.40/2.85/{pd ready fill}/{child},
    2.90/1.55/{pd neutral fill}/{grandchild}}{
      \draw[\s] (\ox,\oy) rectangle ({\ox+\w},{\oy+\h});
      \node[pd label,anchor=south east] at ({\ox+\w-.14},{\oy+\h+.08}) {\name};}
  \draw[pd rule,-{Stealth[length=1.9pt 3.2,width=1.5pt 2.4]}] (\ox,-0.35) -- ({\ox+8.70},-0.35);
  \node[pd label,anchor=west] at ({\ox+8.70},-0.35) {minutes};
  \foreach \x/\l in {2.90/{5}, 5.40/{15}, 8.20/{60}}{
    \draw[pd tick] ({\ox+\x},-0.25) -- ({\ox+\x},-0.45);
    \node[pd label,anchor=north] at ({\ox+\x},-0.55) {\l};}
  \node[pd label,anchor=east,text width=1.15cm,align=right] at ({\ox-.22},{\oy+2.1}) {rights};
  \node[pd note,anchor=north west,text width=10.2cm] at (0.05,-1.10)
    {attenuation is conjunctive: a child is inside its parent on both axes at once};
\end{tikzpicture}
\caption{Capability attenuation as nested sets. Each delegated capability sits
strictly inside its parent on both axes at once --- a shorter lifetime
(horizontal, in minutes) and a narrower right (vertical) --- so a child that is
longer-lived \emph{or} broader is outside its parent's rectangle and refused.
The nesting is the claim; there is nothing else in the picture. [internal]}
\label{fig:tpl-nested-sets}
\end{figure}
