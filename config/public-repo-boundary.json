{
  "version": 1,
  "purpose": "Prevent internal ideation residue, local-only sandboxes, and private model-specific instruction copies from leaking into the public GitHub repo. The one tracked CLAUDE.md is a thin public entrypoint to canonical AGENTS.md, not an independent policy copy. This is an initial denylist contract, not the full future mirror/export manifest.",
  "denylistedTrackedPathRegexes": [
    "^\\.spark/",
    "^\\.spider/",
    "^\\.dogfood/",
    "^\\.remember/",
    "^agentsd_mocks/",
    "^v0-agentsd-main/",
    "^GEMINI\\.md$",
    "^\\.aider\\.chat\\.history\\.md$"
  ],
  "requiredGitignoreEntries": [
    ".spark/",
    ".spider/",
    ".dogfood/",
    ".remember/",
    "agentsd_mocks/",
    "v0-agentsd-main/",
    "GEMINI.md",
    ".aider.chat.history.md"
  ]
}
