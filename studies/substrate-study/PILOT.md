# S2 pilot

`./run.sh --pilot`: corpus `py-library` (psf/requests, the pinned 600-commit
window in `corpora.json`, high file-overlap regime), all 7 substrate/p
combinations (see CHANGELOG.md for why 7, not S2.4's "6"), N ∈ {2, 4, 8},
both temperaments, seeds 1-3, 200 tasks per run. 126 cells total.

**This is a pilot, not the study.** Three seeds per cell is far below the
pre-registered 20; no significance claims are made anywhere in this
document, only what the pilot suggests. The full S2.4 design (3 corpora,
N up to 16, 20 seeds) has not been run — that is `./run.sh` with no
arguments, and it is what `REPORT.md` will eventually be built from.

## Medians per (substrate, N, temperament), across seeds 1-3

PLACEHOLDER — filled in from `results/py-library/*.csv` after the pilot run
completes.

## Pilot-only notes on H1-H4

**H1 (confinement).** PLACEHOLDER.

**H2 (the real question).** PLACEHOLDER.

**H3 (does enforcement matter for coordination, or only confinement?).** PLACEHOLDER.

**H4 (granularity).** PLACEHOLDER.
