% Figure 6 — Signal Detection Theory view of the operator's forced-zoom decision.
% Two overlapping distributions (safe vs dangerous actions); the criterion beta
% slides; the asymmetric cost inequality pins where it should sit.
% Design: error-region labels sit inside their regions (no pointer arrows);
% the cost reasoning lives in the caption, the canvas keeps only the inequality.
\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhink}{HTML}{1B1712}
\providecolor{hhcobalt}{HTML}{003FB8}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}

\begin{figure}[H]
\centering
\resizebox{0.82\textwidth}{!}{%
\begin{tikzpicture}[font=\small,
  declare function={
    safe(\x)   = 5*exp(-((\x-3)^2)/1.4);
    danger(\x) = 5*exp(-((\x-6)^2)/1.4);
  }]
% error regions (under the curves, drawn first)
\fill[hhcobalt!25,domain=0:5,samples=80,smooth] plot (\x,{danger(\x)}) -- (5,0) -- (0,0) -- cycle;
\fill[hhteal!30,domain=5:9.5,samples=80,smooth] plot (\x,{safe(\x)}) -- (9.5,0) -- (5,0) -- cycle;

% axes
\draw[->,thick] (0,0) -- (10.2,0) node[right,font=\footnotesize]{evidence of danger (operator's read)};
\draw[->,thick] (0,0) -- (0,5.8) node[above,font=\footnotesize]{density};

% distributions
\draw[hhteal,very thick,domain=0:9.5,samples=120,smooth] plot (\x,{safe(\x)});
\draw[hhcobalt,very thick,domain=0:9.5,samples=120,smooth] plot (\x,{danger(\x)});
\node[text=hhink,font=\bfseries\footnotesize] at (2.3,5.4) {safe actions};
\node[text=hhink,font=\bfseries\footnotesize] at (7.4,5.4) {dangerous actions};

% criterion line beta
\draw[hhamber,very thick,dashed] (5.0,0) -- (5.0,5.55)
   node[above,text=hhink,font=\bfseries\footnotesize]{criterion $\beta$};
\draw[->,thick,hhamber] (5.2,4.6) -- (6.0,4.6) node[right,font=\scriptsize,text=hhink]{lax (fewer zooms)};
\draw[->,thick,hhamber] (4.8,4.6) -- (4.0,4.6) node[left,font=\scriptsize,text=hhink]{stringent (more zooms)};

% error-region labels: miss fits inside its region; false alarm is a thin
% sliver, so its label sits clear of the curves with one short leader.
\node[text=hhink,font=\scriptsize,align=center] at (4.25,0.8) {miss\\(waved through)};
\node[text=hhink,font=\scriptsize,align=center,anchor=west] (fa) at (6.6,1.3) {false alarm\\(zoomed safe diff)};
\draw[hhink,thin] (fa.west) -- (5.35,0.12);

% d-prime bracket
\draw[<->,thick,hhink] (3,-0.7) -- (6,-0.7) node[midway,below,font=\footnotesize]{$d'$ (operator sensitivity)};

% the one canvas-worthy fact
\node[draw=hhebony,thick,fill=hhpaper,rounded corners=2pt,inner sep=5pt,
      font=\footnotesize,align=center] at (8.6,3.2)
  {\textbf{asymmetric cost}\\[1pt] $C_{\text{miss}}\!\gg\!C_{\text{fa}}$};
\end{tikzpicture}%
}
\caption{The operator's forced-zoom decision as Signal Detection Theory
(Def.~\ref{def:sdt}, Eq.~\ref{eq:sdt}). Most actions are safe (left curve); a few
are dangerous (right). Zooming triggers when evidence of danger exceeds criterion $\beta$. Moving $\beta$ left
is more stringent: it causes more zooms, fewer misses, and more false alarms. Moving $\beta$ right
is more lax: it minimizes attention-seconds but increases misses. Because $C_{\text{miss}}\gg C_{\text{fa}}$, the
optimal criterion sits well to the left of the throughput optimum: \emph{the miss-cost term is what forces the
zoom}. Sensitivity $d'$ (curve separation) is improved by better legibility; $\beta$
is the policy knob $p(r,s,v)$.}
\label{fig:sdt}
\end{figure}
