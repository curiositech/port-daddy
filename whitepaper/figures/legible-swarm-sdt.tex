% Figure I.6 --- forced-zoom decision as signal detection theory.
% The chart is intentionally deterministic and source-controlled: no image asset
% carries labels, equations, or decision semantics.
\begin{figure}[H]
\centering
\resizebox{0.88\textwidth}{!}{%
\begin{tikzpicture}[
  font=\small,
  declare function={
    safe(\x)=4.55*exp(-((\x-3.05)*(\x-3.05))/1.58);
    danger(\x)=4.55*exp(-((\x-6.02)*(\x-6.02))/1.58);
  }
]
  % External header: the decision rule belongs above, never on the curves.
  \node[anchor=west, font=\bfseries\footnotesize, text=hhink] at (0,6.18)
    {FORCED-ZOOM DECISION};
  \node[anchor=east, font=\footnotesize, text=hhgray] at (10.2,6.18)
    {zoom if evidence $\geq \beta$};
  \draw[hhgray!38, line width=.35pt] (0,5.88) -- (10.2,5.88);

  % The data field contains only axes, distributions, their minimal names, and beta.
  \fill[hhpaper] (-.35,-.18) rectangle (10.65,5.58);
  \draw[hhgray!55, line width=.45pt] (-.35,-.18) rectangle (10.65,5.58);
  \draw[-{Latex[length=2mm]}, hhink, line width=.65pt] (0,0) -- (10.35,0)
    node[anchor=west, font=\footnotesize] {evidence of danger};
  \draw[-{Latex[length=2mm]}, hhink, line width=.65pt] (0,0) -- (0,5.35)
    node[anchor=south, font=\footnotesize] {density};

  % Error regions are visual only; their meanings appear in the external legend band.
  \path[fill=hhcobalt!18]
    plot[smooth, domain=3.72:5, samples=80] (\x,{danger(\x)}) -- (5,0) -- (3.72,0) -- cycle;
  \path[fill=hhteal!20]
    plot[smooth, domain=5:8.34, samples=100] (\x,{safe(\x)}) -- (8.34,0) -- (5,0) -- cycle;

  \draw[hhteal, line width=1.15pt]
    plot[smooth, domain=0:8.34, samples=120] (\x,{safe(\x)});
  \draw[hhcobalt, dashed, dash pattern=on 5pt off 2.5pt, line width=1.15pt]
    plot[smooth, domain=1.15:10.05, samples=140] (\x,{danger(\x)});
  \draw[hhamber, dashed, dash pattern=on 2.2pt off 2pt, line width=1.05pt] (5,0) -- (5,5.2);
  \node[anchor=south, font=\bfseries\footnotesize, text=hhamber] at (5,5.2) {$\beta$};

  \node[anchor=south, font=\bfseries\footnotesize, text=hhteal] at (3.03,4.8)
    {safe actions};
  \node[anchor=south, font=\bfseries\footnotesize, text=hhcobalt] at (6.02,4.8)
    {dangerous actions};

  % External error legend: separate from the data field and visually keyed to the tails.
  \draw[hhgray!38, line width=.35pt] (-.35,-.55) -- (10.65,-.55);
  \draw[hhgray!32, line width=.35pt] (5.15,-.76) -- (5.15,-1.48);
  \node[anchor=north west, text width=43mm, align=left, font=\scriptsize, text=hhink]
    at (0,-.78) {\textcolor{hhcobalt}{\rule{1.7ex}{.75pt}}\ \textbf{Miss}\\dangerous action waved through};
  \node[anchor=north west, text width=43mm, align=left, font=\scriptsize, text=hhink]
    at (5.43,-.78) {\textcolor{hhteal}{\rule{1.7ex}{.75pt}}\ \textbf{False alarm}\\safe action forced to zoom};

  % Policy consequences are a second exterior band; no arrows cross the data field.
  \draw[hhgray!38, line width=.35pt] (-.35,-1.68) -- (10.65,-1.68);
  \draw[hhgray!32, line width=.35pt] (5.15,-1.88) -- (5.15,-2.5);
  \node[anchor=north west, text width=43mm, align=left, font=\scriptsize, text=hhink]
    at (0,-1.9) {\textbf{Lower $\beta$}\\more zooms; fewer misses};
  \node[anchor=north west, text width=43mm, align=left, font=\scriptsize, text=hhink]
    at (5.43,-1.9) {\textbf{Higher $\beta$}\\fewer zooms; more misses};
\end{tikzpicture}%
}
\caption{\textbf{Forced zoom trades benign review for fewer waved-through dangers.}
The solid teal distribution represents safe actions; the dashed cobalt distribution represents dangerous actions. The forced-zoom policy triggers when evidence reaches the amber threshold $\beta$. The shaded cobalt tail is the miss region (danger waved through); the shaded teal tail is the false-alarm region (safe action reviewed). Lowering $\beta$ sends more work to a human and reduces misses; raising it saves attention but increases misses. The separation $d'$ is the quality of the operator's read, and the cost asymmetry $C_{\mathrm{miss}} \gg C_{\mathrm{fa}}$ justifies a cautious threshold.}
\label{fig:sdt}
\end{figure}
