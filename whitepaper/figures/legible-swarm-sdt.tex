% Figure 6 — Signal Detection Theory view of the operator's forced-zoom decision.
% Two overlapping distributions (safe vs dangerous actions); the criterion beta
% slides; the asymmetric cost matrix pins where it should sit.
\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhink}{HTML}{1B1712}
\providecolor{hhcobalt}{HTML}{003FB8}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}

\begin{figure}[H]
\centering
\resizebox{0.9\textwidth}{!}{%
\begin{tikzpicture}[font=\small,
  declare function={
    safe(\x)   = 5*exp(-((\x-3)^2)/1.4);
    danger(\x) = 5*exp(-((\x-6)^2)/1.4);
  }]
% axes
\draw[->,thick] (0,0) -- (10.2,0) node[right,font=\footnotesize]{evidence of danger (operator's read)};
\draw[->,thick] (0,0) -- (0,5.8) node[above,font=\footnotesize]{density};

% distributions
\draw[hhteal,very thick,domain=0:9.5,samples=120,smooth] plot (\x,{safe(\x)});
\draw[hhcobalt,very thick,domain=0:9.5,samples=120,smooth] plot (\x,{danger(\x)});
\node[text=hhink,font=\bfseries\footnotesize] at (2.3,5.4) {safe actions};
\node[text=hhink,font=\bfseries\footnotesize] at (7.4,5.4) {dangerous actions};

% criterion line beta
\draw[hhamber,very thick,dashed] (5.0,0) -- (5.0,5.55)
   node[above,text=hhink,font=\bfseries\footnotesize]{criterion $\beta$};
\draw[->,thick,hhamber] (5.2,3.1) -- (6.0,3.1) node[right,font=\scriptsize,text=hhink]{strict};
\draw[->,thick,hhamber] (4.8,3.1) -- (4.0,3.1) node[left,font=\scriptsize,text=hhink]{lax};

% error regions
\fill[hhcobalt!25,domain=0:5,samples=80,smooth] plot (\x,{danger(\x)}) -- (5,0) -- (0,0) -- cycle;
\fill[hhteal!30,domain=5:9.5,samples=80,smooth] plot (\x,{safe(\x)}) -- (9.5,0) -- (5,0) -- cycle;
\node[text=hhink,font=\scriptsize,align=center] at (3.55,0.55) {MISS\\(waved through)};
\node[text=hhink,font=\scriptsize,align=center] at (7.25,0.55) {false alarm\\(zoomed safe diff)};
\draw[hhcobalt,thin,-{Latex[length=1.4mm]}] (4.0,0.7) -- (4.55,0.55);
\draw[hhteal,thin,-{Latex[length=1.4mm]}] (6.75,0.7) -- (5.6,0.4);

% d-prime bracket
\draw[<->,thick,hhink] (3,-0.6) -- (6,-0.6) node[midway,below,font=\footnotesize]{$d'$ (operator sensitivity)};

% cost matrix
\node[draw=hhebony,thick,fill=hhpaper,rounded corners=2pt,inner sep=5pt,
      font=\footnotesize,align=left,text width=44mm] at (12.6,3.2)
  {\textbf{asymmetric cost}\\[2pt]
   $C_{\text{miss}}\!\gg\!C_{\text{fa}}$\\[2pt]
   a miss = irreversible damage;\\a false alarm = wasted seconds.\\[3pt]
   $\Rightarrow$ set $\beta$ where \emph{expected cost} (Eq.~\ref{eq:sdt}) is minimal,
   \emph{not} where throughput is maximal.};
\end{tikzpicture}%
}
\caption{The operator's forced-zoom decision as Signal Detection Theory
(Def.~\ref{def:sdt}, Eq.~\ref{eq:sdt}). Most actions are safe (left curve); a few
are dangerous (right). The operator's read places each action somewhere on the
evidence axis; the criterion $\beta$ decides zoom-or-wave. A lax $\beta$ minimizes
attention-seconds but maximizes \emph{misses} --- the danger-optimizing degenerate
the naive ``seconds-per-decision'' objective rewards. Because $C_{\text{miss}}\gg
C_{\text{fa}}$, the optimal criterion sits well to the left of the throughput
optimum: \emph{the miss-cost term is what forces the zoom}. Sensitivity $d'$ (curve
separation) is improved by better legibility; $\beta$ is the policy knob $p(r,s,v)$.}
\label{fig:sdt}
\end{figure}
