% Reader question: which control prevents a same-user bypass, and which controls only observe or respond after the effect?
% Claim: only complete pre-effect mediation removes the capability; built in-band controls detect and remediate after an effect can already be durable.
% Evidence: sec:enforcement-gap names guard/shim/hooks as built, push broker as designed, credential broker and OS/VM isolation as vision [internal].
% Must distinguish: prevention before commit, the present direct bypass, detection after commit, and remediation that cannot erase durable history.
% Grammar: UML component path with one bypass and a post-effect branch; rejected a maturity stack because it would hide when each layer acts.
% Five-second test: the request reaches the protected effect only through mediation in the target design, while today's dashed bypass remains visible.
\begin{figure}[H]
\centering
\pdfigurehue{pdcobalt}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\yflow{4.62}
  \node[pd artifact,minimum width=1.65cm] (agent) at (.92,\yflow)
    {agent};

  \node[pd uml component,pd focus outline,minimum width=4.85cm,
        minimum height=2.72cm] (prevent) at (4.72,\yflow) {};
  \pdumlicon{prevent}
  \node[pd kind,anchor=west] at (2.53,5.48)
    {prevent \textperiodcentered\ before effect};
  \node[pd label,anchor=west,align=left,text width=3.95cm] at (2.53,4.73)
    {push broker --- \textsc{designed}\\credential broker --- \textsc{vision}};
  \node[pd climax state,minimum width=3.85cm,minimum height=.58cm]
    at (4.72,3.72) {OS/VM boundary --- \textsc{vision}};

  \node[pd focus state,minimum width=2.05cm] (effect) at (9.55,\yflow)
    {protected\\effect};
  \draw[pd focus arrow,shorten <=3pt,shorten >=3pt]
    (agent.east) -- (prevent.west);
  \draw[pd focus arrow,shorten <=3pt,shorten >=3pt]
    (prevent.east) -- (effect.west);

  % The same-user path is present today and avoids the pre-effect component.
  \draw[pd breach arrow,rounded corners=4pt,shorten <=3pt,shorten >=3pt]
    (agent.south) -- ++(0,-1.26) -- ($(effect.south)+(0,-1.26)$) -- (effect.south);
  \node[pd breach label,anchor=north] at (5.20,2.83)
    {same-user bypass today};

  \node[pd legible state,minimum width=4.10cm,minimum height=1.58cm,
        align=left,text width=3.46cm]
    (detect) at (2.72,.88)
    {\pdfiglabelbold detect \textperiodcentered\ observe\\
     guard \textperiodcentered\ shim \textperiodcentered\ hooks\\monitor};

  \node[pd breach state,minimum width=4.10cm,minimum height=1.58cm,
        align=left,text width=3.46cm]
    (repair) at (7.87,.88)
    {\pdfiglabelbold remediate \textperiodcentered\ respond\\
     flag \textperiodcentered\ revoke \textperiodcentered\ halt\\compensate};

  \draw[pd thin arrow,shorten >=3pt]
    (effect.south) to[out=-90,in=25] (detect.north);
  \draw[pd breach arrow,shorten <=3pt,shorten >=3pt]
    (detect.east) -- (repair.west);
  \node[pd note,anchor=north] at (7.87,-.04)
    {response cannot erase an already durable effect};
\end{tikzpicture}
\caption{A UML path separates pre-effect mediation from post-effect detection
and remediation. The built controls record and respond; only the proposed
OS/VM boundary removes the same-user capability before the effect. [internal]}
\label{fig:swk-enforcement-layers}
\end{figure}
