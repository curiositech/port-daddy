% Brief (tikz-diagram-craft). Register row ch1-42 [should], desert
% Sec. 13.2 "The enforcement gate proves; it does not yet compel".
% Reader question: which layer of enforcement actually stops a
%   non-cooperating same-user agent, and which merely proves what happened?
% Claim: prevention is pre-effect complete mediation; detection observes an
%   event, and remediation responds after detection. Only a separate-user
%   (or separate-VM) boundary closes today's same-user bypass.
% Evidence: whitepaper/single-writer-kernel.tex sec:enforcement-gap: the
%   Coordination Guard, shim and hook harness (Built, in-band, advisory);
%   a server-side branch-protection-plus-push-broker (Designed); a
%   credential broker (Vision); a separate-user kernel (Vision, the only
%   layer that removes the capability rather than refusing the verb).
% Must distinguish: pre-effect prevention from post-effect detection and
%   remediation; the present same-user bypass; what each proposed prevention
%   component removes; remediation cannot erase an already durable effect.
% Grammar: UML component path with one explicit bypass and a post-commit
%   detection/remediation branch. Rejected: a maturity stack, which implies
%   one cumulative wall and hides when each boundary acts.
% Five-second test: "prevention sits before the effect; detection and
%   remediation sit after it; the red bypass remains until OS/VM isolation".
\begin{figure}[H]
\centering
\pdfigurehue{pdcobalt}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\yflow{4.55}
  \node[pd uml component,minimum width=1.75cm] (agent) at (.95,\yflow)
    {agent};
  \pdumlicon{agent}

  \node[pd uml component,pd focus outline,minimum width=4.70cm,
        minimum height=2.65cm] (prevent) at (4.60,\yflow) {};
  \pdumlicon{prevent}
  \node[pd kind,anchor=west] at (2.48,5.45) {prevent \textperiodcentered\ before commit};
  \node[pd label,anchor=west,align=left,text width=3.85cm] at (2.48,4.65)
    {push broker -- \textsc{designed}\\credential broker -- \textsc{vision}\\OS/VM isolation -- \textsc{vision}};

  \node[pd uml component,minimum width=2.40cm] (effect) at (8.75,\yflow)
    {protected\\effect};
  \pdumlicon{effect}
  \draw[pd focus arrow,shorten >=3pt] (agent.east) -- (prevent.west);
  \draw[pd focus arrow,shorten >=3pt] (prevent.east) -- (effect.west);

  % The direct same-user route exists today and bypasses the pre-effect gate.
  \draw[pd breach arrow,rounded corners=4pt,shorten >=3pt]
    (agent.south) -- ++(0,-1.35) -- ($(effect.south)+(0,-1.35)$) -- (effect.south);
  \node[pd breach label,anchor=north] at (5.00,2.66)
    {same-user bypass today};

  \node[pd uml component,minimum width=4.05cm,minimum height=1.80cm]
    (detect) at (2.70,1.10) {};
  \pdumlicon{detect}
  \node[pd kind,anchor=west] at (.92,1.42)
    {detect \textperiodcentered\ observe};
  \node[pd label,anchor=west,align=left,text width=3.15cm] at (.92,.83)
    {guard \textperiodcentered\ shim \textperiodcentered\ hooks \textperiodcentered\ monitor};

  \node[pd uml component,minimum width=4.05cm,minimum height=1.80cm]
    (repair) at (7.85,1.10) {};
  \pdumlicon{repair}
  \node[pd kind,anchor=west] at (6.07,1.42)
    {remediate \textperiodcentered\ respond};
  \node[pd label,anchor=west,align=left,text width=3.15cm] at (6.07,.83)
    {flag \textperiodcentered\ revoke \textperiodcentered\ halt \textperiodcentered\ compensate};

  \draw[pd thin arrow,shorten >=3pt] (effect.south) to[out=-90,in=25] (detect.north);
  \draw[pd arrow,shorten >=3pt] (detect.east) -- (repair.west);
  \node[pd note,anchor=north] at (7.85,-.02)
    {response cannot erase an already durable effect};
\end{tikzpicture}
\caption{A component path separates pre-effect prevention from post-effect
detection and remediation. Built in-band controls can record and respond,
but only complete mediation at the designed separate-user or separate-VM
boundary removes the same-user bypass. [internal]}
\label{fig:swk-enforcement-layers}
\end{figure}
