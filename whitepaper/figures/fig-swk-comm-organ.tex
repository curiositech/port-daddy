% Figure — the communication organ: the L0 tube envelope is the carrier that
% the (specified) L1 typed speech act rides inside; stigmergic markers decay;
% and the two delegation objects are disambiguated. The asides ("structural
% defense against blackboard-rot", "never write bare delegation chain") live
% in the caption.

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D8C7A6}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhink}{HTML}{1B1712}
\providecolor{hhcobalt}{HTML}{003FB8}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}

\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[
  font=\small,
  box/.style={draw=hhebony,thick,rounded corners=2pt,fill=hhpaper,inner sep=5pt,align=left},
  built/.style={draw=hhink,line width=1.2pt},
  designed/.style={draw=hhcobalt,line width=1.2pt,dash pattern=on 3pt off 2pt},
]

% ─── The nested envelope: bus envelope wraps the typed speech act ──────────
\node[box,built,fill=hhsand!30,minimum width=88mm,minimum height=42mm,anchor=north west] (l0env) at (0,0) {};
\node[anchor=north west,align=left,text width=82mm,font=\small]
  at ([xshift=2.5mm,yshift=-2mm]l0env.north west)
  {{\bfseries The bus envelope}\hfill{\footnotesize\textsf{implemented}}\\[2pt]
   durable, ordered, at-least-once;\\ channel publish/subscribe + history cursor};
\node[box,designed,fill=hhpaper,text width=72mm,anchor=north west] (l1perf) at (5mm,-24mm)
  {{\bfseries Typed speech act (rides \emph{inside})}\hfill{\footnotesize\textsf{specified}}\\[2pt]
   a performative, a conversation id,\\ a coordination lineage, a reply deadline};

% ─── Stigmergic-marker decay panel ────────────────────────────────────────
\node[box,built,fill=hhsand!30,text width=66mm,minimum height=42mm,anchor=north west] (pher) at (94mm,0)
  {{\bfseries Stigmergic markers}\hfill{\footnotesize\textsf{implemented}}\\[2pt]
   numeric coordination signals in shared state;\\
   per-kind exponential decay $w \cdot r^{\,\Delta t}$,\\
   computed on a background tick \emph{and} at read time};

% ─── The two delegation objects (nomenclature fix) ────────────────────────
\node[box,built,fill=hhsand!30,text width=78mm,anchor=north west] (authchain) at (0,-50mm)
  {{\bfseries Authorization chain}\hfill{\footnotesize\textsf{implemented}, verified}\\[2pt]
   a chain of digital signatures.\\ \emph{Proves who authorized whom.}};
\node[box,designed,fill=hhpaper,text width=78mm,anchor=north west] (deltrace) at (94mm,-50mm)
  {{\bfseries Coordination lineage}\hfill{\footnotesize\textsf{specified}}\\[2pt]
   the task-lineage for loop detection.\\ \emph{Prevents ping-pong.}};

\draw[->,>=Latex,thick,hhink] (deltrace.west) --
  node[above,font=\scriptsize,align=center]{carried \emph{inside}}
  (authchain.east);

\end{tikzpicture}%
}
\caption{The communication organ. The \textbf{bus} envelope --- durable, ordered,
at-least-once, history-cursored (\textsf{implemented}) --- is the carrier
\emph{into which} the typed speech act is layered; the kernel ships the envelope,
the coordination layer ships the semantics that ride inside it. \textbf{Stigmergic
markers} implement coordination with per-kind exponential decay computed both on a
background tick and \emph{at read time}, so the anti-blackboard-rot property is
provided by the substrate rather than left to discipline. Finally, the figure fixes
a dangerous naming collision --- never write bare ``delegation chain,'' it is two
objects: the \emph{authorization chain} is the hop-bound, attenuation-monotonic,
anti-replay chain of signatures that proves \emph{who authorized whom}; the
\emph{coordination lineage} is the coordination object that prevents ping-pong ---
a different thing entirely, carried \emph{inside} the authorization chain.}
\label{fig:swk-comm-organ}
\end{figure}
