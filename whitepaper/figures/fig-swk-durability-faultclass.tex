% Figure 4 — durability is a fault-reach boundary, not a storage checklist.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm,
  cell/.style={pd state,minimum width=2.5cm,minimum height=1.0cm,text width=2.3cm,
    font=\footnotesize,align=center}]
  % column heads
  \foreach \x/\lbl in {2.1/{process crash},4.75/{OS crash},7.4/{power loss}}
    \node[pd row label,anchor=south,text width=2.3cm,align=center] at (\x,4.4) {\lbl};
  % row labels
  \node[pd row label,anchor=east,text width=2.95cm,align=right] at (-.1,3.15) {synchronous = NORMAL};
  \node[pd row label,anchor=east,text width=2.95cm,align=right] at (-.1,1.55) {synchronous = FULL};
  % row 1: NORMAL
  \node[cell,pd focus fill] at (2.1,3.15) {durable};
  \node[cell,pd caution fill] at (4.75,3.15) {may lose\\last commits};
  \node[cell,pd caution fill] at (7.4,3.15) {may lose\\last commits};
  % row 2: FULL
  \node[cell,pd focus fill] at (2.1,1.55) {durable};
  \node[cell,pd focus fill] at (4.75,1.55) {durable};
  \node[cell,pd focus fill] at (7.4,1.55) {durable};
  % note
  \node[pd note,align=center,text width=8.0cm] at (4.75,-.05)
    {the default trades the power-loss cell for one fsync per commit};
\end{tikzpicture}
\caption{Durability by fault class under the two synchronization levels. The
reference implementation ships \texttt{synchronous=NORMAL}: a process crash
never loses a returned write, but an OS crash or power loss can roll back the
most recent commits. \texttt{synchronous=FULL} buys the power-loss cell too,
at the cost of an fsync on every commit. [internal]}
\label{fig:swk-durability-faultclass}
\end{figure}
