% Figure 14 — sequence diagram, redrawn to spec [ch4.md 4.14]. Three
% lifelines, four messages top to bottom, no crossing: operator grants once,
% agents act under the grant, the daemon gates or escalates every act, and
% the override is the one edge running straight from operator to daemon.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=0.82cm,y=1cm]
  \node[pd actor] (op) at (0,8.0) {\textbf{operator}\\[1pt]\footnotesize author};
  \node[pd actor] (dm) at (5.0,8.0) {\textbf{daemon}\\[1pt]\footnotesize actor};
  \node[pd actor] (ag) at (10.0,8.0) {\textbf{agents}\\[1pt]\footnotesize multitude};

  \draw[pd guide] (0,7.35) -- (0,0.3);
  \draw[pd guide] (5.0,7.35) -- (5.0,0.3);
  \draw[pd guide] (10.0,7.35) -- (10.0,0.3);

  % 1. operator grants the daemon a scoped grant g
  \draw[pd focus arrow] (0,6.1) -- (5.0,6.1);
  \node[pd direct label,text=hhteal,anchor=south] at (2.5,6.2) {scoped grant g};

  % 2. an agent's act arrives at the daemon under g
  \draw[pd arrow] (10.0,4.6) -- (5.0,4.6);
  \node[pd direct label,anchor=south] at (7.5,4.7) {act a1 under g};

  % 3. the daemon gates or escalates that act
  \draw[pd arrow] (5.0,3.1) -- (10.0,3.1);
  \node[pd direct label,anchor=south] at (7.5,3.2) {gate / escalate};

  % 4. the override: the one edge from operator to daemon
  \draw[pd caution arrow] (0,1.6) -- (5.0,1.6);
  \node[pd direct label,text=hhamber,anchor=south] at (2.5,1.7) {override: revoke g};

  \node[pd note,anchor=north,align=center,text width=7.6cm] at (5.0,-0.1)
    {\hyphenpenalty=10000\exhyphenpenalty=10000
     one grant; the daemon gates every act;\\only the operator overrides};
\end{tikzpicture}
\caption{The grant-and-gate sequence, three lifelines and four messages, no
crossing. The operator grants once (message 1: scoped grant g). Agents act
under that grant (message 2), and the daemon gates or escalates every such
act (message 3) rather than granting again. The override (message 4) is the
one edge running straight from operator to daemon, bypassing the agents
entirely. Idealized to one representative act; a live fleet repeats messages
2--3 per act, not per grant [internal].}
\label{fig:roles}
\end{figure}
