% Brief (tikz-diagram-craft). Figure 4.1, The Legible Swarm.
% Reader question: what can actually happen when three independently launched
%   agents edit the same developer settings file from the same revision?
% Claim: an ungoverned overlap can converge, break loudly, or remain valid and
%   fail later; visible claims, locks, and downstream evidence are what let an
%   operator distinguish those cases.
% Evidence: one concrete .vscode/settings.json base revision; three proposed
%   key edits; a pointer claim, a whole-file lock, and a second pointer claim;
%   three possible resolutions and their first observable consequences
%   [internal, pedagogical counterexample].
% Must distinguish: launch source from edit intent; pointer claim from file
%   lock; syntactic failure from semantic failure; test-time observation from
%   downstream consequence.
% Grammar chosen: one horizontal three-lane schedule feeding three aligned
%   resolution traces. The common time rail exposes concurrency; the small
%   multiples let the reader compare consequences without re-reading inputs.
% Grammar rejected: actor bubbles around settings.json. A membership graph
%   would show who touched the file but erase read revision, order, overlap,
%   and the delay between a green mutation score and the downstream failure.
% Five-second test: three agents read a1c9; one overlap is safe, one breaks now,
%   and one stays green until a later release consumes the hidden fixture.
\begin{figure}[H]
\centering
\pdfigurehue{pdteal}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\laneleft{2.95}
  \def\laneright{10.35}
  \def\outy{-1.05}
  \tikzset{
    launch/.style={pd title,anchor=east,text width=2.45cm,align=right},
    patch/.style={pd mono label,fill=pdpage,align=left,text width=2.35cm},
    signal/.style={pd label,fill=pdpage,inner xsep=3pt,inner ysep=2pt,
      text width=2.55cm},
    result/.style={text width=2.75cm,align=center,minimum height=1.55cm,
      inner xsep=5pt,inner ysep=7pt}
  }

  % Concrete shared base: every outcome below starts here.
  \node[pd artifact,text width=9.55cm,align=left,anchor=south west] (base) at (0.35,5.55)
    {\textbf{shared input}\quad \texttt{.vscode/settings.json @ a1c9}\\
     \texttt{formatOnSave=false}\quad \texttt{tsdk=node\_modules/...}\\
     \texttt{search.exclude=\{"**/dist": true\}}};
  \node[pd label,anchor=north,text width=9.55cm,align=center] at (5.15,5.42)
    {all three read \texttt{a1c9}; B's file lock overlaps A and C's pointer claims};

  % Lane fills come first; labels sit in three reserved columns.
  \draw[pd focus fill]  (\laneleft,4.05) rectangle (9.00,4.62);
  \draw[pd warn fill]   (\laneleft,2.90) rectangle (9.45,3.47);
  \draw[pd legible fill](\laneleft,1.75) rectangle (9.90,2.32);

  \node[launch] at (2.62,4.335) {02:00\\format cron};
  \node[patch] at (4.18,4.335) {\texttt{formatOnSave}\\\texttt{false -> true}};
  \node[signal] at (7.00,4.335) {claim\\\texttt{/editor.formatOnSave}};
  \node[pd focus datum] at (9.00,4.335) {};
  \node[pd mono label,anchor=west] at (9.24,4.335) {write A};

  \node[launch] at (2.62,3.185) {02:01\\PnP prototype};
  \node[patch] at (4.18,3.185) {\texttt{tsdk}\\\texttt{node\_modules -> .yarn}};
  \node[signal] at (7.00,3.185) {whole-file lock\\\texttt{settings.json}};
  \node[pd warn datum] at (9.45,3.185) {};
  \node[pd mono label,anchor=west] at (9.69,3.185) {write B};

  \node[launch] at (2.62,2.035) {02:02\\search-speed cron};
  \node[patch] at (4.18,2.035) {\texttt{search.exclude}\\\texttt{+ **/fixtures}};
  \node[signal] at (7.00,2.035) {claim\\\texttt{/search.exclude}};
  \node[pd legible datum] at (9.90,2.035) {};
  \node[pd mono label,anchor=west] at (10.14,2.035) {write C};

  \draw[pd rule,->] (\laneleft,1.20) -- (\laneright,1.20);
  \node[pd label,anchor=east] at (\laneleft-.18,1.20) {time};
  \foreach \x/\t in {3.05/{read},7.00/{claim / lock},9.45/{write}}{
    \draw[pd tick] (\x,1.20) -- (\x,1.06);
    \node[pd label,anchor=north] at (\x,1.00) {\t};
  }

  % Same visible overlap, three possible resolutions.
  \node[pd focus datum] (fork) at (6.70,0.22) {};

  \node[pd ready state,result] (good) at (1.85,\outy)
    {\textbf{keywise merge}\\valid JSON\\all 3 keys present\\benign convergence};
  \node[pd breach state,result] (loud) at (5.35,\outy)
    {\textbf{text splice}\\comma lost\\settings fail to parse\\breaks now};
  \node[pd warn state,result] (quiet) at (8.85,\outy)
    {\textbf{valid but blind}\\valid JSON\\\texttt{fixtures/} hidden};

  \draw[pd ready arrow] (fork) -- (good.north);
  \draw[pd breach arrow] (fork) -- (loud.north);
  \draw[pd warn arrow] (fork) -- (quiet.north);

  \node[pd ready datum] (mutants) at (7.10,-2.85) {};
  \draw[pd ready arrow] (quiet.south) -- (mutants);
  \node[pd ready label,anchor=east,text width=2.85cm,align=right] at (6.62,-2.85)
    {\textbf{mutation test: 98\%}\\settings untested};

  \node[pd breach datum] (release) at (7.10,-4.30) {};
  \draw[pd breach arrow] (mutants) -- (release);
  \node[pd breach label,anchor=east,text width=2.85cm,align=right] at (6.62,-4.30)
    {\textbf{next release}\\\texttt{fixtures/v1} unseen; replay fails};
\end{tikzpicture}
\caption{Three agents race from the same \texttt{settings.json} revision to
three possible resolutions. Disjoint keys converge; a text splice breaks the
repo at once; a search exclusion leaves mutation testing green until release
replay needs the hidden fixture. [internal]}
\label{fig:state-of-nature}
\end{figure}
