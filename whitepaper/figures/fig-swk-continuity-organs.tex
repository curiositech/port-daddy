% Figure — the three continuity organs the whole economy rests on: memory
% (implemented), checkpoint (partial: notes, not execution state), and the
% witnessed-outcome ledger (partial substrate). Reputation keys on the richer
% full reputation-grade third organ, which does not yet exist. The canonical
% sentence lives in the caption.

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D8C7A6}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhink}{HTML}{1B1712}
\providecolor{hhcobalt}{HTML}{003FB8}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}

\begin{figure}[H]
\centering
\resizebox{0.92\textwidth}{!}{%
\begin{tikzpicture}[
  font=\small,
  organ/.style={draw=hhebony,thick,rounded corners=3pt,minimum width=50mm,
                minimum height=24mm,align=left,inner sep=6pt,text width=46mm,anchor=north},
  link/.style={->,>=Latex,line width=1.4pt},
]

\node[organ,fill=hhsand!30] (mem) at (0,0)
  {{\bfseries\footnotesize 1.\ Memory}\hfill{\footnotesize\color{hhteal}\textsf{implemented}}\\[3pt]
   append-only session notes; episodic story-beats; the operation log};

\node[organ,fill=hhsand!30,right=8mm of mem] (chk)
  {{\bfseries\footnotesize 2.\ Checkpoint}\hfill{\footnotesize\color{hhcobalt}\textsf{partial}}\\[3pt]
   recovery passes durable \emph{notes} --- not execution state};

\node[organ,fill=hhsand!30,right=8mm of chk] (ledger)
  {{\bfseries\footnotesize 3.\ Witnessed-outcome ledger}\hfill{\footnotesize\color{hhamber}\textsf{partial}}\\[3pt]
   the durable, oracle-bound record of what an agent actually delivered};

\draw[link,hhteal] (mem.east) -- (chk.west);
\draw[link,hhcobalt] (chk.east) -- (ledger.west);

% The weakest-link marker over checkpoint
\node[font=\bfseries\footnotesize,text=hhink,above=2mm of chk]
  {$\bigstar$ the weakest \emph{continuity} link};

\end{tikzpicture}%
}
\caption{The three continuity organs the entire economy stands on --- the canonical
sentence the rest of the series depends on. The through-line of the series ---
memory $\to$ checkpoint $\to$ continuity $\to$ durable identity $\to$ reputation
$\to$ market --- bottoms out \emph{here}, in the kernel. \textbf{Memory} is
\textsf{implemented}, and the kernel even forbids amnesia (a monitor rule keeps an
active session's note count from regressing). \textbf{Checkpoint} is
\textsf{partial}: recovery (heartbeat $\to$ stale/dead $\to$ queue $\to$ salvage
handoff) passes the durable notes but does not capture execution state, so it is the
weakest continuity link --- a checkpoint with teeth (a real execution-state
snapshot, OP-4) is the literal foundation of a cross-machine reputation economy.
\textbf{The witnessed-outcome ledger}, on which reputation actually keys, has a
\textsf{partial} shipped substrate: durable commitments, daemon-derived deadlines,
oracle-bound closure, and overdue detection. Neutral graded outcomes, sanctions,
and reputation binding do not yet exist. One ``the foundation is soft here'' claim,
not three.}
\label{fig:swk-continuity-organs}
\end{figure}
