% Figure 10 — what the next link can inherit steps down twice.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure]
\begin{axis}[
  width=\linewidth,
  height=5.4cm,
  axis lines=left,
  axis line style={draw=hhink},
  tick style={draw=hhgray},
  symbolic x coords={memory,checkpoint,continuity,outcome,identity,market},
  xtick=data,
  xticklabels={memory,check-\\point,continuity,outcome\\ledger,identity,market},
  xticklabel style={align=center,text width=1.6cm,font=\footnotesize},
  ytick={0,1,2,3},
  yticklabels={none,partial,{notes only},full},
  yticklabel style={font=\footnotesize},
  ymin=-.3,ymax=3.6,
  enlarge x limits=0.09,
  ylabel={evidence the next link can inherit},
  ylabel style={font=\footnotesize,align=center},
  clip=false,
]
\addplot[const plot,color=hhteal,line width=1.3pt,mark=*,
  mark options={fill=hhteal,draw=hhpaper,line width=.5pt}] coordinates
  {(memory,3) (checkpoint,2) (continuity,2) (outcome,1) (identity,1) (market,1)};
\coordinate (cp) at (axis cs:checkpoint,2);
\coordinate (oc) at (axis cs:outcome,1);
\end{axis}
\draw[pd guide] (cp) -- ++(0,.85cm);
\node[anchor=south,font=\footnotesize\itshape,text=hhamber,text width=2.9cm,align=center]
  at ($(cp)+(0,.95cm)$) {recovery restores notes, not execution state};
\draw[pd guide] (oc) -- ++(0,.85cm);
\node[anchor=south,font=\footnotesize\itshape,text=hhamber,text width=2.9cm,align=center]
  at ($(oc)+(0,.95cm)$) {outcomes witnessed only in part};
\end{tikzpicture}
\caption{The continuity chain as an evidence-support profile. What checkpoint
inherits from memory already drops from full to notes only; what the outcome
ledger inherits from continuity drops again, to partial; identity and market
inherit only that partial residue. Downstream confidence cannot exceed the
weakest upstream link. [internal]}
\label{fig:swk-continuity-organs}
\end{figure}
