% Figure 10 — the three continuity organs the whole economy rests on. memory
% (BUILT), checkpoint (BUILT-WEAK: notes, not execution state), and the
% witnessed-outcome ledger (DESIGNED). Reputation keys on the THIRD, which does
% not yet exist; the checkpoint organ is the weakest BUILT link. (STITCH C7.)

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D4C5A9}
\providecolor{hhebony}{HTML}{1E1B18}
\providecolor{hhink}{HTML}{2C2A26}
\providecolor{hhcinnabar}{HTML}{CC3D2E}
\providecolor{hhbrass}{HTML}{B08D57}
\providecolor{hhpatina}{HTML}{5C7A6A}
\providecolor{hhpaper}{HTML}{FBF7EE}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  font=\small,
  organ/.style={draw=hhebony,thick,rounded corners=3pt,minimum width=50mm,
                minimum height=34mm,align=left,inner sep=6pt,text width=46mm,anchor=north},
  link/.style={->,>=Latex,line width=1.4pt},
]
\newcommand{\lbl}{\bfseries\footnotesize}

\node[organ,fill=hhsand!30] (mem) at (0,0)
  {{\lbl 1.\ Memory}\hfill{\footnotesize\color{hhpatina}\textsf{implemented}}\\[3pt]
   append-only session notes; episodic story-beats; the operation log.\\[3pt]
   {\itshape A monitor rule forbids amnesia --- an active session's note count never regresses.}};

\node[organ,fill=hhsand!30,right=8mm of mem] (chk)
  {{\lbl 2.\ Checkpoint}\hfill{\footnotesize\color{hhcinnabar}\textsf{partial}}\\[3pt]
   recovery: heartbeat $\to$ stale/dead $\to$ queue $\to$ salvage handoff.\\[3pt]
   {\color{hhcinnabar}\itshape It \textbf{passes notes}; it does not checkpoint execution state. ``Teeth'' = OP-4, unbuilt.}};

\node[organ,fill=hhsand!30,right=8mm of chk] (ledger)
  {{\lbl 3.\ Witnessed-outcome ledger}\hfill{\footnotesize\color{hhbrass}\textsf{specified}}\\[3pt]
   the durable record of \emph{what an agent actually delivered}, oracle-bound.\\[3pt]
   {\itshape Reputation keys on \textbf{this} organ --- and it does not yet exist.}};

\draw[link,hhpatina] (mem.east) -- (chk.west);
\draw[link,hhcinnabar] (chk.east) -- (ledger.west);

% The weakest-link marker over checkpoint
\node[font=\bfseries\footnotesize,text=hhink,above=2mm of chk]
  {$\bigstar$ the weakest \emph{implemented} link};

% The canonical sentence
\node[draw=hhink,thick,fill=hhpaper,rounded corners=2pt,inner sep=6pt,
      text width=168mm,align=left,font=\footnotesize,below=8mm of chk.south]
  {{\bfseries The canonical sentence the rest of the volume depends on:}\quad
   The economy rests on three continuity organs --- memory (\textsf{implemented}),
   checkpoint (\textsf{partial}: notes, not execution state), and a witnessed-outcome
   ledger (\textsf{specified}) --- and reputation keys on the third, which does not
   yet exist; the checkpoint organ is the weakest \emph{implemented} link, and a
   checkpoint with teeth (a real execution-state snapshot, OP-4) is the literal
   foundation of a cross-machine reputation economy.};

\end{tikzpicture}%
}
\caption{The three continuity organs the entire economy stands on. The through-line
of the volume --- memory $\to$ checkpoint $\to$ continuity $\to$ durable identity
$\to$ reputation $\to$ market --- bottoms out \emph{here}, in the kernel.
\textbf{Memory} is \textsf{implemented} and the kernel even forbids amnesia (a
note-monotonicity rule). \textbf{Checkpoint} is \textsf{partial}: recovery passes
the durable handoff \emph{notes} but does not capture execution state, so it is the
weakest implemented link in the chain. \textbf{The witnessed-outcome ledger}, on
which reputation actually keys, is \textsf{specified} only. This figure states the
volume's canonical sentence so the reader meets one ``the foundation is soft here''
claim, not three.}
\label{fig:swk-continuity-organs}
\end{figure}
