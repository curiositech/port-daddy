% Figure 10 — the three continuity organs the whole economy rests on. memory
% (BUILT), checkpoint (BUILT-WEAK: notes, not execution state), and the
% witnessed-outcome ledger (DESIGNED). Reputation keys on the THIRD, which does
% not yet exist; the checkpoint organ is the weakest BUILT link. (STITCH C7.)

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D4C5A9}
\providecolor{hhebony}{HTML}{1E1B18}
\providecolor{hhink}{HTML}{2C2A26}
\providecolor{hhcinnabar}{HTML}{CC3D2E}
\providecolor{hhbrass}{HTML}{B08D57}
\providecolor{hhpatina}{HTML}{5C7A6A}
\providecolor{hhpaper}{HTML}{FBF7EE}

\begin{figure}[H]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  font=\sffamily\small,
  organ/.style={draw=hhebony,thick,rounded corners=3pt,minimum width=50mm,
                minimum height=34mm,align=left,inner sep=6pt,text width=46mm,anchor=north},
  link/.style={->,>=Latex,line width=1.4pt},
]
\newcommand{\lbl}{\sffamily\bfseries\footnotesize}

\node[organ,fill=hhpatina!16] (mem) at (0,0)
  {{\lbl 1.\ Memory}\hfill{\footnotesize\color{hhpatina}\textsf{BUILT}}\\[3pt]
   append-only session notes; episodic story-beats; the activity log.\\[3pt]
   {\itshape NOTE\_MONOTONICITY: the kernel forbids amnesia --- an active session's note count never regresses.}\\[2pt]
   {\ttfamily sessions.ts $\cdot$ episodic-memory.ts}};

\node[organ,fill=hhcinnabar!10,right=8mm of mem] (chk)
  {{\lbl 2.\ Checkpoint}\hfill{\footnotesize\color{hhcinnabar}\textsf{BUILT-WEAK}}\\[3pt]
   resurrection: heartbeat $\to$ stale/dead $\to$ queue $\to$ man-overboard.\\[3pt]
   {\color{hhcinnabar}\itshape It \textbf{passes notes}, it does not checkpoint execution state. ``Teeth'' = OP-4, unbuilt.}\\[2pt]
   {\ttfamily resurrection.ts}};

\node[organ,fill=hhsand!30,right=8mm of chk] (ledger)
  {{\lbl 3.\ Witnessed-outcome ledger}\hfill{\footnotesize\color{hhbrass}\textsf{DESIGNED}}\\[3pt]
   the durable record of \emph{what an agent actually delivered}, oracle-bound.\\[3pt]
   {\itshape Reputation keys on \textbf{this} organ --- and it does not yet exist (Paper 3).}\\[2pt]
   {\ttfamily ---}};

\draw[link,hhpatina] (mem.east) -- (chk.west);
\draw[link,hhcinnabar] (chk.east) -- (ledger.west);

% The weakest-link marker over checkpoint
\node[font=\sffamily\bfseries\footnotesize,text=hhcinnabar,above=2mm of chk]
  {$\bigstar$ the weakest \emph{built} link};

% The canonical C7 sentence
\node[draw=hhbrass,thick,fill=hhpaper,rounded corners=2pt,inner sep=6pt,
      text width=168mm,align=left,font=\sffamily\footnotesize,below=8mm of chk.south]
  {{\bfseries\sffamily The canonical sentence (stated identically in Papers 2 and 3):}\quad
   The economy rests on three continuity organs --- memory (\textsf{BUILT}),
   checkpoint (\textsf{BUILT-WEAK}: notes, not execution state), and the
   witnessed-outcome ledger (\textsf{DESIGNED}) --- and reputation keys on the
   third, which does not yet exist; the checkpoint organ is the weakest
   \emph{built} link, and ``resurrection with teeth'' (a real execution-state
   checkpoint, OP-4) is the literal foundation of L3.};

\end{tikzpicture}%
}
\caption{The three continuity organs the entire economy stands on. The
through-line of the volume --- memory $\to$ checkpoint $\to$ continuity $\to$
person $\to$ reputation $\to$ market --- bottoms out \emph{here}, in the kernel.
\textbf{Memory} is \textsf{BUILT} and the kernel even forbids amnesia (the
Arbiter's \texttt{NOTE\_MONOTONICITY} rule). \textbf{Checkpoint} is
\textsf{BUILT-WEAK}: resurrection passes the durable handoff \emph{notes} but does
not capture execution state, so it is the weakest built link in the chain.
\textbf{The witnessed-outcome ledger}, on which reputation actually keys, is
\textsf{DESIGNED} only (Paper~3). This figure states the volume's canonical C7
sentence, which appears identically in Papers~2 and 3 so the reader meets one
``the foundation is soft here'' claim, not three.}
\label{fig:swk-continuity-organs}
\end{figure}
