% Swiss object-based redraw; geometry lives in shared reusable functions.
% Evidence: corresponding single-writer-kernel.tex section; schematic, not a runtime trace.
\begin{figure}[H]
\centering
\SGMeasuredFigure{fig-swk-stack-map}{\SGStackLayers}
\caption{Read upward from L0. Higher layers depend on the records beneath them, but introduce their own assumptions. The blue tick identifies this chapter. The dashed machine floor is a dependency, not a fifth layer.}
\label{fig:swk-stack-map}
\end{figure}
