% Figure 1 — architectural section through the coordination stack.
\begin{figure}[H]
\centering
\hyphenpenalty=10000\exhyphenpenalty=10000\relax
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \fill[hhink] (0,0) rectangle (10.6,.9);
  \fill[pd focus fill] (0,.9) rectangle (10.6,2.5);
  \fill[pd focus fill] (0,2.5) rectangle (10.6,4.1);
  \fill[pd neutral fill] (0,4.1) rectangle (10.6,5.7);
  \fill[pd neutral fill] (0,5.7) rectangle (10.6,7.3);
  \foreach \y in {.9,2.5,4.1,5.7,7.3}
    \draw[pd hairline] (0,\y)--(10.6,\y);
  \draw[pd rule] (0,0) rectangle (10.6,7.3);

  \node[pd row label,text=hhpaper,anchor=west] at (.25,.45) {machine floor};
  \node[pd axis label,text=hhpaper,anchor=east,text width=5.1cm,align=left] at (10.35,.45)
    {file lock, clock, socket, supervisor};

  \node[pd row label,anchor=west] at (.25,1.7) {substrate};
  \node[pd row label,anchor=west] at (.25,3.3) {coordination};
  \node[pd row label,anchor=west] at (.25,4.9) {legibility};
  \node[pd row label,anchor=west] at (.25,6.5) {economy + federation};

  \node[pd axis label,anchor=east,text width=6.0cm,align=left] at (10.3,1.9)
    {one writer, claims, sessions, bus, memory};
  \node[pd axis label,anchor=east,text width=6.0cm,align=left] at (10.3,3.5)
    {typed acts, commitments, delegation, policy observation};
  \node[pd axis label,anchor=east,text width=6.0cm,align=left] at (10.3,5.1)
    {digest with zoom, review, operator surface};
  \node[pd axis label,anchor=east,text width=6.0cm,align=left] at (10.3,6.7)
    {settlement, reputation, cross-machine federation};

  \draw[pd focus rule,line width=1.6pt] (10.95,1.0)--(10.95,4.0);
  \draw[pd focus rule] (10.7,1.0)--(11.2,1.0);
  \draw[pd focus rule] (10.7,4.0)--(11.2,4.0);
  \node[pd axis label,rotate=90,anchor=south,inner sep=0pt,text width=1.9cm,align=center]
    at (11.35,2.5) {this chapter};
\end{tikzpicture}
\caption{An architectural section through the four-layer coordination stack.
This chapter is the bottom two bands: the transactional substrate (this
paper's core) and, riding directly on it, the implemented half of the
coordination protocol. The machine floor beneath both is the narrow
dependency boundary the substrate assumes: a file lock, a clock, a socket,
and a supervisor. [internal]}
\label{fig:swk-stack-map}
\end{figure}
