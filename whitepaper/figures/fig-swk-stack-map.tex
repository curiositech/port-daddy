% Figure 1 — architectural section through the coordination stack.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \fill[hhink] (0,0) rectangle (9.4,.9);
  \fill[pd neutral fill] (0,.9) rectangle (9.4,2.5);
  \fill[pd focus fill] (0,2.5) rectangle (9.4,4.1);
  \fill[hhsand!16] (0,4.1) rectangle (9.4,5.7);
  \fill[hhsand!30] (0,5.7) rectangle (9.4,7.3);
  \foreach \y in {.9,2.5,4.1,5.7,7.3}
    \draw[pd hairline] (0,\y)--(9.4,\y);
  \draw[pd rule] (0,0) rectangle (9.4,7.3);

  \node[font=\footnotesize\bfseries,text=white,anchor=west,text width=3.3cm,align=left]
    at (.25,.45) {MACHINE FLOOR};
  \node[font=\footnotesize,text=white,anchor=east,text width=5.1cm,align=right] at (9.15,.45)
    {file lock, clock, socket, supervisor};

  \node[pd row label,anchor=west] at (.25,1.7) {substrate};
  \node[pd row label,anchor=west] at (.25,3.3) {coordination};
  \node[pd row label,anchor=west] at (.25,4.9) {legibility};
  \node[pd row label,anchor=west,text width=3.4cm] at (.25,6.5) {economy + federation};

  \node[pd axis label,anchor=east,text width=5.1cm,align=right] at (9.1,1.9)
    {one writer, claims, sessions, bus, memory};
  \node[pd axis label,anchor=east,text width=5.1cm,align=right] at (9.1,3.5)
    {typed acts, commitments, delegation, policy observation};
  \node[pd axis label,anchor=east,text width=5.1cm,align=right] at (9.1,5.1)
    {digest with zoom, review, operator surface};
  \node[pd axis label,anchor=east,text width=5.1cm,align=right] at (9.1,6.7)
    {settlement, reputation, cross-machine federation};

  \draw[pd focus rule,line width=2.0pt] (9.75,1.0)--(9.75,4.0);
  \draw[pd focus rule] (9.5,1.0)--(10.0,1.0);
  \draw[pd focus rule] (9.5,4.0)--(10.0,4.0);
  \node[pd direct label,rotate=90,anchor=south,text=hhteal,text width=1.9cm,align=center]
    at (10.15,2.5) {this chapter};
\end{tikzpicture}
\caption{An architectural section through the four-layer coordination stack.
This chapter is the second band from the floor: the transactional substrate,
providing the one-writer record and claims that every higher layer rests on.
The machine floor beneath it is the narrow dependency boundary the substrate
assumes: a file lock, a clock, a socket, and a supervisor. [internal]}
\label{fig:swk-stack-map}
\end{figure}
