% Figure 3 — two claim trees, redrawn to spec [ch4.md 4.3]. Left: every claim
% the digest makes resolves to a named artifact. Right: the same shape of
% summary whose claims resolve to nothing. The leaf text is the readable fact;
% color is doubled by shape (pd state boxes vs. caution diamonds) and by
% arrow style (solid resolves-to vs. dashed caution) so the claim survives
% grayscale. Fan-out arrows to the off-center branches route through a bend
% so the final approach into each child is vertical, never diagonal into text.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  % ---------------- top tree: digest as lens ----------------
  \node[pd panel title,anchor=west] at (0,7.55) {digest as lens};
  \node[pd direct label,anchor=south] (lroot) at (4.0,7.15) {PR 123: 3 claims};

  \node[pd direct label,anchor=south,align=center] (lc1) at (1.0,5.35) {auth refactor};
  \node[pd direct label,anchor=south,align=center] (lc2) at (4.0,5.35) {tests pass};
  \node[pd direct label,anchor=south,align=center] (lc3) at (7.0,5.35) {1 critique};

  \draw[pd focus arrow] (lroot.south) -- (4.0,6.55) -- (1.0,6.55) -- (lc1.north);
  \draw[pd focus arrow] (lroot.south) -- (lc2.north);
  \draw[pd focus arrow] (lroot.south) -- (4.0,6.55) -- (7.0,6.55) -- (lc3.north);
  \node[pd axis label,anchor=west] at (5.35,6.25) {claims};

  \node[pd state,text width=2.1cm] (ll1) at (1.0,3.9) {diff a1c\ldots};
  \node[pd state,text width=2.1cm] (ll2) at (4.0,3.9) {run \#4812};
  \node[pd state,text width=2.1cm] (ll3) at (7.0,3.9) {comment \#7};

  \draw[pd focus arrow] (lc1.south) -- (ll1.north);
  \draw[pd focus arrow] (lc2.south) -- (ll2.north);
  \draw[pd focus arrow] (lc3.south) -- (ll3.north);
  \node[pd axis label,text=hhteal,anchor=west,align=left,text width=2.4cm] at (5.35,4.65) {resolves to};

  % ---------------- bottom tree: digest as facade ----------------
  \node[pd panel title,anchor=west] at (0,2.05) {digest as facade};
  \node[pd direct label,anchor=south] (rroot) at (4.0,1.65) {ALL GREEN};

  \node[pd direct label,anchor=south,inner sep=7pt] (rc1) at (1.0,-0.15) {\ldots};
  \node[pd direct label,anchor=south,inner sep=7pt] (rc2) at (4.0,-0.15) {\ldots};
  \node[pd direct label,anchor=south,inner sep=7pt] (rc3) at (7.0,-0.15) {\ldots};

  \draw[pd arrow,shorten >=1.5pt] (rroot.south) -- (4.0,0.95) -- (1.0,0.95) -- (rc1.north);
  \draw[pd arrow,shorten >=1.5pt] (rroot.south) -- (rc2.north);
  \draw[pd arrow,shorten >=1.5pt] (rroot.south) -- (4.0,0.95) -- (7.0,0.95) -- (rc3.north);
  \node[pd axis label,anchor=west] at (5.35,0.75) {claims};

  \node[pd caution datum] (rl1) at (1.0,-1.55) {};
  \node[pd caution datum] (rl2) at (4.0,-1.55) {};
  \node[pd caution datum] (rl3) at (7.0,-1.55) {};
  \node[pd axis label,text=hhamber,anchor=north] at (1.0,-1.75) {none};
  \node[pd axis label,text=hhamber,anchor=north] at (4.0,-1.75) {none};
  \node[pd axis label,text=hhamber,anchor=north] at (7.0,-1.75) {none};

  \draw[pd caution arrow] (rc1.south) -- (rl1.north);
  \draw[pd caution arrow] (rc2.south) -- (rl2.north);
  \draw[pd caution arrow] (rc3.south) -- (rl3.north);
  \node[pd axis label,text=hhamber,anchor=west,align=left,text width=2.4cm] at (5.35,-0.85) {resolves to};
\end{tikzpicture}
\caption{Two claim trees of identical shape. Top: the legible digest's three
named claims each resolve to one artifact --- a diff, a test run, a review
comment. Bottom: a same-shaped summary whose three claims resolve to nothing
an operator can open. A safe summary is an index into evidence; a calm one
that terminates early is a Potemkin digest, whichever claim count it
advertises [internal].}
\label{fig:zoom-vs-potemkin}
\end{figure}
