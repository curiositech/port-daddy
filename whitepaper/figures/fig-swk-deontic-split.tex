% Figure — the kernel's deontic split (Jones & Sergot). Prohibition is handled
% two ways (regimented pre-commit vs detected post-commit); obligation is
% enforced by oracle-bound commitments; permission is a recorded capability.
% The worked example lists and the unifying sentence live in the caption.

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D8C7A6}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhink}{HTML}{1B1712}
\providecolor{hhcobalt}{HTML}{6B4500}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}

\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[
  font=\small,
  col/.style={draw=hhebony,thick,rounded corners=3pt,fill=hhsand!30,
              minimum width=54mm,minimum height=34mm,align=left,
              inner sep=6pt,text width=50mm,anchor=north},
]

% ─── Prohibition ──────────────────────────────────────────────────────────
\node[col] (prohib) at (0,0)
  {{\bfseries Prohibition}\quad\textit{``you must not''}\\[4pt]
   {\color{hhamber}\textbf{Regimented}}: pre-commit, truly unreachable
   \emph{(uniqueness constraint / boot gate)}\\[4pt]
   {\color{hhteal}\textbf{Enforced}}: post-commit, detected \& compensated
   \emph{(the policy monitor)}};

% ─── Obligation ───────────────────────────────────────────────────────────
\node[col,right=5mm of prohib] (oblig)
  {{\bfseries Obligation}\quad\textit{``you ought''}\\[4pt]
   durable, \emph{violable} commitments\\[4pt]
   \textbf{Law 1} (I5): the deadline is daemon-derived, never agent-supplied\\[4pt]
   \textbf{Law 2} (I6): closing as `done' demands an oracle reference};

% ─── Permission ───────────────────────────────────────────────────────────
\node[col,right=5mm of oblig] (perm)
  {{\bfseries Permission}\quad\textit{``you may''}\\[4pt]
   a recorded \textbf{capability} attached to an actor\\[4pt]
   capability $=$ \emph{ability};\\ permission $=$ \emph{normative status}\\[2pt]
   --- ``able but forbidden'' stays expressible};

\end{tikzpicture}%
}
\caption{The kernel's deontic split (Jones \& Sergot), the formal core of its
obligation/enforcement organ: \emph{prohibition} is regimented \emph{or} detected,
\emph{obligation} is enforced by oracle-bound commitments, \emph{permission} is a
recorded capability --- and the coordination layer's deontic binding is a direct
lift of this split. The regimented set (double-claim, duplicate-identity squat,
serve-from-test-database, refuse-to-serve on a failed self-check) is rejected
pre-commit by a uniqueness constraint or fail-closed boot gate; everything the
policy monitor \emph{subscribes} to (heartbeat freshness, note monotonicity,
capability escalation, lock-owner validity) is detected post-commit and compensated.
Commitments follow Cohen \& Levesque's persistent goals: open $\to$ done $\mid$
abandoned $\mid$ superseded, with Law~1 closing the Goodhart hole on deadlines and
Law~2 refusing free-text closure. And capability (ability) is kept distinct from
permission (normative status) so that ``able but forbidden'' --- e.g.\ a dangerous
override flag that \emph{exists} but \emph{must not be used} --- remains
representable, the very state a guardrail-bypass discipline depends on.}
\label{fig:swk-deontic-split}
\end{figure}
