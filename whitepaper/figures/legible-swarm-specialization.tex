% Restyle of fig-specialization to the tikz-diagram-craft v2 standard (v1
% names and raw colours migrated to roles; the plot itself and the two
% filled regions are unchanged, ch4.md restyles).
%
% Reader question: when does a sole specialist beat a pooled service, and
% where does the proposed shortcut err?
% Claim: the true boundary is the Erlang-C threshold; the worked instance
% clears it into the sole-owner region; a simpler proposed threshold
% diverges from it rather than saturating, so it is falsified.
% Evidence: the Erlang-C boundary $g_A(\rho,3)$, the superseded $\tilde g$,
% and the worked point $\rho=.556,r=2.5$, whitepaper/legible-swarm.tex
% [internal, closed form].
% Must distinguish: the exact boundary (solid) from the falsified proposed
% one (dashed); which side of the exact boundary is pooled versus sole.
% Grammar chosen: phase-boundary plot -- two filled, edged regions split at
% the computed curve, the worked point marked on it.
% Grammar rejected: a bare curve with no fill would leave "which side wins"
% to the reader's arithmetic instead of to the picture.
% Five-second test: which region does the worked instance fall in?
\begin{figure}[H]
\centering
\pdfigurehue{pdteal}%
\begin{tikzpicture}[pd figure,x=.60cm,y=.68cm]
  \begin{scope}
    \clip (0,0) rectangle (12,6);
    \fill[pd focus fill]
      (0,.18) -- plot[smooth] coordinates {(0.25,.18)(1.5,.86)(3,1.57)(4.5,2.22)(6,2.82)(7.5,3.31)(9,3.69)(10.5,3.94)(11.7,4.00)}
      -- (12,4.03) -- (12,0) -- (0,0) -- cycle;
    \fill[pd neutral fill]
      (0,.18) -- plot[smooth] coordinates {(0.25,.18)(1.5,.86)(3,1.57)(4.5,2.22)(6,2.82)(7.5,3.31)(9,3.69)(10.5,3.94)(11.7,4.00)}
      -- (12,4.03) -- (12,6) -- (0,6) -- cycle;
    \draw[pd breach rule,line width=.9pt]
      plot[smooth] coordinates {(.25,.10)(1.5,.65)(3.0,1.71)(4.5,2.57)(6,4.00)(7.2,6.0)};
    \draw[pd focus rule,line width=1.2pt]
      plot[smooth] coordinates {(.25,.18)(1.5,.86)(3,1.57)(4.5,2.22)(6,2.82)(7.5,3.31)(9,3.69)(10.5,3.94)(11.7,4.00)};
    \node[pd focus datum] at (6.67,3.00) {};
  \end{scope}

  \draw[pd arrow] (0,0)--(12.35,0);
  \draw[pd arrow] (0,0)--(0,6.35);
  \foreach \x/\lab in {0/0,3/.25,6/.50,9/.75,12/1}{
    \draw[pd tick] (\x,0)--(\x,-.12);
    \node[pd label,anchor=north] at (\x,-.18) {\lab};}
  \foreach \y/\lab in {0/1,2/2,4/3,6/4}{
    \draw[pd tick] (0,\y)--(-.12,\y);
    \node[pd label,anchor=east] at (-.18,\y) {\lab};}
  \node[pd label,anchor=north,align=center,text width=3.6cm] at (6,-1.02)
    {pooled utilization $\rho$};
  \node[pd label,rotate=90,anchor=south,align=center,text width=3.0cm] at (-.82,3.1)
    {skill premium $r$};

  \node[pd focus label,anchor=west,align=left,text width=2.0cm] at (7.75,5.15)
    {exact Erlang--C\\boundary};
  \node[pd label,text=pderror,anchor=west,align=left,text width=2.5cm] at (2.05,4.20)
    {proposed shortcut\\falsified};
  \node[pd title,anchor=west] at (.75,5.32) {sole owner wins};
  \node[pd title,anchor=west] at (8.45,1.10) {pool wins};

  \node[pd label,anchor=west,align=left] at (7.08,2.42)
    {$\rho=.556$\\$r=2.5$};
  \draw[pd hairline] (6.96,2.58)--(6.67,3.00);
\end{tikzpicture}
\caption{The specialization boundary at a three-agent pool. Below the
Erlang--C boundary $g_A(\rho,3)$ (teal), pooling wins; above it, sole
ownership wins --- the two regions are now filled and edged, not hatched.
The superseded threshold $\tilde g$ (dashed) is kept for comparison: it
diverges rather than saturating at the pool capacity ratio. The worked
instance ($\rho=.556$, $r=2.5$) narrowly clears the correct boundary into the
sole-owner region [internal, closed form].}
\label{fig:specialization}
\end{figure}
