% Figure 8 — counterfactual pre-effect mediation versus actual post-commit observation.
\begin{figure}[H]
\centering
\hyphenpenalty=10000\exhyphenpenalty=10000\relax
\begin{tikzpicture}[pd figure,x=.85cm,y=1cm]
  \node[pd panel title,anchor=east,text width=2.1cm,align=right] at (-.15,4.4) {pre-effect gate};
  \draw[pd arrow] (.1,4.4)--(2.1,4.4);
  \draw[pd caution rule,line width=1.6pt] (2.1,3.9)--(2.1,4.9);
  \draw[pd focus arrow] (2.22,4.4)--(5.0,4.4);
  \node[pd focus datum] at (5.0,4.4) {};
  \node[pd direct label,anchor=south] at (1.1,4.55) {request};
  \node[pd direct label,anchor=north,text=hhamber] at (2.1,3.75) {decision};
  \node[pd direct label,anchor=south,text=hhteal,text width=3.3cm,align=center]
    at (5.0,4.55) {commit if admitted};
  \node[pd note,anchor=north west,text width=3.0cm] at (5.35,3.75)
    {forbidden state never exists};

  \node[pd panel title,anchor=east,text width=2.1cm,align=right] at (-.15,1.5) {actual monitor};
  \draw[pd arrow] (.1,1.5)--(2.0,1.5);
  \node[pd datum] at (2.0,1.5) {};
  \draw[pd caution rule,line width=2.4pt] (2.0,1.5)--(4.9,1.5);
  \node[pd caution datum] at (3.45,1.5) {};
  \node[pd focus datum] at (4.9,1.5) {};
  \draw[pd focus arrow] (4.9,1.5)--(6.9,1.5);
  \node[pd direct label,anchor=south] at (2.0,1.65) {commit};
  \node[pd direct label,anchor=north,text=hhamber,text width=3.0cm,align=center]
    at (3.45,1.35) {exposure: effect already happened};
  \node[pd direct label,anchor=south,text=hhteal] at (4.9,1.65) {compensate};
  \node[pd note,anchor=west,text width=3.4cm] at (7.15,1.5) {past effect remains in history};

  \draw[pd hairline,-{Stealth[length=1.65mm,width=1.05mm]}] (0,.05)--(9.8,.05);
  \node[pd axis label,anchor=west] at (9.85,.05) {time};
\end{tikzpicture}
\caption{The reference-monitor test as a causal counterfactual. A true
mediator decides before the commit point, so a forbidden state never exists.
The present policy monitor instead subscribes after commit: the caution
segment is the exposure interval in which the effect has already happened,
before it can observe, halt subsequent work, and compensate. Only uniqueness
constraints and fail-closed boot gates belong to the upper trace; subscriber
rules belong to the lower one. [internal]}
\label{fig:swk-reference-monitor}
\end{figure}
