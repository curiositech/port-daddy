% Figure 9 — typed evidence passes a narrow closure aperture.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \node[pd state] (open) at (.9,1.0) {OPEN};
  \draw[pd focus rule,line width=1.6pt] (5.6,.4)--(5.6,7.6);

  \draw[pd focus arrow] (1.8,1.0)--(5.35,1.0);
  \node[pd direct label,anchor=south] at (3.55,1.15) {attempt closure};
  \draw[pd focus arrow] (5.85,1.0)--(7.9,1.0);
  \node[pd terminal] (done) at (8.65,1.0) {DONE};
  \node[pd direct label,anchor=west,text=hhteal] at (5.85,4.0) {typed oracle};

  \node[pd direct label,anchor=east,text width=2.7cm,align=right] at (3.1,7.6) {released claim};
  \draw[pd focus arrow] (3.35,7.6)--(5.55,7.3);
  \node[pd direct label,anchor=east,text width=2.7cm,align=right] at (3.1,6.6) {merged commit};
  \draw[pd focus arrow] (3.35,6.6)--(5.55,6.6);
  \node[pd direct label,anchor=east,text width=2.7cm,align=right] at (3.1,5.6) {passing test id};
  \draw[pd focus arrow] (3.35,5.6)--(5.55,5.9);
  \node[pd direct label,anchor=east,text width=2.7cm,align=right] at (3.1,4.6) {policy sub-check};
  \draw[pd focus arrow] (3.35,4.6)--(5.55,5.2);

  \node[pd direct label,anchor=west,text=hhamber,text width=2.6cm] at (7.3,-.6)
    {free-text assertion};
  \draw[pd caution arrow] (7.1,-.3)--(5.85,.6);
  \draw[pd caution rule,line width=1.3pt] (5.62,.42)--(5.96,.76);
  \draw[pd caution rule,line width=1.3pt] (5.62,.76)--(5.96,.42);

  \draw[pd caution arrow,dashed]
    (5.6,.4)--(5.6,-1.5)--(.5,-1.5)--(.5,.7);
  \node[pd direct label,anchor=north,text=hhamber,text width=4.0cm,align=center]
    at (3.05,-1.65) {refused; remains open};

  \node[pd note,anchor=north west,text width=3.3cm] at (1.15,.55)
    {deadline set by the daemon, not the agent};
\end{tikzpicture}
\caption{Typed evidence passes through a narrow closure aperture. A
commitment closes as done only when the daemon can re-run a check from the
finite oracle vocabulary: a released claim, merged commit, passing test id,
or satisfied policy sub-check. A free-text assertion is refused and the
commitment remains open. The deadline is set by the daemon, not the agent.
[internal]}
\label{fig:swk-commitment-oracle}
\end{figure}
