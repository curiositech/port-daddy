% Figure — the dual-runtime soundness hazard (I11). The runtime under which
% truth is COMPUTED (the compiled binary) is not the runtime under which it is
% TESTED. A shim bridges pragmas/options; the hazard and OP-3 (differential
% testing) live in the caption.

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D8C7A6}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhink}{HTML}{1B1712}
\providecolor{hhcobalt}{HTML}{6B4500}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}

\begin{figure}[H]
\centering
\resizebox{0.7\textwidth}{!}{%
\begin{tikzpicture}[
  font=\small,
  api/.style={draw=hhebony,thick,rounded corners=3pt,fill=hhsand!30,
              minimum width=60mm,minimum height=12mm,align=center,font=\bfseries},
  rt/.style={draw=hhebony,thick,rounded corners=2pt,minimum width=52mm,
             minimum height=18mm,align=center,font=\footnotesize},
]

\node[api] (iface) at (0,0) {one database interface\\ {\footnotesize\normalfont (a single abstraction over the bindings)}};

\node[rt,fill=hhsand!30,below left=14mm and -4mm of iface] (test)
  {{\bfseries test runtime}\\ test-suite SQLite bindings\\ {\color{hhteal}truth is \emph{verified} here}};
\node[rt,fill=hhsand!30,below right=14mm and -4mm of iface] (prod)
  {{\bfseries deployment runtime}\\ the compiled single-binary daemon\\ {\color{hhamber}truth is \emph{computed} here}};

\draw[->,>=Latex,thick,hhteal] (iface.south) -- (test.north);
\draw[->,>=Latex,thick,hhamber] (iface.south) -- (prod.north);

% The shim
\node[draw=hhink,thick,fill=hhpaper,rounded corners=2pt,inner sep=3pt,
      font=\scriptsize,align=center,below=10mm of iface]
  (shim) {a runtime-shim layer\\ normalizes pragma/option differences};

\end{tikzpicture}%
}
\caption{The dual-runtime substrate, a genuine kernel property easily mistaken for
an implementation detail. One database interface is satisfied by \emph{two} sets of
SQLite bindings: the test-suite bindings (where invariants are \emph{verified}) and
the deployment bindings in the compiled daemon (where truth is actually
\emph{computed}). A shim normalizes their pragma and option interfaces, but the
bridge is not a proof: an invariant green under test can fail in deployment if the
bindings diverge in lock or pragma semantics --- the recurring ``green in the test
runtime, broken in deployment'' failure, which is why CI must \emph{boot the
deployment runtime} and exercise its endpoints, not merely build it. Invariant I11
(runtime parity) is therefore \textsf{partial}, and open problem OP-3 asks for the
differential-test harness --- identical operation sequences against both runtimes,
asserting identical observable state --- that would make it a theorem.}
\label{fig:swk-dual-runtime}
\end{figure}
