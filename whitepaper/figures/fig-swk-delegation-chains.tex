% Brief (tikz-diagram-craft). Register row ch1-20 [should].
% Reader question: which of the two "delegation chain" objects does a
%   given claim in this chapter mean?
% Claim: "delegation chain" names two distinct objects that must never be
%   merged -- the cryptographic authorization chain (Built) and the
%   coordination lineage (Designed) -- and the second rides strictly
%   inside the first, its authenticity guaranteed by the outer chain.
% Evidence: whitepaper/single-writer-kernel.tex sec:comm (Two delegation
%   chains, one dangerous name): authorization chain = hop-bound,
%   attenuation-monotonic, anti-replay, anti-splice signatures, Built,
%   mechanically verified; coordination lineage = task-lineage for loop
%   detection and upward-block, Designed; "we never again write bare
%   delegation chain."
% Must distinguish: which object is cryptographic vs. coordination; that
%   containment is the claim (the lineage rides inside the chain, not
%   beside it); the open tension the pitfall names (a semantic task-shape
%   equivalence is not deterministically signable).
% Grammar: nested UML package plus component: the signed authorization
%   package contains the coordination-lineage component it authenticates.
%   Rejected: two side-by-side boxes, which would assert independence, and
%   concentric colour fields, which show containment but not object kind.
% Five-second test: "the coordination-lineage component is inside the signed
%   authorization package; its task-shape binding is still open".
\begin{figure}[H]
\centering
\pdfigurehue{pdcobalt}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\w{10.55}
  % UML package: the tab and frame name the signed container.
  \draw[pd focus fill,rounded corners=1pt] (0,0) rectangle (\w,5.15);
  \draw[pd focus fill,rounded corners=1pt] (0,5.15) rectangle (4.05,5.72);
  \node[pd kind,anchor=west] at (.24,5.44) {signed package};
  \node[pd title,anchor=west] at (.28,4.68)
    {authorization chain \textperiodcentered\ \textsc{built}};
  \node[pd label,anchor=west,text width=9.8cm,align=left] at (.28,4.02)
    {hop-bound \textperiodcentered\ attenuating \textperiodcentered\ anti-replay
     \textperiodcentered\ anti-splice};

  % UML component: a distinct coordination object carried in the package.
  \node[pd uml component,minimum width=8.85cm,minimum height=2.25cm]
    (lineage) at (5.28,2.05) {};
  \pdumlicon{lineage}
  \node[pd title,anchor=west] at (1.18,2.58)
    {coordination lineage \textperiodcentered\ \textsc{designed}};
  \node[pd label,anchor=west] at (1.18,2.02)
    {loop detection \textperiodcentered\ upward-block over the task shape};
  \node[pd breach label,anchor=west] at (1.18,1.46)
    {signed task-shape binding: open};
  \node[pd kind,anchor=south west] at (.55,3.30)
    {carried inside every signed hop};
  \draw[pd focus rule] (.55,3.15) -- (9.95,3.15);
\end{tikzpicture}
\caption{A UML package separates the built signed authorization chain from
the designed coordination-lineage component it carries. The outer signatures
authenticate the lineage, while signable semantic task-shape equivalence
remains open. [internal]}
\label{fig:swk-delegation-chains}
\end{figure}
