% Brief (tikz-diagram-craft). Register row ch1-20 [should].
% Reader question: which of the two "delegation chain" objects does a
%   given claim in this chapter mean?
% Claim: "delegation chain" names two distinct objects that must never be
%   merged -- the cryptographic authorization chain (Built) and the
%   coordination lineage (Designed) -- and the second rides strictly
%   inside the first, its authenticity guaranteed by the outer chain.
% Evidence: whitepaper/single-writer-kernel.tex sec:comm (Two delegation
%   chains, one dangerous name): authorization chain = hop-bound,
%   attenuation-monotonic, anti-replay, anti-splice signatures, Built,
%   mechanically verified; coordination lineage = task-lineage for loop
%   detection and upward-block, Designed; "we never again write bare
%   delegation chain."
% Must distinguish: which object is cryptographic vs. coordination; that
%   containment is the claim (the lineage rides inside the chain, not
%   beside it); the open tension the pitfall names (a semantic task-shape
%   equivalence is not deterministically signable).
% Grammar: nested envelope, outer = authorization chain, inner =
%   coordination lineage. Rejected: two side-by-side boxes, which would
%   assert two independent objects instead of the one-rides-inside-the-
%   other relationship the section states.
% Five-second test: "one outer envelope, Built, signed; one inner
%   envelope, Designed, task-shaped".
\begin{figure}[H]
\centering
\pdfigurehue{pdcobalt}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\ox{0.20}  \def\oy{0.20}
  \draw[pd focus fill] (\ox,-1.05) rectangle ({\ox+9.9},{\oy+5.4});
  \node[pd title,anchor=north west,text width=6.6cm,align=left] at ({\ox+.25},{\oy+5.4-.15})
    {authorization chain -- \textsc{built}};
  \node[pd label,anchor=north west,text width=6.6cm,align=left] at ({\ox+.25},{\oy+4.55})
    {hop-bound, attenuation-monotonic, anti-replay, anti-splice signatures: who authorized whom};

  \draw[pd neutral fill] ({\ox+.55},{\oy+.55}) rectangle ({\ox+9.9-1.9},{\oy+2.35});
  \node[pd title,anchor=north west,text width=5.6cm,align=left] at ({\ox+.80},{\oy+2.35-.15})
    {coordination lineage -- \textsc{designed}};
  \node[pd label,anchor=north west,text width=5.6cm,align=left] at ({\ox+.80},{\oy+1.60})
    {task lineage: loop detection and upward-block over the task shape};

  \node[pd note,anchor=north west,text width=9.0cm] at ({\ox+.25},{-0.05})
    {the lineage rides inside the chain: loop detection runs over a
    lineage whose authenticity the outer signatures guarantee};
\end{tikzpicture}
\caption{Two objects, never merged: the cryptographic authorization chain
(\textsc{built}, mechanically verified) and the coordination lineage
(\textsc{designed}) it carries. The nesting is the claim -- loop detection
runs over a task lineage whose authenticity the outer chain of signatures
guarantees, so the chapter never again writes bare ``delegation chain.''
Making the inner task-shape field itself signable at each hop, given that
loop detection needs a semantic equivalence rather than a keyword match, is
an open tension the chapter flags rather than claims solved. [internal]}
\label{fig:swk-delegation-chains}
\end{figure}
