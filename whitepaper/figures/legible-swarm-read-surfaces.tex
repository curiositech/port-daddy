% Figure 9 — one source log, five reader-specific instruments.  The painted
% journal is deliberately non-textual; names, readers, and relations remain
% typeset here so the claim stays inspectable and correctable.
\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhink}{HTML}{1B1712}
\providecolor{hhcobalt}{HTML}{003FB8}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}
\providecolor{hhgray}{HTML}{5C5650}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
  x=1cm,y=1cm,
  % These cards deliberately occupy the five paper slips painted on the plate.
  % A single source-owned rail makes their edges and baselines exact instead of
  % leaving five small labels adrift over five larger generated rectangles.
  reader/.style={draw=none,fill=hhpaper!98,align=left,
                 inner sep=2.7pt,font=\scriptsize,text width=37mm,
                 minimum height=10mm,text=hhink},
  raw/.style={draw=none,fill=hhpaper!78,rounded corners=1pt,align=center,
              inner sep=3pt,font=\scriptsize,text width=31mm,text=hhink},
  route/.style={-{Latex[length=1.6mm]},line width=.7pt,hhteal!85},
]
  \node[inner sep=0] (plate) at (0,0)
    {\includegraphics[width=.90\textwidth]{figures/assets/legible-swarm-read-surfaces-log.jpg}};

  % The left book is the durable source; every right-hand object is a different
  % read product.  These arrows are the semantic layer, not the painted routes.
  \node[raw] (log) at (-4.05,0.30)
    {\textsc{Raw ship's log}\\[-1pt]\emph{transcripts, diffs, events}};
  \node[reader] (brief) at (4.57,3.16)
    {\textbf{Briefing:} arrival state\\[-1pt]\emph{reader: arriving agent}};
  \node[reader] (attention) at (4.57,1.82)
    {\textbf{Attention:} what matters now\\[-1pt]\emph{reader: operator queue}};
  \node[reader] (memory) at (4.57,0.27)
    {\textbf{Episodic memory:} what happened before\\[-1pt]\emph{reader: future self}};
  \node[reader] (pheromone) at (4.57,-1.46)
    {\textbf{Pheromone:} shared trace map\\[-1pt]\emph{reader: the swarm}};
  \node[reader] (resurrection) at (4.57,-3.19)
    {\textbf{Resurrection:} handoff, not checkpoint\\[-1pt]\emph{reader: successor}};

  \draw[route] (log.east) to[out=12,in=180] (brief.west);
  \draw[route] (log.east) to[out=7,in=180] (attention.west);
  \draw[route] (log.east) -- (memory.west);
  \draw[route] (log.east) to[out=-7,in=180] (pheromone.west);
  \draw[route] (log.east) to[out=-12,in=180] (resurrection.west);
  \node[font=\scriptsize\itshape,text=hhgray,fill=hhpaper!72,inner sep=1.5pt]
    at (-.25,4.58) {one voyage, five readings};
\end{tikzpicture}
\caption{One durable ship's log becomes five reader-specific instruments, not one
general-purpose summary.  The same raw trajectory yields an arrival briefing,
an operator attention queue, episodic recall, a decaying swarm trace, and a
successor handoff; each compacts for its reader while preserving a zoom path to
the underlying artifact.  Thus the coordination layer is a \emph{distributed
compaction engine}: its cost, recency-decay (Prop.~\ref{prop:decay}), and
legibility obligations are shared even though its readings differ.}
\label{fig:read-surfaces}
\end{figure}
