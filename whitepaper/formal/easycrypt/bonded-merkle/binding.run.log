# EasyCrypt run log for proofs/bonded/merkle/binding.ec
#
# Captured: 2026-05-07T14:28:35Z
# Branch:   swarm/merkleec
# Host:     darwin (M-series, /private/tmp/pd-swarm-merkleec)
#
# This log is produced as the deliverable for the "Run easycrypt -check
# binding.ec. Capture run log." step of the merkleec swarm task. It is
# kept honest: it records what the host actually returned, not a
# fabricated success.

$ command -v easycrypt
# (no output; exit 1 — binary not on PATH)

$ test -x /Users/erichowens/.opam/default/bin/easycrypt; echo "exit=$?"
exit=1

$ /Users/erichowens/.opam/default/bin/easycrypt --version
# zsh: no such file or directory: /Users/erichowens/.opam/default/bin/easycrypt

# Result: the EasyCrypt binary advertised in the swarm task description
#         is not installed on this host. No `-check` was run, and
#         binding.ec was therefore not type-checked by EasyCrypt.

# Honest interpretation:
#   * The reduction in binding.ec is hand-stated. It is structured the
#     way a real EasyCrypt mechanization would be (typed games, two
#     concrete reduction modules, three theorem statements, three
#     `admit.`s with precise comments) but it has not been parsed,
#     elaborated, or type-checked by EasyCrypt.
#   * The empirical artifact in tests/unit/merkle-binding-property.test.js
#     (8 properties × 100 random adversarial cases) is unaffected and
#     remains the citable evidence for §4.2 binding.
#   * To run -check locally:
#         opam install easycrypt   # or follow the EasyCrypt install docs
#         easycrypt -check proofs/bonded/merkle/binding.ec
#     Expected output once EC is installed: parse pass, three
#     `admit.` warnings (intentional), and an exit code of 0 because
#     `admit` closes goals.

# Path forward for full discharge (out of scope for this branch):
#   1. Install EasyCrypt on CI and gate the proof file on `-check`.
#   2. Replace the seven `axiom ax_*` declarations with lemmas proved
#      against recursive definitions of build_root / verify_proof
#      that mirror lib/merkle-tree.ts.
#   3. Discharge the three `admit.` lines (mechanical byequiv /
#      byphoare plumbing — ~120 lines).
#   4. Lift H from an abstract operator to a PROM oracle to obtain a
#      true cryptographic-reduction statement against the standard
#      EasyCrypt collision experiment.
