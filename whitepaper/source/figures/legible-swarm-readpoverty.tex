% Figure 9 — paired scaling views. The left encodes lookup cost; the right
% distinguishes value that exists at n=1 from discovery value that arrives later.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=.90cm,y=.92cm]
  \node[pd panel title,anchor=west] at (.15,4.85) {read cost};
  \node[pd panel title,anchor=west] at (8.25,4.85) {value of the legibility layer};

  \draw[pd arrow] (.15,.35)--(7.15,.35);
  \draw[pd arrow] (.15,.35)--(.15,4.48);
  \draw[pd rule,line width=1.05pt] plot[smooth] coordinates {(.35,.55)(1.8,1.25)(3.3,2.05)(4.8,2.92)(6.65,4.15)};
  \draw[pd focus rule,line width=1.05pt] plot[smooth] coordinates {(.35,.72)(1.8,1.18)(3.3,1.42)(4.8,1.58)(6.65,1.72)};
  \node[pd direct label,anchor=west] at (4.55,3.38) {$O(n)$ eyeball search};
  \node[pd direct label,text=hhteal,anchor=west] at (4.35,1.86) {$O(\log n)$ index};
  \node[pd axis label,anchor=west] at (6.35,.08) {population $n$};
  \node[pd axis label,rotate=90,anchor=south] at (-.35,2.4) {cost to find};

  \draw[pd arrow] (8.25,.35)--(15.25,.35);
  \draw[pd arrow] (8.25,.35)--(8.25,4.48);
  \fill[hhsand!20] (12.75,.35) rectangle (15.1,4.32);
  \draw[pd focus rule,line width=1.05pt]
    plot[smooth] coordinates {(8.48,1.30)(9.8,1.82)(11.2,2.36)(12.6,2.78)(14.9,3.23)};
  \node[pd focus datum] at (8.48,1.30) {};
  \node[pd direct label,text=hhteal,anchor=west,align=left] at (8.72,1.08)
    {$n=1$\\already pays};
  \draw[pd caution rule,dashed] (12.75,.35)--(12.75,4.32);
  \node[pd direct label,text=hhamber,anchor=west,align=left] at (12.92,3.92)
    {read-poverty regime\\discovery begins to pay};
  \node[pd axis label,anchor=west] at (14.65,.08) {swarm size $n$};
  \node[pd axis label,rotate=90,anchor=south] at (7.75,2.4) {value};
  \node[pd note,anchor=north] at (11.72,-.04)
    {onset is a design target, not a measurement};
\end{tikzpicture}
\caption{Read-poverty has two scales. Left, unindexed eyeball search grows with
population while a directory keeps lookup near logarithmic. Right, the
legibility layer has value at $n=1$ through briefing, attestation, footgun
guards, and honest completion; discovery adds value only after the corpus
becomes too large to scan. The shaded onset is a design regime, not an empirical
estimate of a universal swarm-size threshold.}
\label{fig:readpoverty}
\end{figure}
