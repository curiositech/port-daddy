% Semantic contract: each signed hop binds adjacent identities to one task hash;
% a skipped or transplanted hop breaks the visible braid.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \foreach \x/\id in {-5.1/1,-1.7/2,1.7/3,5.1/4} {
    \node[circle,draw=hhink,line width=.7pt,fill=hhpaper,minimum size=10mm,
      inner sep=0pt,font=\bfseries] (id\id) at (\x,2.1) {$\mathrm{id}_{\id}$};
    \draw[pd hairline] (\x,1.58) -- (\x,-.7);
  }

  \foreach \a/\b/\mid/\n in {1/2/-3.4/1,2/3/0/2,3/4/3.4/3} {
    \draw[pd focus arrow] (id\a.east) to[out=5,in=175] (id\b.west);
    \node[pd direct label,text=hhteal,anchor=south] at (\mid,2.42)
      {$\operatorname{sig}_{\a}$};
    \node[pd focus datum,minimum size=5pt,inner sep=0pt] (nonce\n) at (\mid,.75) {};
    \node[pd axis label,anchor=west] at (\mid+.14,.75) {$n_{\n}$ fresh};
    \draw[pd focus rule] (\mid,1.82) -- (nonce\n);
    \draw[pd focus rule] (nonce\n) -- (\mid,-.32);
  }

  \draw[pd rule] (-5.55,-.32) -- (5.55,-.32);
  \foreach \x in {-5.1,-1.7,1.7,5.1} \fill[hhteal] (\x,-.32) circle (1.45pt);
  \node[pd direct label,anchor=north west] at (-5.55,-.52)
    {chain-wide task hash $h$};

  \draw[pd caution arrow] (id2.north) to[out=55,in=125] (id4.north);
  \node[pd direct label,text=hhamber,anchor=south] at (1.7,3.22)
    {splice skips $\mathrm{id}_3$};
  \node[circle,draw=hhamber,line width=.9pt,fill=hhpaper,minimum size=6.5mm,
    inner sep=0pt] at (1.7,2.72) {};
  \draw[draw=hhamber,line width=.9pt] (1.48,2.5) -- (1.92,2.94);
  \draw[draw=hhamber,line width=.9pt] (1.48,2.94) -- (1.92,2.5);

  \node[pd note,anchor=west] at (-5.55,-1.15)
    {each crossing binds parent, child, fresh nonce, and the same $h$};
\end{tikzpicture}
\caption{The delegation chain is a signed provenance braid. Identity continuity
runs across the top, fresh nonces pin each crossing, and one task hash anchors
the chain below. Replaying a link fails freshness; substituting or splicing a
link creates a visible discontinuity at an adjacent-identity boundary.}
\label{fig:anchor-delegation-inline}
\end{figure}
