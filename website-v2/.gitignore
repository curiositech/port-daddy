# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
storybook-static
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Recording daemon runtime state (DB, socket, env file, PID, log).
# The seed script itself (scripts/seed-recording-world.mjs) IS committed.
.recording-daemon/

# Binary recording fixture — built locally; only the SQL dump (demo.sql) is committed.
recordings-fixtures/*.sqlite
