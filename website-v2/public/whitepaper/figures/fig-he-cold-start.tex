% Figure IV.12 --- measured liquidity and the policy applied to it are separate
% aligned signals, tied by one shared guide at the depth trigger.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \node[pd panel title,anchor=west] at (-.9,3.35)
    {the regime changes when depth crosses {\normalsize$\lambda^\star$}, not when time passes};

  % Upper plot: measured market state.
  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (0,.35)--(8.6,.35);
  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (0,.35)--(0,2.9);
  \node[pd axis label,rotate=90,anchor=south,text width=2.4cm,align=center]
    at (-.35,1.62) {realized liquidity};
  \draw[pd focus rule,line width=1.35pt,smooth]
    plot coordinates {(.2,.48)(1.0,.54)(1.8,.68)(2.6,.95)(3.4,1.38)(4.2,1.95)
      (5.0,2.42)(5.8,2.68)(6.6,2.78)(8.4,2.82)};
  \draw[pd caution rule,dashed] (.2,1.90)--(8.4,1.90);
  \node[pd direct label,text=hhamber,anchor=west,inner sep=2.6pt] at (6.4,2.18) {{\normalsize$\lambda^\star$}};

  % Shared guide: the same trigger point crosses both plots.
  \draw[pd guide,line width=1.1pt] (4.15,-2.25)--(4.15,3.0);
  \node[pd caution datum] at (4.15,1.90) {};
  \node[pd caution datum] at (4.15,-.72) {};
  \node[pd direct label,text=hhamber,anchor=west,text width=2.1cm] at (4.35,2.55)
    {depth trigger};

  % Lower plot: policy schedule, not another state curve.
  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (0,-2.05)--(8.6,-2.05);
  \draw[pd rule] (0,-2.05)--(0,-.45);
  \node[pd axis label,rotate=90,anchor=south,text width=2.0cm,align=center]
    at (-.35,-1.25) {supplier price};
  \draw[pd caution rule,line width=1.45pt]
    (.2,-1.65)--(2.5,-1.65)--(2.5,-1.25)--(4.15,-1.25)--(4.15,-.72)--(8.4,-.72);
  \node[pd direct label,text=hhamber,anchor=south] at (1.0,-1.52) {SEED $<0$};
  \node[pd direct label,text=hhamber,anchor=south,text width=1.3cm,align=center,
    inner sep=2.6pt] at (3.3,-1.12) {IMPORT $=0$};
  \node[pd direct label,text=hhamber,anchor=south] at (6.2,-.59)
    {CHARGE $>0$};

  \foreach \x in {.2,1.6,3.0,4.4,5.8,7.2,8.4}
    \draw[pd guide] (\x,-2.12)--(\x,-1.98);
  \node[pd axis label,anchor=west,text width=2.4cm,align=left] at (8.72,-2.05)
    {settled\\outcomes};
  \node[pd note,anchor=west,text width=9.8cm] at (0,-2.95)
    {if liquidity never crosses the threshold, the subsidized state remains
     active and recoverable};
\end{tikzpicture}
\caption{\linespread{1.08}\selectfont\textbf{Cold start separates the measured
market state from the policy schedule.} The upper trace is realized liquidity
over settled outcomes. The lower trace is the supplier-price regime applied to
that state: seed supply below zero, credit imported work at zero, then charge
only after liquidity crosses {\normalsize$\lambda^\star$}. The shared guide ties one
trigger point on both traces. Calendar time is absent, and a market that never
crosses the threshold does not silently advance to the charging regime
[internal].}
\label{fig:he-cold-start}
\end{figure}
