% Cycle versus cut. Radii from sheaf_consistency_radius.py, seed 20260816.
%
% Reader question: why does the same edge disagreement produce a residual on
%   one six-harbor topology and disappear on another?
% Claim: edge 3--4 closes a visible cycle on C6 (r=1.2247) but is a bridge on
%   P6, where the missing 6--1 edge leaves no cycle and r=0.
% Evidence: the two computed radii and six numbered harbors.
% Must distinguish: the disagreeing edge from the separate missing closure.
% Grammar chosen: aligned topology small multiples. Rejected: one overlaid
%   graph, because it would hide the single missing edge that changes the rank.
% Five-second test: identify edge 3--4 in both panels and the absent 6--1 edge.
\pdfigurehue{pdgold}%
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\cx{1.70}  \def\cy{1.15}  \def\rad{1.18}
  \def\px{5.62}  \def\pitch{.86}

  \node[pd title,anchor=north,text width=4.2cm,align=center] at (\cx,4.05)
    {A. cycle: edge 3--4 closes the loop};
  \foreach \i in {1,...,6}
    {\node[pd state,circle,minimum size=6.4mm,inner sep=0pt] (c\i)
       at ({\cx+\rad*cos(90-60*(\i-1))},{\cy+\rad*sin(90-60*(\i-1))}) {\i};}
  \foreach \i/\j in {1/2,2/3,4/5,5/6,6/1}
    {\draw[pd rule] (c\i) -- (c\j);}
  \draw[pd breach rule] (c3) -- (c4);
  \node[pd breach datum] at ($(c3)!0.5!(c4)$) {};
  \node[pd breach label,anchor=west] at (2.98,.22) {edge 3--4};
  \node[pd focus state,minimum width=2.5cm,minimum height=.70cm,
    inner xsep=5pt,inner ysep=4pt] at (\cx,-1.35) {$r=1.2247$: visible};

  \node[pd title,anchor=north,text width=5.0cm,align=center] at (7.78,4.05)
    {B. path: edge 3--4 is a cut};
  \foreach \i in {1,...,6}
    {\node[pd state,circle,minimum size=6.4mm,inner sep=0pt] (p\i)
       at ({\px+(\i-1)*\pitch},1.15) {\i};}
  \foreach \i/\j in {1/2,2/3,4/5,5/6}
    {\draw[pd rule] (p\i) -- (p\j);}
  \draw[pd breach rule] (p3) -- (p4);
  \node[pd breach datum] at ($(p3)!0.5!(p4)$) {};
  \node[pd breach label,anchor=south,text width=2.8cm,align=center]
    at ($(p3)!0.5!(p4)+(0,.24)$) {same edge 3--4};

  % Two short stubs name the absent closure without drawing a false edge.
  \draw[pd guide] (p1.south west) -- ++(-.34,-.30);
  \draw[pd guide] (p6.south east) -- ++(.34,-.30);
  \node[pd label,anchor=north,text width=4.1cm,align=center] at (7.78,.45)
    {no 6--1 closing edge};
  \node[pd state,minimum width=2.5cm,minimum height=.70cm,
    inner xsep=5pt,inner ysep=4pt] at (7.78,-1.35) {$r=0$: invisible};
\end{tikzpicture}
\caption{The same disagreement is detectable on the cycle ($r=1.2247$)
but invisible on the path ($r=0$). Detection requires a visible cycle.
[verified, \protect\path{sheaf_consistency_radius.py}; seed 20260816]}
\label{fig:fh-cycle-vs-cut}
\end{figure}
