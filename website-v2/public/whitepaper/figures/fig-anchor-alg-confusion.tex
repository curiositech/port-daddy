% Semantic contract: identical token bytes diverge only where control over the
% verification rule changes hands. Two stacked sequence ladders, same message,
% opposite outcome, isolate the one causal difference: who chooses the algorithm.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm,
    box/.style={align=left,text width=3.5cm,inner xsep=5pt,inner ysep=5pt},
    outcome/.style={draw,line width=1.1pt,fill=hhpaper,rounded corners=1.5pt,
      inner xsep=7pt,inner ysep=5pt,font=\footnotesize\bfseries,
      align=center,text width=3.2cm}]

  % ---------- panel A: header-selected ----------
  \node[pd panel title,anchor=west] at (0,11.5) {A \quad header-selected};
  \node[pd row label,anchor=south] (tokA) at (0,10.75) {token};
  \node[pd row label,anchor=south] (verA) at (8.4,10.75) {verifier};
  \draw[pd arrow] (0,10.15) -- (8.4,10.15);
  \node[pd direct label,align=center,text width=7.4cm,anchor=south] at (4.2,10.2)
    {\textbf{1} token: \texttt{alg=none}, tampered payload};
  \node[pd caution fill,box,anchor=north] (rA) at (8.4,9.7)
    {\textbf{2} alg := \texttt{header.alg}\\[.2ex] (token picks its own rule)};
  \draw[pd rule,-{Stealth[length=1.6mm,width=1.0mm]}] (verA.south) -- (rA.north);
  \node[outcome,draw=hhamber,text=hhamber,anchor=north] (oA) at (8.4,7.9) {ACCEPT (exploited)};
  \draw[pd rule,-{Stealth[length=1.6mm,width=1.0mm]}] (rA.south) -- (oA.north);
  \draw[pd rule] (tokA.south) -- (0,7.15);

  % ---------- shared note ----------
  \node[pd note,align=center,text width=9.6cm,anchor=north] (nAB) at (4.2,6.5)
    {\hyphenpenalty=10000\exhyphenpenalty=10000
     same bytes on the wire; the only thing that differs below is who chose
     the verifier};

  % ---------- panel B: policy-pinned ----------
  \node[pd panel title,anchor=west] at (0,5.15) {B \quad policy-pinned};
  \node[pd row label,anchor=south] (tokB) at (0,4.4) {token};
  \node[pd row label,anchor=south] (verB) at (8.4,4.4) {verifier};
  \draw[pd arrow] (0,3.8) -- (8.4,3.8);
  \node[pd direct label,align=center,text width=7.4cm,anchor=south] at (4.2,3.85)
    {\textbf{1} token: \texttt{alg=none}, tampered payload};
  \node[pd focus fill,box,anchor=north] (rB) at (8.4,3.35)
    {\textbf{2} alg := Ed25519\\[.2ex] (issuer policy, out-of-band)};
  \draw[pd rule,-{Stealth[length=1.6mm,width=1.0mm]}] (verB.south) -- (rB.north);
  \node[pd focus fill,box,anchor=north] (cB) at (8.4,1.45)
    {\textbf{3} signature check on tampered payload: fails};
  \draw[pd rule,-{Stealth[length=1.6mm,width=1.0mm]}] (rB.south) -- (cB.north);
  \node[outcome,draw=hhteal,text=hhteal,anchor=north] (oB) at (8.4,-.4) {REJECT (safe)};
  \draw[pd rule,-{Stealth[length=1.6mm,width=1.0mm]}] (cB.south) -- (oB.north);
  \draw[pd rule] (tokB.south) -- (0,-1.35);
\end{tikzpicture}
\caption{Two verifier ladders for the same wire bytes: \textbf{token} sends
\texttt{alg=none} with a tampered payload to \textbf{verifier} in both panels.
Panel A lets the token's own header choose the algorithm and message 2
accepts the forgery; panel B pins the algorithm to Ed25519 from issuer policy,
so message 3's signature check fails and the token is rejected. This is the
design invariant behind the ProVerif v1 (HS256) query
$\neg\,\text{attacker}(\mathit{master\_key})$ holding TRUE: the accepting path
in panel A is exactly the path Algorithmic Pinning removes
[verified, v1\_refined.pv].}
\label{fig:anchor-alg-confusion}
\end{figure}
