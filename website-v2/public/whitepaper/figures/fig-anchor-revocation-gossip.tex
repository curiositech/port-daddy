% Semantic contract: each verifier has its own exposure interval; logarithmic
% dissemination is an expectation, not a wall-clock deadline.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\xzero{-4.5}
  \def\xend{5.4}
  \foreach \y/\peer/\wake in {2.4/A/-4.5,1.35/B/-1.4,.3/C/2.0,-.75/D/-.6,-1.8/E/2.0} {
    \node[pd axis label,anchor=east] at (\xzero-.32,\y) {daemon \peer};
    \draw[draw=hhamber,line width=2.6pt,line cap=round] (\xzero,\y) -- (\wake,\y);
    \draw[draw=hhpaper,line width=1pt,line cap=round] (\xzero,\y) -- (\wake,\y);
    \draw[draw=hhteal,line width=2.6pt,line cap=round] (\wake,\y) -- (\xend,\y);
    \node[pd focus datum,minimum size=4.8pt,inner sep=0pt] at (\wake,\y) {};
  }
  \draw[pd focus rule]
    (-4.5,2.4) -- (-4.5,1.95) -- (-1.4,1.95) -- (-1.4,1.35)
    (-4.5,1.95) -- (-.6,1.95) -- (-.6,-.75)
    (-1.4,1.35) -- (-1.4,.82) -- (2,.82) -- (2,.3)
    (-.6,-.75) -- (-.6,-1.28) -- (2,-1.28) -- (2,-1.8);
  \node[pd direct label,text=hhteal,anchor=south west] at (-4.35,2.62)
    {authoritative append};

  \draw[pd rule,-{Stealth[length=1.7mm,width=1.05mm]}] (\xzero,-2.32) -- (\xend,-2.32);
  \foreach \x/\lab in {-4.5/$t$,-1.4/$t+\Delta$,2/$t+2\Delta$} {
    \draw[pd tick] (\x,-2.23) -- (\x,-2.42);
    \node[pd axis label,anchor=north] at (\x,-2.48) {\lab};
  }
  \node[pd axis label,anchor=west] at (\xend,-2.32) {time};
  \node[pd direct label,text=hhamber,anchor=north west] at (-4.5,-2.98)
    {amber interval: verifier may still admit the card};

  \draw[pd caution rule,dashed] (4.05,-2.9) -- (4.05,2.85);
  \draw[pd caution arrow] (4.05,-2.78) -- (5.2,-2.78);
  \node[pd direct label,text=hhamber,anchor=west] at (5.25,-2.78) {partition $\Rightarrow$ no finite deadline};
\end{tikzpicture}
\caption{A revocation advances as an observation frontier, leaving a distinct
stale-exposure interval on every verifier lane. This execution converges after
two rounds; the protocol claim is only expected $\Theta(\log m)$ dissemination
under connected reliable rounds. The dashed partition marker separates that
expectation from a guarantee: an unbounded partition removes every finite
delivery deadline.}
\label{fig:anchor-revocation-gossip}
\end{figure}
