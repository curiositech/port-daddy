% Minimal compromise sets for the four federated-security guarantees. The
% breach marker is a plain circle everywhere except the one conjunctive cut,
% which spells the word AND rather than relying on a diamond glyph.
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  pd figure,x=1cm,y=1cm,
  secret/.style={circle,draw=hhink,line width=.65pt,fill=hhpaper,
    minimum size=7.5mm,inner sep=1pt,font=\footnotesize},
  breach/.style={circle,draw=hhamber,line width=1pt,fill=hhamber!10,
    minimum size=8.5mm,inner sep=1pt,font=\footnotesize\bfseries},
  condition/.style={font=\footnotesize,align=center,text=hhink},
]
  \node[pd panel title,anchor=west,text width=9.6cm,align=left] at (-.2,6.35)
    {minimum compromise sets: one conjunction, three single points};

  \foreach \x in {2.55,5.35,8.15}
    \draw[pd guide] (\x,.6)--(\x,5.55);

  % Guarantee headings.
  \node[pd panel title,align=center,text width=2.5cm] at (1.2,5.3)
    {FORGE\\A CARD};
  \node[pd panel title,align=center,text width=2.5cm] at (3.9,5.3)
    {READ\\EVIDENCE};
  \node[pd panel title,align=center,text width=2.5cm] at (6.6,5.3)
    {IMPERSONATE\\USER};
  \node[pd panel title,align=center,text width=2.5cm] at (9.5,5.3)
    {ROLL BACK\\FOREST};

  % Single-key cut: daemon signing key.
  \node[breach] (b1) at (1.2,4.15) {};
  \node[secret] (s1) at (1.2,2.85) {{\normalsize$sk_D$}};
  \draw[pd caution arrow] (s1.north)--(b1.south);
  \node[condition,text width=2.2cm] at (1.2,2.15) {daemon signing key};
  \draw[pd focus rule] (.3,1.35)--(2.1,1.35);
  \node[pd note,text=hhteal,align=center,text width=2.2cm] at (1.2,.95)
    {rotation +\\revocation};

  % Single-key cut: harbor session key.
  \node[breach] (b2) at (3.9,4.15) {};
  \node[secret] (s2) at (3.9,2.85) {{\normalsize$k_H$}};
  \draw[pd caution arrow] (s2.north)--(b2.south);
  \node[condition,text width=2.2cm] at (3.9,2.15) {harbor session key};
  \draw[pd focus rule] (2.75,1.35)--(4.55,1.35);
  \node[pd note,text=hhteal,align=center,text width=2.2cm] at (3.9,.95)
    {keyring +\\host sandbox};

  % The only conjunctive cut: email AND passphrase, spelled out in words.
  \node[breach] (b3) at (6.6,4.15) {AND};
  \node[secret] (email) at (6.0,2.85) {@};
  \node[secret] (pass) at (7.2,2.85) {$\phi$};
  \draw[pd caution arrow] (email.north)--(b3.south west);
  \draw[pd caution arrow] (pass.north)--(b3.south east);
  \node[condition,text width=1.1cm] at (6.0,2.15) {email};
  \node[condition,text width=1.5cm] at (7.2,2.15) {passphrase};
  \draw[pd focus rule] (5.7,1.35)--(7.5,1.35);
  \node[pd note,text=hhteal,align=center,text width=2.2cm] at (6.6,.95)
    {two-factor\\recovery};

  % Single-witness cut: equivocation or deletion by the KMS witness.
  \node[breach] (b4) at (9.5,4.15) {};
  \node[secret] (s4) at (9.5,2.85) {{\normalsize$\sigma_K$}};
  \draw[pd caution arrow] (s4.north)--(b4.south);
  \node[condition,text width=2.2cm] at (9.5,2.15) {KMS witness\\signature};
  \draw[pd focus rule] (7.85,1.35)--(9.65,1.35);
  \node[pd note,text=hhteal,align=center,text width=2.2cm] at (9.5,.95)
    {cross-witness\\roots};

  \node[pd note,align=center,text width=9.4cm] at (4.75,-.15)
    {circles are sufficient cut sets; teal baselines name consequence-limiting
     controls; scope excludes same-user code execution on the daemon host};
\end{tikzpicture}
\caption{\linespread{1.08}\selectfont\textbf{The custody theorem has four
small attack cut sets, one of them conjunctive.} Card forgery, evidence
decryption, and forest rollback each have one sufficient secret; user
impersonation is the sole conjunctive failure, spelled AND rather than drawn
as a diamond, and requires both email and passphrase. The teal baselines name
the consequence-limiting control for each cut. Same-user code execution stays
outside the theorem because that adversary can read every secret already
available to the user process [internal].}
\label{fig:bonded-key-custody}
\end{figure}
