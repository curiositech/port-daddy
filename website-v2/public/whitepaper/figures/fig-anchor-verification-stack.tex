% Semantic contract: obligations descend across two human-asserted bridges;
% evidence returns upward but cannot erase either assumption gap.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \foreach \y/\title/\tool/\claim in {
    2.7/{Protocol model}/{ProVerif}/{secrecy \quad correspondence \quad attenuation},
    .25/{Rust source}/{Kani + tests}/{subset logic \quad bounds \quad non-panic},
    -2.2/{Running binary}/{conformance suite}/{selected attacks \quad FFI boundary}} {
    \draw[pd hairline] (-4.4,\y) -- (4.6,\y);
    \node[pd panel title,anchor=east] at (-4.62,\y) {\title};
    \node[pd direct label,anchor=west] at (-4.25,\y+.22) {\tool};
    \node[pd axis label,anchor=west] at (-4.25,\y-.25) {\claim};
    \fill[hhink] (4.6,\y) circle (1.6pt);
  }

  \draw[pd arrow,line width=1.0pt] (-5.35,3.05) -- (-5.35,-2.55);
  \node[pd direct label,anchor=south,rotate=90] at (-5.55,.25) {obligations};
  \draw[pd focus arrow,line width=1.05pt] (5.35,-2.55) -- (5.35,3.05);
  \node[pd direct label,text=hhteal,anchor=north,rotate=90] at (5.58,.25) {evidence};

  \foreach \y/\gap in {1.48/{specification gap},-0.98/{deployment gap}} {
    \draw[pd caution rule,dashed] (-4.4,\y) -- (-.7,\y);
    \draw[pd caution rule,dashed] (.7,\y) -- (4.6,\y);
    \draw[pd caution rule] (-.7,\y) to[out=25,in=155] (.7,\y);
    \node[pd caution datum,minimum size=4.8pt,inner sep=0pt] at (0,\y+.12) {};
    \node[pd direct label,text=hhamber,anchor=north] at (0,\y-.18) {\gap};
  }

  \draw[pd focus arrow] (4.6,-2.2) -- (4.6,.25);
  \draw[pd focus arrow] (4.6,.25) -- (4.6,2.7);
  \draw[pd arrow] (-4.4,2.7) -- (-4.4,.25);
  \draw[pd arrow] (-4.4,.25) -- (-4.4,-2.2);
  \node[pd note,anchor=north] at (0,-2.75)
    {returning evidence narrows the gaps; only a human asserts each bridge};
\end{tikzpicture}
\caption{The assurance case is a two-rail bridge. Obligations descend from
protocol model to source to deployed binary; observed evidence returns upward
through the same layers. The amber spans are irreducible assumption gaps: tool
output on either bank can narrow them, but only a human asserts that the modeled
protocol is this source and that the inspected source is this running binary.}
\label{fig:anchor-verification-stack}
\end{figure}
