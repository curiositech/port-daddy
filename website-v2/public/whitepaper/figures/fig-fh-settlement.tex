% Figure IV.8 --- the custody theorem is tested against its bypass counterfactual.
\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[pd figure]
  \node[pd panel title,anchor=west] at (-6.9,3.0) {A. non-bypassable custody};
  \node[pd panel title,anchor=west] at (.55,3.0) {B. operator bypass exists};
  \node[pd note,anchor=west] at (-6.9,2.64) {one terminal transition};
  \node[pd note,text=hhamber,anchor=west] at (.55,2.64) {same ceremony, weaker authority boundary};

  % Safe outcome graph.
  \node[pd datum] (fundeda) at (-6.0,.65) {};
  \node[pd decision] (gatea) at (-4.25,.65) {criteria};
  \node[pd focus datum] (cleara) at (-2.15,1.45) {};
  \node[pd datum] (refusea) at (-2.15,-.15) {};
  \draw[pd arrow] (fundeda)--(gatea);
  \draw[pd focus arrow] (gatea)--(cleara);
  \draw[pd arrow] (gatea)--(refusea);
  \node[pd direct label,anchor=south] at (-6.0,.87) {funded};
  \node[pd direct label,text=hhteal,anchor=west] at (-1.90,1.45) {CLEAR};
  \node[pd direct label,anchor=west] at (-1.90,-.15) {REFUSE};
  \node[pd note,anchor=west] at (-1.90,1.08) {compensate claimant};
  \node[pd note,anchor=west] at (-1.90,-.52) {return bond};
  \draw[pd focus rule] (-6.15,-1.15)--(-1.05,-1.15);
  \node[pd direct label,text=hhteal] at (-3.60,-1.45)
    {worst-case custody loss $\leq\varphi$};

  % Panel B shifted right by 0.55: panel A's `compensate claimant` note is
  % 2.8cm of grotesk running east from x=-1.90, and panel B's `funded` label
  % began at x=0.75, so the two overlapped by 17.7% of the smaller box (20.8%
  % at the merge base -- pre-existing, not introduced here). Wrapping the note
  % instead was tried and is worse: CLEAR sits 0.37 units above it and the
  % second line lands on that. The picture is inside \resizebox{0.95\textwidth},
  % so widening it costs nothing on the page.
  % Counterfactual graph with an extra unauthorized transition.
  \node[pd datum] (fundedb) at (1.90,.65) {};
  \node[pd decision] (gateb) at (3.65,.65) {criteria};
  \node[pd focus datum] (clearb) at (5.75,1.45) {};
  \node[pd datum] (refuseb) at (5.75,-.15) {};
  \node[pd caution datum] (lossb) at (7.10,-1.50) {};
  \draw[pd arrow] (fundedb)--(gateb);
  \draw[pd focus arrow] (gateb)--(clearb);
  \draw[pd arrow] (gateb)--(refuseb);
  \draw[pd caution arrow,dashed,line width=1.25pt]
    (fundedb) to[out=-50,in=170] (lossb);
  \node[pd direct label,anchor=south] at (1.90,.87) {funded};
  \node[pd direct label,text=hhteal,anchor=west] at (6.00,1.45) {CLEAR};
  \node[pd direct label,anchor=west] at (6.00,-.15) {REFUSE};
  \node[pd direct label,text=hhamber,anchor=east,align=right] at (6.77,-1.34)
    {redirect / second close\\full balance exposed};
  \node[pd note,text=hhamber,anchor=west] at (2.65,-.70) {unauthorized transition};
  \draw[pd caution rule] (.95,-2.10)--(7.30,-2.10);
  \node[pd direct label,text=hhamber] at (4.40,-2.40)
    {worst case $=$ full custody balance};

  \node[pd axis label] at (0,-3.05)
    {Custody Assumption: whitelist $+$ fee cap $+$ one terminal transition are the theorem's hypothesis, not decoration};
\end{tikzpicture}}
\caption{\textbf{The custody bound is the absence of an extra transition.}
Panel A permits exactly one terminal move from funded custody to \textbf{Clear}
or \textbf{Refuse}; with a recipient whitelist and fee cap, worst-case loss is
bounded by the agreed fee $\varphi$. Panel B adds one operator-controlled bypass.
The visible extra edge reaches redirect or a second close, exposing the full
balance. Threshold signing is only one candidate implementation; the loss theorem
holds only when the three authority restrictions are actually non-bypassable.}
\label{fig:fh-settlement}
\end{figure}
