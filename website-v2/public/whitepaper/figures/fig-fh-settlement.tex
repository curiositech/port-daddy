% Cross-harbor settlement — bonded escrow, conditional atomic clear.

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D8C7A6}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhcobalt}{HTML}{003FB8}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}
\providecolor{maydayred}{HTML}{8B0000}

\begin{figure}[H]
\centering
\resizebox{0.92\textwidth}{!}{%
\begin{tikzpicture}[
  font=\sffamily\small,
  party/.style={draw=hhebony,thick,rounded corners=3pt,fill=hhsand!50,
                inner sep=5pt,minimum width=34mm,minimum height=18mm,
                align=center,font=\sffamily\bfseries\small},
  escrow/.style={draw=hhcobalt,very thick,rounded corners=3pt,fill=hhpaper,
                 inner sep=5pt,minimum width=38mm,minimum height=20mm,
                 align=center,font=\sffamily\small},
  stepbox/.style={draw=hhebony,thick,fill=hhpaper,rounded corners=2pt,
               inner sep=3pt,font=\sffamily\footnotesize,align=left,
               text width=44mm},
  edge/.style={->,>=Latex,thick,draw=hhebony},
  constraint/.style={draw=maydayred,thick,fill=maydayred!8,rounded corners=3pt,
                     inner sep=5pt,align=left,font=\sffamily\footnotesize,
                     text width=42mm},
]

% ─── Parties at top: Alice's principal (poster), Bob's principal (claimant) ─
\node[party] (PA) at (0,0)
  {Alice (principal)\\\footnotesize bond poster};
\node[escrow,right=22mm of PA] (E)
  {\textbf{Bonded Escrow}\\\footnotesize recipient whitelist; fee cap;\\\footnotesize one atomic terminal transition};
\node[party,right=22mm of E] (PB)
  {Bob (principal)\\\footnotesize damage claimant};

% ─── Step boxes below the line ──────────────────────────────────────
\node[stepbox,below=14mm of PA.south,xshift=-2mm] (s1) {%
  \textbf{1.} Alice's harbor mints work card for agent $\alpha$; agent operates at $B$. Bond $B_\alpha = \$B$ posted at $A$, locked in escrow.};
\node[stepbox,below=14mm of E.south] (s2) {%
  \textbf{2.} Damage detected at $B$ (acceptance criteria fail, or invariant violation). Bob's harbor signs claim $\mathsf{cl}_B = (R_B^{(e)}, \mathrm{evidence})$.};
\node[stepbox,below=14mm of PB.south,xshift=2mm] (s3) {%
  \textbf{3.} Escrow verifies inclusion of $\alpha$'s actions in $R_B^{(e)}$ and matches against acceptance preconditions signed at step 1. \emph{Two outcomes only.}};

% ─── Assumptions and outcomes, with orthogonal routing ───────────────────────
\node[constraint,below=24mm of s1.south] (guard) {%
  \textbf{Required authority boundary:} neither harbor can bypass the
  recipient whitelist, fee cap, or atomic terminal-state transition.};
\node[party,fill=hhteal!30,below=24mm of s2.south] (Op) {%
  \textbf{Clear:}\\\footnotesize $B$ paid out of bond,\\\footnotesize remainder refunded};
\node[party,fill=hhamber!20,below=24mm of s3.south] (Or) {%
  \textbf{Refuse:}\\\footnotesize bond returned to $A$,\\\footnotesize escrow logs reason};

% ─── Flow edges ────────────────────────────────────────────────────
\draw[edge] (PA.south) -- (s1.north);
\draw[edge] (E.south) -- (s2.north);
\draw[edge] (PB.south) -- (s3.north);
\draw[edge] (s1.east) -- (s2.west);
\draw[edge] (s2.east) -- (s3.west);
% Outcomes from the verification step share a clear orthogonal branch.
\coordinate (fork) at ($(s3.south)+(0,-10mm)$);
\draw[edge] (s3.south) -- (fork) -| (Op.north);
\draw[edge] (fork) -- (Or.north);

\end{tikzpicture}%
}
\caption{Cross-harbor settlement protocol. The custody bound is conditional,
not automatic: the escrow authority must enforce a recipient whitelist, a fee
cap, and exactly one atomic terminal transition, and neither harbor may possess
an out-of-band bypass. Under those assumptions the outcome space is clear or
refuse. Without them, the worst-case loss is the full balance held in custody.}
\label{fig:fh-settlement}
\end{figure}
