% Fig 6 — Sybil / whitewashing defeated by a daemon-minted non-forgeable id.
\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D4C5A9}
\providecolor{hhebony}{HTML}{1E1B18}
\providecolor{hhink}{HTML}{2C2A26}
\providecolor{hhcinnabar}{HTML}{CC3D2E}
\providecolor{hhbrass}{HTML}{B08D57}
\providecolor{hhpatina}{HTML}{5C7A6A}
\providecolor{hhpaper}{HTML}{FBF7EE}

\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[
  font=\sffamily\small,
  box/.style={draw=hhebony,thick,rounded corners=2pt,inner sep=4pt,align=center,
              minimum height=10mm},
  bad/.style={->,>=Latex,thick,draw=hhcinnabar,dashed},
  good/.style={->,>=Latex,thick,draw=hhpatina},
]
% TOP — the attacks against self-asserted strings
\node[font=\sffamily\bfseries,anchor=west] at (-0.3,3.2)
  {Self-asserted id (\texttt{project:stack:context}) --- both attacks are free:};
\node[box,fill=hhsand] (bad1) at (0,1.8) {agent ``X''\\(bad record)};
\node[box,fill=hhsand,right=18mm of bad1] (bad2) {agent ``X'\,'\\(fresh, clean)};
\draw[bad] (bad1) -- node[midway,above,font=\sffamily\scriptsize,text=hhcinnabar]
  {respawn = \textbf{whitewash}} (bad2);
\node[box,fill=hhsand,right=22mm of bad2] (syb)
  {1000 $\times$ ``X$_i$''\\(forged quorum)};
\draw[bad] (bad2) -- node[midway,above,font=\sffamily\scriptsize,text=hhcinnabar]
  {replicate = \textbf{Sybil}} (syb);

% BOTTOM — the fix: daemon-minted non-forgeable id bound to credential + principal
\node[font=\sffamily\bfseries,anchor=west] at (-0.3,-0.6)
  {Daemon-minted non-forgeable \texttt{actor\_id} (ADR-0040), bound to a principal:};
\node[box,fill=hhpatina!14,draw=hhpatina,thick] (daemon) at (1.2,-2.3)
  {daemon\\mints \texttt{actor\_id}};
\node[box,fill=hhpaper,right=14mm of daemon] (cred) {non-re-pickable\\credential (key)};
\node[box,fill=hhpaper,right=12mm of cred] (princ) {principal\\(owns the fleet,\\posts bonds)};
\draw[good] (daemon) -- (cred);
\draw[good] (cred) -- (princ);
\node[draw=hhbrass,thick,fill=hhsand!30,rounded corners=2pt,below=10mm of cred,
      align=left,text width=120mm,inner sep=5pt,font=\sffamily\footnotesize] (note)
  {\textbf{Newcomer policy = a shape, not a scalar.} A fresh id may have full
   \emph{work} ability but a reduced \emph{economic ceiling} until it earns a
   witnessed history (Friedman--Resnick). Identity churn then costs \emph{more}
   than a clean record is worth, and a sanction \emph{survives} a respawn.};
\end{tikzpicture}}
\caption{\textbf{The non-forgeable root forecloses the two classic attacks.} With
self-asserted strings (top), whitewashing and Sybil are free. A daemon-minted
\texttt{actor\_id} bound to a non-re-pickable credential and a principal (bottom)
makes both costly: churn costs more than a clean record, and sanctions survive
respawns. This is the one genuinely architectural piece of the L3 program.}
\label{fig:sybil}
\end{figure}
