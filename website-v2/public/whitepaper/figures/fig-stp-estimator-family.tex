% Fig 8 — the estimator family by signal type (BT/Elo/TrueSkill, EigenTrust, feedback).
\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D4C5A9}
\providecolor{hhebony}{HTML}{1E1B18}
\providecolor{hhink}{HTML}{2C2A26}
\providecolor{hhcinnabar}{HTML}{CC3D2E}
\providecolor{hhbrass}{HTML}{B08D57}
\providecolor{hhpatina}{HTML}{5C7A6A}
\providecolor{hhpaper}{HTML}{FBF7EE}

\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[
  font=\small,
  sig/.style={draw=hhink,thick,fill=hhpaper,rounded corners=2pt,inner sep=4pt,
              align=center,minimum width=34mm,font=\bfseries\footnotesize},
  est/.style={draw=hhebony,thick,fill=hhsand!30,rounded corners=2pt,inner sep=4pt,
              align=center,minimum width=40mm,text width=38mm,font=\footnotesize},
  arr/.style={->,>=Latex,thick,draw=hhink},
]
\node[font=\bfseries,anchor=west] at (-0.3,3.3)
  {Choose the estimator by the \emph{signal type} (the wrong choice is a real error):};

\node[sig] (s1) at (0,2) {pairwise / tournament\\``$A$ beat $B$''};
\node[sig,below=8mm of s1] (s2) {trust propagation\\``who does the net trust''};
\node[sig,below=8mm of s2] (s3) {marketplace feedback\\``buyers rate sellers''};

\node[est,right=22mm of s1] (e1)
  {\textbf{Bradley--Terry} (full-history MLE) / \textbf{Elo} (online special case) /
   \textbf{TrueSkill} ($\sigma$ for cold start)};
\node[est,right=22mm of s2] (e2)
  {\textbf{EigenTrust} --- principal eigenvector of the local-trust matrix;
   PageRank for reputation; \emph{not a bandit}};
\node[est,right=22mm of s3] (e3)
  {\textbf{Feedback systems} --- Resnick--Zeckhauser ${\sim}8\%$ price premium;
   Tadelis: answer to adverse selection};

\draw[arr] (s1) -- (e1);
\draw[arr] (s2) -- (e2);
\draw[arr] (s3) -- (e3);

\node[draw=hhink,thick,fill=hhsand!30,rounded corners=2pt,below=8mm of e3,
      anchor=north,text width=120mm,align=left,inner sep=5pt,font=\footnotesize] (note)
  {A SQLite harbor holds the \emph{whole} settled-outcome history, so it is in the
   \textbf{Bradley--Terry regime}, not the streaming-Elo regime. None of these is
   the hard part: the estimator is \emph{cheap and last}. The substrate beneath ---
   non-forgeable identity, continuity, an oracle-witnessed ledger --- is the gate.};
\end{tikzpicture}}
\caption{\textbf{The estimator family, by signal type.} Pairwise outcomes call for
Bradley--Terry / Elo / TrueSkill; network trust calls for EigenTrust; marketplace
feedback calls for feedback systems with measured price premia. All are
well-understood and quick to build --- which is exactly why they are the cheap,
last part of the program.}
\label{fig:estimators}
\end{figure}
