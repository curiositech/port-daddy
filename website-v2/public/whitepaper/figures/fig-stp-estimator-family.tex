% Fig 8 — estimator families grouped by the signal they consume.
\begin{figure}[H]
\centering
\resizebox{0.92\textwidth}{!}{%
\begin{tikzpicture}[font=\small]
  \node[stp heading] at (-3.5,3.1) {observed signal};
  \node[stp heading] at (3.5,3.1) {matching estimator family};

  \node[stp box,minimum width=54mm,text width=47mm] (s1) at (-3.5,2)
    {\textbf{pairwise / tournament}\\{\scriptsize ``agent A beat agent B''}};
  \node[stp accent box,minimum width=61mm,text width=54mm] (e1) at (3.5,2)
    {\textbf{Bradley--Terry} (full-history MLE)\\
     {\scriptsize Elo \(\cdot\) TrueSkill for streaming updates}};
  \draw[stp accent arrow] (s1) -- (e1);

  \node[stp box,minimum width=54mm,text width=47mm] (s2) at (-3.5,0)
    {\textbf{trust propagation}\\{\scriptsize ``whom does the network trust?''}};
  \node[stp box,minimum width=61mm,text width=54mm] (e2) at (3.5,0)
    {\textbf{EigenTrust}\\{\scriptsize principal eigenvector of local trust}};
  \draw[stp arrow] (s2) -- (e2);

  \node[stp box,minimum width=54mm,text width=47mm] (s3) at (-3.5,-2)
    {\textbf{marketplace feedback}\\{\scriptsize ``buyers rate sellers''}};
  \node[stp box,minimum width=61mm,text width=54mm] (e3) at (3.5,-2)
    {\textbf{feedback systems}\\{\scriptsize Resnick--Zeckhauser \(\cdot\) Tadelis}};
  \draw[stp arrow] (s3) -- (e3);
\end{tikzpicture}}
\caption{\textbf{The estimator family, by signal type} --- the wrong choice is a
real error. Pairwise outcomes call for Bradley--Terry / Elo / TrueSkill; network
trust calls for EigenTrust (PageRank for reputation --- \emph{not} a bandit);
marketplace feedback calls for feedback systems with measured price premia
(Resnick--Zeckhauser's ${\sim}8\%$; Tadelis on adverse selection). A SQLite harbor holds the \emph{whole} settled-outcome
history, so it is in the Bradley--Terry regime, not the streaming-Elo regime. None
of these is the hard part: the estimator is \emph{cheap and last}, while the
substrate beneath --- non-forgeable identity, continuity, an oracle-witnessed
ledger --- is the gate.}
\label{fig:estimators}
\end{figure}
