% Figure III.8 --- estimator choice follows the topology of the observed signal.
\begin{figure}[H]
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tikzpicture}[pd figure]
  \node[pd panel title] at (-4.75,2.85) {pairwise outcomes};
  \node[pd panel title] at (0,2.85) {directed trust};
  \node[pd panel title] at (4.75,2.85) {buyer feedback};

  % Pairwise-comparison graph.
  \foreach \x/\y/\n in {-5.8/1.35/A,-3.75/1.65/B,-4.75/-.15/C,-5.95/-.65/D} {
    \node[pd actor,circle,minimum size=7mm,inner sep=0pt] (p\n) at (\x,\y) {\n};
  }
  \draw[pd focus arrow] (pA) -- (pB);
  \draw[pd focus arrow] (pC) -- (pB);
  \draw[pd focus arrow] (pD) -- (pC);
  \draw[pd focus arrow] (pA) -- (pC);
  \node[pd direct label,anchor=north,text=hhamber] at (-4.75,-1.05)
    {Bradley--Terry\\[-1pt]{\tiny full-history MLE selected here}};

  % Trust network: node size/position exposes propagation topology.
  \node[pd actor,circle,minimum size=7mm,inner sep=0pt] (tA) at (-1.45,1.1) {A};
  \node[pd actor,circle,minimum size=10mm,inner sep=0pt,draw=hhteal,line width=1pt] (tB) at (0,1.55) {B};
  \node[pd actor,circle,minimum size=7mm,inner sep=0pt] (tC) at (1.45,.9) {C};
  \node[pd actor,circle,minimum size=6mm,inner sep=0pt] (tD) at (-.75,-.45) {D};
  \node[pd actor,circle,minimum size=6mm,inner sep=0pt] (tE) at (.95,-.55) {E};
  \draw[pd focus arrow] (tA) -- (tB);
  \draw[pd focus arrow] (tC) -- (tB);
  \draw[pd focus arrow] (tD) -- (tB);
  \draw[pd focus arrow] (tE) -- (tB);
  \draw[pd arrow] (tA) -- (tD);
  \node[pd direct label,anchor=north] at (0,-1.05)
    {EigenTrust\\[-1pt]{\tiny centrality over local trust}};

  % Marketplace feedback distribution.
  \draw[pd rule] (3.1,-.55) -- (6.45,-.55);
  \foreach \x/\h in {3.25/.25,3.75/.5,4.25/.85,4.75/1.25,5.25/1.75,5.75/1.35,6.25/.65}
    \fill[hhteal!72] (\x-.14,-.55) rectangle (\x+.14,{-.55+\h});
  \node[pd axis label,anchor=north] at (3.25,-.72) {low};
  \node[pd axis label,anchor=north] at (6.25,-.72) {high};
  \node[pd direct label,anchor=north] at (4.75,-1.05)
    {feedback model\\[-1pt]{\tiny ratings, prices, adverse selection}};

  \draw[pd hairline] (-7,-2.15) -- (7,-2.15);
  \node[pd note,anchor=west] at (-6.85,-2.48)
    {the scoring formula is cheap and last; identity, continuity, and oracle-grounded outcomes are the gate};
\end{tikzpicture}}
\caption{\textbf{Estimator families are keyed to signal topology.} Pairwise wins
form a comparison graph and call for Bradley--Terry; a complete harbor history
supports the full-history fit rather than streaming Elo. Directed local trust forms
a network and calls for EigenTrust. Buyer ratings and price effects form a feedback
distribution and call for marketplace models. These are not interchangeable score
recipes. None repairs a forgeable identity or an agent-authored oracle: the
trustworthy substrate still comes first.}
\label{fig:estimators}
\end{figure}
