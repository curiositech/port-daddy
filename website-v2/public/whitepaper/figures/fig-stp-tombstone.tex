% Fig 13 — append a compensating tombstone; never rewrite settled history.
\begin{figure}[H]
\centering
\resizebox{0.94\textwidth}{!}{%
\begin{tikzpicture}[font=\small]
  \node[stp box,minimum width=29mm] (e1) at (-6,0)
    {outcome $o_1$\\{\scriptsize honest}};
  \node[stp box,minimum width=29mm] (e2) at (-3,0)
    {outcome $o_2$\\{\scriptsize honest}};
  \node[stp box,minimum width=29mm] (e3) at (0,0)
    {outcome $o_3$\\{\scriptsize fraudulent}};
  \node[stp box,minimum width=29mm] (e4) at (3,0)
    {outcome $o_4$\\{\scriptsize honest}};
  \node[stp accent box,minimum width=34mm] (t) at (6.4,0)
    {\textbf{tombstone}\\{\scriptsize nullifies $o_3$}};
  \draw[stp arrow] (e1) -- (e2);
  \draw[stp arrow] (e2) -- (e3);
  \draw[stp arrow] (e3) -- (e4);
  \draw[stp accent arrow] (e4) -- (t);
  \draw[stp dashed arrow] (t.north) .. controls +(0,12mm) and +(0,12mm) .. (e3.north);
  \node[stp label] at (3.2,1.25) {compensating saga entry};
\end{tikzpicture}}
\caption{\textbf{Tombstone revocation on an append-only ledger} (never mutate in
place). A fraudulent outcome is not deleted (that would break append-only and
auditability); it is nullified by an appended \emph{tombstone} --- a saga-style
compensating entry --- which propagates across harbors bounded by the
revocation-convergence window (Chapter~IV); the poisoned point was spendable only
during that window. ``Reputation never ends'' is corrected to: monotone in
\emph{honest} outcomes (they are never silently erased), individually revocable via
tombstone, with bounded memory (Liu--Skrzypacz) as a tunable parameter rather than a
virtue --- $\infty$-memory freezes types and removes the incentive to keep
performing.}
\label{fig:tombstone}
\end{figure}
