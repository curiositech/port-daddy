% Reader question: who sends each transfer message, and when does A leave the synchronous path?
% Claim: only offer and ack cross the boundary; after ack, B verifies and delivers locally.
% Evidence: xfer_req<C_A>, xfer_offer<C_A,att_A,R_A^(e)>, xfer_ack<C'_B,R_B^(e)>, xfer_deliver<C'_B>.
% Must distinguish: local agent-daemon messages from the two cross-harbor messages and later asynchronous revocation.
% Grammar chosen: four-lifeline UML sequence inside two dashed harbor groups; rejected: self-loops that looked outgoing.
% Five-second test: messages 2 and 3 cross the wire, and message 3 ends A's synchronous role.
\pdfigurehue{pdgold}%
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\xS{0.75} \def\xA{3.45} \def\xB{7.10} \def\xT{9.80}
  \node[pd title,anchor=west,text width=10.5cm] at (-0.35,1.55)
    {two wire crossings; acknowledgement hands the hot path to $B$};

  % Dashed scopes make locality explicit; the four lifelines remove ambiguous self-loops.
  \draw[pd panel] (-0.35,0.72) rectangle (4.52,-6.02);
  \draw[pd panel] (6.03,0.72) rectangle (10.90,-6.02);
  \node[pd kind] at (2.08,0.96) {Harbor $A$};
  \node[pd kind] at (8.46,0.96) {Harbor $B$};

  \node[pd artifact,minimum width=1.75cm] (hS) at (\xS,0) {agent};
  \node[pd label,text=pdinkmuted,anchor=north] at (\xS,-0.40) {source};
  \node[pd state,minimum width=1.85cm] (hA) at (\xA,0) {$D_A$};
  \node[pd label,text=pdinkmuted,anchor=north] at (\xA,-0.40) {daemon};
  \node[pd focus state,minimum width=1.85cm] (hB) at (\xB,0) {$D_B$};
  \node[pd label,text=pdinkmuted,anchor=north] at (\xB,-0.40) {daemon};
  \node[pd artifact,minimum width=1.75cm] (hT) at (\xT,0) {agent};
  \node[pd label,text=pdinkmuted,anchor=north] at (\xT,-0.40) {recipient};
  \foreach \x in {\xS,\xA,\xB,\xT}{\draw[pd guide] (\x,-1.10) -- (\x,-5.72);}

  \node[pd tag,anchor=south] at (2.10,-1.48)
    {$\mathsf{xfer\_req}\langle C_A\rangle$};
  \node[pd badge] (m1) at (2.10,-1.85) {1};
  \draw[pd arrow,-,pd to badge] (\xS,-1.85) -- (m1);
  \draw[pd arrow,pd from badge] (m1) -- (\xA,-1.85);

  \node[pd tag,anchor=south] at (5.28,-2.43)
    {$\mathsf{xfer\_offer}\langle C_A,\mathsf{att}_A,R_A{}^{(e)}\rangle$};
  \node[pd badge] (m2) at (5.28,-2.80) {2};
  \draw[pd protocol arrow,-,pd to badge] (\xA,-2.80) -- (m2);
  \draw[pd protocol arrow,pd from badge] (m2) -- (\xB,-2.80);

  \node[pd tag,anchor=south] at (5.28,-3.68)
    {$\mathsf{xfer\_ack}\langle C^{\prime}(B),R_B{}^{(e)}\rangle$};
  \node[pd badge] (m3) at (5.28,-4.05) {3};
  \draw[pd protocol arrow,-,pd to badge] (\xB,-4.05) -- (m3);
  \draw[pd protocol arrow,pd from badge] (m3) -- (\xA,-4.05);

  \node[pd tag,anchor=south] at (8.45,-4.93)
    {$\mathsf{xfer\_deliver}\langle C^{\prime}(B)\rangle$};
  \node[pd focus badge] (m4) at (8.45,-5.30) {4};
  \draw[pd focus arrow,-,pd to badge] (\xB,-5.30) -- (m4);
  \draw[pd focus arrow,pd from badge] (m4) -- (\xT,-5.30);

  \node[pd note,anchor=north west,text width=10.2cm] at (-0.05,-6.30)
    {after 3, $B$ verifies $C^{\prime}(B)$ under $\mathit{sk}_B$ alone; a later root from $A$ matters only on the asynchronous revocation path};
  \draw[pd hairline] (-0.05,-7.35) -- (10.55,-7.35);
  \node[pd kind,anchor=north west] at (-0.05,-7.52) {forecloses};
  \node[pd label,anchor=north west,align=left,text width=8.45cm] at (2.05,-7.52)
    {spoofing $\mathit{sk}_A$; transfer-time capability inflation; replay at $B$ against stale $R_A{}^{(e)}$};
  \node[pd kind,anchor=north west] at (-0.05,-8.52) {defers};
  \node[pd label,anchor=north west,align=left,text width=8.45cm] at (2.05,-8.52)
    {revocation latency; settlement disputes; admission of unbonded harbors};
  \draw[pd hairline] (-0.05,-9.68) -- (10.55,-9.68);
\end{tikzpicture}
\caption{\textbf{The transfer ceremony crosses the wire twice, then $B$ is on
its own.} Message (1) is local: the agent presents $C_A$ to $D_A$. Messages
(2) and (3), drawn in indigo, are the only ones that cross the trust boundary:
the signed offer binds $\mathrm{att}_A$ and $A$'s epoch root, and the signed
acknowledgement returns the $B$-bound attenuated card
$C^{\prime}(B)=\mathrm{att}_B(\mathrm{att}_A(C_A))\subseteq C_A$
(Theorem~\ref{lem:fh-att}) with $B$'s epoch root. After (3), gold marks the hot
path: $D_B$ delivers $C^{\prime}(B)$ locally (4) and every later presentation verifies
under $\mathit{sk}_B$ alone. Harbor $A$ still matters asynchronously --- a
later $R_A{}^{(e+1)}$ can revoke the transfer once it reaches $B$'s audit surface
(Figure~\ref{fig:fh-revocation}) --- but that path is not on the hot path drawn
here. [internal]}
\label{fig:fh-xfer}
\end{figure}
