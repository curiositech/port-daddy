% Cross-harbor capability transfer — four-message ceremony, sequence diagram.

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D8C7A6}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhcobalt}{HTML}{003FB8}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}

% ─── Cross-Harbor Capability Transfer Ceremony ────────────────────────────────
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  lane/.style={draw=hhink, line width=0.65pt, fill=hhsand!25, rounded corners=2pt, inner sep=6pt, minimum width=32mm, align=center},
  msgarr/.style={-{Stealth[length=2.0mm,width=1.3mm]}, draw=hhcobalt, line width=0.75pt},
  flowarr/.style={-{Stealth[length=2.0mm,width=1.3mm]}, draw=hhink, line width=0.55pt},
  notebox/.style={draw=hhink, line width=0.5pt, fill=hhpaper, rounded corners=2pt, inner sep=5pt, font=\scriptsize, text width=62mm, align=left},
  lbl/.style={font=\scriptsize\itshape, fill=hhpaper, inner sep=1.5pt}
]

  % ─── Four Lifelines ─────────────────────────────────────────────────────────
  \node[lane] (LA) at (-5.1, 0) {\textbf{\scshape Alice's Agent}\\[1pt]{\scriptsize Claim Holder}};
  \node[lane] (HA) at (-1.7, 0) {\textbf{\scshape Harbor $A$}\\[1pt]{\scriptsize Alice's Daemon}};
  \node[lane] (HB) at (1.7, 0) {\textbf{\scshape Harbor $B$}\\[1pt]{\scriptsize Bob's Daemon}};
  \node[lane] (LB) at (5.1, 0) {\textbf{\scshape Bob's Agent}\\[1pt]{\scriptsize Verifier}};

  % Lifeline dashed tracks
  \draw[hhgray!40, line width=0.4pt, dashed] (LA.south) -- ++(0, -5.4);
  \draw[hhgray!40, line width=0.4pt, dashed] (HA.south) -- ++(0, -5.4);
  \draw[hhgray!40, line width=0.4pt, dashed] (HB.south) -- ++(0, -5.4);
  \draw[hhgray!40, line width=0.4pt, dashed] (LB.south) -- ++(0, -5.4);

  % ─── Step 1: Transfer request ───────────────────────────────────────────────
  \draw[msgarr] (-5.1, -1.0) -- node[lbl, above] {$\mathsf{xfer\_req}(C_A, B)$} (-1.7, -1.0);

  % ─── Step 2: Transfer offer with signed epoch root ──────────────────────────
  \draw[msgarr] (-1.7, -2.2) -- node[lbl, above] {$\mathsf{xfer\_offer}\langle C_A, \mathrm{att}, R_A^{(e)}\rangle$} (1.7, -2.2);

  % ─── Step 3: Attenuated ack with recipient epoch root ───────────────────────
  \draw[msgarr] (1.7, -3.4) -- node[lbl, above] {$\mathsf{xfer\_ack}\langle C_B', R_B^{(e)}\rangle$} (-1.7, -3.4);

  % ─── Step 4: Card delivery to destination verifier ──────────────────────────
  \draw[msgarr] (1.7, -4.6) -- node[lbl, above] {$\mathsf{xfer\_deliver}\langle C_B'\rangle$} (5.1, -4.6);

  % ─── Summary Callout Box Below ──────────────────────────────────────────────
  \node[notebox, below=5.8cm of HA, xshift=-1.7cm] {
    \textbf{\scshape Invariants Foreclosed:}\\
    $\bullet$ Identity spoofing of $\mathit{sk}_A$ across trust boundary\\
    $\bullet$ Capability escalation ($\text{cap}_B' \subseteq \text{cap}_A$ enforced)\\
    $\bullet$ Replay attack against stale epoch root $R_A$
  };
  \node[notebox, below=5.8cm of HB, xshift=1.7cm] {
    \textbf{\scshape Deferred Properties:}\\
    $\bullet$ Revocation propagation latency (\S\ref{sec:fh-revocation})\\
    $\bullet$ 2-of-3 non-custodial settlement (\S\ref{sec:fh-settlement})\\
    $\bullet$ Witness log equivocation proof (\S\ref{sec:fh-authority})
  };

\end{tikzpicture}
\caption{The four-message cross-harbor capability transfer ceremony. The
critical property is that no message after (3) needs to reach back to
Harbor~$A$ in the hot path: $C_B'$ is verifiable under $\mathit{sk}_B$ alone,
which is why federation does not turn every authorization into a synchronous
inter-machine round trip. The epoch root $R_A^{(e)}$ bound into the envelope is
the load-bearing primitive — a revocation issued by $A$ after epoch $e$
invalidates $C_B'$ as soon as the new $R_A^{(e+1)}$ reaches the witness log
that $B$ audits.}
\label{fig:fh-xfer}
\end{figure}
