% Figure IV.6 --- authority attenuates over the transfer ceremony; source
% reachback disappears after the destination acknowledgement.
\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[pd figure]
  \node[pd panel title,anchor=west] at (-6.8,3.15)
    {authority shrinks while destination-local verification takes over};
  \foreach \x/\n/\lab in {-5.4/1/request,-1.8/2/offer,1.8/3/acknowledge,5.4/4/deliver}
    {\draw[pd guide] (\x,-2.35)--(\x,2.30);
     \node[pd datum] at (\x,2.15) {};
     \node[pd direct label,anchor=south] at (\x,2.42) {\n. \lab};}
  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (-6.15,2.15)--(6.15,2.15);

  % Capability envelope narrows at each authority-changing message.
  \fill[hhteal!10]
    (-5.4,1.35)--(-1.8,1.20)--(1.8,.78)--(5.4,.78)--
    (5.4,-.22)--(1.8,-.22)--(-1.8,-.70)--(-5.4,-1.05)--cycle;
  \draw[pd focus rule,line width=1.2pt]
    (-5.4,1.35)--(-1.8,1.20)--(1.8,.78)--(5.4,.78);
  \draw[pd focus rule,line width=1.2pt]
    (-5.4,-1.05)--(-1.8,-.70)--(1.8,-.22)--(5.4,-.22);
  \node[pd direct label,text=hhteal] at (-4.35,.25) {$C_A$: source authority};
  \node[pd direct label,text=hhteal] at (3.55,.28) {$C'_B\subseteq C_A$};
  \node[pd note,anchor=south] at (-1.8,1.48) {$R_A^{(e)}$ bound into offer};
  \node[pd note,anchor=north] at (1.8,-.48) {$R_B^{(e)}$ + attenuated card};

  % Hot-path source dependency ends at message 3.
  \draw[pd caution rule,line width=1.2pt] (-5.4,-1.72)--(1.8,-1.72);
  \draw[pd focus rule,line width=1.2pt] (1.8,-1.72)--(5.4,-1.72);
  \node[pd caution datum] at (-5.4,-1.72) {};
  \node[pd focus datum] at (1.8,-1.72) {};
  \node[pd focus datum] at (5.4,-1.72) {};
  \node[pd direct label,text=hhamber,anchor=south] at (-1.8,-1.58)
    {Harbor A still participates};
  \node[pd direct label,text=hhteal,anchor=south] at (3.6,-1.58)
    {destination verifies locally};
  \node[pd axis label,anchor=north] at (0,-2.05)
    {revocation remains an asynchronous epoch-root dependency, not a hot-path call};
  \node[pd note,anchor=north] at (0,-2.45)
    {deferred to the settlement and witness rails --- Conditionally bounded escrow custody; dispute evidence};
\end{tikzpicture}}
\caption{\textbf{The transfer ceremony attenuates authority and then removes
source reachback from the hot path.} The green envelope narrows from $C_A$ to
$C'_B\subseteq C_A$ as the signed offer and acknowledgement bind source and
destination epoch roots. After message 3, Harbor B can verify and deliver the
attenuated card locally. Harbor A still matters asynchronously: a later epoch
root can revoke the transfer once that root reaches B's audit surface. Transfer
therefore neither copies full authority nor makes every use synchronous.}
\label{fig:fh-xfer}
\end{figure}
