% Fig 4 — the three continuity organs, drawn in the shared `stp box` visual grammar.
% Maturity labels use the paper's \BUILT / \BUILTWEAK macros so the in-figure label
% and the caption cannot drift apart.

% ─── The Three Continuity Organs ──────────────────────────────────────────────
\begin{figure}[H]
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tikzpicture}[font=\small]

  \node[stp box,text width=44mm,align=left,minimum height=26mm] (o1) at (-5.2,0) {
    \textbf{\scshape 1. Episodic memory}\\[1pt]
    {\scriptsize\BUILT}\\[4pt]
    \textit{What happened.}\\[2pt]
    {\footnotesize Typed episodes scored by recency, importance, and relevance.}
  };

  \node[stp box,text width=44mm,align=left,minimum height=26mm] (o2) at (0,0) {
    \textbf{\scshape 2. Checkpoint organ}\\[1pt]
    {\scriptsize\BUILTWEAK}\\[4pt]
    \textit{Restorable state.}\\[2pt]
    {\footnotesize Forwards a summary and worktree lineage --- not execution state.}
  };

  \node[stp accent box,text width=44mm,align=left,minimum height=26mm] (o3) at (5.2,0) {
    \textbf{\scshape 3. Outcome ledger}\\[1pt]
    {\scriptsize\BUILTWEAK}\\[4pt]
    \textit{Witnessed delivery.}\\[2pt]
    {\footnotesize Commitments close against oracles; neutral grading does not ship.}
  };

  \draw[stp arrow] (o1) -- (o2);
  \draw[stp arrow] (o2) -- (o3);

  \node[stp box,text width=44mm,below=9mm of o3] (rep) {
    \textbf{\scshape Multi-dim reputation}\\[1pt]
    {\scriptsize\DESIGNED}\\[2pt]
    {\scriptsize Keys on the witnessed-outcome ledger (the third organ)}
  };
  \draw[stp accent arrow] (o3) -- (rep);

\end{tikzpicture}}
\caption{\textbf{The three continuity organs, honestly labelled.} Episodic memory
(\BUILT) records what happened --- typed episodes promoted out of transient history.
The checkpoint (\BUILTWEAK) is meant to preserve task lineage and worktree state
across compaction boundaries, but today forwards a summary rather than execution
state. The witnessed-outcome ledger (\BUILTWEAK) is the sole anchor reputation keys
on --- the accented box --- and it is only partial: durable commitments and
oracle-bound closure ship, neutral graded outcomes and sanctions do not
(\S\ref{sec:organs}). Each organ's in-figure grade is set from the same
\BUILT/\BUILTWEAK/\DESIGNED\ macros the body text uses, so the picture and the prose
cannot disagree.}
\label{fig:organs}
\end{figure}
