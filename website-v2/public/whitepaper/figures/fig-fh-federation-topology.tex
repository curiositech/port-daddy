% ─── Federation Topology: The Four-Element Gestalt ────────────────────────────
\begin{figure}[H]
\centering
\begin{tikzpicture}[
  node distance=6mm and 8mm,
  harbor/.style={draw=hhink, line width=0.65pt, fill=hhsand!25, rounded corners=2pt, inner sep=7pt, minimum width=52mm, minimum height=24mm, align=center},
  witness/.style={draw=hhink, line width=0.65pt, fill=hhpaper, rounded corners=2pt, inner sep=6pt, minimum width=122mm, minimum height=20mm, align=center},
  escrow/.style={draw=hhcobalt, line width=0.85pt, fill=hhcobalt!7, rounded corners=2pt, inner sep=6pt, minimum width=62mm, minimum height=18mm, align=center},
  flowarr/.style={-{Stealth[length=2.0mm,width=1.3mm]}, draw=hhink, line width=0.55pt},
  accentarr/.style={-{Stealth[length=2.0mm,width=1.3mm]}, draw=hhcobalt, line width=0.75pt},
  dasharr/.style={accentarr, dashed},
  lbl/.style={font=\scriptsize\itshape, fill=hhpaper, inner sep=1.5pt}
]

  % ─── Witness Log (Top) ──────────────────────────────────────────────────────
  \node[witness] (W) at (0, 3.2) {
    \textbf{\scshape Federated Witness Log} \quad {\footnotesize (Auditor-Verifiable, Append-Only)}\\[2pt]
    {\footnotesize Published Epoch Roots $R_A^{(e)}, R_B^{(e)}$ \textperiodcentered\ Cross-Signatures \textperiodcentered\ Equivocation Evidence}
  };

  % ─── Two Sovereign Harbors (Middle) ─────────────────────────────────────────
  \node[harbor, minimum width=44mm] (HA) at (-4.6, 0.0) {
    \textbf{\scshape Harbor $A$}\\[2pt]
    {\footnotesize Alice's Workspace / Local Daemon}\\[1pt]
    {\scriptsize Signing Key $\mathit{sk}_A$ \textperiodcentered\ Local Agents}
  };

  \node[harbor, minimum width=44mm] (HB) at (4.6, 0.0) {
    \textbf{\scshape Harbor $B$}\\[2pt]
    {\footnotesize Bob's Workspace / Local Daemon}\\[1pt]
    {\scriptsize Signing Key $\mathit{sk}_B$ \textperiodcentered\ Local Agents}
  };

  % ─── Settlement Escrow (Bottom) ─────────────────────────────────────────────
  \node[escrow] (E) at (0, -3.2) {
    \textbf{\scshape Settlement Escrow}\\[1pt]
    {\footnotesize Custody bounded \emph{only if}}\\
    {\footnotesize Prop.~\ref{thm:fh-escrow-bound}'s assumptions hold}
  };

  % ─── Causal Flows & Protocol Channels ───────────────────────────────────────
  % Harbors publish roots up to Witness Log
  \draw[flowarr] (HA.north) -- node[lbl, left=3pt, midway] {Publish $R_A^{(e)}$} (HA.north |- W.south);
  \draw[flowarr] (HB.north) -- node[lbl, right=3pt, midway] {Publish $R_B^{(e)}$} (HB.north |- W.south);

  % Inter-harbor capability transfer & revocation gossip
  \draw[accentarr] (HA.15) -- node[lbl, above=2pt] {$\mathsf{xfer}_{A \to B}$ Capability Transfer} (HB.165);
  \draw[-{Stealth[length=2.0mm,width=1.3mm]}, draw=hhink!70, line width=0.55pt, dashed] (HB.195) -- node[lbl, below=2pt] {Revocation Gossip $\Delta$} (HA.345);

  % Settlement & Bond flows
  \draw[accentarr] (HA.south) to[out=-90, in=180] node[lbl, below left] {Bond posted at $A$} (E.west);
  \draw[accentarr] (E.east) to[out=0, in=-90] node[lbl, below right] {Damage measured at $B$} (HB.south);

\end{tikzpicture}
\caption{The Federated Harbor as a four-element gestalt. Neither harbor is a root of authority for the other. The witness log above provides shared correlation: each harbor publishes its signed epoch root, enabling decentralized equivocation detection. The settlement escrow below is structurally bounded \emph{conditional on} a non-bypassable custody mechanism (\S\ref{sec:fh-escrow}); 2-of-3 threshold signing is one candidate, and this paper does not establish that condition for a deployed system. The solid blue arrow denotes the capability transfer ceremony (\S\ref{sec:fh-xfer}); the dashed line denotes epidemic revocation gossip (\S\ref{sec:fh-revocation}).}
\label{fig:fh-topology}
\end{figure}
