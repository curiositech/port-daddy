% Figure IV.5 --- accounting quantities, not architecture boxes. Panel A: the
% escrow-commit step of the three-sided settlement (S:one-settlement), CR 560
% moves wallet -> escrow with zero net. Panel B: the two-currency exercise
% (ex:he-two-currency-example), 10 A-coin @ $1 and 20 B-coin whose price moves
% $2 -> $3 with no transfer at all -- native balances stay flat, the marked
% total does not. \usepackage{pgfplots}+groupplots added to this chapter's
% preamble for this figure [deviation, see commit message].
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure]
  \begin{groupplot}[
      group style={group size=2 by 1,horizontal sep=1.7cm},
      width=4.55cm,height=4.05cm,
      ybar,
      x tick label style={font=\footnotesize,text=hhink,align=center},
      y tick label style={font=\footnotesize,text=hhink,xshift=-2pt},
      ylabel style={font=\footnotesize,text=hhink,xshift=-3pt},
      title style={font=\footnotesize\bfseries,text=hhink,align=left,
        text width=4.6cm},
      nodes near coords,nodes near coords align={vertical},
      every node near coord/.style={font=\footnotesize,text=hhink},
      axis line style={draw=hhink},tick style={draw=hhink},
      major tick length=1.2pt,
      enlarge x limits=0.22,
    ]
    \nextgroupplot[
      bar width=7pt,
      symbolic x coords={$\Delta W$,$\Delta E$,$\Delta C$,net},
      xtick={$\Delta W$,$\Delta E$,$\Delta C$,net},
      ylabel={CR},ymin=-900,ymax=900,ytick={-500,0,500},
      title={A. native unit\\closes exactly},
    ]
    \addplot[pd caution fill]
      coordinates {($\Delta W$,-560)};
    \addplot[pd focus fill]
      coordinates {($\Delta E$,560)};
    \addplot[pd neutral fill,nodes near coords style={yshift=6pt}]
      coordinates {($\Delta C$,0)};
    \addplot[pd focus fill,nodes near coords style={yshift=6pt}]
      coordinates {(net,0)};

    \nextgroupplot[
      bar width=7pt,
      symbolic x coords={$\Delta$A,$\Delta$B,price move,net},
      xtick={$\Delta$A,$\Delta$B,price move,net},
      xticklabels={$\Delta$A,$\Delta$B,price\\move,net},
      ylabel={USD, marked},ymin=-4,ymax=30,ytick={0,10,20,30},
      enlarge x limits=0.2,
      title={B. conversion\\adds a priced term},
    ]
    \addplot[pd neutral fill,nodes near coords style={yshift=6pt}]
      coordinates {($\Delta$A,0)};
    \addplot[pd neutral fill,nodes near coords style={yshift=6pt}]
      coordinates {($\Delta$B,0)};
    \addplot[pd caution fill]
      coordinates {(price move,20)};
    \addplot[pd caution fill]
      coordinates {(net,20)};
  \end{groupplot}
\end{tikzpicture}
\caption{\textbf{Conservation closes inside a native unit exactly; conversion adds a
named, priced valuation term that a native sum does not carry.} Panel A is the escrow-commit step: wallet and escrow move
by $560$\,\textsc{cr} in opposite directions and the three-way sum is exactly
$0$ (Design Invariant~\ref{prop:conservation}). Panel B is the two-currency
exercise: native holdings of $10$ A-coin and $20$ B-coin do not move at all, yet
B's price moving \$2 to \$3 marks the portfolio up \$20 with zero transfer ---
a valuation term, not a ledger leak (Property~\ref{thm:lax}). Native-unit
conservation is exact; a cross-unit scalar total is a valuation choice
[internal].}
\label{fig:he-conservation-functor}
\end{figure}
