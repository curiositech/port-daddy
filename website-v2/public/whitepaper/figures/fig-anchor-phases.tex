% Three-phase Harbor Card progression — inline at top of §3 Verification Algorithms.
%
% ─── ORPHANED (2026-08). ─────────────────────────────────────────────────────
% This file is NOT \input by any document in website-v2/public/whitepaper/. It is
% the three-phase predecessor of figures/fig-anchor-four-phases.tex (\input by
% anchor-protocol-whitepaper.tex, \label{fig:anchor-four-phases}), which adds
% Phase 4 (cuckoo-filter revocation) and strictly subsumes this one. It is
% retained rather than deleted so that any out-of-tree consumer (slide deck,
% blog post) is not broken silently. Its content has NOT been kept in sync
% with the live figure, so do not copy content from here. Its TYPOGRAPHY has
% not either: its \sffamily keys were the surviving instance of a figure
% asking for a face of its own, which is the defect P18 now catches, and
% leaving them would have left the one example anybody copying this file
% would copy. If nothing outside this repo needs it, delete it.
% ─────────────────────────────────────────────────────────────────────────────

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D8C7A6}
\providecolor{hhebony}{HTML}{121212}
\providecolor{hhink}{HTML}{1B1712}
\providecolor{hhcobalt}{HTML}{003FB8}
\providecolor{hhamber}{HTML}{6B4500}
\providecolor{hhteal}{HTML}{00564C}
\providecolor{hhpaper}{HTML}{FBF7EF}

\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,
  phase/.style={draw=hhebony,thick,fill=hhpaper,rounded corners=3pt,
                inner sep=6pt,minimum width=46mm,align=left},
  hdr/.style={font=\pdfiglabelbold,anchor=west,inner sep=2pt},
  badge/.style={draw=hhcobalt,fill=hhpaper,thick,rounded corners=1pt,
                inner sep=2pt,text=hhcobalt},
  okbadge/.style={draw=hhteal,fill=hhpaper,thick,rounded corners=1pt,
                  inner sep=2pt,text=hhteal},
]
% Phase 1
\node[phase,fill=hhsand!50] (p1) at (0,0)
  {\textbf{Phase 1 --- HS256}\\[2pt]
   shared symmetric key\\
   1 daemon $\leftrightarrow$ 1 harbor\\
   no delegation\\[4pt]
   {\pdfigsub good for: local dev, 1 process}};
% Phase 2
\node[phase,fill=hhsanddeep!60,right=8mm of p1] (p2)
  {\textbf{Phase 2 --- Ed25519}\\[2pt]
   asymmetric (pk, sk)\\
   harbor only stores pk\\
   no shared secrets\\[4pt]
   {\pdfigsub good for: multi-process, machine boundary}};
% Phase 3
\node[phase,fill=hhamber!30,right=8mm of p2] (p3)
  {\textbf{Phase 3 --- Delegation}\\[2pt]
   capability-attenuated\\
   parent token signs child\\
   nonce + prev/next binding\\[4pt]
   {\pdfigsub good for: multi-hop agent chains}};
% Phase badges (verification properties)
\node[okbadge,below=2mm of p1.south] {ProVerif: auth + injective};
\node[okbadge,below=2mm of p2.south] {ProVerif: alg-pinning safe};
\node[okbadge,below=2mm of p3.south] {ProVerif: 17/17 replay-safe};
% Arrows showing progression
\draw[pd focus rule,->,>=Latex,thick] (p1.east) -- ($(p2.west)+(0,0)$);
\draw[pd focus rule,->,>=Latex,thick] (p2.east) -- ($(p3.west)+(0,0)$);
% Above the NORTH edges, not above the gap's mid-height. Anchored at mid-height
% these labels always sat inside the phase boxes' vertical band, and were only
% clear of them while they were set in a narrower face; the moment the font=
% keys took the house handle and the labels came back into the edition's grotesk
% they got wider and "chain extension" ran into Phase 3's first line.
\node[font=\pdfiglabelitalic,text=hhcobalt,above=1.5mm of $(p1.north east)!0.5!(p2.north west)$]
  {trust split};
\node[font=\pdfiglabelitalic,text=hhcobalt,above=1.5mm of $(p2.north east)!0.5!(p3.north west)$]
  {chain extension};
\end{tikzpicture}
\caption{The three Harbor Card phases. Each phase is a refinement of the previous, both in capability and in adversary model. Phase~1 (HS256) is a teaching baseline; Phase~2 (Ed25519) removes the shared-secret trust boundary; Phase~3 (delegation) adds capability attenuation across an agent chain. All three are mechanized in ProVerif (\S\ref{app:mech}).}
\label{fig:anchor-phases}
\end{figure}
