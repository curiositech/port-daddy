% Ledger grid for Sec. sealed-conservation ("The budget is a ledger, and the ledger conserves"): which conservation invariant each mutant breaks, and in how many steps. Numbers copied from website-v2/public/whitepaper/sealed-harbor.tex, the "Model-checked property" claim (Property sealed-ledger-checked) [internal, a3_epsilon_ledger.py].
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm,
  hdr/.style={font=\footnotesize\bfseries,text=hhink,align=center},
  rowhdr/.style={font=\footnotesize\bfseries,text=hhink,align=left},
  cell/.style={font=\footnotesize,text=hhink,align=center},
  blankcell/.style={font=\footnotesize,text=hhgray,align=center}]
  \node[pd panel title,anchor=west] at (-0.2,6.15) {which mutant breaks which invariant, and how fast};
  % --- grid rules (ledger ruling) ---
  \foreach \y in {5.6,4.4,2.6,0.8} {\draw[pd hairline] (-0.2,\y) -- (11.6,\y);}
  \foreach \x in {-0.2,4.4,8.0,11.6} {\draw[pd hairline] (\x,0.8) -- (\x,5.6);}
  % --- headers ---
  \node[hdr,text width=3.0cm] at (6.2,5.0) {spend-without-\\append};
  \node[hdr,text width=3.0cm] at (9.8,5.0) {torn write};
  \node[blankcell,text width=3.8cm,font=\footnotesize\itshape] at (2.1,5.0) {mutant $\rightarrow$};
  \node[rowhdr,text width=4.0cm] at (2.1,3.5) {ledger balance\\(the recorded sum)};
  \node[rowhdr,text width=4.0cm] at (2.1,1.7) {budget cap\\(spend at or under the limit)};
  % --- data cells ---
  \node[pd caution datum] at (5.3,3.5) {};
  \node[cell,text width=1.9cm] at (6.75,3.5) {caught, 1 step};
  \node[blankcell,text width=3.0cm] at (6.2,1.7) {not exercised};
  \node[pd caution datum] at (8.9,3.5) {};
  \node[cell,text width=1.9cm] at (10.3,3.5) {caught, 1 step};
  \node[pd caution datum] at (8.9,1.7) {};
  \node[cell,text width=1.9cm] at (10.3,1.7) {caught, 3 steps};
  \node[pd note,anchor=north west,text width=11.8cm,font=\footnotesize\itshape] at (-0.2,0.35) {Model-checked instance: two concurrent clients racing programs $(2,2)$ and $(1,2)$ against a budget cap of $4$. All 15 reachable states (order-sensitive; 11 as a multiset) satisfy both invariants under every interleaving, and 2000 random instances of program, budget, and interleaving show no violation. The torn write logs the correct amount but adds one unit less: by 3 steps the recorded ledger reaches a total of $5$, over the cap of $4$ -- an auditable over-spend. Reverting either mutation restores both invariants and the 15 states [internal, \texttt{a3\_epsilon\_ledger.py}].};
\end{tikzpicture}
\caption{The two canonical atomicity breaks against the ledger's two invariants: spend-without-append (a release that adds to the balance without journaling it) and the torn write (a release that journals the wrong amount). Each caught cell marks the invariant broken and the shortest trace that catches it; the ledger's balance is the invariant both mutants break immediately, but only the torn write's deeper crime -- recorded spend past the contracted maximum -- takes three steps to show.}
\label{fig:sealed-mutant-grid}
\end{figure}
