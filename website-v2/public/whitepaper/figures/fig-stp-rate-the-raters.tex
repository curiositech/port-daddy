% Reader question: why does recursive evaluator audit terminate, and what
% assumption remains open? Claim: recursive auditing becomes finite only
% because remaining corrupt opportunity G_k contracts each level under sealed
% sampling; height alone proves nothing. Evidence: the chapter's reported
% depths, heterogeneous sealed sampling across C=8 disjoint cliques reaching
% the honest-root boundary near level 27, a monoculture (C=1) needing roughly
% 53 levels; the governing conditions rho*d*B>=G_k and G_(k+1)=(1-rho*d)*G_k.
% Must distinguish: the parameterized contraction curve from an asserted
% termination, and the exogenously honest root (not manufactured by the
% theorem) from the contraction result itself. Grammar: a recursive
% contraction tower with a theorem/conditions panel, rejected: a two-tier
% loop or infinite spiral (asserts recursion without the contraction that
% bounds it) and self-looping bubbles (no measured quantity). Five-second
% test: a reader says "heterogeneous pools reach the floor twice as fast."
\begin{figure}[H]
\centering
\pdfigurehue{pdviolet}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \node[pd title,anchor=west] at (0,5.65) {remaining corrupt opportunity across audit levels};
  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (0.9,0) -- (9.10,0);
  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (0.9,0) -- (0.9,4.65);
  \node[pd label,anchor=north east] at (9.10,-.75) {audit level $k$};
  \node[pd label,anchor=south,rotate=90,text width=2.6cm,align=center] at (0.20,2.30) {$G_k$ (log scale)};
  \foreach \x/\lab in {0.9/0,2.19/10,3.47/20,4.76/30,6.05/40,7.34/50,8.43/56} {
    \draw[pd tick] (\x,-.12) -- (\x,.12);
    \node[pd label,anchor=north] at (\x,-.22) {\lab};
  }
  \foreach \y in {2.30,3.45,4.60} \draw[pd guide] (0.9,\y) -- (9.10,\y);
  % ---- parameterized example: heterogeneous pools contract faster -----------
  \draw[pd focus rule,line width=1.55pt]
    plot[smooth] coordinates {(0.9,4.30) (1.84,3.55) (2.77,2.70) (3.55,1.70) (4.35,1.30)};
  \draw[pd warn rule,line width=1.55pt]
    plot[smooth] coordinates {(0.9,4.30) (2.19,3.65) (3.47,2.95) (4.76,2.15) (6.05,1.30) (7.70,1.30)};
  \node[pd focus label,anchor=south] at (4.10,1.60) {$C=8$};
  \node[pd label,anchor=south] at (6.05,1.50) {$C=1$};
  % ---- the theorem ends at a separately owed root ----------------------------
  \draw[pd warn fill] (0.9,-0.28) rectangle (9.10,0.95);
  \node[pd tag,anchor=center,text width=7.6cm,align=center,fill=pdpage,inner sep=2pt] at (5.0,0.42)
    {exogenously honest root required; the contraction theorem does not manufacture it};
  \node[pd label,anchor=north west,text width=3.0cm] at (0.9,-1.35)
    {$\rho d B\ge G_k$ at each level};
  \node[pd label,anchor=north west,text width=4.6cm] at (4.10,-1.35)
    {$G_{k+1}=(1-\rho d)G_k$ only under sealed sampling across $C$ disjoint cliques};
\end{tikzpicture}
\caption{In this parameterized example, eight cliques reach the honest-root
boundary near level 27; one clique needs roughly 53. The bound assumes an
honest root---it does not create one. [internal]}
\label{fig:rate-raters}
\end{figure}
