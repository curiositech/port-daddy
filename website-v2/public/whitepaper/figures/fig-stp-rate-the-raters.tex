% Figure III.12 — audit opportunity must contract at each level.
% Visual question: what must shrink for recursive review to terminate?
\begin{figure}[H]
\centering
\resizebox{0.90\textwidth}{!}{%
\begin{tikzpicture}[font=\small]
  \node[stp heading] at (0,3.2) {HETEROGENEOUS AUDIT POOL};
  \node[stp box,text width=28mm] (f1) at (-4.1,2.2) {model family A};
  \node[stp box,text width=28mm] (f2) at (0,2.2) {model family B};
  \node[stp box,text width=28mm] (f3) at (4.1,2.2) {model family C};
  \node[stp accent box,text width=100mm] (g0) at (0,.6)
    {level 0: corrupt opportunity $G_0$};
  \node[stp box,text width=70mm] (g1) at (0,-.75)
    {level 1: $G_1\leq\lambda G_0$};
  \node[stp box,text width=42mm] (g2) at (0,-2.1)
    {level 2: $G_2\leq\lambda G_1$};
  \node[stp heading] (root) at (0,-3.25) {verified terminal condition};
  \draw[stp dashed arrow] (f1) -- (g0.north west);
  \draw[stp dashed arrow] (f2) -- (g0.north);
  \draw[stp dashed arrow] (f3) -- (g0.north east);
  \draw[stp arrow] (g0) -- (g1);
  \draw[stp arrow] (g1) -- (g2);
  \draw[stp accent arrow] (g2) -- (root);
  \node[stp label,anchor=west] at (5.9,.6) {$q_k B_k>G_k$ at every level};
  \node[stp label,anchor=west] at (4.6,-1.4) {$0\leq\lambda<1$};
\end{tikzpicture}}
\caption{\textbf{Recursive review terminates only if corruption opportunity
contracts.} Heterogeneous judge families reduce shared-mode failure, while each
sampled level must satisfy both expected-forfeiture deterrence $q_kB_k>G_k$ and a
uniform contraction $G_{k+1}\leq\lambda G_k$. The narrowing geometry states the
proof obligation; it does not depict the conjecture as established.}
\label{fig:rate-raters}
\end{figure}
