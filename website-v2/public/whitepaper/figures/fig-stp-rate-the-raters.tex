% Figure III.12 --- recursive audit closes by measurable contraction, not by height.
\begin{figure}[H]
\centering
\resizebox{0.96\textwidth}{!}{%
\begin{tikzpicture}[pd figure]
  \node[pd panel title,anchor=west] at (-6.8,3.15)
    {remaining corrupt opportunity across audit levels};
  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (-6.0,-2.10) -- (6.4,-2.10);
  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (-6.0,-2.10) -- (-6.0,2.55);
  \node[pd axis label,anchor=west] at (6.5,-2.10) {audit level $k$};
  \node[pd axis label,anchor=center,rotate=90] at (-6.43,.20) {$G_k$ (log scale)};

  \foreach \x/\lab in {-5.8/0,-3.75/10,-1.7/20,.35/30,2.4/40,4.45/50,5.7/56} {
    \draw[pd tick] (\x,-2.22) -- (\x,-1.98);
    \node[pd axis label,anchor=north] at (\x,-2.32) {\lab};
  }
  \foreach \y in {-1.05,.0,1.05,2.10} \draw[pd guide] (-5.95,\y) -- (6.0,\y);

  % Parameterized example: heterogeneous pools contract faster.
  \draw[pd focus rule,line width=1.55pt]
    plot[smooth] coordinates {(-5.8,2.15) (-4.75,1.72) (-3.70,1.25) (-2.65,.72) (-1.60,.15) (-.55,-.48) (.05,-1.55)};
  \draw[pd caution rule,dashed,line width=1.55pt]
    plot[smooth] coordinates {(-5.8,2.15) (-3.75,1.78) (-1.70,1.28) (.35,.72) (2.40,.10) (4.45,-.62) (5.45,-1.55)};
  \node[pd direct label,text=hhteal,anchor=west] at (-2.65,1.02)
    {$C=8$: heterogeneous cliques\\[-1pt]{\tiny reaches root near level 27}};
  \node[pd direct label,text=hhamber,anchor=west] at (2.20,.55)
    {$C=1$: monoculture\\[-1pt]{\tiny reaches root near level 53}};

  % The theorem ends at a separately owed root.
  \fill[hhamber!9] (-5.95,-1.95) rectangle (6.0,-1.45);
  \draw[pd caution rule] (-5.95,-1.45) -- (6.0,-1.45);
  \node[pd direct label,text=hhamber,anchor=west] at (-5.65,-1.70)
    {exogenously honest root required; the contraction theorem does not manufacture it};

  \node[pd direct label,anchor=west] at (-5.75,-3.05)
    {$\rho d B\ge G_k$ at each level};
  \node[pd direct label,anchor=west] at (-.65,-3.05)
    {$G_{k+1}=(1-\rho d)G_k$ only under sealed sampling across $C$ disjoint cliques};
\end{tikzpicture}}
\caption{\textbf{Recursive auditing becomes finite only when corrupt opportunity
contracts.} The parameterized example plots the chapter's reported depths:
heterogeneous sealed sampling across $C=8$ cliques reaches the honest-root boundary
near level 27, while a monoculture ($C=1$) requires roughly 53 levels. More boxes
or more height would not prove this result; the governing conditions are local
deterrence $\rho dB\ge G_k$ and contraction
$G_{k+1}=(1-\rho d)G_k$. The amber floor is deliberately separate: the imported
theorem bounds the tower but does not supply its exogenously honest root or the
telemetry that estimates $d$.}
\label{fig:rate-raters}
\end{figure}
