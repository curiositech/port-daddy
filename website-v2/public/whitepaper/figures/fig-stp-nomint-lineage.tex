% Reader question: why does inheritance have to debit the source, and what
% does the copy-fork mutant mint? Claim: the budget-only phrasing lets every
% derivation stay live and sums past the witnessed weight; transfer debits the
% source so only the leaf is live; copy-fork, which forks the undebited
% record, mints reputation the checker refuses. Evidence: one witnessed
% episode of weight 1 at gamma=0.9, the three closures 2.439 / 0.729 / 8.2x,
% from the chapter's worked example and a6_no_mint.py (seed 20260816). Must
% distinguish: live (reputation-bearing) nodes from spent (debited) nodes, and
% the refused copy-fork edges from the honest derivation edges. Grammar: three
% small lineage DAGs, time top to bottom, rejected: a single abstract tree
% (hides which nodes stay live) and a table of the three totals (states the
% number but not the mechanism that produces it). Five-second test: a reader
% points at panel C and says "eight live copies of one episode, refused."
\begin{figure}[H]
\centering
\pdfigurehue{pdviolet}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm,
  live/.style={pd state,circle,minimum size=7.5mm,inner sep=0pt,font=\pdfiglabel,fill=pdrust!24,draw=pdrust},
  spent/.style={pd state,circle,minimum size=7.5mm,inner sep=0pt,font=\pdfiglabel,fill=pdpage,draw=pdinkmuted!70,text=pdinkmuted}]
  % panel A: budget-only, refuted
  \node[pd title,anchor=south,align=center,text width=2.9cm] at (0,2.55) {budget-only\\\pdfigsub refuted};
  \node[live] (a0) at (0,1.6) {1};
  \node[live] (a1) at (0,0.4) {.9};
  \node[live] (a2) at (0,-0.8) {.81};
  \node[live] (a3) at (0,-2.0) {.729};
  \draw[pd arrow] (a0) -- node[pd label,right=2pt] {$\gamma=0.9$} (a1);
  \draw[pd arrow] (a1) -- (a2);
  \draw[pd arrow] (a2) -- (a3);
  \node[pd note,anchor=north,align=center,text width=2.9cm] at (0,-2.7) {live total $2.439>1$\\three terms, one episode};
  % panel B: transfer, the theorem
  \begin{scope}[xshift=3.6cm]
  \node[pd title,anchor=south,align=center,text width=2.9cm] at (0,2.55) {transfer\\\pdfigsub the theorem};
  \node[spent] (b0) at (0,1.6) {0};
  \node[spent] (b1) at (0,0.4) {0};
  \node[spent] (b2) at (0,-0.8) {0};
  \node[live] (b3) at (0,-2.0) {.729};
  \draw[pd arrow] (b0) -- node[pd label,right=2pt] {debit $1$, grant $.9$} (b1);
  \draw[pd arrow] (b1) -- node[pd label,right=2pt] {debit $.9$, grant $.81$} (b2);
  \draw[pd arrow] (b2) -- node[pd label,right=2pt] {debit $.81$, grant $.729$} (b3);
  \node[pd note,anchor=north,align=center,text width=2.9cm] at (0,-2.7) {live total $0.729\le 1$\\a supermartingale};
  \end{scope}
  % panel C: copy-fork mutant
  \begin{scope}[xshift=8.6cm]
  \node[pd title,anchor=south,align=center,text width=3.0cm] at (0,2.55) {copy-fork mutant\\\pdfigsub refused by the checker};
  \node[live] (c0) at (0,1.6) {1};
  \foreach \i/\x in {1/-1.95,2/-1.30,3/-0.65,4/0.00} {
    \node[live,minimum size=5.0mm] (c\i) at (\x,-0.2) {1};
    \draw[pd breach arrow] (c0) -- (c\i);
  }
  \node[pd label,anchor=west] at (0.30,-0.2) {$\ldots$};
  \draw[pd breach arrow] (c0) -- (1.00,-0.2);
  \node[pd note,anchor=north,align=center,text width=3.0cm] at (0,-1.0) {eight copies of one episode\\$8.2\times$ its quorum weight\\shortest crime: two operations};
  \end{scope}
\end{tikzpicture}
\caption{Three lineages of one witnessed episode of weight $1$, derived at
$\gamma=0.9$. Under the budget-only phrasing every derivation stays live and the
closure sums to $0.9+0.81+0.729=2.439>1$: reputation minted from nothing, which is why
that phrasing was refuted before proving. Under transfer each derivation grants the child
$\gamma\,w_p$ and debits the source $w_p$, so only the leaf is live and the total never
exceeds what was witnessed. The copy-fork mutant, which forks the undebited record,
turns one episode into $8.2\times$ its quorum weight in two operations, and the checker
refuses it. [verified for the chain by hand; internal, \texttt{a6\_no\_mint.py}, seed
20260816]}
\label{fig:stp-nomint-lineage}
\end{figure}
