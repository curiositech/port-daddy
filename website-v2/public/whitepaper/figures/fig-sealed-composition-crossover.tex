% Redraw of fig-sealed-composition-crossover to the v2 plot grammar.
%
% Reader question: when does the advanced composition bound first pay?
% Claim: at per-release epsilon=0.1 and delta'=10^-6, basic composition is
% still smaller at k=32; the advanced bound first becomes smaller at k=35.
% Evidence: the closed forms k*epsilon and sqrt(2 k ln(1/delta')) epsilon +
% k epsilon (e^epsilon-1), verified by a3_epsilon_ledger.py.
% Must distinguish: the two directly labelled curves; the first integer k at
% which their ordering changes; the chapter's k=32 checkpoint.
% Grammar chosen: a measured-quantity plot with a direct label on each series
% and a guide at the first paying integer, k=35.
% Grammar rejected: a legend plus a marker at k=32, which made that checkpoint
% look like the crossing even though 3.31 is still above 3.20 there.
% Five-second test: answer "k=35" and identify which curve is lower afterward.
\begin{figure}[H]
\centering
\pdfigurehue{pdcobalt}%
\begin{tikzpicture}[pd figure]
  \begin{axis}[pd axis,
    width=9.6cm,height=5.6cm,
    title={advanced composition first pays at $k=35$},
    xmin=0,xmax=64,ymin=0,ymax=7,
    xlabel={releases $k$ (per-release $\varepsilon=0.1$)},
    ylabel={spent-budget bound},
    xtick={0,16,32,35,48,64},
    ytick={0,2,4,6}]
    \addplot[domain=0:64,samples=120,pd series]
      {0.1*x};
    \addplot[domain=0:64,samples=120,pd focus series]
      {sqrt(2*x*ln(1/0.000001))*0.1 + x*0.1*(exp(0.1)-1)};
    \draw[pd guide] (axis cs:35,0) -- (axis cs:35,3.50);
    \node[pd focus datum] at (axis cs:35,3.489) {};
    \node[pd tag,anchor=north west,align=left,text width=2.75cm]
      at (axis cs:37.0,3.20) {first paying integer:\\$k=35$};
    \node[pd label,anchor=south east] at (axis cs:63.5,6.62)
      {basic bound};
    \node[pd focus label,anchor=north east] at (axis cs:63.5,3.72)
      {advanced bound};
  \end{axis}
\end{tikzpicture}
\caption{Basic and advanced composition bounds against release count. With
per-release epsilon 0.1 and delta-prime one in a million, advanced first
becomes smaller at $k=35$; at the chapter's $k=32$ checkpoint, basic is 3.20
versus 3.31.
[verified, \texttt{a3\_epsilon\_ledger.py}]}
\label{fig:sealed-composition-crossover}
\end{figure}
