% Figure IV.10 --- assurance maturity as position, not a prose matrix.
\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[pd figure]
  \node[pd panel title,anchor=west] at (-6.9,3.45)
    {assurance advances only when a threat acquires an artifact or theorem};
  \foreach \y/\lab in {2.55/identity + replay,1.75/revocation,.95/equivocation,.15/settlement,-.65/cold-start admission,-1.45/cartel monitoring,-2.25/centralization}
    {\node[pd direct label,anchor=east] at (-4.35,\y) {\lab};
     \draw[pd guide] (-4.05,\y)--(6.35,\y);}

  % Evidence positions and uncertainty intervals.
  \draw[pd focus rule,line width=1.2pt] (-4.05,2.55)--(5.15,2.55);
  \node[pd focus datum] at (5.15,2.55) {};
  \node[pd note,anchor=west] at (5.42,2.55) {signed card + epoch root};

  \draw[pd caution rule,line width=1.2pt] (-4.05,1.75)--(1.95,1.75);
  \node[pd caution datum] at (1.95,1.75) {};
  \node[pd note,anchor=west] at (2.22,1.75) {$\Theta(\log m)$ only under reliable rounds};

  \draw[pd caution rule,line width=1.2pt] (-4.05,.95)--(1.55,.95);
  \node[pd caution datum] at (1.55,.95) {};
  \node[pd note,anchor=west] at (1.82,.95) {proof after roots co-locate};

  \draw[pd caution rule,line width=2.0pt] (-.55,.15)--(2.05,.15);
  \draw[pd caution rule,line width=1.2pt] (-4.05,.15)--(1.35,.15);
  \node[pd caution datum] at (1.35,.15) {};
  \node[pd note,anchor=west] at (2.28,.15) {bound ranges from bypassable to gated};

  \draw[pd caution rule,line width=1.2pt] (-4.05,-.65)--(-.70,-.65);
  \node[pd caution datum] at (-.70,-.65) {};
  \node[pd note,anchor=west] at (-.43,-.65) {thin reputation cannot yet price bond};

  \draw[pd caution rule,line width=1.2pt] (-4.05,-1.45)--(-1.05,-1.45);
  \node[pd caution datum] at (-1.05,-1.45) {};
  \node[pd note,anchor=west] at (-.78,-1.45) {monitoring game incomplete};

  \draw[pd caution rule,line width=1.2pt] (-4.05,-2.25)--(-1.55,-2.25);
  \node[pd caution datum] at (-1.55,-2.25) {};
  \node[pd note,anchor=west] at (-1.28,-2.25) {pushback is not a bound};

  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (-4.05,-2.85)--(6.15,-2.85);
  \foreach \x/\lab in {-4.05/OPEN,.75/CONDITIONAL,5.55/MECHANIZED}
    {\draw[pd rule] (\x,-2.96)--(\x,-2.74);
     \node[pd axis label,anchor=north] at (\x,-3.02) {\lab};}
  \node[pd axis label,anchor=south] at (.75,-2.74) {assurance position};
\end{tikzpicture}}
\caption{\textbf{The assurance landscape is uneven and falsifiable.} Position
encodes how far each threat has moved from an acknowledged gap toward a mechanized
artifact. Identity and replay have the strongest concrete evidence. Revocation,
equivocation, and settlement are conditional on named connectivity or custody
hypotheses; the thick settlement interval shows how widely its assurance moves if
the gate is bypassable. Cold start, cartel monitoring, and centralization remain
open. A row advances only when the missing artifact or theorem exists.}
\label{fig:fh-threat-bands}
\end{figure}
