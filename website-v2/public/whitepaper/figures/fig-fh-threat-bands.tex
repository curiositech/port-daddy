% Threat-band layout — what each layer of the Federated Harbor stops, what it
% defers, and what remains open. Defense-in-depth in the Anchor tradition.

\providecolor{hhsand}{HTML}{E9DCC4}
\providecolor{hhsanddeep}{HTML}{D4C5A9}
\providecolor{hhebony}{HTML}{1E1B18}
\providecolor{hhcinnabar}{HTML}{CC3D2E}
\providecolor{hhbrass}{HTML}{B08D57}
\providecolor{hhpatina}{HTML}{5C7A6A}
\providecolor{hhpaper}{HTML}{FBF7EE}

\begin{figure}[H]
\centering
\resizebox{0.95\textwidth}{!}{%
\begin{tikzpicture}[
  font=\sffamily\small,
  bandhead/.style={draw=hhebony,thick,fill=hhpaper,rounded corners=2pt,
                   inner sep=4pt,minimum width=56mm,align=center,
                   font=\sffamily\bfseries\small},
  bandbody/.style={draw=hhebony,thick,rounded corners=3pt,
                   inner sep=6pt,minimum width=56mm,minimum height=42mm,
                   align=left,text width=52mm,
                   font=\sffamily\footnotesize,anchor=north},
  bandred/.style={fill=hhcinnabar!12},
  bandbrass/.style={fill=hhbrass!18},
  bandsage/.style={fill=hhpatina!18},
  arrow/.style={->,>=Latex,thick,draw=hhebony},
]

% ─── Band 1: stopped (mechanized) ──────────────────────────────────
\node[bandhead,fill=hhpatina!30] (h1) at (0,0) {\textbf{Stopped} — mechanized};
\node[bandbody,bandsage,below=0pt of h1.south] (b1) {%
  \textbf{ProVerif:}\\
  $\bullet$ cross-harbor card authenticity (\S\ref{sec:fh-xfer})\\
  $\bullet$ attenuation across boundary (\S\ref{sec:fh-xfer})\\
  $\bullet$ replay against epoch-bound root\\[3pt]
  \textbf{Runtime / property tests:}\\
  $\bullet$ cuckoo-filter no-FN under gossip\\
  $\bullet$ epoch root monotonicity\\
  $\bullet$ escrow no-redirect (\S\ref{sec:fh-settlement})};

% ─── Band 2: bounded (named threat, named bound) ───────────────────
\node[bandhead,fill=hhbrass!50,right=10mm of h1] (h2) {\textbf{Bounded} — threat $\subseteq$ window};
\node[bandbody,bandbrass,below=0pt of h2.south] (b2) {%
  \textbf{Gossip latency:}\\
  $\bullet$ revocation lag $\le \Delta(1+\ln m)$ (\S\ref{sec:fh-revocation})\\
  $\bullet$ bonded against by Conservation\\[3pt]
  \textbf{Cross-witness equivocation:}\\
  $\bullet$ detectable in $O(\log m)$ audit rounds\\
  $\bullet$ slashable via witness bond\\[3pt]
  \textbf{Settlement contention:}\\
  $\bullet$ atomic-clear or rollback (\S\ref{sec:fh-settlement})};

% ─── Band 3: open (acknowledged) ──────────────────────────────────
\node[bandhead,fill=hhcinnabar!40,right=10mm of h2] (h3) {\textbf{Open} — acknowledged};
\node[bandbody,bandred,below=0pt of h3.south] (b3) {%
  \textbf{Cold-start admission:}\\
  $\bullet$ new harbor with no reputation cannot price its own bond fairly (\S\ref{sec:fh-admission})\\[3pt]
  \textbf{Cross-federation cartel:}\\
  $\bullet$ folk-theorem weakening across harbors (\S\ref{sec:fh-equilibria}); precise bound owed\\[3pt]
  \textbf{Centralization gravity:}\\
  $\bullet$ historical pull toward hub-and-spoke (\S\ref{sec:fh-failure-modes}); structural pushback rather than proof};

% ─── Spine: load-bearing reading direction ─────────────────────────
\draw[arrow] (b1.east) -- node[font=\sffamily\scriptsize\itshape,above,midway]{deeper into threat space}(b2.west);
\draw[arrow] (b2.east) -- node[font=\sffamily\scriptsize\itshape,above,midway]{out of formal reach}(b3.west);

\end{tikzpicture}%
}
\caption{Threat-band layout for the Federated Harbor. The Anchor paper's
defense-in-depth structure carries over: every claim either has a mechanization
artifact (sage), a named bound that the bond can price against (brass), or an
explicit acknowledgement of the open frontier (cinnabar). The paper is
disciplined about which band each claim lives in --- see Appendix~\ref{app:fh-mech}
for the artifact registry. Bands shift left over time as future work mechanizes
what is presently bounded.}
\label{fig:fh-threat-bands}
\end{figure}
