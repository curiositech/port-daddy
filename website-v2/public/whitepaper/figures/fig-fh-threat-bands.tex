% Figure IV.10 / VIII --- assurance maturity as position on one axis, not a
% prose matrix. Shared by harbor-economy.tex \S\ref{sec:0040b} and
% federated-harbor-whitepaper.tex \S\ref{sec:fh-threat-band-viz}.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \node[pd panel title,anchor=west,align=left] at (-3.9,6.15)
    {assurance advances only when a threat acquires\\an artifact or theorem};

  \foreach \y in {4.6,3.45,2.3,1.15,0,-1.15,-2.3}
    \draw[pd guide] (-2.5,\y)--(2.5,\y);
  \node[pd row label,anchor=east,align=right] at (-2.65,4.6)
    {identity +\\replay};
  \node[pd row label,anchor=east] at (-2.65,3.45)
    {revocation};
  \node[pd row label,anchor=east] at (-2.65,2.3)
    {equivocation};
  \node[pd row label,anchor=east] at (-2.65,1.15)
    {settlement};
  \node[pd row label,anchor=east,align=right] at (-2.65,0)
    {cold-start\\admission};
  \node[pd row label,anchor=east,align=right] at (-2.65,-1.15)
    {cartel\\monitoring};
  \node[pd row label,anchor=east] at (-2.65,-2.3)
    {centralization};

  % Evidence positions along the OPEN--MECHANIZED axis. Annotations sit in
  % one right-aligned column, independent of where each bar happens to end.
  \draw[pd focus rule,line width=1.2pt] (-2.5,4.6)--(1.724,4.6);
  \node[pd focus datum] at (1.724,4.6) {};
  \node[pd note,anchor=east,align=right,text width=2.5cm] at (5.9,4.6)
    {signed card + epoch root};

  \draw[pd caution rule,line width=1.2pt] (-2.5,3.45)--(0.345,3.45);
  \node[pd caution datum] at (0.345,3.45) {};
  \node[pd note,anchor=east,align=right,text width=2.5cm] at (5.9,3.45)
    {$\Theta(\log m)$, reliable rounds only};

  \draw[pd caution rule,line width=1.2pt] (-2.5,2.3)--(0.086,2.3);
  \node[pd caution datum] at (0.086,2.3) {};
  \node[pd note,anchor=east,align=right,text width=2.5cm] at (5.9,2.3)
    {proof after roots co-locate};

  \draw[pd caution rule,line width=2.0pt] (-0.603,1.15)--(0.603,1.15);
  \draw[pd caution rule,line width=1.2pt] (-2.5,1.15)--(-0.043,1.15);
  \node[pd caution datum] at (-0.043,1.15) {};
  \node[pd note,anchor=east,align=right,text width=2.5cm] at (5.9,1.15)
    {gate:\\bypassable $\to$ gated};

  \draw[pd caution rule,line width=1.2pt] (-2.5,0)--(-1.207,0);
  \node[pd caution datum] at (-1.207,0) {};
  \node[pd note,anchor=east,align=right,text width=2.5cm] at (5.9,0)
    {reputation too thin to price a bond};

  \draw[pd caution rule,line width=1.2pt] (-2.5,-1.15)--(-1.466,-1.15);
  \node[pd caution datum] at (-1.466,-1.15) {};
  \node[pd note,anchor=east,align=right,text width=2.5cm] at (5.9,-1.15)
    {monitoring game incomplete};

  \draw[pd caution rule,line width=1.2pt] (-2.5,-2.3)--(-1.81,-2.3);
  \node[pd caution datum] at (-1.81,-2.3) {};
  \node[pd note,anchor=east,align=right,text width=2.5cm] at (5.9,-2.3)
    {pushback is not a bound};

  \draw[pd rule,-{Stealth[length=1.8mm,width=1.1mm]}] (-2.5,-3.1)--(2.75,-3.1);
  \foreach \x/\lab in {-2.5/OPEN,0/CONDITIONAL,2.5/MECHANIZED}
    {\draw[pd rule] (\x,-3.21)--(\x,-2.99);
     \node[pd axis label,anchor=north,text width=2.0cm,align=center]
       at (\x,-3.27) {\lab};}
  \node[pd axis label,anchor=north,text width=3.4cm,align=center] at (0,-3.85)
    {assurance position};
\end{tikzpicture}
\caption{\textbf{The assurance landscape is uneven and falsifiable.} Position
on the open--conditional--mechanized axis encodes how far each threat has
moved from an acknowledged gap toward a mechanized artifact. Identity and
replay have the strongest concrete evidence: a signed card plus epoch root.
Revocation, equivocation, and settlement are conditional on named connectivity
or custody hypotheses; the thick settlement interval moves with the gate, from
bypassable to fully gated. Cold-start admission, cartel monitoring, and
centralization remain open. A row advances only when the missing artifact or
theorem exists [internal].}
\label{fig:fh-threat-bands}
\end{figure}
