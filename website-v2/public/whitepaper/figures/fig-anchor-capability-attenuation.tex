% Semantic contract: a child must stay inside its parent's rights-by-lifetime
% envelope. Nested geometry carries both inequalities without a prose table.
\begin{figure}[H]
\centering
\begin{tikzpicture}[pd figure,x=.16cm,y=1.08cm]
  \draw[pd rule,-{Stealth[length=1.7mm,width=1.05mm]}] (0,0) -- (67,0);
  \draw[pd rule,-{Stealth[length=1.7mm,width=1.05mm]}] (0,0) -- (0,4.15);
  \node[pd axis label,anchor=west] at (67,0) {remaining lifetime (min)};
  \node[pd axis label,anchor=south,rotate=90] at (-4.7,4.15) {capability reach};

  \foreach \y/\rightname in {1/{\texttt{db:write}},2/{+ \texttt{fs:read}},3/{+ \texttt{net:*}}} {
    \draw[pd guide] (0,\y) -- (63,\y);
    \node[pd axis label,anchor=east] at (-1.0,\y) {\rightname};
  }
  \foreach \x/\tickname in {5/5,15/15,60/60} {
    \draw[pd tick] (\x,0) -- (\x,-.12);
    \node[pd axis label,anchor=north] at (\x,-.18) {\tickname};
  }

  \path[fill=hhsand!24,draw=hhgray!70,line width=.62pt]
    (0,0) rectangle (60,3.35);
  \path[fill=hhteal!7,draw=hhteal!72,line width=.82pt]
    (0,0) rectangle (15,2.35);
  \path[fill=hhteal!17,draw=hhteal,line width=1.02pt]
    (0,0) rectangle (5,1.35);

  \node[pd direct label,anchor=north east] at (59.4,3.28)
    {$C_0$ root \quad 60 min};
  \node[pd direct label,text=hhteal,anchor=north east] at (14.5,2.28)
    {$C_1$ \quad 15 min};
  \node[pd direct label,text=hhteal,anchor=west] at (5.7,.72)
    {$C_2$ \quad 5 min};

  \node[pd caution datum] (bad) at (5,3) {};
  \draw[pd caution arrow] (bad) -- ++(8,0)
    node[pd direct label,text=hhamber,anchor=west] {attempted re-grant of \texttt{net:*}};
  \node[pd note,anchor=west] at (25,.5)
    {$C_2\subseteq C_1\subseteq C_0$ and $T_2<T_1<T_0$};
\end{tikzpicture}
\caption{Delegation must remain inside a two-dimensional parent envelope. The
child's capability set and remaining lifetime both contract at every hop. The
amber re-grant lies outside $C_1$ even though its requested lifetime is short;
the verifier rejects it because attenuation is conjunctive, not a trade between
rights and time.}
\label{fig:anchor-capability-attenuation}
\end{figure}
