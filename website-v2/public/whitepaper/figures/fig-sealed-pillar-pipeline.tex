% Redraw of fig-sealed-pillar-pipeline to the tikz-diagram-craft v2 standard.
%
% Reader question: which fences and gates does one work order pass, and which
% script checks each resulting property?
% Claim: one work order is mediated by two fences, which alone make
% enforceability possible, and released only through two gates, which alone
% produce the other three properties.
% Evidence: the "two fences" / "whole-worker taint and two gates" paragraphs
% and the four-links table, Sec.~sealed-design, in
% website-v2/public/whitepaper/sealed-harbor.tex.
% Must distinguish: produces (solid arrow) from is-checked-by (now a keyed
% legend table, not a second arrow family); which property is produced by the
% fences directly (Enforceability) versus by the gates (the other three).
% Grammar chosen: tree/DAG, time top to bottom, two columns (mechanism |
% properties) -- the pipeline is a lineage, not a state machine (nothing
% re-enters an earlier stage). A two-column layout (rather than one column per
% stage) leaves the property column room for its un-hyphenatable single-word
% names at column width; panel titles sit inside the panel's top edge, clear
% of the vertical arrow that only reaches the edge, never past it.
% Grammar rejected: a four-row table alone would lose the fences/gates
% structure that explains *why* enforceability comes from the fences and the
% other three from the gates; the table survives only as the checker legend.
% Five-second test: which stage makes each property possible, and what
% checks it once made.
\begin{figure}[H]
\centering
\pdfigurehue{pdcobalt}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\ax{1.15}   % mechanism column (work order, fences, gates)
  \def\px{4.85}   % property column
  % --- the two fences and the two gates, each a subordinate panel ----------
  \begin{scope}[on background layer]
    \node[pd panel,fit={(\ax-1.30,9.00) (\ax+1.30,6.00)}] (fences) {};
    \node[pd panel,fit={(\ax-1.30,5.15) (\ax+1.30,1.95)}] (gates) {};
  \end{scope}
  \node[pd title,anchor=north] at (fences.north) {two fences};
  \node[pd title,anchor=north] at (gates.north) {two gates};
  \node[pd artifact,text width=1.9cm] (WO) at (\ax,10.05) {work order $C$};
  \node[pd focus outline,text width=1.9cm] (F1) at (\ax,8.00) {in-guest monitor};
  \node[pd focus outline,text width=1.9cm] (F2) at (\ax,6.60) {outer gateway};
  \node[pd focus outline,text width=1.9cm] (G1) at (\ax,4.15) {Derek's gate};
  \node[pd focus outline,text width=1.9cm] (G2) at (\ax,2.60) {Erin's gate};
  \draw[pd focus arrow] (WO) -- (fences.north);
  \draw[pd focus arrow] (fences.south) -- (gates.north);
  % --- the four properties, in the order they compose at run time ---------
  \node[pd ready state,text width=2.6cm] (PE) at (\px,7.30)
    {\bfseries Enforceability\\[2pt]\itshape the channel, never the token};
  \node[pd ready state,text width=2.75cm] (PN) at (\px,4.55)
    {\bfseries Noninterference\\[2pt]\itshape silence except through the slot};
  \node[pd ready state,text width=2.6cm] (PC) at (\px,2.35)
    {\bfseries Conservation\\[2pt]\itshape the ledger conserves};
  \node[pd ready state,text width=2.6cm] (PD) at (\px,0.15)
    {\bfseries Detection\\[2pt]\itshape power curve and alarm clock};
  % --- produces: solid, chapter-hue arrows ---------------------------------
  \draw[pd focus arrow] (fences.east) -- (PE.west);
  \draw[pd focus arrow] (gates.east) -- (PN.west);
  \draw[pd focus arrow] (gates.east) -- (PC.west);
  \draw[pd focus arrow] (gates.east) -- (PD.west);
\end{tikzpicture}

\vspace{4pt}
\footnotesize
\begin{tabular}{@{}ll@{}}
\toprule
property & is checked by \\
\midrule
Enforceability & \texttt{b3\_controllability.py} \\
Noninterference & \texttt{c1\_noninterference.py} \\
Conservation & \texttt{a3\_epsilon\_ledger.py} \\
Detection & \texttt{a4\_canary\_sprt.py} \\
\bottomrule
\end{tabular}
\caption{One work order entering the room, mediated by two fences (the in-guest monitor, the outer gateway) and released only through two gates (Derek's, Erin's); solid arrows read ``produces''. The fences alone are what make the channel enforceable at all; the gates, once installed on that channel, are what produce the other three properties -- silence except through the slot, a conserving ledger, and a watched detector. The table keys each property to the script that checks it.}
\label{fig:sealed-pillar-pipeline}
\end{figure}
