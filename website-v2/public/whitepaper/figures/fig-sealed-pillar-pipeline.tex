% Redraw of fig-sealed-pillar-pipeline to the tikz-diagram-craft v2 standard.
%
% Reader question: which fences and gates does one work order pass, and which
% checker establishes each resulting property?
% Claim: one work order moves left to right through two fences and then one of
% two release gates; the fences establish enforceability, while the gates make
% noninterference, conservation, and detection meaningful.
% Evidence: the "two fences" / "whole-worker taint and two gates" paragraphs
% and the four-links table, Sec.~sealed-design, in
% website-v2/public/whitepaper/sealed-harbor.tex.
% Must distinguish: fences from gates; the one property established by the
% fences from the three properties that depend on gated release; mechanism
% flow from checker provenance.
% Grammar chosen: a full-width UML component/dataflow rail with paired release
% gates and a compact property strip. The left-to-right direction is the
% runtime order, while direct labels under the rail map properties to checkers.
% Grammar rejected: the former top-to-bottom tree, which spent a full page on
% a narrow stack and made three fan-out arrows look like three separate flows.
% Five-second test: follow C left to right, then name the one fences property
% and any one gates property with its checker.
\begin{figure*}[t]
\centering
\pdfigurehue{pdcobalt}%
\begin{tikzpicture}[pd figure,x=1cm,y=1cm]
  \def\flowy{3.15}
  \def\propy{0.02}
  % --- the runtime rail ----------------------------------------------------
  \node[pd artifact,minimum width=1.95cm] (WO) at (0.85,\flowy)
    {work order $C$};
  \node[pd uml component,minimum width=2.30cm,text width=1.72cm,align=center]
    (MON) at (3.15,\flowy)
    {in-guest\\monitor};
  \node[pd uml component,minimum width=2.30cm,text width=1.72cm,align=center]
    (GW) at (5.85,\flowy)
    {outer\\gateway};
  \node[pd uml component,minimum width=2.25cm,text width=1.70cm,align=center]
    (DG) at (8.65,{\flowy+.66})
    {Derek's gate};
  \node[pd uml component,minimum width=2.25cm,text width=1.70cm,align=center]
    (EG) at (8.65,{\flowy-.66})
    {Erin's gate};
  \node[pd climax state,minimum width=2.45cm,text width=1.90cm,align=center]
    (OUT) at (12.20,\flowy)
    {declared release};
  \pdumlicon{MON}\pdumlicon{GW}\pdumlicon{DG}\pdumlicon{EG}
  % Explicit split and merge junctions keep the branch topology visible and
  % give each arrowhead a straight, readable approach at final Book size.
  \coordinate (split) at (7.18,\flowy);
  \coordinate (dg-in) at ($(DG.west)+(-.62,0)$);
  \coordinate (eg-in) at ($(EG.west)+(-.62,0)$);
  \coordinate (dg-out) at ($(DG.east)+(.58,0)$);
  \coordinate (eg-out) at ($(EG.east)+(.58,0)$);
  \coordinate (merge) at (10.25,\flowy);
  \begin{scope}[on background layer]
    \draw[pd focus arrow] (WO.east) -- (MON.west);
    \draw[pd focus arrow] (MON.east) -- (GW.west);
    \draw[pd focus rule] (GW.east) -- (split);
    \draw[pd focus rule] (split) |- (dg-in);
    \draw[pd focus rule] (split) |- (eg-in);
    \draw[pd focus arrow] (dg-in) -- (DG.west);
    \draw[pd focus arrow] (eg-in) -- (EG.west);
    \draw[pd focus rule] (DG.east) -- (dg-out) -| (merge);
    \draw[pd focus rule] (EG.east) -- (eg-out) -| (merge);
    \draw[pd focus arrow] (merge) -- (OUT.west);
  \end{scope}
  \fill[pdfocus] (split) circle[radius=1.6pt];
  \fill[pdfocus] (merge) circle[radius=1.6pt];
  \begin{scope}[on background layer]
    \node[pd panel,fit={(MON)(GW)},inner xsep=7pt,inner ysep=11pt] (FENCES) {};
    \node[pd panel,fit={(DG)(EG)},inner xsep=7pt,inner ysep=7pt] (GATES) {};
  \end{scope}
  \node[pd title,anchor=south] at (FENCES.north) {two fences};
  \node[pd title,anchor=south] at (GATES.north) {two gates};

  % --- property-to-checker mapping, directly under the mechanism ----------
  \node[pd kind,anchor=south] at (1.85,0.93) {from the fences};
  \node[pd kind,anchor=south] at (9.25,0.93) {from gated release};
  \node[pd ready state,minimum width=3.35cm,minimum height=8.5mm] (PE) at (1.85,\propy)
    {\textbf{Enforceability}};
  \node[pd ready state,minimum width=3.35cm,minimum height=8.5mm] (PN) at (5.55,\propy)
    {\textbf{Noninterference}};
  \node[pd ready state,minimum width=3.35cm,minimum height=8.5mm] (PC) at (9.25,\propy)
    {\textbf{Conservation}};
  \node[pd ready state,minimum width=3.35cm,minimum height=8.5mm] (PD) at (12.95,\propy)
    {\textbf{Detection}};
  \node[pd mono label,anchor=north] at (1.85,-0.52)
    {b3\_controllability.py};
  \node[pd mono label,anchor=north] at (5.55,-0.52)
    {c1\_noninterference.py};
  \node[pd mono label,anchor=north] at (9.25,-0.52)
    {a3\_epsilon\_ledger.py};
  \node[pd mono label,anchor=north] at (12.95,-0.52)
    {a4\_canary\_sprt.py};
\end{tikzpicture}
\caption{One work order passes two fences and two gates; four property cards
name the checker for each resulting claim. [internal]}
\label{fig:sealed-pillar-pipeline}
\end{figure*}
