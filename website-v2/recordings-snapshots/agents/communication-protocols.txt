  $ pd channels discover git

  LOGICAL                   SCOPE       SOURCE      ACTIVE    PHYSICAL
  git:committed             repo        declared    0         repo:<REPO_KEY>:git:committed
  git:pr-opened             repo        declared    0         repo:<REPO_KEY>:git:pr-opened

  $ pd channels ensure git:committed --scope repo --description "commit trigger event"
  SUCCESS: Updated git:committed
    scope: repo
    physical: repo:<REPO_KEY>:git:committed
    worktree: <WORKTREE_ID>
    branch: <BRANCH>

  $ pd channels describe git:committed

  logical:  git:committed
  physical: repo:<REPO_KEY>:git:committed
  scope:    repo
  source:   declared
  active:   0
  worktree: <WORKTREE_ID>
  branch:   <BRANCH>
  aliases:  -
  desc:     commit trigger event

  $ pd pub docs:agents-recording '{"surface":"agents","section":"communication-protocols"}' --raw-channel
  SUCCESS: Published to docs:agents-recording (id: 1)
