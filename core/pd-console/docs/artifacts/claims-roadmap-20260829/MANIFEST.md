# Claims ledger and Jira roadmap proof

Captured 2026-08-29 and re-proved 2026-08-30 from branch
`codex/pd-console-claims-roadmap-20260829` in
`/Users/erichowens/coding/tmp/pd-console-claims-roadmap-20260829`.

## Runtime witness

- Exact-source development daemon: `claims-roadmap-20260829`
- Exact source revision: `76b925455`
- Daemon URL: `http://127.0.0.1:19958`
- Source directory reported by `/health`:
  `/Users/erichowens/coding/tmp/pd-console-claims-roadmap-20260829`
- Live `/files` cohort rendered by Claims: 372 active claims
- Live `/roadmap/jira`: configured `false`, with the four managed FleetBar
  credential keys named and no secret values exposed
- Release renderer:
  `core/target/release/pd-console-repl`

The daemon and claim cohort are live. Jira is deliberately shown in its truthful
unconfigured state because this harbor does not currently have a Jira connection
saved in FleetBar Credentials. The route and renderer tests cover configured,
paged, partial, failed, sorted, and selected Jira states without claiming that a
fixture is a live Jira account.

## Artifacts

- `claims-ledger-wide.png`: path-sorted live claims, a selected live row, and its
  complete inspector
- `claims-ledger-owner-sort.png`: the same live cohort sorted by owner
- `claims-ledger-narrow.png`: 520-pixel lossless wrapping proof
- `roadmap-wide.png`: local Port Daddy authority plus separate Jira read-through
- `roadmap-narrow.png`: 520-pixel Planner proof with wrapped Gantt, source state,
  and local summary
- `proof.gif`: path sort, owner sort, then roadmap source switch

## Capture provenance

macOS denied ScreenCaptureKit permission to the invoking Codex process, so no
GPUI screenshot or recording is claimed. The checked-in images were generated by
the repository's render-agnostic `Block` raster against the live exact-source
daemon. Every image carries the `HEADLESS CAPTURE` title and footer watermark.
The roadmap images are top-viewport crops of the complete raster so a 590-item
local roadmap does not turn the review artifact into a 24,000-pixel scroll.

The persistent capture entry points are:

```text
pd-console-repl --capture-claims <png> [--capture-sort <key>] [--capture-select <row>] [--capture-ledger-rows <n>] [--capture-width <px>]
pd-console-repl --capture-planner <png> [--capture-sort <key>] [--capture-select <row>] [--capture-ledger-rows <n>] [--capture-width <px>]
```

This proves live data binding, sorting/selection projection, the complete
render-agnostic metadata contract, and deterministic narrow-width layout. It
does not claim Metal framebuffer equivalence or a live Jira credential round trip.
