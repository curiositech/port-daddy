# Changelog — typescript-narrowing-expert

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter;
added deterministic audit helper (`scripts/typescript_narrowing_expert_audit.mjs`), draft-07
schema (`schemas/typescript-narrowing-expert-plan.schema.json`), verified sample input
(`examples/sample-input.json`); added Quality Gates.

## [0.1.0]

Initial authoring: discriminated unions and exhaustiveness, satisfies vs annotation vs as,
user-defined type guards, branded ID types with factories, closure narrowing loss,
conditional/distributive/mapped types, anti-patterns, the OrderID/UserID worked example,
and quality gates.
