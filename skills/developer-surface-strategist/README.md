# Developer Surface Strategist

Use this skill to choose SDK, CLI, MCP, GUI, API, webhook, and tube workflow surfaces for an agentic product.

Run:

```bash
node skills/developer-surface-strategist/scripts/surface_matrix.mjs --input surfaces.json
```

It is biased toward shared contracts with multiple adapters, not one-off affordances.
