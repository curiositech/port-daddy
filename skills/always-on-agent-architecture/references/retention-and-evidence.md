# Retention and evidence ledger

**Source:** canonical `always-on-agent-architecture` snapshot, read 2026-09-24. **Access:** local source full text; it supplied no external benchmark corpus.

| Original heading | Retained contribution | Correction boundary |
| --- | --- | --- |
| Memory Framework Selection Tree | runtime, temporal graph, hybrid retrieval, existing-runtime, and minimal-dependency decision factors | named products are candidates, not rankings or current capability claims; use dated primary documents and workload measurements |
| Core Memory Eviction Triggers | size, age, relevance, user override, and conflict triggers | fixed sizes, TTLs, and scores become declared local budgets with deletion and rebuild behavior |
| Vector DB Selection Criteria | corpus, existing database, hybrid retrieval, and operations ownership inform a comparison | vendor latency/corpus claims are removed; record p50/p95, quality, recovery, cost, and failure evidence for the declared workload |
| Memory Tier Routing Decision | identity/preference updates, recall, archival knowledge, and external tools remain distinct routes | owned state, event history, derived views, index, policy, and effects are now explicitly separate |
| Failure Modes | corruption, retrieval staleness, persistence contention, context growth, and unbounded growth remain useful risks | a symptom does not prove one cause; recovery needs version, provenance, read-back, and replay evidence |
| Research-assistant example | tiered research memory, framework screen, agent-loop pseudocode, and novice/expert distinctions | all values are constructed; explicit scope replaces lexical routing; code is pseudocode, not a product API |
| Deployment Validation Checklist | restart, retrieval, sizing, durability, cost, compaction, cold-start, and backup checks remain | every numerical acceptance value is declared for a workload rather than asserted universally |
| NOT-FOR Boundaries | routing to inputs, applications, safety, agent patterns, schema, and cost work remains | routing is instructional; it grants no implementation authority |

**Book candidate:** derived memory as a versioned, provenance-bound view over event history, demonstrated with deletion and rebuild. This is a candidate mechanism, not a novel result.

## Root review corrections

The framework dimensions are evaluated together rather than presented as mutually exclusive vendor branches. The retrieval route applies authority filters and the repository hybrid contract before operational selection. The research example now preserves temporal corrections, verifies scope, handles unavailable context, keeps retrieved text separate from trusted policy, and records only permitted history. Deletion proceeds through confirmed or restricted outcomes; a tombstone is not erasure. MemGPT, LongMemEval and LoCoMo method sections are linked in the new memory-evaluation reference with an original local fixture protocol. No provider experiment, recovery run, or benchmark reproduction is claimed.
