# Changelog — graphql-n-plus-one-dataloader

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter
(pairs-with re-pointed at skills that exist in this repo); added deterministic audit helper
(`scripts/graphql_n_plus_one_dataloader_audit.mjs`), draft-07 schema
(`schemas/graphql-n-plus-one-dataloader-plan.schema.json`), verified sample input
(`examples/sample-input.json`); verified the existing Quality Gates checklist.

## [0.1.0]

Initial authoring: the N+1 math, the DataLoader batch contract (same length / same order),
per-request loader scoping, the one-tick batching rule, five failure modes, a 51-queries-to-2
worked example, and layered defenses (APQ, depth limits, cost analysis) with cited sources.
