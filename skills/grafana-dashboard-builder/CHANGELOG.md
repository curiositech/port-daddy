# Changelog — grafana-dashboard-builder

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter;
added deterministic audit helper (`scripts/grafana_dashboard_builder_audit.mjs`), draft-07
schema (`schemas/grafana-dashboard-builder-plan.schema.json`), verified sample input
(`examples/sample-input.json`); verified the existing Quality Gates checklist.

## [0.1.0]

Initial authoring: PromQL essentials (rate vs irate vs increase, histogram_quantile and le,
label_replace), recording rules, alert rules with dwell times, LogQL, template variables,
SLO dashboard structure, dashboards as code, annotations, and six anti-patterns.
