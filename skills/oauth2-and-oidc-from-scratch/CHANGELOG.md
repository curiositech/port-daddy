# Changelog — oauth2-and-oidc-from-scratch

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter;
added deterministic audit helper (`scripts/oauth2_and_oidc_from_scratch_audit.mjs`), draft-07
schema (`schemas/oauth2-and-oidc-from-scratch-plan.schema.json`), verified sample input
(`examples/sample-input.json`); added Quality Gates.

## [0.1.0]

Initial authoring: grant-type selection table, BFF pattern with a worked Hono callback,
browser token-storage hierarchy, state-vs-nonce distinction, ID-token validation checklist,
refresh-token rotation, migration off implicit/ROPC, anti-pattern catalog, quality gates,
grounded in the OAuth 2.1 draft and the browser-based-apps BCP.
