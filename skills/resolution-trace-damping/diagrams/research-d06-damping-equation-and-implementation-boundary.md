# D06 — Damping equation and consumer boundary

Finite nonnegative inputs are required for the displayed bound. A computed priority is not proof that navigation consumes it. Independent work eligibility and authority constrain which candidates may be ranked.

```mermaid
flowchart TD
 R[Validate inputs and work-version evidence] --> F["Effective = raw * max(0, 1 - d * res)"]
 F --> P[Pinned effective-read path]
 P --> C{Consumer uses that value?}
 C -->|Verified| A[Test ranking and workload effects]
 C -->|No or unknown| U[No gradient or revisit claim]
 G[Work eligibility and authority] --> A
```
