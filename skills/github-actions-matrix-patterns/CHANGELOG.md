# Changelog — github-actions-matrix-patterns

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter;
added deterministic audit helper (`scripts/github_actions_matrix_patterns_audit.mjs`), draft-07
schema (`schemas/github-actions-matrix-patterns-plan.schema.json`), verified sample input
(`examples/sample-input.json`); verified the existing Quality Gates checklist.

## [0.1.0]

Initial authoring: matrix include/exclude surgery, reusable workflows (`workflow_call`), OIDC
trust policies, concurrency cancellation, runtime-computed matrices, cache keys, test sharding,
environment protection rules, and six anti-patterns.
