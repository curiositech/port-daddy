# Changelog — react-server-components-boundary

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter;
added deterministic audit helper (`scripts/react_server_components_boundary_audit.mjs`),
draft-07 schema (`schemas/react-server-components-boundary-plan.schema.json`), verified
sample input (`examples/sample-input.json`); added Quality Gates.

## [0.1.0]

Initial authoring: 'use client' as a module-dependency (not render) boundary, leaf-pushing,
the children slot pattern, the serializable-props table, third-party hook-lib wrapping,
environment poisoning and server-only/client-only guards, provider placement, anti-pattern
detection table, novice/expert comparison, and quality gates. Grounded in react.dev and
Next.js 16 docs.
