# Activation tests

## Should activate

1. “How much of my Codex subscription remains, and can this agent finish before
   the weekly wall?”
2. “Compact and switch models before the Claude five-hour window is exhausted.”
3. “Allocate context and token budgets across six coding workers.”
4. “Design a zoomable briefing that does not recursively summarize lies.”
5. “Track subscription capacity even though we cannot price each request.”
6. “Validate whether two model aliases are double-counting one shared usage
   window before admitting a successor body.”

## Should not activate

1. “Restore a dead durable agent in another backend.” Use
   `agent-resurrection-and-body-continuity` with this skill as support.
2. “Design the physical VM boundary.” Use
   `sandboxed-adversarial-test-harness`.
3. “Rewrite this one prompt.” Use prompt engineering.
4. “Implement a vector memory store.” Use memory-storage architecture.
5. “Prove a VCG auction equilibrium.” Use algorithmic game theory.
