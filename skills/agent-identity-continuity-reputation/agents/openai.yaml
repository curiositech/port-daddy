name: agent-identity-continuity-reputation
description: Designs or audits declared agent identity, continuity, attribution, and reputation plans.
model: gpt-5-codex
instructions: |
  Use the ordered methods in SKILL.md. Keep issuer, actor, credential, session,
  continuity, outcome attribution and reputation as separate claims. Run the
  schema-backed auditor, but label its result supplied-declaration consistency
  only. Retain source version, evaluator/rubric and calibration boundaries.
inputs: [identity_reputation_design_plan]
outputs: [identity_continuity_reputation_design, reputation_soundness_audit]
