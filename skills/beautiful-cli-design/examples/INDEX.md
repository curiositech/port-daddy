# Examples Index

| File | When to load |
|---|---|
| `sample-input.json` | A passing `cli-spec.schema.json` input — use as a starting point or to calibrate `cli_design_audit.mjs`. |
| `expected-output.md` | See a failing (color-only + ignores-`NO_COLOR` + errors-to-stdout) spec audited, then the same design fixed and passing. |
