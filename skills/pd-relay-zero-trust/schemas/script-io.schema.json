{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://port-daddy.dev/schemas/pd-relay-zero-trust/script-io.json",
  "title": "Script I/O Envelope",
  "description": "All scripts in this skill accept and emit JSON wrapped in this envelope so callers (humans, subagents, CI) can chain them deterministically.",
  "type": "object",
  "oneOf": [
    {"$ref": "#/$defs/Request"},
    {"$ref": "#/$defs/Response"}
  ],
  "$defs": {
    "Request": {
      "type": "object",
      "required": ["kind", "version", "command", "payload"],
      "properties": {
        "kind": {"const": "request"},
        "version": {"const": "1"},
        "command": {
          "type": "string",
          "enum": [
            "pki.score",
            "handshake.verify",
            "chain.verify",
            "chain.anchor",
            "card.attenuate",
            "envelope.encrypt",
            "envelope.decrypt",
            "threat.review",
            "skill.validate"
          ]
        },
        "payload": {"type": "object"},
        "trace_id": {"type": "string", "description": "Optional correlation id"}
      },
      "additionalProperties": false
    },
    "Response": {
      "type": "object",
      "required": ["kind", "version", "ok"],
      "properties": {
        "kind": {"const": "response"},
        "version": {"const": "1"},
        "ok": {"type": "boolean"},
        "result": {"type": "object"},
        "error": {
          "type": "object",
          "required": ["code", "message"],
          "properties": {
            "code": {"type": "string"},
            "message": {"type": "string"},
            "hint": {"type": "string"}
          },
          "additionalProperties": false
        },
        "trace_id": {"type": "string"}
      },
      "additionalProperties": false,
      "if": {"properties": {"ok": {"const": true}}},
      "then": {"required": ["result"]},
      "else": {"required": ["error"]}
    }
  }
}
