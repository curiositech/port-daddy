# Changelog — vite-build-optimizer

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter;
added deterministic audit helper (`scripts/vite_build_optimizer_audit.mjs`), draft-07 schema
(`schemas/vite-build-optimizer-plan.schema.json`), verified sample input
(`examples/sample-input.json`); added Quality Gates.

## [0.1.0]

Initial authoring: dependency pre-bundling, server warmup, manualChunks, resolve.dedupe,
SSR externals, the plugin hook lifecycle, profiling, anti-patterns (thrash, HMR default
exports, polyfill bloat, sourcemaps), and quality gates.
