# Changelog — cloudflare-workers-debugging

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with
frontmatter; added deterministic audit helper (`scripts/workers_debugging_audit.mjs`),
draft-07 schema (`schemas/cloudflare-workers-debug-plan.schema.json`), verified sample
input (`examples/sample-input.json`); added Quality Gates.

## [0.1.0]

Initial authoring: wrangler auth model (OAuth vs API tokens), the empty-secret alias
trap, D1 local-vs-remote migrations, custom domains vs routes, cookies on a 302,
observability/tail, runtime binding verification, Workers limits, anti-patterns, and
quality gates.
