# Changelog — hono-patterns

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter;
added deterministic audit helper (`scripts/hono_patterns_audit.mjs`), draft-07 schema
(`schemas/hono-patterns-plan.schema.json`), verified sample input
(`examples/sample-input.json`); verified the existing Quality Gates checklist.

## [0.1.0]

Initial authoring: the Hono Bindings/Variables generic, route ordering, middleware chain and
await next(), zod validation slots, the deferred-headers model (Set-Cookie on redirect),
SSE/NDJSON streaming, onError, cookie helpers, CORS with credentials, the typed RPC client,
and six anti-patterns.
