# Changelog — tailwind-v4-expert

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter; added deterministic audit helper (`scripts/tailwind_v4_audit.mjs`), draft-07 schema, verified sample input; added Quality Gates.

## [0.1.0]

Initial authoring: single-file `SKILL.md` covering the v4 import model (`@import "tailwindcss"` + `@source`), CSS-first `@theme` tokens and their utility namespaces, `@layer` cascade, custom `@variant`s, container queries, three dark-mode strategies, Next.js App Router specifics, and the Oxide static-scanning anti-patterns.
