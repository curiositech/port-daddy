# Changelog — mcp-server-design

## [0.2.0] — 2026-07-04

Brought to the agentic-family standard: added io-contract/provenance/pairs-with frontmatter;
added deterministic audit helper (`scripts/mcp_server_design_audit.mjs`), draft-07 schema
(`schemas/mcp-server-plan.schema.json`), verified sample input
(`examples/sample-input.json`); added Quality Gates.

## [0.1.0]

Initial authoring: the stdio JSON-RPC handshake, SDK minimal server, tool descriptions as
the discovery surface, lazy initialization, zod schema design, isError conventions, secrets
handling, path-traversal safety, telemetry placement, distribution patterns, the CI
handshake test, anti-patterns, and quality-gate checklist.
