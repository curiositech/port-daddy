# examples/

Three Book figures redrawn to the standard. Each header carries the figure's
brief and the facts it was checked against.

| File | Redraws | Grammar |
|---|---|---|
| `redraw-workunit-machine.tex` | `fig-swk-workunit-machine` (ch. 1) | state machine: spine, off-ramp, badges, legend table |
| `redraw-commitment-oracle.tex` | `fig-swk-commitment-oracle` (ch. 1) | converging references into a closure aperture |
| `redraw-anchor-card-lifecycle.tex` | `fig-anchor-card-lifecycle` (ch. 2) | timeline of admissible intervals per card |
