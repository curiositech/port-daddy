% TEMPLATE: regime plot (a region between two curves, a worked point dropped
% to both axes). The two curves bound the range a quantity may legally take;
% the tinted region between them -- not either line alone -- is the reader's
% answer, per S11 (fillbetween, edged, worked point on the tick list).
% QA: PASS in swiss, maritime and technical. (A "wide" failure once
% reported in technical was a sub-point page speck the technical edition
% leaves at the page corner, not the pattern fill; book_figure_qa.py now
% ignores specks under 1 pt.)
\begin{figure}[H]
\centering
\pdfigurehue{pdcobalt}%
\begin{tikzpicture}[pd figure]
\begin{axis}[pd axis,
  width=7.6cm,height=4.3cm,
  xmin=0,xmax=120,ymin=0,ymax=100,
  xtick={0,25,60,100,120},ytick={0,25,35,44,80,100},
  xlabel={requests per minute},
  ylabel={bond required}]
  \addplot[name path=lower,draw=none,forget plot,domain=0:120,samples=61] {20+0.2*x};
  \addplot[name path=upper,draw=none,forget plot,domain=0:120,samples=61] {20+0.6*x};
  \addplot[pd focus fill,forget plot] fill between[of=upper and lower];
  \addplot[pd series,domain=0:120,samples=61] {20+0.2*x};
  \addplot[pd focus series,domain=0:120,samples=61] {20+0.6*x};
  \node[pd label,anchor=north west] at (axis cs:100,{20+0.2*100}) {no history};
  \node[pd focus label,anchor=south east] at (axis cs:100,{20+0.6*100}) {thin history};
  \node[pd label] at (axis cs:90,50) {admissible band};
  % the worked point: the chapter's benchmark load, dropped to both bounds
  \draw[pd guide] (axis cs:25,0) -- (axis cs:25,35);
  \draw[pd guide] (axis cs:0,25) -- (axis cs:25,25);
  \draw[pd guide] (axis cs:0,35) -- (axis cs:25,35);
  \node[pd datum] at (axis cs:25,25) {};
  \node[pd focus datum] at (axis cs:25,35) {};
\end{axis}
\end{tikzpicture}
\caption{A regime plot of the admissible bond band against requests per
minute: the lower curve is the minimum bond with no request history, the
upper curve the minimum bond with a thin one, and the tinted region between
them is the range a bond may legally take. At the chapter's benchmark load of
25 requests per minute the band runs from 25 to 35 (pd units), read directly
off the two dropped guides; the two bounds are drawn as straight lines only
for this template's illustrative numbers, not the chapter's true model.
[internal, illustrative]}
\label{fig:tpl-regime-plot}
\end{figure}
