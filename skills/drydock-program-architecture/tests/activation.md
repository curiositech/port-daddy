# Activation Tests

## Positive: use this skill

1. “Combine Drydock VM containment, agent resurrection, subscription limits,
   context compaction, and operator controls into one implementation plan.”
2. “Create the full Drydock hypertree and diagrams, labeling what is built and
   what remains speculative.”
3. “Review whether our controller, broker, reaper, console, and evidence log form
   a non-circular trusted architecture.”
4. “Show how a worker moves from intent through admission, VM birth, work,
   compaction, backend switch, and settlement.”
5. “Turn these scattered Drydock design volumes into one progressive-disclosure
   architecture package.”

## Negative: use another skill

1. “Run Port Daddy in a VM now.” The skill cannot authorize or perform a run.
2. “Audit only this seccomp policy.” Use the focused sandbox/security skill.
3. “Recover this one dead agent session.” Use
   `agent-resurrection-and-body-continuity`.
4. “Estimate remaining subscription capacity for this model.” Use
   `context-economics-for-agent-swarms`.
5. “Train a policy with rollout rewards.” Use `agent-rl-sandbox-trainer` after
   containment is independently established.
