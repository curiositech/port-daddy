# Script Index

| File | Use |
|---|---|
| `audit-drydock-program-skill.mjs` | Verify the bundle, moved corpus, local links, focused-skill routing, activation split, and 17-view atlas. |
| `validate-drydock-resurrection-hypertree.mjs` | Verify hypertree identity, DAG order, launcher gating, and canonical plan digest. Pass `-` to validate JSON on stdin. |
| `validate-hypertree-execution.mjs` | Verify the sealed T0 fixture's exact plan binding, ordered events, role separation, review reservations, bounded rework, authority model, and deterministic shared projection. Pass `-` to validate JSON on stdin. It is a fixture oracle, not runtime event authority. |
| `seal-hypertree-execution-fixtures.ts` | Recompute the inert review-loop execution digest, terminal evidence root, expected projection, and every golden prefix. Pass `--check` to fail on drift without writing. |
