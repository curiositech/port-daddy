# Source is text, and git should keep saying so.
#
# git auto-detects binary by sniffing for a NUL byte in the first 8000 bytes. A
# single literal NUL in a source file — easy to write by accident as a join
# separator — flips that detection, and then `git diff` prints
# `Bin 7463 -> 9147 bytes` instead of code, `git blame` has nothing to
# attribute, and `grep`/ripgrep skip the file silently. That happened to
# apps/fleet-executor/src/verdict.ts and cost real debugging time twice.
# THE MERGE FAILURE, AND WHY NO ATTRIBUTE HERE FIXES IT.
# The same NUL sniff is applied by git's three-way merge. When it trips, git
# prints `Cannot merge binary files: <path>`, abandons hunk-level merging, and
# takes one whole side. In 47964247a that is exactly what happened to
# lib/roadmap-items.ts: "ours" was taken wholesale and #9352's ADR-0086
# planner-column work vanished with no conflict marker and a green build. It
# took a hand-run `git merge-file` over de-NUL'd stages (2dcd80f9f) to recover.
# It is tempting to think `merge` below prevents that. It does NOT. The binary
# check lives inside git's built-in merge driver, ahead of the attribute, and
# was measured to be unreachable by configuration: with a raw NUL present,
# `text diff`, `text diff merge`, `merge=union`, and even a custom driver ALL
# still produce `Cannot merge binary files` and still drop a side. The ONLY
# thing that prevents the silent-loss merge is not writing the NUL — which is
# why tests/unit/source-is-text.test.js, not this file, is the real guard.
# WHAT `merge` DOES BUY. It pins the built-in text driver for these extensions
# so a local `merge.default` cannot redirect them. Measured: with
# `merge.default=binary` set in a clone's config and only `text diff` here, an
# ordinary NUL-FREE source file merges as binary and silently loses a side;
# with `merge` set it merges normally. That is a real hole, independent of the
# NUL problem, and this closes it.
# tests/unit/source-is-text.test.js walks every tracked file matching the
# extensions below for raw NULs, and asserts all three attributes stay `set` —
# so "simplifying" this file back into the broken state fails CI.
*.ts   text diff merge
*.tsx  text diff merge
*.js   text diff merge
*.mjs  text diff merge
*.cjs  text diff merge
*.rs   text diff merge
*.json text diff merge
whitepaper-foundlings/**/*.png filter=lfs diff=lfs merge=lfs -text
whitepaper-foundlings/**/*.pdf filter=lfs diff=lfs merge=lfs -text
skill_candidates/**/*.pdf filter=lfs diff=lfs merge=lfs -text

# EVIDENCE MEDIA IN LFS. DEPLOYED MEDIA DELIBERATELY NOT.
#
# The directories below hold *evidence*: PR screenshots, capture artifacts,
# console pane grabs, before/after GIFs. Nothing deploys them and no build,
# test, component or checker reads their bytes — that was verified path by
# path, not assumed (see CONTRIBUTING.md § Binary media and Git LFS).
#
# WHY THIS SPLIT EXISTS AT ALL. No workflow in .github/workflows/ sets
# `lfs: true` on actions/checkout — all 78 checkout steps take the default,
# which leaves LFS paths as 130-byte pointer files in CI. For evidence media
# that is harmless (nothing opens it). For DEPLOYED media it is fatal:
# website-v2/public/** is what Cloudflare Pages uploads, so a pointer there
# would have the live site serve 130 bytes of text where a PNG belongs.
# That is why website-v2/public/** — including the whitepaper PDFs and
# plates — is NOT listed here and must not be added without first making
# the deploy workflow do an LFS checkout.
#
# GitHub still renders these inline in PR bodies: the `?raw=1` blob form
# 302s to github.com/<repo>/raw/<sha>/<path>, which smudges the pointer and
# returns real image bytes. Plain raw.githubusercontent.com does NOT — it
# returns the pointer as text/plain. See CONTRIBUTING.md for the URL form
# agents must use.
#
# scripts/check-binary-lfs.mjs fails CI when a large binary lands outside
# these paths, so the rule cannot quietly drift as agents add evidence.
docs/pr-assets/**/*.png  filter=lfs diff=lfs merge=lfs -text
docs/pr-assets/**/*.jpg  filter=lfs diff=lfs merge=lfs -text
docs/pr-assets/**/*.jpeg filter=lfs diff=lfs merge=lfs -text
docs/pr-assets/**/*.gif  filter=lfs diff=lfs merge=lfs -text
docs/pr-assets/**/*.webp filter=lfs diff=lfs merge=lfs -text
docs/pr-assets/**/*.mp4  filter=lfs diff=lfs merge=lfs -text
docs/pr-assets/**/*.mov  filter=lfs diff=lfs merge=lfs -text
docs/pr-assets/**/*.webm filter=lfs diff=lfs merge=lfs -text
docs/pr-assets/**/*.pdf  filter=lfs diff=lfs merge=lfs -text
docs/artifacts/**/*.png  filter=lfs diff=lfs merge=lfs -text
docs/artifacts/**/*.jpg  filter=lfs diff=lfs merge=lfs -text
docs/artifacts/**/*.jpeg filter=lfs diff=lfs merge=lfs -text
docs/artifacts/**/*.gif  filter=lfs diff=lfs merge=lfs -text
docs/artifacts/**/*.webp filter=lfs diff=lfs merge=lfs -text
docs/artifacts/**/*.mp4  filter=lfs diff=lfs merge=lfs -text
docs/artifacts/**/*.mov  filter=lfs diff=lfs merge=lfs -text
docs/artifacts/**/*.webm filter=lfs diff=lfs merge=lfs -text
docs/artifacts/**/*.pdf  filter=lfs diff=lfs merge=lfs -text
docs/pr-media/**/*.png  filter=lfs diff=lfs merge=lfs -text
docs/pr-media/**/*.jpg  filter=lfs diff=lfs merge=lfs -text
docs/pr-media/**/*.jpeg filter=lfs diff=lfs merge=lfs -text
docs/pr-media/**/*.gif  filter=lfs diff=lfs merge=lfs -text
docs/pr-media/**/*.webp filter=lfs diff=lfs merge=lfs -text
docs/pr-media/**/*.mp4  filter=lfs diff=lfs merge=lfs -text
docs/pr-media/**/*.mov  filter=lfs diff=lfs merge=lfs -text
docs/pr-media/**/*.webm filter=lfs diff=lfs merge=lfs -text
docs/pr-media/**/*.pdf  filter=lfs diff=lfs merge=lfs -text
docs/pr-artifacts/**/*.png  filter=lfs diff=lfs merge=lfs -text
docs/pr-artifacts/**/*.jpg  filter=lfs diff=lfs merge=lfs -text
docs/pr-artifacts/**/*.jpeg filter=lfs diff=lfs merge=lfs -text
docs/pr-artifacts/**/*.gif  filter=lfs diff=lfs merge=lfs -text
docs/pr-artifacts/**/*.webp filter=lfs diff=lfs merge=lfs -text
docs/pr-artifacts/**/*.mp4  filter=lfs diff=lfs merge=lfs -text
docs/pr-artifacts/**/*.mov  filter=lfs diff=lfs merge=lfs -text
docs/pr-artifacts/**/*.webm filter=lfs diff=lfs merge=lfs -text
docs/pr-artifacts/**/*.pdf  filter=lfs diff=lfs merge=lfs -text
website-v2/screenshots/**/*.png  filter=lfs diff=lfs merge=lfs -text
website-v2/screenshots/**/*.jpg  filter=lfs diff=lfs merge=lfs -text
website-v2/screenshots/**/*.jpeg filter=lfs diff=lfs merge=lfs -text
website-v2/screenshots/**/*.gif  filter=lfs diff=lfs merge=lfs -text
website-v2/screenshots/**/*.webp filter=lfs diff=lfs merge=lfs -text
website-v2/screenshots/**/*.mp4  filter=lfs diff=lfs merge=lfs -text
website-v2/screenshots/**/*.mov  filter=lfs diff=lfs merge=lfs -text
website-v2/screenshots/**/*.webm filter=lfs diff=lfs merge=lfs -text
website-v2/screenshots/**/*.pdf  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/pr-artifacts/**/*.png  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/pr-artifacts/**/*.jpg  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/pr-artifacts/**/*.jpeg filter=lfs diff=lfs merge=lfs -text
website-v2/docs/pr-artifacts/**/*.gif  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/pr-artifacts/**/*.webp filter=lfs diff=lfs merge=lfs -text
website-v2/docs/pr-artifacts/**/*.mp4  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/pr-artifacts/**/*.mov  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/pr-artifacts/**/*.webm filter=lfs diff=lfs merge=lfs -text
website-v2/docs/pr-artifacts/**/*.pdf  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/artifacts/**/*.png  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/artifacts/**/*.jpg  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/artifacts/**/*.jpeg filter=lfs diff=lfs merge=lfs -text
website-v2/docs/artifacts/**/*.gif  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/artifacts/**/*.webp filter=lfs diff=lfs merge=lfs -text
website-v2/docs/artifacts/**/*.mp4  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/artifacts/**/*.mov  filter=lfs diff=lfs merge=lfs -text
website-v2/docs/artifacts/**/*.webm filter=lfs diff=lfs merge=lfs -text
website-v2/docs/artifacts/**/*.pdf  filter=lfs diff=lfs merge=lfs -text
core/pd-console/docs/artifacts/**/*.png  filter=lfs diff=lfs merge=lfs -text
core/pd-console/docs/artifacts/**/*.jpg  filter=lfs diff=lfs merge=lfs -text
core/pd-console/docs/artifacts/**/*.jpeg filter=lfs diff=lfs merge=lfs -text
core/pd-console/docs/artifacts/**/*.gif  filter=lfs diff=lfs merge=lfs -text
core/pd-console/docs/artifacts/**/*.webp filter=lfs diff=lfs merge=lfs -text
core/pd-console/docs/artifacts/**/*.mp4  filter=lfs diff=lfs merge=lfs -text
core/pd-console/docs/artifacts/**/*.mov  filter=lfs diff=lfs merge=lfs -text
core/pd-console/docs/artifacts/**/*.webm filter=lfs diff=lfs merge=lfs -text
core/pd-console/docs/artifacts/**/*.pdf  filter=lfs diff=lfs merge=lfs -text

# CARVE-OUT, and it is not cosmetic.
# tests/unit/spawn-whitepaper-contract.test.js pins the sha256 of
# all-seven-volumes-color-contact-sheet.png and -color-tour.gif, and asserts
# the PNG is exactly 3792x3576 by parsing the IHDR out of the file it reads.
# Under LFS those reads return a 130-byte pointer, the digests do not match
# and the dimension parse fails. This subtree therefore stays in plain git.
docs/artifacts/whitepaper-figure-semantics/** -filter -diff -merge -text
