Process 0 (that is, the initial process):
{1}out(c, pk(relay_sk));
(
    {2}!
    {3}in(c, (sub: bitstring,harbor: bitstring));
    {4}event AcceptPublish(harbor,sub)
) | (
    {5}!
    {6}out(c, (daemonD,harborB))
)

-- Query not event(AcceptPublish(harborB[],d)) in process 0.
Translating the process into Horn clauses...
Completing...
Starting query not event(AcceptPublish(harborB[],d))
goal reachable: attacker(d) -> event(AcceptPublish(harborB[],d))

Derivation:

1. The attacker initially knows harborB[].
attacker(harborB[]).

2. We assume as hypothesis that
attacker(d).

3. By 2, the attacker may know d.
By 1, the attacker may know harborB[].
Using the function 2-tuple the attacker may obtain (d,harborB[]).
attacker((d,harborB[])).

4. The message (d,harborB[]) that the attacker may have by 3 may be received at input {3}.
So event AcceptPublish(harborB[],d) may be executed at {4}.
event(AcceptPublish(harborB[],d)).

5. By 4, event(AcceptPublish(harborB[],d)).
The goal is reached, represented in the following fact:
event(AcceptPublish(harborB[],d)).


A more detailed output of the traces is available with
  set traceDisplay = long.

out(c, ~M) with ~M = pk(relay_sk) at {1}

in(c, (a,harborB)) at {3} in copy a_1

event AcceptPublish(harborB,a) at {4} in copy a_1 (goal)

The event AcceptPublish(harborB,a) is executed at {4} in copy a_1.
A trace has been found.
RESULT not event(AcceptPublish(harborB[],d)) is false.

--------------------------------------------------------------
Verification summary:

Query not event(AcceptPublish(harborB[],d)) is false.

--------------------------------------------------------------

